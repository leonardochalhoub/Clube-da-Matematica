---
titulo: "Lesson 5 — Composition and inverse functions"
slug: "licao-05-composicao-inversa"
categoria: "aulas"
subcategoria: "ano-1-trim-1"
descricao: "Composition f∘g as sequential operations. Inverse f⁻¹ undoing the operation. Conditions for invertibility: bijection or domain restriction."
ordem: 5
publicado: true
tags: ["composição", "inversa", "bijeção", "domínio", "ano-1", "trim-1"]
prerrequisitos: ["licao-04-quadratica"]
autores: ["Clube da Matemática"]
atualizadoEm: "2026-05-05"
usadoEm: ["1.º ano do EM (15 anos)", "Math I japonês cap. 3", "Klasse 10 alemã — Funktionen"]
versao: "v1"
---

<EquacaoCanonica
  formula="(f \circ g)(x) = f(g(x)), \quad f^{-1}(f(x)) = x"
  legenda={<><strong>Composition</strong> chains two functions — the output of <Eq>{`g`}</Eq> becomes the input of <Eq>{`f`}</Eq>. <strong>Inverse</strong> undoes the operation: <Eq>{`f^{-1}`}</Eq> exists if and only if <em>f</em> is bijective (or the domain is restricted). The identity <Eq>{`f^{-1}(f(x)) = x`}</Eq> is the universal verification.</>}
  audioTexto="Composition and inverse functions. F composed with g of x equals f of g of x: apply g first, then f. The inverse f inverse satisfies f inverse of f of x equals x for all x in the domain. The inverse only exists when f is bijective — both injective and surjective at the same time."
/>

<aside className="not-prose my-6 rounded-xl border border-clube-mist-soft/40 bg-clube-cream-soft p-5 text-clube-ink">
  <h3 className="!mt-0 mb-3 text-sm font-semibold uppercase tracking-wider text-clube-mist">
    Books covering this lesson
  </h3>
  <ul className="space-y-2.5 text-sm leading-relaxed">
    <li>
      <a href="https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions" target="_blank" rel="noopener noreferrer" className="font-semibold text-clube-teal hover:text-clube-teal-deep">
        OpenStax — College Algebra 2e
      </a>
      <span className="text-clube-mist"> · §3.4 Composition and §5.7 Inverses · CC-BY 4.0 · </span>
      <em className="text-clube-mist">Composition, decomposition, inverse, horizontal line test — primary source for direct calculation exercises.</em>
    </li>
    <li>
      <a href="https://www.stitz-zeager.com/szprecalculus07042013.pdf" target="_blank" rel="noopener noreferrer" className="font-semibold text-clube-teal hover:text-clube-teal-deep">
        Stitz–Zeager — Precalculus
      </a>
      <span className="text-clube-mist"> · §5.1 Composition and §5.2 Inverses · CC-BY-NC-SA · </span>
      <em className="text-clube-mist">Domain of composite, domain restriction for invertibility, decomposition and challenge exercises.</em>
    </li>
    <li>
      <a href="https://textbooks.aimath.org/textbooks/approved-textbooks/yoshiwara/" target="_blank" rel="noopener noreferrer" className="font-semibold text-clube-teal hover:text-clube-teal-deep">
        Yoshiwara — Modeling, Functions, and Graphs
      </a>
      <span className="text-clube-mist"> · ch. 4 Inverse Functions · free · </span>
      <em className="text-clube-mist">Inverse in real modeling: unit conversion, cost, exchange rate — primary source for the modeling block.</em>
    </li>
  </ul>
</aside>

<DuasPortas>

<Porta nivel="formal" titulo="Rigorous definition">

### Composition of functions

<Definicao titulo="Composition">

Let $f: B \to C$ and $g: A \to B$. The **composition** $f \circ g : A \to C$ is the function defined by

$$
(f \circ g)(x) = f(g(x)), \quad \forall x \in A.
$$

The domain of $f \circ g$ is the set of all $x \in \operatorname{dom}(g)$ such that $g(x) \in \operatorname{dom}(f)$:

$$
\operatorname{dom}(f \circ g) = \{x \in \operatorname{dom}(g) : g(x) \in \operatorname{dom}(f)\}.
$$

</Definicao>

> "When we combine functions such that the output of one function becomes the input of another, we create a **composition of functions**. The resulting function is called a *composite function*." — [OpenStax College Algebra 2e, §3.4](https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions)

<Insight>

**Order matters.** In general $f \circ g \neq g \circ f$. For $f(x) = 2x$ and $g(x) = x + 3$: $(f \circ g)(x) = 2x + 6$ and $(g \circ f)(x) = 2x + 3$. Composition is like getting dressed: socks before shoes is different from shoes before socks.

</Insight>

<figure className="not-prose my-5 rounded-xl border border-clube-mist-soft/40 bg-clube-surface p-4">
<svg viewBox="0 0 520 160" className="w-full">
<defs>
  <marker id="arrc" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
    <path d="M0,0 L10,5 L0,10 z" fill="rgb(232 199 122)"/>
  </marker>
</defs>
<ellipse cx="80" cy="80" rx="55" ry="55" fill="none" stroke="rgb(26 77 92)" strokeWidth="1.5"/>
<text x="80" y="20" textAnchor="middle" fontSize="13" fontFamily="serif" fontStyle="italic" fill="currentColor">A</text>
<circle cx="80" cy="80" r="3" fill="rgb(26 77 92)"/>
<text x="95" y="84" fontSize="11" fontFamily="serif" fontStyle="italic" fill="currentColor">x</text>

<ellipse cx="260" cy="80" rx="55" ry="55" fill="none" stroke="rgb(201 163 95)" strokeWidth="1.5"/>
<text x="260" y="20" textAnchor="middle" fontSize="13" fontFamily="serif" fontStyle="italic" fill="currentColor">B</text>
<circle cx="260" cy="80" r="3" fill="rgb(201 163 95)"/>
<text x="272" y="84" fontSize="11" fontFamily="serif" fontStyle="italic" fill="currentColor">g(x)</text>

<ellipse cx="440" cy="80" rx="55" ry="55" fill="none" stroke="rgb(61 122 95)" strokeWidth="1.5"/>
<text x="440" y="20" textAnchor="middle" fontSize="13" fontFamily="serif" fontStyle="italic" fill="currentColor">C</text>
<circle cx="440" cy="80" r="3" fill="rgb(61 122 95)"/>
<text x="452" y="84" fontSize="11" fontFamily="serif" fontStyle="italic" fill="currentColor">f(g(x))</text>

<line x1="135" y1="80" x2="202" y2="80" stroke="rgb(232 199 122)" strokeWidth="1.5" markerEnd="url(#arrc)"/>
<text x="168" y="70" textAnchor="middle" fontSize="11" fontFamily="serif" fontStyle="italic" fill="rgb(201 163 95)">g</text>

<line x1="315" y1="80" x2="382" y2="80" stroke="rgb(232 199 122)" strokeWidth="1.5" markerEnd="url(#arrc)"/>
<text x="348" y="70" textAnchor="middle" fontSize="11" fontFamily="serif" fontStyle="italic" fill="rgb(201 163 95)">f</text>

<path d="M 80,135 Q 260,175 440,135" fill="none" stroke="rgb(26 77 92)" strokeWidth="1.5" strokeDasharray="5 3" markerEnd="url(#arrc)"/>
<text x="260" y="172" textAnchor="middle" fontSize="11" fontFamily="serif" fontStyle="italic" fill="rgb(26 77 92)">f ∘ g</text>
</svg>
<figcaption className="mt-2 text-center text-xs text-clube-mist">
  Composition: each solid arrow is a function; the dashed arrow below is the composite <em>f</em> ∘ <em>g</em> — a shortcut that "skips" the intermediate set B.
</figcaption>
</figure>

### Inverse function

<Definicao titulo="Inverse">

$f: A \to B$ admits an **inverse function** if and only if $f$ is **bijective**. In that case, the function $f^{-1}: B \to A$ is the unique one satisfying simultaneously:

$$
f^{-1}(f(x)) = x \quad \forall x \in A, \qquad f(f^{-1}(y)) = y \quad \forall y \in B.
$$

Equivalently, $f \circ f^{-1} = \operatorname{id}_B$ and $f^{-1} \circ f = \operatorname{id}_A$.

</Definicao>

> "In order for a function to have an inverse function, it needs to be a **one-to-one** function. A function is one-to-one if each output value corresponds to exactly one input value." — [Stitz–Zeager Precalculus, §5.2](https://www.stitz-zeager.com/szprecalculus07042013.pdf)

<Definicao titulo="Algorithm for computing the inverse">

Given $y = f(x)$, to find $f^{-1}$:

1. Write $y = f(x)$.
2. Swap $x \leftrightarrow y$: write $x = f(y)$.
3. Solve for $y$ — the result is $f^{-1}(x)$.

</Definicao>

<figure className="not-prose my-5 rounded-xl border border-clube-mist-soft/40 bg-clube-surface p-4">
<svg viewBox="-10 -10 240 240" className="w-full max-w-xs mx-auto block">
<defs>
  <marker id="arrd" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="5" markerHeight="5" orient="auto">
    <path d="M0,0 L10,5 L0,10 z" fill="currentColor" fillOpacity="0.5"/>
  </marker>
</defs>
<line x1="0" y1="115" x2="220" y2="115" stroke="currentColor" strokeOpacity="0.25" markerEnd="url(#arrd)"/>
<line x1="105" y1="220" x2="105" y2="0" stroke="currentColor" strokeOpacity="0.25" markerEnd="url(#arrd)"/>
<line x1="15" y1="205" x2="205" y2="15" stroke="currentColor" strokeOpacity="0.2" strokeDasharray="4 3"/>
<text x="208" y="24" fontSize="9" fontFamily="serif" fontStyle="italic" fill="currentColor" fillOpacity="0.4">y = x</text>
<polyline points="15,193 27,179 39,166 51,154 63,143 75,132 87,121 99,111 111,102 123,93 135,84 147,76 159,68 171,61 183,54 195,48 207,42" fill="none" stroke="rgb(26 77 92)" strokeWidth="2"/>
<polyline points="42,207 48,195 54,183 61,171 68,159 76,147 84,135 93,123 102,111 111,99 121,87 132,76 143,65 154,55 166,46 178,38 193,28" fill="none" stroke="rgb(199 107 63)" strokeWidth="2"/>
<text x="155" y="38" fontSize="9" fontFamily="serif" fill="rgb(26 77 92)">f(x)</text>
<text x="120" y="115" fontSize="9" fontFamily="serif" fill="rgb(199 107 63)">f ⁻¹(x)</text>
</svg>
<figcaption className="mt-2 text-center text-xs text-clube-mist">
  <em>f</em> and its inverse are symmetric with respect to the line <em>y = x</em>. Reflecting the graph of <em>f</em> across that diagonal gives the graph of <em>f</em>⁻¹.
</figcaption>
</figure>

<Teorema titulo="Inverse of a composite">

If $f: B \to C$ and $g: A \to B$ are both bijective, then $f \circ g: A \to C$ is bijective and

$$(f \circ g)^{-1} = g^{-1} \circ f^{-1}.$$

**Proof (sketch).** Compute $(g^{-1} \circ f^{-1}) \circ (f \circ g)$. By associativity of composition: $g^{-1} \circ (f^{-1} \circ f) \circ g = g^{-1} \circ \operatorname{id}_B \circ g = g^{-1} \circ g = \operatorname{id}_A$. ∎

</Teorema>

<Insight>

**Socks-and-shoes mnemonic.** To undo "put on socks then shoes," you must first **take off the shoes**, then the socks — reversed order. This is $(f \circ g)^{-1} = g^{-1} \circ f^{-1}$.

</Insight>

</Porta>

<Porta nivel="5" titulo="For a 5-year-old">

You have **two machines**.

The first machine takes a number and **adds 1**. Put in 3, get 4. Put in 7, get 8.

The second machine takes a number and **multiplies by 2**. Put in 4, get 8. Put in 5, get 10.

If you connect them in a line, what comes out of the first machine goes into the second. Put 3 in the first: out comes 4. Into the second: out comes 8. That is **composition** — two machines in a row.

And there is a machine that **undoes** things. If the machine adds 1, the machine that undoes it subtracts 1. You put in 8: out comes 7. The machine that undoes is the **inverse**.

</Porta>

<Porta nivel="10" titulo="For a 10-year-old">

**Composition** means applying two formulas in sequence.

Example: $g(x) = x - 2$ and $f(u) = 3u + 5$. Composition $f$ after $g$:

1. Apply $g$ to $x$: gives $x - 2$.
2. Feed the result into $f$: gives $3(x - 2) + 5 = 3x - 1$.

Therefore $(f \circ g)(x) = 3x - 1$.

**Inverse** means undoing. If the formula is $f(x) = 2x + 3$, to undo it: subtract 3, then divide by 2. Result: $f^{-1}(x) = (x - 3)/2$.

**Visual trick.** On the graph, the inverse is the reflection of the original across the **diagonal line** $y = x$.

**Not every function has an inverse.** $f(x) = x^2$ does not. Why? Because $f(2) = 4$ and $f(-2) = 4$: if you are only given "4," you cannot tell whether it came from 2 or from −2. There is no way to undo it.

</Porta>

<Porta nivel="15" titulo="For a 15-year-old">

**Composition** chains two operations: the output of $g$ becomes the input of $f$.

**Watch the order:** $f \circ g$ is read "f after g," and in general $f \circ g \neq g \circ f$.

For $f(x) = 2x$ and $g(x) = x + 3$:
- $(f \circ g)(x) = f(g(x)) = f(x+3) = 2(x+3) = 2x + 6$
- $(g \circ f)(x) = g(f(x)) = g(2x) = 2x + 3$

Different!

**Domain of the composite.** When computing $f(g(x))$, the value $g(x)$ must land in the domain of $f$. Restrictions arise: for $f(x) = \sqrt{x}$ and $g(x) = x^2 - 4$, we need $g(x) \geq 0$, i.e., $x^2 \geq 4$, so $x \leq -2$ or $x \geq 2$.

**Algorithm to find $f^{-1}$:** swap $x \leftrightarrow y$, then isolate.

Example: $f(x) = 4x - 7$.
$$y = 4x - 7 \quad \to \quad x = 4y - 7 \quad \to \quad y = \frac{x + 7}{4}$$
Therefore $f^{-1}(x) = (x + 7)/4$.

**Verification:** $f(f^{-1}(x)) = 4 \cdot \dfrac{x+7}{4} - 7 = x$. Correct!

<Insight>

**Condition for $f^{-1}$ to exist: bijection.** A function has an inverse if and only if it is injective (each output has at most one input) and surjective (each output is hit by at least one input). This is tested graphically with the **horizontal line test**: if no horizontal line crosses the graph more than once, the function is injective.

</Insight>

</Porta>

<Porta nivel="25" titulo="For an engineering student">

Composition is the central mechanism of calculus: the **chain rule** (Trim 6) is exactly $(f \circ g)'(x) = f'(g(x)) \cdot g'(x)$. Correctly decomposing an expression into inner/outer parts is a prerequisite for differentiating correctly.

### Domain of the composite — carefully

$\operatorname{dom}(f \circ g) = \{x \in \operatorname{dom}(g) : g(x) \in \operatorname{dom}(f)\}$.

For $f(x) = 1/(x-2)$ and $g(x) = 1/x$: we need $x \neq 0$ (for $g$ to be defined) and $g(x) \neq 2$, i.e., $1/x \neq 2$, i.e., $x \neq 1/2$. Therefore $\operatorname{dom}(f \circ g) = \mathbb{R} \setminus \{0, 1/2\}$.

### Inverse: bijective or restrict

$f$ has a global inverse $\iff$ $f$ is bijective. In practice:

- **Not injective?** Restrict the domain. $f(x) = x^2$ becomes injective and invertible on $[0, +\infty)$ with $f^{-1}(y) = \sqrt{y}$.
- **Not surjective?** Restrict the codomain to the image.

### Symmetric group

The bijective functions from a set $A$ to itself, under composition, form a **group** called $\text{Sym}(A)$ (or $S_n$ when $A$ has $n$ finite elements). The identity element is $\operatorname{id}_A$; the inverse of $f$ is $f^{-1}$. This is the fundamental structure of abstract algebra.

### Simple cryptography

A **substitution cipher** encrypts via $E: \{a,\ldots,z\} \to \{0,\ldots,25\}$ and decrypts via $D = E^{-1}$. If $E$ is not bijective, decryption is impossible. The Caesar cipher is $E(x) = (x + k) \bmod 26$ with inverse $E^{-1}(y) = (y - k) \bmod 26$.

### Notation: $f^{-1} \neq 1/f$

$f^{-1}(x)$ denotes the inverse function of $f$. It is NOT $1/f(x)$. This confusion persists into the second year of university.

</Porta>

<Porta nivel="40" titulo="For a senior professional">

**Functional composition is the foundation of every pipeline.** In Haskell, `(f . g) x = f (g x)`. In Python, `functools.reduce(compose, [f, g, h])`. In SQL, subqueries are compositions. In sklearn, `Pipeline` is a composition of transformers. Every abstraction of "data flow" is, at its core, an algebra of compositions.

### Inverse problems and regularization

In engineering, the classic inverse problem is: **given $y$, find $x$ such that $f(x) = y$.** In computed tomography, $f$ is the Radon operator; in seismology, it is the wave propagation operator. If $f$ is not injective (underdetermined system), or if there is noise, "$f^{-1}$" is ill-posed in Hadamard's sense. The solution: **Tikhonov regularization** minimizes $\|f(x) - y\|^2 + \lambda \|x\|^2$, producing a stable pseudo-inverse. Ridge regression in ML is exactly this.

### Iterated functions and dynamics

$f^n(x) = \underbrace{f \circ f \circ \cdots \circ f}_{n}(x)$. The study of orbits $\{x, f(x), f^2(x), \ldots\}$ is the core of **dynamical systems theory**: fixed points, cycles, attractors. The Mandelbrot set is $\{c \in \mathbb{C} : f_c^n(0)\text{ does not diverge}\}$ where $f_c(z) = z^2 + c$.

**Neural networks are compositions.** An $L$-layer network is $f_\theta = f^{(L)} \circ \cdots \circ f^{(1)}$, where each $f^{(k)}(x) = \sigma(W^{(k)} x + b^{(k)})$. Training optimizes the parameters of this composition via backpropagation — which is exactly the chain rule applied recursively.

### Inverses at industrial scale

Computing $f^{-1}$ analytically is rare in production. **Newton-Raphson** is used to solve $f(x) = y$ numerically. Computing the implied volatility of Black-Scholes (lesson 119) is exactly this: given the market price of an option, numerically invert the BS formula to extract $\sigma$.

</Porta>

<Porta nivel="pratica" titulo="Practice — real-world examples">

### Celsius/Fahrenheit conversion

Directly, from Brazil to the US: $F(C) = \dfrac{9}{5}C + 32$.

Inverse (Fahrenheit to Celsius): swap, isolate:

$$x = \frac{9}{5}y + 32 \quad\Rightarrow\quad y = \frac{5}{9}(x - 32)$$

Therefore $F^{-1}(x) = \dfrac{5}{9}(x - 32)$.

Application: 100 °F (fever in the US) $\Rightarrow$ $\dfrac{5}{9}(68) = 37.78$ °C. Normal temperature: $F^{-1}(98.6) = 37$ °C.

### Normalization pipeline (ML / data science)

In preprocessing, z-score normalization is the composition:

1. $g(x) = x - \bar{x}$ (centers)
2. $f(y) = y/\sigma$ (scales)

Composition: $(f \circ g)(x) = (x - \bar{x})/\sigma$.

Inverse to untransform model predictions: $(f \circ g)^{-1} = g^{-1} \circ f^{-1}$, i.e., first $f^{-1}(z) = z \cdot \sigma$, then $g^{-1}(y) = y + \bar{x}$. Result: $(f \circ g)^{-1}(z) = z \cdot \sigma + \bar{x}$.

### Caesar cipher

Encode each letter $\ell \in \{0, \ldots, 25\}$ with shift $k$:

$$E_k(\ell) = (\ell + k) \bmod 26$$

Decryption = inverse: $D_k(\ell) = (\ell - k) \bmod 26 = E_{-k}(\ell)$.

For $k = 3$: "A" (=0) $\to$ "D" (=3). Decrypt: "D" (=3) $\to$ "A" (=0). The decryption function is literally $E_k^{-1}$.

</Porta>

</DuasPortas>

## Worked examples

<Exemplo titulo="Computing (f∘g)(x) — noting that order changes the result" numero="1">

**Problem:** Given $f(x) = 3x + 1$ and $g(x) = x^2$, compute $(f \circ g)(x)$ and $(g \circ f)(x)$ and compare.

**Strategy:** Apply the definition $(f \circ g)(x) = f(g(x))$ — first evaluate $g(x)$, then substitute the result into $f$. Repeat in the opposite order for $(g \circ f)$.

**Solution:**

1. $(f \circ g)(x) = f(g(x)) = f(x^2) = 3x^2 + 1$.
2. $(g \circ f)(x) = g(f(x)) = g(3x + 1) = (3x + 1)^2 = 9x^2 + 6x + 1$.
3. Compare: $3x^2 + 1 \neq 9x^2 + 6x + 1$. The order changed everything.

**Verification:** At $x = 2$: $(f \circ g)(2) = 3 \cdot 4 + 1 = 13$ and $(g \circ f)(2) = 7^2 = 49$. Distinct, confirming non-commutativity.

**Source.** [OpenStax College Algebra 2e §3.4](https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions) — CC-BY 4.0.
</Exemplo>

<Exemplo titulo="Finding the inverse of a linear function and verifying" numero="2">

**Problem:** Determine $f^{-1}(x)$ for $f(x) = 4x - 7$. Verify that $f \circ f^{-1} = f^{-1} \circ f = \operatorname{id}$.

**Strategy:** Standard algorithm: write $y = f(x)$, swap $x \leftrightarrow y$, solve for $y$. Then validate.

**Solution:**

1. $y = 4x - 7$.
2. Swap: $x = 4y - 7$.
3. Isolate: $4y = x + 7 \Rightarrow y = (x + 7)/4$.
4. Therefore $f^{-1}(x) = (x + 7)/4$.

**Verification:**
- $f(f^{-1}(x)) = 4 \cdot \dfrac{x+7}{4} - 7 = (x + 7) - 7 = x$. ✓
- $f^{-1}(f(x)) = \dfrac{(4x - 7) + 7}{4} = \dfrac{4x}{4} = x$. ✓

**Source.** [OpenStax College Algebra 2e §5.7](https://openstax.org/books/college-algebra-2e/pages/5-7-inverses-and-radical-functions) — CC-BY 4.0.
</Exemplo>

<Exemplo titulo="Domain of a composite with a square root — beware of restrictions" numero="3">

**Problem:** Let $f(x) = \sqrt{x}$ and $g(x) = x^2 - 4$. Determine $(f \circ g)(x)$ and its maximum domain.

**Strategy:** The composite exists where $g(x)$ falls in the domain of $f$, i.e., where $g(x) \geq 0$.

**Solution:**

1. $(f \circ g)(x) = f(g(x)) = \sqrt{x^2 - 4}$.
2. For it to exist: $x^2 - 4 \geq 0 \Rightarrow x^2 \geq 4 \Rightarrow |x| \geq 2$.
3. Domain: $(-\infty, -2] \cup [2, +\infty)$.

**Verification:** At $x = 3$: $g(3) = 5$; $f(5) = \sqrt{5}$. And $\sqrt{9-4} = \sqrt{5}$. ✓. At $x = 1$: $g(1) = -3 < 0$, outside the domain of $f$ — correct.

**Source.** [Stitz–Zeager Precalculus §5.1](https://www.stitz-zeager.com/szprecalculus07042013.pdf) — CC-BY-NC-SA.
</Exemplo>

<Exemplo titulo="Inverse of a quadratic function with domain restriction" numero="4">

**Problem:** $f(x) = x^2 - 4x + 7$ is not invertible on $\mathbb{R}$. Restrict the domain so that it becomes invertible and determine $f^{-1}$.

**Strategy:** Complete the square to identify the vertex. Restrict to one branch (convention: right branch) and invert.

**Solution:**

1. $f(x) = (x-2)^2 + 3$. Vertex $(2, 3)$, parabola increasing for $x \geq 2$.
2. Restrict to $A = [2, +\infty)$: $f$ is strictly increasing, hence injective.
3. $y = (x-2)^2 + 3 \Rightarrow (x-2)^2 = y - 3$. Since $x \geq 2$, we have $x - 2 = \sqrt{y-3}$, so $x = 2 + \sqrt{y-3}$.
4. $f^{-1}: [3, +\infty) \to [2, +\infty)$, $f^{-1}(y) = 2 + \sqrt{y-3}$.

**Verification:** $f^{-1}(7) = 2 + \sqrt{4} = 4$. $f(4) = (4-2)^2 + 3 = 7$. ✓

**Source.** [OpenStax College Algebra 2e §5.7](https://openstax.org/books/college-algebra-2e/pages/5-7-inverses-and-radical-functions) — CC-BY 4.0.
</Exemplo>

<Exemplo titulo="Chain composition: currency conversion modeled as inverse of a composite" numero="5">

**Problem:** Dollar-to-real conversion via $f(d) = 5.00 \cdot d$ (simplified rate). Real-to-loyalty-points conversion via $g(r) = r/10$. (a) Model the composite function dollar to points. (b) Determine the inverse points to dollar. (c) How many dollars are 150 points worth?

**Strategy:** Direct composition is $g \circ f$. The inverse is $(g \circ f)^{-1} = f^{-1} \circ g^{-1}$ — note the reversed order.

**Solution:**

1. $(g \circ f)(d) = g(f(d)) = g(5d) = 5d/10 = d/2$.
2. $f^{-1}(r) = r/5$ and $g^{-1}(p) = 10p$.
3. $(g \circ f)^{-1}(p) = f^{-1}(g^{-1}(p)) = f^{-1}(10p) = 10p/5 = 2p$.
4. For $p = 150$: $2 \cdot 150 = 300$ dollars.

**Verification:** 300 USD to 1,500 BRL to 150 points. ✓

**Source.** [Yoshiwara — Modeling, Functions, and Graphs ch. 4](https://textbooks.aimath.org/textbooks/approved-textbooks/yoshiwara/) — free.
</Exemplo>

<ListaExercicios seed="licao-05">

<Exercicio numero="5.1" dificuldade="compreensao"
  opcoes={[
    { texto: "All $x$ where $f$ and $g$ are defined and $g(x) \\neq 0$.", correta: true },
    { texto: "All $x$ where $f(x) \\neq 0$." },
    { texto: "All $x$ where $f$ and $g$ are defined, ignoring $g(x)=0$." },
    { texto: "Only the $x$ where $f(x)=g(x)$." },
  ]}
  solucao={<>The domain of <Eq>{`f/g`}</Eq> includes all values where both functions are defined, excluding the points where the denominator <Eq>{`g(x)`}</Eq> is zero.</>}
  passos={<>
    <ol>
      <li>1. Determine the domain of <Eq>{`f`}</Eq> and the domain of <Eq>{`g`}</Eq> separately.</li>
      <li>2. Take the intersection of those two domains.</li>
      <li>3. Exclude from the intersection all <Eq>{`x`}</Eq> such that <Eq>{`g(x) = 0`}</Eq>.</li>
      <li>4. The resulting set is the domain of <Eq>{`f/g`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 1", licenca: "CC-BY 4.0" }}
>

How do you determine the domain of the quotient of two functions, $f/g$?

</Exercicio>

<Exercicio numero="5.2" dificuldade="compreensao"
  opcoes={[
    { texto: "The output of $g$ becomes the input of $f$: $(f\\circ g)(x) = f(g(x))$.", correta: true },
    { texto: "The output of $f$ becomes the input of $g$: $(f\\circ g)(x) = g(f(x))$." },
    { texto: "It is the sum $f(x) + g(x)$." },
    { texto: "It is the product $f(x) \\cdot g(x)$." },
  ]}
  solucao={<>By definition, <Eq>{`(f \\circ g)(x) = f(g(x))`}</Eq>: evaluate <Eq>{`g`}</Eq> first, and pass the result to <Eq>{`f`}</Eq>.</>}
  passos={<>
    <ol>
      <li>1. Evaluate <Eq>{`g`}</Eq> at the point <Eq>{`x`}</Eq>.</li>
      <li>2. Use that result as the argument of <Eq>{`f`}</Eq>.</li>
      <li>3. The final value is <Eq>{`(f \\circ g)(x)`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 2", licenca: "CC-BY 4.0" }}
>

What is the composition of two functions, <Eq>{`f \\circ g`}</Eq>?

</Exercicio>

<Exercicio numero="5.3" dificuldade="compreensao"
  opcoes={[
    { texto: "Yes, for example $f(x)=x$ and $g(x)=x$ satisfy $f \\circ g = g \\circ f$.", correta: true },
    { texto: "No, never: composition never commutes." },
    { texto: "Yes, always: $f \\circ g = g \\circ f$ for any functions." },
    { texto: "Yes, but only when $f(x) = -g(x)$." },
  ]}
  solucao={<>In general <Eq>{`f \\circ g \\neq g \\circ f`}</Eq>, but for equal functions the compositions coincide. Example: <Eq>{`f(x) = 2x`}</Eq> and <Eq>{`g(x) = 2x`}</Eq> give <Eq>{`(f \\circ g)(x) = 4x = (g \\circ f)(x)`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 3", licenca: "CC-BY 4.0" }}
>

If the order is reversed when composing two functions, can the result be the same as in the original order? If so, give an example.

</Exercicio>

<Exercicio numero="5.4" dificuldade="compreensao"
  opcoes={[
    { texto: "The set of $x$ in the domain of $g$ such that $g(x)$ belongs to the domain of $f$.", correta: true },
    { texto: "The set of $x$ where $f(x)$ is defined." },
    { texto: "The set of $x$ where $g(x)$ is defined." },
    { texto: "The simple intersection of the domains of $f$ and $g$, with no additional condition." },
  ]}
  solucao={<>For <Eq>{`f(g(x))`}</Eq> to make sense, <Eq>{`x`}</Eq> must be in the domain of <Eq>{`g`}</Eq> and additionally <Eq>{`g(x)`}</Eq> must fall within the domain of <Eq>{`f`}</Eq>.</>}
  passos={<>
    <ol>
      <li>1. Write the domain of <Eq>{`g`}</Eq>.</li>
      <li>2. For each <Eq>{`x`}</Eq> in that domain, compute <Eq>{`g(x)`}</Eq> and check whether it belongs to the domain of <Eq>{`f`}</Eq>.</li>
      <li>3. Keep only the <Eq>{`x`}</Eq> that satisfy both conditions.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 4", licenca: "CC-BY 4.0" }}
>

How do you determine the domain of the composition <Eq>{`f \\circ g`}</Eq>?

</Exercicio>

<Exercicio numero="5.5" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f(g(2)) = 243$ and $f(g(x)) = 2(3x+5)^2+1$", correta: true },
    { texto: "$f(g(2)) = 125$ and $f(g(x)) = 6x^2+11$" },
    { texto: "$f(g(2)) = 99$ and $f(g(x)) = (3x+5)^2+2$" },
    { texto: "$f(g(2)) = 243$ and $f(g(x)) = 9x^2+30x+26$" },
  ]}
  solucao={<><Eq>{`g(2) = 3(2)+5 = 11`}</Eq>; <Eq>{`f(11) = 2(121)+1 = 243`}</Eq>. For the general formula: <Eq>{`f(g(x)) = 2(3x+5)^2+1`}</Eq>.</>}
  passos={<>
    <ol>
      <li>1. Compute <Eq>{`g(2) = 3 \\cdot 2 + 5 = 11`}</Eq>.</li>
      <li>2. Compute <Eq>{`f(11) = 2 \\cdot 11^2 + 1 = 2 \\cdot 121 + 1 = 243`}</Eq>.</li>
      <li>3. For the general formula: substitute <Eq>{`g(x) = 3x+5`}</Eq> into <Eq>{`f`}</Eq>: <Eq>{`f(g(x)) = 2(3x+5)^2 + 1`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 76–77", licenca: "CC-BY 4.0" }}
>

With <Eq>{`f(x) = 2x^2 + 1`}</Eq> and <Eq>{`g(x) = 3x + 5`}</Eq>, compute $f(g(2))$ and find $f(g(x))$.

</Exercicio>

<Exercicio numero="5.6" dificuldade="aplicacao"
  opcoes={[
    { texto: "$g(f(-3)) = 62$", correta: true },
    { texto: "$g(f(-3)) = 9$" },
    { texto: "$g(f(-3)) = -13$" },
    { texto: "$g(f(-3)) = 54$" },
  ]}
  solucao={<><Eq>{`f(-3) = 2(-3)^2+1 = 2 \\cdot 9+1 = 19`}</Eq>; then <Eq>{`g(19) = 3(19)+5 = 62`}</Eq>.</>}
  passos={<>
    <ol>
      <li>1. Compute <Eq>{`f(-3) = 2(-3)^2+1 = 18+1 = 19`}</Eq>.</li>
      <li>2. Compute <Eq>{`g(19) = 3 \\cdot 19+5 = 57+5 = 62`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 78", licenca: "CC-BY 4.0" }}
>

With <Eq>{`f(x) = 2x^2 + 1`}</Eq> and <Eq>{`g(x) = 3x + 5`}</Eq>, compute $g(f(-3))$.

</Exercicio>

<Exercicio numero="5.7" dificuldade="aplicacao"
  opcoes={[
    { texto: "$(g \\circ g)(x) = 9x + 20$", correta: true },
    { texto: "$(g \\circ g)(x) = 6x + 5$" },
    { texto: "$(g \\circ g)(x) = 9x + 10$" },
    { texto: "$(g \\circ g)(x) = 3x + 10$" },
  ]}
  solucao={<><Eq>{`(g \\circ g)(x) = g(g(x)) = g(3x+5) = 3(3x+5)+5 = 9x+15+5 = 9x+20`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 79", licenca: "CC-BY 4.0" }}
>

With $g(x) = 3x + 5$, find $(g \circ g)(x)$.

</Exercicio>

<Exercicio numero="5.8" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f(g(0)) = 36$ and $g(f(0)) = -57$", correta: true },
    { texto: "$f(g(0)) = 8$ and $g(f(0)) = 7$" },
    { texto: "$f(g(0)) = 36$ and $g(f(0)) = 57$" },
    { texto: "$f(g(0)) = 28$ and $g(f(0)) = -57$" },
  ]}
  solucao={<><Eq>{`g(0) = 7`}</Eq>, so <Eq>{`f(g(0)) = 4(7)+8 = 36`}</Eq>. On the other hand, <Eq>{`f(0) = 8`}</Eq>, so <Eq>{`g(f(0)) = 7-8^2 = 7-64 = -57`}</Eq>.</>}
  passos={<>
    <ol>
      <li>1. Compute <Eq>{`g(0) = 7 - 0^2 = 7`}</Eq>.</li>
      <li>2. Compute <Eq>{`f(7) = 4(7)+8 = 36`}</Eq>.</li>
      <li>3. Compute <Eq>{`f(0) = 4(0)+8 = 8`}</Eq>.</li>
      <li>4. Compute <Eq>{`g(8) = 7 - 64 = -57`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 72", licenca: "CC-BY 4.0" }}
>

With $f(x) = 4x + 8$ and $g(x) = 7 - x^2$, compute $f(g(0))$ and $g(f(0))$.

</Exercicio>

<Exercicio numero="5.9" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f(g(0)) = 27$ and $g(f(0)) = -4$", correta: true },
    { texto: "$f(g(0)) = 7$ and $g(f(0)) = 4$" },
    { texto: "$f(g(0)) = 4$ and $g(f(0)) = -28$" },
    { texto: "$f(g(0)) = 27$ and $g(f(0)) = 4$" },
  ]}
  solucao={<><Eq>{`g(0) = 4`}</Eq>, so <Eq>{`f(4) = 5(4)+7 = 27`}</Eq>. Also <Eq>{`f(0) = 7`}</Eq> and <Eq>{`g(7) = 4 - 2(49) = 4-98 = -94`}</Eq>. (Ans: <Eq>{`f(g(0))=27`}</Eq>, <Eq>{`g(f(0))=-94`}</Eq>)</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 73", licenca: "CC-BY 4.0" }}
>

With $f(x) = 5x + 7$ and $g(x) = 4 - 2x^2$, compute $f(g(0))$ and $g(f(0))$. (Ans: 27 and $-94$)

</Exercicio>

<Exercicio numero="5.10" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f(g(0)) = 16$ and $g(f(0)) = 12$", correta: true },
    { texto: "$f(g(0)) = 12$ and $g(f(0)) = 16$" },
    { texto: "$f(g(0)) = 4$ and $g(f(0)) = 12$" },
    { texto: "$f(g(0)) = 16$ and $g(f(0)) = -12$" },
  ]}
  solucao={<><Eq>{`g(0) = 12 - 0 = 12`}</Eq>, so <Eq>{`f(12) = 12+4 = 16`}</Eq>. Also <Eq>{`f(0) = 4`}</Eq> and <Eq>{`g(4) = 12 - 4^3 = 12-64 = -52`}</Eq>. (Ans: 16 and <Eq>{`-52`}</Eq>)</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 74", licenca: "CC-BY 4.0" }}
>

With $f(x) = x + 4$ and $g(x) = 12 - x^3$, compute $f(g(0))$ and $g(f(0))$. (Ans: 16 and $-52$)

</Exercicio>

<Exercicio numero="5.11" dificuldade="aplicacao"
  opcoes={[
    { texto: "$\\frac{1}{2}$ and $3$", correta: true },
    { texto: "$2$ and $\\frac{1}{3}$" },
    { texto: "$\\frac{1}{2}$ and $\\frac{1}{3}$" },
    { texto: "$3$ and $2$" },
  ]}
  solucao={<><Eq>{`g(0) = 4(0)+3 = 3`}</Eq> and <Eq>{`f(3) = \\frac{1}{3+2} = \\frac{1}{5}`}</Eq>. (Ans: <Eq>{`f(g(0)) = 1/5`}</Eq>)</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 75", licenca: "CC-BY 4.0" }}
>

With $f(x) = \frac{1}{x+2}$ and $g(x) = 4x + 3$, compute $f(g(0))$ and $g(f(0))$. (Ans: $\frac{1}{5}$ and $5$)

</Exercicio>

<Exercicio numero="5.12" dificuldade="compreensao"
  opcoes={[
    { texto: "$(f \\circ g)(x) = x = (g \\circ f)(x)$, both compositions equal the identity.", correta: true },
    { texto: "$(f \\circ g)(x) = x^3 + 1$ and $(g \\circ f)(x) = x - 1$." },
    { texto: "$(f \\circ g)(x) = x + 1$ and $(g \\circ f)(x) = x - 1$." },
    { texto: "The compositions differ: $(f \\circ g)(x) \\neq (g \\circ f)(x)$." },
  ]}
  solucao={<>With <Eq>{`f(x) = x^3+1`}</Eq> and <Eq>{`g(x) = \\sqrt[3]{x-1}`}</Eq>: <Eq>{`(f \\circ g)(x) = (\\sqrt[3]{x-1})^3+1 = x`}</Eq> and <Eq>{`(g \\circ f)(x) = \\sqrt[3]{x^3+1-1} = x`}</Eq>. Both are the identity, confirming that <Eq>{`g = f^{-1}`}</Eq>.</>}
  passos={<>
    <ol>
      <li>1. Compute <Eq>{`f(g(x)) = f(\\sqrt[3]{x-1}) = (\\sqrt[3]{x-1})^3+1 = x-1+1 = x`}</Eq>.</li>
      <li>2. Compute <Eq>{`g(f(x)) = g(x^3+1) = \\sqrt[3]{x^3+1-1} = \\sqrt[3]{x^3} = x`}</Eq>.</li>
      <li>3. Conclude: both compositions give <Eq>{`x`}</Eq>, so <Eq>{`f`}</Eq> and <Eq>{`g`}</Eq> are inverses of each other.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 80", licenca: "CC-BY 4.0" }}
>

With <Eq>{`f(x) = x^3 + 1`}</Eq> and <Eq>{`g(x) = \\sqrt[3]{x-1}`}</Eq>, compute <Eq>{`(f \\circ g)(x)`}</Eq> and <Eq>{`(g \\circ f)(x)`}</Eq> and compare the results.

</Exercicio>

<Exercicio numero="5.13" dificuldade="aplicacao"
  opcoes={[
    { texto: "$(f \\circ g)(2) = 2$ and $(g \\circ f)(2) = 2$", correta: true },
    { texto: "$(f \\circ g)(2) = 9$ and $(g \\circ f)(2) = 2$" },
    { texto: "$(f \\circ g)(2) = 2$ and $(g \\circ f)(2) = 9$" },
    { texto: "$(f \\circ g)(2) = 3$ and $(g \\circ f)(2) = 3$" },
  ]}
  solucao={<><Eq>{`g(2) = \\sqrt[3]{2-1} = 1`}</Eq>, so <Eq>{`f(1) = 1+1 = 2`}</Eq>. Also <Eq>{`f(2) = 9`}</Eq> and <Eq>{`g(9) = \\sqrt[3]{8} = 2`}</Eq>. Both yield 2.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 81", licenca: "CC-BY 4.0" }}
>

With <Eq>{`f(x) = x^3 + 1`}</Eq> and <Eq>{`g(x) = \\sqrt[3]{x-1}`}</Eq>, compute $(f \circ g)(2)$ and $(g \circ f)(2)$.

</Exercicio>

<Exercicio numero="5.14" dificuldade="compreensao"
  opcoes={[
    { texto: "All of $\\mathbb{R}$, since $x^3+1$ is defined for all $x$ and the cube root accepts any real.", correta: true },
    { texto: "Only $x \\geq 0$, since the cube root requires a non-negative argument." },
    { texto: "Only $x \\geq 1$." },
    { texto: "$x \\neq 0$." },
  ]}
  solucao={<><Eq>{`(g \\circ f)(x) = \\sqrt[3]{x^3}`}</Eq>, and the cube root is defined for all reals. Therefore the domain is <Eq>{`\\mathbb{R}`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 82", licenca: "CC-BY 4.0" }}
>

With <Eq>{`f(x) = x^3 + 1`}</Eq> and <Eq>{`g(x) = \\sqrt[3]{x-1}`}</Eq>, what is the domain of $(g \circ f)(x)$?

</Exercicio>

<Exercicio numero="5.15" dificuldade="desafio"
  opcoes={[
    { texto: "True: $(f \\circ g)(x) = (x+1)^5 = F(x)$.", correta: true },
    { texto: "False: $(f \\circ g)(x) = x^5+1 \\neq F(x)$." },
    { texto: "False: $(f \\circ g)(x) = x^6$." },
    { texto: "True, but only for $x \\geq 0$." },
  ]}
  solucao={<><Eq>{`(f \\circ g)(x) = f(g(x)) = f(x+1) = (x+1)^5 = F(x)`}</Eq>. True.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 86", licenca: "CC-BY 4.0" }}
>

Let $F(x) = (x+1)^5$, $f(x) = x^5$ and $g(x) = x+1$. True or false: $(f \circ g)(x) = F(x)$?

</Exercicio>

<Exercicio numero="5.16" dificuldade="desafio"
  opcoes={[
    { texto: "$(f \\circ g)(6) = 6$ and $(g \\circ f)(6) = 6$", correta: true },
    { texto: "$(f \\circ g)(6) = 38$ and $(g \\circ f)(6) = 6$" },
    { texto: "$(f \\circ g)(6) = 6$ and $(g \\circ f)(6) = 36$" },
    { texto: "$(f \\circ g)(6) = 38$ and $(g \\circ f)(6) = 36$" },
  ]}
  solucao={<>For <Eq>{`f(x) = x^2+2`}</Eq> (<Eq>{`x \\geq 0`}</Eq>) and <Eq>{`g(x) = \\sqrt{x-2}`}</Eq>: <Eq>{`g(6) = 2`}</Eq>, <Eq>{`f(2) = 6`}</Eq>; and <Eq>{`f(6) = 38`}</Eq>, <Eq>{`g(38) = 6`}</Eq>. Both yield 6.</>}
  passos={<>
    <ol>
      <li>1. <Eq>{`g(6) = \\sqrt{6-2} = \\sqrt{4} = 2`}</Eq>.</li>
      <li>2. <Eq>{`f(2) = 4+2 = 6`}</Eq>. Therefore <Eq>{`(f \\circ g)(6) = 6`}</Eq>.</li>
      <li>3. <Eq>{`f(6) = 36+2 = 38`}</Eq>.</li>
      <li>4. <Eq>{`g(38) = \\sqrt{38-2} = \\sqrt{36} = 6`}</Eq>. Therefore <Eq>{`(g \\circ f)(6) = 6`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 87", licenca: "CC-BY 4.0" }}
>

With $f(x) = x^2 + 2$ (for $x \geq 0$) and $g(x) = \sqrt{x-2}$, compute $(f \circ g)(6)$ and $(g \circ f)(6)$.

</Exercicio>

<Exercicio numero="5.17" dificuldade="modelagem"
  opcoes={[
    { texto: "$C(D(6))$: first find the quantity demanded at price 6, then compute production cost.", correta: true },
    { texto: "$D(C(6))$: apply the demand function to the cost." },
    { texto: "$D(C(x)) = 6$: solve the equation." },
    { texto: "$C(D(p)) = 6$: solve the equation for p." },
  ]}
  solucao={<>To find the production cost when the price is 6, first determine how many items will be demanded — <Eq>{`D(6)`}</Eq> — then compute the cost of producing that quantity: <Eq>{`C(D(6))`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 90", licenca: "CC-BY 4.0" }}
>

The function $D(p)$ gives the number of items demanded when the price is $p$. The function $C(x)$ is the cost of producing $x$ items. To determine the production cost when the price is 6, which composition would you evaluate?

</Exercicio>

<Exercicio numero="5.18" dificuldade="modelagem"
  opcoes={[
    { texto: "Solve $A(m(t)) = 4$: find the instant $t$ when pain reaches level 4.", correta: true },
    { texto: "Evaluate $A(m(4))$." },
    { texto: "Evaluate $m(A(4))$." },
    { texto: "Solve $m(A(d)) = 4$." },
  ]}
  solucao={<>Pain is a function of dose, which is a function of time: <Eq>{`A(m(t))`}</Eq> gives the pain level as a function of time. To find when pain reaches 4, solve <Eq>{`A(m(t)) = 4`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 91", licenca: "CC-BY 4.0" }}
>

The function $A(d)$ gives the pain level (0–10) with $d$ mg of analgesic. The amount of drug after $t$ minutes is $m(t)$. What would you do to find when the patient will have pain level 4?

</Exercicio>

<Exercicio numero="5.19" dificuldade="modelagem"
  opcoes={[
    { texto: "$P(x) = 0.595x$", correta: true },
    { texto: "$P(x) = 0.45x$" },
    { texto: "$P(x) = 0.55x$" },
    { texto: "$P(x) = 0.85x$" },
  ]}
  solucao={<>First apply the 30% discount: <Eq>{`d(x) = 0.7x`}</Eq>. Then the additional 15% discount at checkout: <Eq>{`P(x) = 0.85 \\cdot d(x) = 0.85 \\cdot 0.7x = 0.595x`}</Eq>.</>}
  passos={<>
    <ol>
      <li>1. 30% discount: price becomes <Eq>{`d(x) = 0.7x`}</Eq>.</li>
      <li>2. Additional 15% discount: <Eq>{`P(x) = 0.85 \\cdot 0.7x`}</Eq>.</li>
      <li>3. Multiply: <Eq>{`0.85 \\times 0.7 = 0.595`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 92", licenca: "CC-BY 4.0" }}
>

A store offers 30% off the price $x$ of selected items, then an additional 15% off at checkout. Write the final price function $P(x)$ using composition.

</Exercicio>

<Exercicio numero="5.20" dificuldade="modelagem"
  opcoes={[
    { texto: "$A(t) = \\pi(25\\sqrt{t}+2)^2$ and $A(2) = \\pi(25\\sqrt{2}+2)^2 \\approx 5765$ sq in", correta: true },
    { texto: "$A(t) = 25\\pi t + 4$ and $A(2) = 54\\pi$" },
    { texto: "$A(t) = \\pi(25t+2)^2$ and $A(2) = \\pi(52)^2$" },
    { texto: "$A(t) = 25\\pi\\sqrt{t}$ and $A(2) \\approx 111$ sq in" },
  ]}
  solucao={<>The radius grows as <Eq>{`r(t) = 25\\sqrt{t}+2`}</Eq>. The area is <Eq>{`A = \\pi r^2`}</Eq>, so the composition gives <Eq>{`A(t) = \\pi(25\\sqrt{t}+2)^2`}</Eq>. At <Eq>{`t=2`}</Eq>: <Eq>{`r = 25\\sqrt{2}+2 \\approx 37.36`}</Eq>, <Eq>{`A \\approx \\pi(37.36)^2 \\approx 4384`}</Eq> sq in.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 93", licenca: "CC-BY 4.0" }}
>

A raindrop creates a circular ripple. The radius (in inches) grows with time (in minutes) as $r(t) = 25\sqrt{t}+2$. Express the area of the ripple as a function of time and evaluate it at $t=2$.

</Exercicio>

<Exercicio numero="5.21" dificuldade="desafio"
  opcoes={[
    { texto: "$N(T(t)) = 23(5t+1.5)^2 - 56(5t+1.5)+1$", correta: true },
    { texto: "$N(T(t)) = 23t^2 - 56t + 1.5$" },
    { texto: "$N(T(t)) = 115t^2 - 280t + 1$" },
    { texto: "$N(T(t)) = 23(5t)^2 - 56(5t)$" },
  ]}
  solucao={<>Substitute <Eq>{`T(t) = 5t+1.5`}</Eq> into <Eq>{`N(T) = 23T^2 - 56T+1`}</Eq>: <Eq>{`N(T(t)) = 23(5t+1.5)^2 - 56(5t+1.5)+1`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 97", licenca: "CC-BY 4.0" }}
>

The number of bacteria in a food is $N(T) = 23T^2 - 56T + 1$ (with $3 < T < 33$ °C). The temperature after $t$ hours out of the refrigerator is $T(t) = 5t + 1.5$. Find the composite function $N(T(t))$.

</Exercicio>

<Exercicio numero="5.22" dificuldade="compreensao"
  opcoes={[
    { texto: "The test checks whether any horizontal line crosses the graph more than once: if so, the function is not injective.", correta: true },
    { texto: "The test checks whether the graph passes through the origin." },
    { texto: "The test checks whether any vertical line crosses the graph more than once." },
    { texto: "The test checks whether the function is surjective." },
  ]}
  solucao={<>A function is injective (one-to-one) if and only if no horizontal line crosses its graph more than once. The horizontal line test checks precisely this condition visually.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 1", licenca: "CC-BY 4.0" }}
>

Why is the horizontal line test effective for determining whether a function is injective (one-to-one)?

</Exercicio>

<Exercicio numero="5.23" dificuldade="compreensao"
  opcoes={[
    { texto: "Because $f(x) = x^2$ is not injective on $\\mathbb{R}$: $f(2) = f(-2) = 4$, preventing a global inverse.", correta: true },
    { texto: "Because $f(x) = x^2$ is not surjective." },
    { texto: "Because the inverse of $x^2$ would be a constant function." },
    { texto: "Because $x^2$ grows too fast." },
  ]}
  solucao={<>For a global inverse to exist, <Eq>{`f`}</Eq> must be bijective. Since <Eq>{`f(2) = f(-2) = 4`}</Eq>, the function is not injective on <Eq>{`\\mathbb{R}`}</Eq>. Restricting to <Eq>{`[0, +\\infty)`}</Eq>, it becomes injective and invertible, with <Eq>{`f^{-1}(x) = \\sqrt{x}`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 2", licenca: "CC-BY 4.0" }}
>

Why do we restrict the domain of $f(x) = x^2$ to find its inverse function?

</Exercicio>

<Exercicio numero="5.24" dificuldade="demonstracao"
  opcoes={[
    { texto: "Yes, for example $f(x) = x$ (identity) satisfies $f^{-1} = f$.", correta: true },
    { texto: "No, a function can never be its own inverse." },
    { texto: "Yes, but only constant functions are their own inverses." },
    { texto: "Yes, but only exponential functions." },
  ]}
  solucao={<>A function is its own inverse if <Eq>{`f(f(x)) = x`}</Eq> for all <Eq>{`x`}</Eq>. Examples: <Eq>{`f(x) = x`}</Eq> (identity), <Eq>{`f(x) = 1/x`}</Eq> (for <Eq>{`x \\neq 0`}</Eq>), and <Eq>{`f(x) = a - x`}</Eq> for any constant <Eq>{`a`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 3", licenca: "CC-BY 4.0" }}
>

Can a function be its own inverse? Explain and give an example.

</Exercicio>

<Exercicio numero="5.25" dificuldade="compreensao"
  opcoes={[
    { texto: "Swap $x$ and $y$, then isolate $y$: write $y = f(x)$, swap to $x = f(y)$, solve for $y$.", correta: true },
    { texto: "Take the reciprocal of $f(x)$: $f^{-1}(x) = 1/f(x)$." },
    { texto: "Differentiate $f(x)$ and invert the result." },
    { texto: "Reflect the graph across the $y$-axis." },
  ]}
  solucao={<>Standard algorithm: (1) write <Eq>{`y = f(x)`}</Eq>; (2) swap <Eq>{`x \\leftrightarrow y`}</Eq> obtaining <Eq>{`x = f(y)`}</Eq>; (3) isolate <Eq>{`y`}</Eq> — the result is <Eq>{`f^{-1}(x)`}</Eq>. Always verify with <Eq>{`f(f^{-1}(x)) = x`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 5", licenca: "CC-BY 4.0" }}
>

How do you find the inverse of a function algebraically?

</Exercicio>

<Exercicio numero="5.26" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f^{-1}(x) = x - 3$", correta: true },
    { texto: "$f^{-1}(x) = x + 3$" },
    { texto: "$f^{-1}(x) = \\frac{1}{x+3}$" },
    { texto: "$f^{-1}(x) = 3 - x$" },
  ]}
  solucao={<>Swap <Eq>{`y = x+3`}</Eq> to <Eq>{`x = y+3`}</Eq>, isolate: <Eq>{`y = x-3`}</Eq>. Therefore <Eq>{`f^{-1}(x) = x-3`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 7", licenca: "CC-BY 4.0" }}
>

Find $f^{-1}(x)$ for $f(x) = x + 3$.

</Exercicio>

<Exercicio numero="5.27" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f^{-1}(x) = 2 - x$", correta: true },
    { texto: "$f^{-1}(x) = x - 2$" },
    { texto: "$f^{-1}(x) = \\frac{1}{2-x}$" },
    { texto: "$f^{-1}(x) = x + 2$" },
  ]}
  solucao={<>Swap <Eq>{`y = 2-x`}</Eq> to <Eq>{`x = 2-y`}</Eq>, isolate: <Eq>{`y = 2-x`}</Eq>. Therefore <Eq>{`f^{-1}(x) = 2-x`}</Eq> — the function is its own inverse!</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 9", licenca: "CC-BY 4.0" }}
>

Find $f^{-1}(x)$ for $f(x) = 2 - x$.

</Exercicio>

<Exercicio numero="5.28" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f^{-1}(x) = \\frac{2x}{1-x}$", correta: true },
    { texto: "$f^{-1}(x) = \\frac{x}{x+2}$" },
    { texto: "$f^{-1}(x) = \\frac{1-x}{2x}$" },
    { texto: "$f^{-1}(x) = \\frac{x+2}{x}$" },
  ]}
  solucao={<>Swap: <Eq>{`x = \\frac{y}{y+2}`}</Eq>. Solve: <Eq>{`x(y+2) = y`}</Eq>, <Eq>{`xy+2x = y`}</Eq>, <Eq>{`2x = y - xy = y(1-x)`}</Eq>, therefore <Eq>{`y = \\frac{2x}{1-x}`}</Eq>.</>}
  passos={<>
    <ol>
      <li>1. Write <Eq>{`y = \\frac{x}{x+2}`}</Eq> and swap <Eq>{`x \\leftrightarrow y`}</Eq>: <Eq>{`x = \\frac{y}{y+2}`}</Eq>.</li>
      <li>2. Multiply both sides by <Eq>{`(y+2)`}</Eq>: <Eq>{`x(y+2) = y`}</Eq>.</li>
      <li>3. Expand: <Eq>{`xy + 2x = y`}</Eq>.</li>
      <li>4. Isolate <Eq>{`y`}</Eq>: <Eq>{`2x = y(1-x)`}</Eq>, therefore <Eq>{`f^{-1}(x) = \\frac{2x}{1-x}`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 11", licenca: "CC-BY 4.0" }}
>

Find $f^{-1}(x)$ for <Eq>{`f(x) = \\frac{x}{x+2}`}</Eq>.

</Exercicio>

<Exercicio numero="5.29" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f^{-1}(x) = \\frac{4x-3}{5-2x}$", correta: true },
    { texto: "$f^{-1}(x) = \\frac{5x+4}{2x+3}$" },
    { texto: "$f^{-1}(x) = \\frac{3-4x}{2x-5}$" },
    { texto: "$f^{-1}(x) = \\frac{2x+3}{5x+4}$" },
  ]}
  solucao={<>Swap: <Eq>{`x = \\frac{2y+3}{5y+4}`}</Eq>. Solve: <Eq>{`x(5y+4) = 2y+3`}</Eq>, <Eq>{`5xy+4x = 2y+3`}</Eq>, <Eq>{`5xy-2y = 3-4x`}</Eq>, <Eq>{`y(5x-2) = 3-4x`}</Eq>. Therefore <Eq>{`f^{-1}(x) = \\frac{3-4x}{5x-2} = \\frac{4x-3}{2-5x}`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 12", licenca: "CC-BY 4.0" }}
>

Find <Eq>{`f^{-1}(x)`}</Eq> for <Eq>{`f(x) = \\frac{2x+3}{5x+4}`}</Eq>.

</Exercicio>

<Exercicio numero="5.30" dificuldade="aplicacao"
  opcoes={[
    { texto: "Domain $[-7, +\\infty)$ and $f^{-1}(x) = \\sqrt{x} - 7$", correta: true },
    { texto: "Domain $(-\\infty, 0]$ and $f^{-1}(x) = \\sqrt{x+7}$" },
    { texto: "Domain $[-7, +\\infty)$ and $f^{-1}(x) = \\sqrt{x+7}$" },
    { texto: "Domain $[0, +\\infty)$ and $f^{-1}(x) = \\sqrt{x} - 7$" },
  ]}
  solucao={<>Restrict to <Eq>{`[-7, +\\infty)`}</Eq> where <Eq>{`f`}</Eq> is increasing. Swap: <Eq>{`x = (y+7)^2`}</Eq>, isolate: <Eq>{`y = \\sqrt{x}-7`}</Eq>. Therefore <Eq>{`f^{-1}(x) = \\sqrt{x}-7`}</Eq>, domain <Eq>{`[0,+\\infty)`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 13", licenca: "CC-BY 4.0" }}
>

Find a domain where $f(x) = (x+7)^2$ is injective and non-decreasing. Then find $f^{-1}$ restricted to that domain.

</Exercicio>

<Exercicio numero="5.31" dificuldade="aplicacao"
  opcoes={[
    { texto: "Domain $[0, +\\infty)$ and $f^{-1}(x) = \\sqrt{x+5}$", correta: true },
    { texto: "Domain $(-\\infty, 0]$ and $f^{-1}(x) = \\sqrt{x+5}$" },
    { texto: "Domain $[0, +\\infty)$ and $f^{-1}(x) = \\sqrt{x} + 5$" },
    { texto: "Domain $[-5, +\\infty)$ and $f^{-1}(x) = \\sqrt{x}-5$" },
  ]}
  solucao={<>On <Eq>{`[0, +\\infty)`}</Eq>, <Eq>{`f(x) = x^2-5`}</Eq> is increasing. Swap: <Eq>{`x = y^2-5`}</Eq>, isolate: <Eq>{`y = \\sqrt{x+5}`}</Eq>. Therefore <Eq>{`f^{-1}(x) = \\sqrt{x+5}`}</Eq>, domain <Eq>{`[-5, +\\infty)`}</Eq>.</>}
  passos={<>
    <ol>
      <li>1. Restrict to <Eq>{`[0, +\\infty)`}</Eq> (the branch where <Eq>{`f`}</Eq> is increasing).</li>
      <li>2. Write <Eq>{`y = x^2-5`}</Eq> and swap: <Eq>{`x = y^2-5`}</Eq>.</li>
      <li>3. Isolate <Eq>{`y`}</Eq>: <Eq>{`y^2 = x+5`}</Eq>, <Eq>{`y = \\sqrt{x+5}`}</Eq> (positive since <Eq>{`y \\geq 0`}</Eq>).</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 15", licenca: "CC-BY 4.0" }}
>

Find a domain where $f(x) = x^2 - 5$ is injective and non-decreasing, then determine $f^{-1}$ on that domain.

</Exercicio>

<Exercicio numero="5.32" dificuldade="demonstracao"
  opcoes={[
    { texto: "Yes: $f(g(x)) = x$ and $g(f(x)) = x$, therefore they are inverses.", correta: true },
    { texto: "No: the compositions do not yield the identity." },
    { texto: "Yes, but only for $x \\geq 0$." },
    { texto: "No: $f(g(x)) = x$ but $g(f(x)) \\neq x$." },
  ]}
  solucao={<><Eq>{`f(g(x)) = f(\\sqrt[3]{x-1}) = (\\sqrt[3]{x-1})^3+1 = x`}</Eq>. And <Eq>{`g(f(x)) = g(x^3+1) = \\sqrt[3]{x^3} = x`}</Eq>. Both compositions are the identity, confirming they are inverses.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 17", licenca: "CC-BY 4.0" }}
>

Use function composition to verify that $f(x) = \sqrt[3]{x-1}$ and $g(x) = x^3 + 1$ are inverses of each other.

</Exercicio>

<Exercicio numero="5.33" dificuldade="demonstracao"
  opcoes={[
    { texto: "Yes: both compositions yield $x$, confirming they are inverses.", correta: true },
    { texto: "No: $f(g(x)) \\neq x$." },
    { texto: "No: $g(f(x)) \\neq x$." },
    { texto: "No: neither composition gives the identity." },
  ]}
  solucao={<><Eq>{`f(g(x)) = f\\!\\left(\\frac{x-5}{-3}\\right) = -3 \\cdot \\frac{x-5}{-3}+5 = (x-5)+5 = x`}</Eq>. And <Eq>{`g(f(x)) = g(-3x+5) = \\frac{-3x+5-5}{-3} = \\frac{-3x}{-3} = x`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 18", licenca: "CC-BY 4.0" }}
>

Use composition to verify that <Eq>{`f(x) = -3x+5`}</Eq> and <Eq>{`g(x) = \\frac{x-5}{-3}`}</Eq> are inverses of each other.

</Exercicio>

<Exercicio numero="5.34" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f^{-1}(x) = \\frac{5}{9}(x-32)$ — converts Fahrenheit to Celsius", correta: true },
    { texto: "$f^{-1}(x) = \\frac{9}{5}(x+32)$" },
    { texto: "$f^{-1}(x) = \\frac{5}{9}x - 32$" },
    { texto: "$f^{-1}(x) = \\frac{9}{5}x - 32$" },
  ]}
  solucao={<>Swap: <Eq>{`x = \\frac{9}{5}y+32`}</Eq>. Isolate: <Eq>{`x-32 = \\frac{9}{5}y`}</Eq>, therefore <Eq>{`y = \\frac{5}{9}(x-32)`}</Eq>. The inverse converts Fahrenheit to Celsius.</>}
  passos={<>
    <ol>
      <li>1. Start from <Eq>{`y = \\frac{9}{5}x+32`}</Eq> and swap <Eq>{`x \\leftrightarrow y`}</Eq>: <Eq>{`x = \\frac{9}{5}y+32`}</Eq>.</li>
      <li>2. Subtract 32: <Eq>{`x-32 = \\frac{9}{5}y`}</Eq>.</li>
      <li>3. Multiply by <Eq>{`\\frac{5}{9}`}</Eq>: <Eq>{`y = \\frac{5}{9}(x-32)`}</Eq>.</li>
      <li>4. Interpret: given a value in °F, the formula returns °C.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 45", licenca: "CC-BY 4.0" }}
>

The Celsius-to-Fahrenheit conversion is $f(x) = \frac{9}{5}x + 32$. Find the inverse function and explain its meaning.

</Exercicio>

<Exercicio numero="5.35" dificuldade="modelagem"
  opcoes={[
    { texto: "$r(C) = \\frac{C}{2\\pi}$ and $r(36\\pi) = 18$ (radius as a function of circumference)", correta: true },
    { texto: "$r(C) = 2\\pi C$ and $r(36\\pi) = 72\\pi^2$" },
    { texto: "$r(C) = \\frac{2\\pi}{C}$ and $r(36\\pi) = \\frac{1}{18}$" },
    { texto: "$r(C) = \\frac{C}{\\pi}$ and $r(36\\pi) = 36$" },
  ]}
  solucao={<>From the formula <Eq>{`C = 2\\pi r`}</Eq>, isolate <Eq>{`r = C/(2\\pi)`}</Eq>. Therefore <Eq>{`r(C) = C/(2\\pi)`}</Eq>. For <Eq>{`C = 36\\pi`}</Eq>: <Eq>{`r = 36\\pi/(2\\pi) = 18`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 46", licenca: "CC-BY 4.0" }}
>

The circumference of a circle is $C(r) = 2\pi r$. Express the radius as a function of the circumference, call it $r(C)$, and compute $r(36\pi)$.

</Exercicio>

<Exercicio numero="5.36" dificuldade="modelagem"
  opcoes={[
    { texto: "$t(d) = \\frac{d}{50}$ and $t(180) = 3.6$ hours", correta: true },
    { texto: "$t(d) = 50d$ and $t(180) = 9000$ hours" },
    { texto: "$t(d) = \\frac{50}{d}$ and $t(180) \\approx 0.28$ hours" },
    { texto: "$t(d) = d - 50$ and $t(180) = 130$ hours" },
  ]}
  solucao={<>From <Eq>{`d = 50t`}</Eq>, isolate <Eq>{`t = d/50`}</Eq>. For <Eq>{`d = 180`}</Eq>: <Eq>{`t = 180/50 = 3.6`}</Eq> hours. Interpretation: it takes 3.6 hours to travel 180 miles at 50 mph.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 47", licenca: "CC-BY 4.0" }}
>

A car at 50 mph travels $d(t) = 50t$ miles in $t$ hours. Express the time as a function of distance, call it $t(d)$, and compute $t(180)$.

</Exercicio>

<Exercicio numero="5.37" dificuldade="aplicacao"
  opcoes={[
    { texto: "$(f \\circ g)(x) = f(g(x)) = f(x^2 + 1) = \\sqrt{x^2+1}$, with domain $\\mathbb{R}$", correta: true },
    { texto: "$(f \\circ g)(x) = \\sqrt{x^2}+1 = |x|+1$" },
    { texto: "$(f \\circ g)(x) = x+1$ for $x \\geq 0$" },
    { texto: "$(f \\circ g)(x)$ is not defined for $x < 0$" },
  ]}
  solucao={<>Substituting <Eq>{`g(x) = x^2+1`}</Eq> into <Eq>{`f(u) = \\sqrt{u}`}</Eq>: <Eq>{`(f \\circ g)(x) = \\sqrt{x^2+1}`}</Eq>. Since <Eq>{`x^2+1 \\geq 1 > 0`}</Eq> for all <Eq>{`x`}</Eq>, the domain is <Eq>{`\\mathbb{R}`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-4-composition-of-functions", secao: "§3.4", exercicio: "ex. 88", licenca: "CC-BY 4.0" }}
>

With $f(x) = \sqrt{x}$ and $g(x) = x^2 + 1$, determine $(f \circ g)(x)$ and its domain.

</Exercicio>

<Exercicio numero="5.38" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f^{-1}(x) = x + 5$", correta: true },
    { texto: "$f^{-1}(x) = x - 5$" },
    { texto: "$f^{-1}(x) = 5 - x$" },
    { texto: "$f^{-1}(x) = \\frac{1}{x+5}$" },
  ]}
  solucao={<>Swap: <Eq>{`x = y-5`}</Eq> gives <Eq>{`y = x+5`}</Eq>. Therefore <Eq>{`f^{-1}(x) = x+5`}</Eq>. Verification: <Eq>{`f(f^{-1}(x)) = (x+5)-5 = x`}</Eq>.</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 8", licenca: "CC-BY 4.0" }}
>

Find $f^{-1}(x)$ for $f(x) = x - 5$.

</Exercicio>

<Exercicio numero="5.39" dificuldade="aplicacao"
  opcoes={[
    { texto: "$f^{-1}(x) = 3 - x$", correta: true },
    { texto: "$f^{-1}(x) = x - 3$" },
    { texto: "$f^{-1}(x) = x + 3$" },
    { texto: "$f^{-1}(x) = \\frac{1}{3-x}$" },
  ]}
  solucao={<>Swap: <Eq>{`x = 3-y`}</Eq>, isolate: <Eq>{`y = 3-x`}</Eq>. Therefore <Eq>{`f^{-1}(x) = 3-x`}</Eq>, which is the function itself (self-inverse).</>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 10", licenca: "CC-BY 4.0" }}
>

Find $f^{-1}(x)$ for $f(x) = 3 - x$ and verify that $f$ is its own inverse.

</Exercicio>

<Exercicio numero="5.40" dificuldade="desafio"
  opcoes={[
    { texto: "$f(x) = a - x$ is its own inverse for any real $a$: $f(f(x)) = a-(a-x) = x$.", correta: true },
    { texto: "$f(x) = a - x$ is its own inverse only for $a = 0$." },
    { texto: "$f(x) = a - x$ is never its own inverse, since $f(f(x)) = 2a - x \\neq x$." },
    { texto: "$f(x) = a - x$ is its own inverse only for $a = 1$." },
  ]}
  solucao={<>Compute <Eq>{`f(f(x)) = f(a-x) = a-(a-x) = a-a+x = x`}</Eq>. Since <Eq>{`f(f(x)) = x`}</Eq> for any <Eq>{`a`}</Eq>, the function is its own inverse for all real <Eq>{`a`}</Eq>.</>}
  passos={<>
    <ol>
      <li>1. Apply <Eq>{`f`}</Eq> once: <Eq>{`f(x) = a-x`}</Eq>.</li>
      <li>2. Apply <Eq>{`f`}</Eq> again to the result: <Eq>{`f(a-x) = a-(a-x)`}</Eq>.</li>
      <li>3. Simplify: <Eq>{`a-a+x = x`}</Eq>. Therefore <Eq>{`f \\circ f = \\mathrm{id}`}</Eq> for any <Eq>{`a`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax College Algebra 2e", url: "https://openstax.org/books/college-algebra-2e/pages/3-7-inverse-functions", secao: "§3.7", exercicio: "ex. 6", licenca: "CC-BY 4.0" }}
>

Show that $f(x) = a - x$ is its own inverse for every real number $a$.

</Exercicio>

</ListaExercicios>

## Sources

> Only books that directly fed the text and exercises. Full catalog at [**/livros**](/livros).

- [**OpenStax College Algebra 2e**](https://openstax.org/details/books/college-algebra-2e) — Jay Abramson et al. · 2022, 2nd ed · EN · CC-BY 4.0 · §3.4 (composition) and §5.7 (inverse). **Primary source for blocks A and C.**
- [**Stitz–Zeager Precalculus**](https://www.stitz-zeager.com/) — Carl Stitz, Jeff Zeager · 2013, v3 · EN · CC-BY-NC-SA · §5.1 (composition and domain) and §5.2 (inverses). **Primary source for block B.**
- [**Yoshiwara — Modeling, Functions, and Graphs**](https://textbooks.aimath.org/textbooks/approved-textbooks/yoshiwara/) — Katherine Yoshiwara · 2020 · EN · free · ch. 4 (inverse in unit modeling). **Primary source for block D.**
- [**Hammack — Book of Proof (3rd ed)**](https://richardhammack.github.io/BookOfProof/) — Richard Hammack · 2018 · EN · free · ch. 12 (composition, inverse, bijection, proofs). **Primary source for block E.**
- [**Active Calculus 2.0**](https://activecalculus.org/single/) — Matt Boelkins · 2024 · EN · CC-BY-NC-SA · §1.5 (composition as anticipation of the chain rule).
