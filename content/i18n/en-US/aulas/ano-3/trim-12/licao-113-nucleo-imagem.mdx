---
titulo: "Lesson 113 — Kernel and Image"
slug: "licao-113-nucleo-imagem"
categoria: "aulas"
subcategoria: "ano-3-trim-12"
descricao: "Kernel (null space), image (range), rank and nullity. Rank-nullity theorem. The two structures tied to every linear transformation that answer 'who dies?' and 'how far does it reach?'"
ordem: 113
publicado: true
tags: ["álgebra linear", "núcleo", "imagem", "posto-nulidade", "ano-3", "trim-12"]
prerrequisitos: ["licao-112-transformacoes-lineares"]
autores: ["Clube da Matemática"]
atualizadoEm: "2026-05-11"
usadoEm: ["3rd year High School advanced", "Equiv. Lineare Algebra Leistungskurs German", "Equiv. H2 Mathematics Singapore", "Equiv. Linear Algebra MIT 18.06"]
versao: "v1"
---

<EquacaoCanonica
  formula="\dim V = \dim \ker T + \dim \operatorname{Im}(T)"
  legenda={<>The <strong>rank-nullity theorem</strong> says that the dimension of the domain is exactly the sum of the dimension of what "dies" with <Eq>{`T`}</Eq> (the <strong>kernel</strong>) and the dimension of what is reachable (the <strong>image</strong>). It is the most central result of applied linear algebra.</>}
  audioTexto="Rank-nullity theorem. The dimension of the domain equals the dimension of the kernel plus the dimension of the image of T. The kernel gathers the vectors that T maps to the zero vector. The image gathers all vectors that T can produce. Together, they perfectly partition the dimension of the domain."
/>

<aside>

**Books covering this lesson**

- [**A First Course in Linear Algebra**](http://linear.ups.edu/) — Robert A. Beezer · §ILT, §KER, §RNG, §RNNM · GNU FDL · *Complete proofs of the rank-nullity theorem and characterizations of injectivity and surjectivity via kernel and image.*
- [**Linear Algebra Done Right** (4th ed.)](https://linear.axler.net/) — Sheldon Axler · §3D · CC-BY-NC · *Elegant approach without determinants, pivoting the fundamental theorem on invariant subspace structure.*
- [**Linear Algebra**](https://hefferon.net/linearalgebra/) — Jim Hefferon · ch. 3 §II · CC-BY-SA · *Extensive exercises and accessible proofs on kernel, image and the four fundamental subspaces.*

</aside>

<DuasPortas>

<Porta nivel="formal" titulo="Definitions and central theorem">

### Kernel and image

<Definicao titulo="Kernel and image of a linear transformation">

Let $T: V \to W$ be linear.

- **Kernel** (null space): $\ker T = \{v \in V : T(v) = 0_W\}$ — subspace of $V$.
- **Image** (range): $\operatorname{Im}(T) = \{T(v) : v \in V\} = T(V)$ — subspace of $W$.
- **Nullity**: $\operatorname{nul}(T) = \dim \ker T$.
- **Rank**: $\operatorname{rank}(T) = \dim \operatorname{Im}(T)$.

</Definicao>

> "The null space of $T$, denoted $\mathcal{N}(T)$, is the set $\mathcal{N}(T) = \{v \in V : T(v) = 0\}$." — [Beezer, *A First Course in Linear Algebra*, §KER](http://linear.ups.edu/html/section-KER.html)

> "The range of $T$, denoted $\mathcal{R}(T)$, is the set $\mathcal{R}(T) = \{T(v) : v \in V\}$." — [Beezer, *A First Course in Linear Algebra*, §RNG](http://linear.ups.edu/html/section-RNG.html)

<Teorema titulo="Rank-Nullity Theorem">

Let $T: V \to W$ be linear with $\dim V = n$ finite. Then:

<Equation explicacao="The dimension of the domain is the sum of nullity and rank. Each dimension lost in the kernel reappears in the image." latex="\operatorname{nul}(T) + \operatorname{rank}(T) = n" />

</Teorema>

### The four fundamental subspaces

For $A \in \mathbb{R}^{m \times n}$ with rank $r$:

<figure className="not-prose my-5 rounded-xl border border-clube-mist-soft/40 bg-clube-surface p-4">
<svg viewBox="0 0 560 180" className="w-full">
<defs><marker id="arr113" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="5" markerHeight="5" orient="auto"><path d="M0,0 L10,5 L0,10 z" fill="currentColor"/></marker></defs>
<g>
  <rect x="10" y="20" width="240" height="60" rx="8" fill="rgb(26 77 92 / 0.15)" stroke="rgb(26 77 92)" strokeWidth="1.5"/>
  <text x="130" y="38" textAnchor="middle" fontSize="11" fontFamily="ui-monospace,monospace" fill="currentColor">Domain: ℝⁿ</text>
  <text x="60" y="60" textAnchor="middle" fontSize="10" fontFamily="serif" fill="rgb(26 77 92)">Row space</text>
  <text x="60" y="73" textAnchor="middle" fontSize="10" fontFamily="serif" fill="rgb(26 77 92)">dim r</text>
  <text x="195" y="60" textAnchor="middle" fontSize="10" fontFamily="serif" fill="rgb(201 163 95)">Kernel ker A</text>
  <text x="195" y="73" textAnchor="middle" fontSize="10" fontFamily="serif" fill="rgb(201 163 95)">dim n−r</text>
  <line x1="125" y1="20" x2="125" y2="80" stroke="currentColor" strokeOpacity="0.4" strokeWidth="1" strokeDasharray="4,2"/>
</g>
<g>
  <line x1="260" y1="50" x2="300" y2="50" stroke="currentColor" strokeOpacity="0.7" strokeWidth="1.5" markerEnd="url(#arr113)"/>
  <text x="280" y="44" textAnchor="middle" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor">A</text>
</g>
<g>
  <rect x="310" y="20" width="240" height="60" rx="8" fill="rgb(61 122 95 / 0.15)" stroke="rgb(61 122 95)" strokeWidth="1.5"/>
  <text x="430" y="38" textAnchor="middle" fontSize="11" fontFamily="ui-monospace,monospace" fill="currentColor">Codomain: ℝᵐ</text>
  <text x="375" y="60" textAnchor="middle" fontSize="10" fontFamily="serif" fill="rgb(61 122 95)">Image Col(A)</text>
  <text x="375" y="73" textAnchor="middle" fontSize="10" fontFamily="serif" fill="rgb(61 122 95)">dim r</text>
  <text x="490" y="60" textAnchor="middle" fontSize="10" fontFamily="serif" fill="rgb(199 107 63)">Kernel of Aᵀ</text>
  <text x="490" y="73" textAnchor="middle" fontSize="10" fontFamily="serif" fill="rgb(199 107 63)">dim m−r</text>
  <line x1="435" y1="20" x2="435" y2="80" stroke="currentColor" strokeOpacity="0.4" strokeWidth="1" strokeDasharray="4,2"/>
</g>
<g>
  <text x="130" y="130" textAnchor="middle" fontSize="10" fontFamily="serif" fill="currentColor" fillOpacity="0.7">Row space ⊥ ker(A)</text>
  <text x="430" y="130" textAnchor="middle" fontSize="10" fontFamily="serif" fill="currentColor" fillOpacity="0.7">Col(A) ⊥ ker(Aᵀ)</text>
</g>
</svg>
<figcaption className="mt-2 text-center text-xs text-clube-mist">
Strang's four fundamental subspaces. Rank r appears in two places; nullity n−r and m−r fill the orthogonal complement.
</figcaption>
</figure>

### Characterizations of injectivity and surjectivity

<Definicao titulo="Injective and surjective via kernel and image">

- $T$ **injective** $\iff \ker T = \{0\} \iff \operatorname{nul}(T) = 0$.
- $T$ **surjective** $\iff \operatorname{Im}(T) = W \iff \operatorname{rank}(T) = \dim W$.
- $T$ **bijective** (equal finite dims) $\iff$ injective $\iff$ surjective.

</Definicao>

<Insight titulo="General solution of Ax = b">

$Ax = b$ has a solution $\iff b \in \operatorname{Im}(A)$. When it exists, the general solution is $x = x_p + h$, with $x_p$ any particular solution and $h \in \ker A$ arbitrary. Geometrically: the solution is not a point, but an **affine subspace** parallel to the kernel.

</Insight>

</Porta>

<Porta nivel="5" titulo="For a 5-year-old">

Imagine a **mixing machine**. You throw colored balls in and it mixes and spits out balls.

Two interesting places:

1. **Which balls disappear** — the machine eats and spits out "nothing" (zero). This group is the **kernel**.
2. **Which balls the machine can spit out** — this group is the **image**.

The big magic rule: **how much disappears + how much arrives = the total**. If you had 5 types of ball, and 2 types disappeared in the kernel, only 3 types can come out through the image.

</Porta>

<Porta nivel="10" titulo="For a 10-year-old">

Every linear transformation $T$ has **two questions**:

1. **Who is mapped to zero?** — this set is the **kernel** (null space).
2. **Where can the transformation reach?** — this set is the **image**.

Example: the projection $T(x, y) = (x, 0)$ "squashes" the $y$ axis to zero. So $\ker T = $ the $y$ axis (all $(0, y)$). And $T$ only reaches the $x$ axis, so $\operatorname{Im}(T) = $ the $x$ axis.

The big rule: **dimension of kernel + dimension of image = dimension where you started**. If you started on the plane (dim 2), and the kernel has dim 1, the image has dim 1. Half got lost, half arrived.

</Porta>

<Porta nivel="15" titulo="For a 15-year-old teen">

The rank-nullity theorem is **the fundamental equation of applied linear algebra**. Every analysis of a linear system passes through it.

### Why is it fundamental?

When you solve $Ax = b$ ($A$ is $m \times n$):

- **Existence** depends on $b \in \operatorname{Im}(A)$ — a question in $m$ dimensions.
- **Uniqueness** depends on $\ker A = \{0\}$ — a question in $n$ dimensions.

The relationship between them:

| Case | $\ker A$ | $\operatorname{Im}(A)$ | System $Ax = b$ |
|---|---|---|---|
| $A$ square invertible | $\{0\}$ | all of $\mathbb{R}^n$ | unique solution always |
| more equations than unknowns ($m > n$) | possibly $\{0\}$ | proper subspace | generally no solution |
| more unknowns than equations ($n > m$) | always nontrivial | possibly all of $\mathbb{R}^m$ | generally infinitely many solutions |

### How to compute in practice

1. Row-reduce $A$ to echelon form (Gaussian elimination).
2. Pivots = rank.
3. Free variables = nullity.
4. Image = span of columns of $A$ with pivots (in the **original** $A$, not the reduced one).
5. Kernel = solutions of $Ax = 0$ via parameters in free variables.

</Porta>

<Porta nivel="25" titulo="For an engineering student">

Rank and nullity answer central engineering questions: **does the system have a solution? is it unique? how sensitive is it?**

### Strang's 4 subspaces

For $A \in \mathbb{R}^{m \times n}$ with rank $r$:

| Space | Definition | In | Dimension |
|---|---|---|---|
| Column space $\operatorname{Col}(A)$ | $\operatorname{span}$ of columns | $\mathbb{R}^m$ | $r$ |
| Row space $\operatorname{Row}(A)$ | $\operatorname{span}$ of rows | $\mathbb{R}^n$ | $r$ |
| Kernel $\ker(A)$ | $\{x : Ax = 0\}$ | $\mathbb{R}^n$ | $n - r$ |
| Left kernel $\ker(A^T)$ | $\{y : A^T y = 0\}$ | $\mathbb{R}^m$ | $m - r$ |

And $\operatorname{Row}(A) \perp \ker(A)$, $\operatorname{Col}(A) \perp \ker(A^T)$.

### Least squares

$Ax = b$ with no exact solution? Project $b$ onto $\operatorname{Col}(A)$ and solve $A^T A x = A^T b$ (normal equations). Solution always exists; is unique if $\ker(A) = \{0\}$.

If $\ker(A) \neq \{0\}$: family of solutions parametrized by $\ker(A)$. In ML, this is regularization: Ridge $A^T A + \lambda I$ makes $\ker = \{0\}$.

### In control

- **Controllability**: rank of $[B, AB, A^2 B, \ldots, A^{n-1}B] = n$. Trivial kernel = system controllable.
- **Observability**: rank of $[C^T, A^T C^T, \ldots]^T = n$.

### In ML

- **Multicollinearity**: $\operatorname{rank}(X) < n$ when columns of $X$ are dependent. $X^T X$ singular — Ridge or PCA to regularize.
- **Underdetermined regression**: $n > m$ — infinitely many solutions, optimal choice via pseudoinverse (SVD, Lesson 117).

</Porta>

<Porta nivel="40" titulo="For a senior professional">

### Exact sequences and homological algebra

Rank-nullity is a special case of homological algebra. The sequence:

$$0 \to \ker T \xrightarrow{\iota} V \xrightarrow{T} \operatorname{Im}(T) \to 0$$

is **exact** (image of each map = kernel of the next). In finite dimension, the $\dim$ function is Euler-additive over short exact sequences, hence $\dim V = \dim \ker T + \dim \operatorname{Im}(T)$.

Generalizes to **chain complexes**: $\cdots \to V_{n+1} \to V_n \to V_{n-1} \to \cdots$ — foundation of algebraic topology (singular homology, de Rham cohomology) and modern homological algebra.

### Moore-Penrose pseudoinverse

For $A$ any ($m \times n$, rank $r$):

$$A^+ = V \Sigma^+ U^T$$

where $\Sigma^+$ inverts nonzero singulars and zeros the null ones. Properties:

- $A^+ A$ = orthogonal projector onto $\operatorname{Row}(A)$.
- $A A^+$ = orthogonal projector onto $\operatorname{Col}(A)$.
- $x^* = A^+ b$ is the minimum-norm solution to $\min \|Ax - b\|$.

In production: use SVD directly, do not compute $A^+$ explicitly.

### Numerical rank vs algebraic rank

Matrices with small singulars (but nonzero) have **numerical rank** $r_\varepsilon = $ number of singulars $> \varepsilon$. Critical in rank-revealing QR (RRQR) and truncated SVD. Control via condition number $\kappa(A) = \sigma_1 / \sigma_r$.

### Advanced connections

- **Persistent homology**: rank of simplicial complexes — TDA (Topological Data Analysis).
- **Tensor ranks**: extensions of rank to higher-order tensors (ML applications: Tucker, tensor train).
- **Error-correcting codes**: $\ker H$ is the linear code; minimum distance $\leftrightarrow$ rank of parity matrix $H$.
- **Mixed quantum states**: density matrices with rank $r < n$ represent mixed states; rank = dimension of support.

</Porta>

<Porta nivel="pratica" titulo="Practical application">

### Computation roadmap — by hand

**To find the kernel of $A$ ($m \times n$):**

1. Set up the augmented system $[A \mid 0]$.
2. Row-reduce to reduced row echelon form (RREF).
3. Identify pivot variables and free variables.
4. Express pivot variables in terms of free variables.
5. For each free variable: set it to 1 and the others to 0; form a vector. These vectors generate $\ker A$.

**To find the image of $A$:**

1. Row-reduce $A$.
2. The columns of **original** $A$ that correspond to pivot columns form a basis for $\operatorname{Im}(A)$.

### In Python

```python
import numpy as np
from scipy.linalg import null_space

A = np.array([[1, 2, 3],
              [2, 4, 6],
              [3, 6, 9]], dtype=float)

r = np.linalg.matrix_rank(A)
print("Rank:", r)          # 1

# Kernel basis (scipy normalizes)
N = null_space(A)
print("Kernel basis (columns):\n", N)

# Image basis: columns of U corresponding to nonzero singulars
U, S, Vt = np.linalg.svd(A)
Im_base = U[:, :r]
print("Image basis (columns):\n", Im_base)
```

### Quick checks

- $\dim \ker A + \dim \operatorname{Im}(A)$ should give the number of columns of $A$.
- $Av = 0$ for every $v$ in the kernel basis (check numerically).
- $Au_i = $ column of $\operatorname{Im}(A)$ for the right unit vectors.

### When reduced rank is a warning sign

In ML with 50 features and 5 samples: $X$ is $5 \times 50$, so $\operatorname{rank}(X) \leq 5$. Matrix $X^T X$ ($50 \times 50$) has rank $\leq 5$ — singular, not directly invertible. Solution: Ridge, Lasso, PCA before regression.

</Porta>

</DuasPortas>

## Worked examples

<Exemplo titulo="Kernel and image of a projection (basic)" numero="1">

**Problem.** Let $T: \mathbb{R}^3 \to \mathbb{R}^3$ be the projection $T(x, y, z) = (x, y, 0)$. Determine $\ker T$, $\operatorname{Im}(T)$, rank and nullity. Verify the rank-nullity theorem.

**Strategy.** Solve $T(x,y,z) = (0,0,0)$ for the kernel. Describe $\{T(v) : v \in \mathbb{R}^3\}$ for the image. Check dimensions.

**Solution.**

$T(x,y,z) = (0,0,0) \iff (x,y,0) = (0,0,0) \iff x = 0$ and $y = 0$. Hence $\ker T = \{(0, 0, z) : z \in \mathbb{R}\}$ — the $z$ axis.

$\operatorname{Im}(T) = \{(x, y, 0) : x, y \in \mathbb{R}\}$ — the $xy$ plane.

$\dim \ker T = 1$; $\dim \operatorname{Im}(T) = 2$.

Rank-nullity check: $1 + 2 = 3 = \dim \mathbb{R}^3$. ✓

**Verification.** $T$ is not injective ($\ker T \neq \{0\}$). $T$ is not surjective ($\operatorname{Im}(T) \neq \mathbb{R}^3$). Geometrically: $T$ "flattens" the $z$ direction.

**Source.** [Beezer, *A First Course in Linear Algebra*, §ILT, Example ILT.STLT](http://linear.ups.edu/html/section-ILT.html) — GNU FDL.

</Exemplo>

<Exemplo titulo="Kernel and image via row reduction (intermediate)" numero="2">

**Problem.** For $A = \begin{pmatrix} 1 & 2 & 3 \\ 2 & 4 & 6 \end{pmatrix}$, determine $\ker A$, $\operatorname{Im}(A)$, rank and nullity.

**Strategy.** Row-reduce $[A \mid 0]$ for the kernel. Use pivot columns of original $A$ for the image.

**Solution.**

Row-reducing:

$$\begin{pmatrix} 1 & 2 & 3 \\ 2 & 4 & 6 \end{pmatrix} \xrightarrow{L_2 \leftarrow L_2 - 2L_1} \begin{pmatrix} 1 & 2 & 3 \\ 0 & 0 & 0 \end{pmatrix}$$

Pivot in column 1. Free variables: $x_2$ and $x_3$.

$x_1 = -2x_2 - 3x_3$. Setting $(x_2, x_3) = (1,0)$: $(-2, 1, 0)$. Setting $(x_2, x_3) = (0,1)$: $(-3, 0, 1)$.

$$\ker A = \operatorname{span}\{(-2, 1, 0), (-3, 0, 1)\}, \quad \dim = 2$$

Pivot column is the 1st column of original $A$: $\operatorname{Im}(A) = \operatorname{span}\{(1, 2)\}$, $\dim = 1$.

Rank-nullity: $2 + 1 = 3$. ✓

**Verification.** $A \cdot (-2, 1, 0)^T = (-2 + 2, -4 + 4)^T = (0,0)^T$. ✓

**Source.** [Beezer, *A First Course in Linear Algebra*, §RNNM, Example RNNM.NSEEF](http://linear.ups.edu/html/section-RNNM.html) — GNU FDL.

</Exemplo>

<Exemplo titulo="Injectivity, surjectivity and rank-nullity (intermediate)" numero="3">

**Problem.** Let $T: \mathbb{R}^4 \to \mathbb{R}^3$ with matrix $A = \begin{pmatrix} 1 & 0 & -1 & 2 \\ 0 & 1 & 2 & -1 \\ 1 & 1 & 1 & 1 \end{pmatrix}$. Compute rank and nullity. Is $T$ injective? Surjective?

**Strategy.** Row-reduce $A$. Count pivots = rank. Nullity = $4 - \operatorname{rank}$.

**Solution.**

$$A \xrightarrow{L_3 \leftarrow L_3 - L_1 - L_2} \begin{pmatrix} 1 & 0 & -1 & 2 \\ 0 & 1 & 2 & -1 \\ 0 & 0 & 0 & 0 \end{pmatrix}$$

Two pivots: $\operatorname{rank}(A) = 2$. Nullity $= 4 - 2 = 2$.

$T$ **is not injective** (nullity $> 0$; $\ker T$ has dim 2).

$T$ **is not surjective** (rank $= 2 < 3 = \dim \mathbb{R}^3$; $\operatorname{Im}(T)$ is a plane in $\mathbb{R}^3$).

**Verification.** $2 + 2 = 4 = \dim \mathbb{R}^4$. ✓

**Source.** [Hefferon, *Linear Algebra*, ch. 3, §II.2, Example 2.16](https://hefferon.net/linearalgebra/) — CC-BY-SA.

</Exemplo>

<Esempio titulo="Differentiation as a linear transformation (advanced)" numero="4">

**Problem.** Let $D: \mathcal{P}_3 \to \mathcal{P}_3$ be the differentiation operator ($D(p) = p'$). Determine $\ker D$, $\operatorname{Im}(D)$, rank and nullity. Verify rank-nullity.

**Strategy.** Use the basis $\{1, t, t^2, t^3\}$ of $\mathcal{P}_3$. Apply $D$ to each basis element.

**Solution.**

$D(1) = 0$, $D(t) = 1$, $D(t^2) = 2t$, $D(t^3) = 3t^2$.

$\ker D = \{p \in \mathcal{P}_3 : p' = 0\} = \{p : p \text{ is constant}\} = \operatorname{span}\{1\}$.

$\operatorname{Im}(D) = \operatorname{span}\{1, 2t, 3t^2\} = \mathcal{P}_2$.

$\dim \ker D = 1$, $\dim \operatorname{Im}(D) = 3$.

Rank-nullity: $1 + 3 = 4 = \dim \mathcal{P}_3$. ✓

**Verification.** $D$ is not injective (constants vanish). $D$ is not surjective (no degree-3 polynomial in the image — $\dim \operatorname{Im} = 3 < 4$).

**Source.** [Beezer, *A First Course in Linear Algebra*, §LT, Example LT.MFLT](http://linear.ups.edu/html/section-LT.html) — GNU FDL.

</Esempio>

<Esempio titulo="Proof: injective iff trivial kernel (proof)" numero="5">

**Problem.** Prove: $T: V \to W$ linear is injective if and only if $\ker T = \{0\}$.

**Strategy.** Prove both directions. Use the definition of injectivity ($T(u) = T(v) \Rightarrow u = v$) and linearity of $T$.

**Solution.**

($\Rightarrow$) Suppose $T$ is injective. If $v \in \ker T$, then $T(v) = 0 = T(0)$. By injectivity, $v = 0$. Hence $\ker T = \{0\}$.

($\Leftarrow$) Suppose $\ker T = \{0\}$. Let $u, v \in V$ with $T(u) = T(v)$. Then $T(u) - T(v) = 0$, and by linearity $T(u - v) = 0$. Hence $u - v \in \ker T = \{0\}$, so $u - v = 0$, i.e., $u = v$. Therefore $T$ is injective.

**Verification.** The proof is complete (both directions). The argument uses only linearity of $T$ and the definition of kernel — no extra hypotheses.

**Source.** [Beezer, *A First Course in Linear Algebra*, §ILT, Theorem KILT](http://linear.ups.edu/html/section-ILT.html) — GNU FDL.

</Esempio>

<ListaExercicios seed="licao-113-nucleo-imagem">

<Exercicio numero="113.1" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-KER.html", secao: "§KER", pagina: "324", exercicio: "C20", licenca: "GNU FDL" }}
  solucao={<>For <Eq>{`T(x,y) = (x+y,\\; x+y)`}</Eq>: <Eq>{`T(x,y) = (0,0) \\iff x + y = 0`}</Eq>. Hence <Eq>{`\\ker T = \\{(t, -t) : t \\in \\mathbb{R}\\} = \\operatorname{span}\\{(1,-1)\\}`}</Eq>, of dimension 1.</>}
  passos={<>
    <ol>
      <li>Write the condition <Eq>{`T(x,y) = (0,0)`}</Eq>, which gives <Eq>{`x + y = 0`}</Eq>. <em>Why:</em> the kernel is exactly the set of inputs mapped to the zero vector.</li>
      <li>Parametrize: set <Eq>{`x = t`}</Eq>, then <Eq>{`y = -t`}</Eq>. Generating vector: <Eq>{`(1, -1)`}</Eq>.</li>
      <li>Conclusion: <Eq>{`\\ker T = \\operatorname{span}\\{(1,-1)\\}`}</Eq>, dimension 1.</li>
    </ol>
    <p><em>Shortcut:</em> kernel of an $m \times n$ transformation has dimension $n - \operatorname{rank}$. Here $\operatorname{rank} = 1$ (the image is the line $y = x$), so nullity $= 2 - 1 = 1$.</p>
  </>}
>
Determine the kernel of $T: \mathbb{R}^2 \to \mathbb{R}^2$, $T(x, y) = (x + y, \; x + y)$.
</Exercicio>

<Exercicio numero="113.2" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNG.html", secao: "§RNG", pagina: "332", exercicio: "C22", licenca: "GNU FDL" }}
  solucao={<><Eq>{`\\operatorname{Im}(T) = \\{T(x,y) : x,y \\in \\mathbb{R}\\} = \\{(x+y, x+y) : x,y \\in \\mathbb{R}\\} = \\{(s,s) : s \\in \\mathbb{R}\\}`}</Eq>. The image is the line $y = x$, dimension 1.</>}
>
Determine the image of $T(x,y) = (x+y, x+y)$ and its dimension.
</Exercicio>

<Exercicio numero="113.3" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNNM.html", secao: "§RNNM", pagina: "340", exercicio: "C30", licenca: "GNU FDL" }}
  solucao={<>Nullity $= 1$, rank $= 1$. Sum: $1 + 1 = 2 = \dim \mathbb{R}^2$. Rank-nullity theorem verified.</>}
>
Verify the rank-nullity theorem for $T(x,y) = (x+y,\; x+y)$ using the results of the two previous exercises.
</Exercicio>

<Exercicio numero="113.4" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNNM.html", secao: "§RNNM", pagina: "341", exercicio: "C31", licenca: "GNU FDL" }}
  solucao={<>Row-reducing: <Eq>{`\\begin{pmatrix}1&2\\\\3&6\\end{pmatrix} \\to \\begin{pmatrix}1&2\\\\0&0\\end{pmatrix}`}</Eq>. Rank $= 1$. Nullity $= 2-1=1$. <Eq>{`\\ker A = \\operatorname{span}\\{(-2,1)\\}`}</Eq>. <Eq>{`\\operatorname{Im}(A) = \\operatorname{span}\\{(1,3)\\}`}</Eq>.</>}
  passos={<>
    <ol>
      <li>Row-reduce <Eq>{`A`}</Eq>: <Eq>{`L_2 \\leftarrow L_2 - 3L_1`}</Eq> zeros the second row. Pivot only in column 1.</li>
      <li>Rank $= 1$. Nullity $= 2 - 1 = 1$.</li>
      <li>Kernel: free variable <Eq>{`x_2 = t`}</Eq>, then <Eq>{`x_1 = -2t`}</Eq>. Generator: <Eq>{`(-2, 1)`}</Eq>.</li>
      <li>Image: 1st column of original <Eq>{`A`}</Eq>: <Eq>{`(1, 3)`}</Eq>.</li>
    </ol>
    <p><em>Remark:</em> the 2nd row being a multiple of the 1st is the sign of reduced rank before computing.</p>
  </>}
>
For $A = \begin{pmatrix} 1 & 2 \\ 3 & 6 \end{pmatrix}$, determine kernel, image, rank and nullity.
</Exercicio>

<Exercicio numero="113.5" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNNM.html", secao: "§RNNM", pagina: "342", exercicio: "C32", licenca: "GNU FDL" }}
  solucao={<>Row-reducing <Eq>{`A`}</Eq>: two rows, two pivots, rank $= 2$. Free variable: <Eq>{`x_3`}</Eq>. <Eq>{`\\ker A = \\operatorname{span}\\{(-1,-1,1)\\}`}</Eq>. Image = $\mathbb{R}^2$ (surjective).</>}
>
For $A = \begin{pmatrix} 1 & 0 & 1 \\ 0 & 1 & 1 \end{pmatrix}$, determine kernel and image.
</Exercicio>

<Exercicio numero="113.6" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "ch. 3, §II.2", pagina: "218", exercicio: "ex. 2.17", licenca: "CC-BY-SA" }}
  solucao={<>Row-reducing by Gaussian elimination, the 2nd row becomes <Eq>{`(0,1,0)`}</Eq> after elimination and the 3rd row zeros. Rank $= 2$.</>}
>
Determine the rank of $A = \begin{pmatrix} 1 & 2 & 3 \\ 4 & 5 & 6 \\ 7 & 8 & 9 \end{pmatrix}$. (Ans: 2.)
</Exercicio>

<Exercicio numero="113.7" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-LT.html", secao: "§LT", pagina: "290", exercicio: "C25", licenca: "GNU FDL" }}
  solucao={<><Eq>{`D(p) = 0 \\iff p' = 0 \\iff p`}</Eq> is constant. Therefore <Eq>{`\\ker D = \\{p \\in \\mathcal{P}_3 : p \\text{ is constant}\\} = \\operatorname{span}\\{1\\}`}</Eq>, dimension 1.</>}
>
Determine the kernel of the differentiation operator $D: \mathcal{P}_3 \to \mathcal{P}_3$, $D(p) = p'$. (Ans: constant polynomials.)
</Exercicio>

<Exercicio numero="113.8" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-LT.html", secao: "§LT", pagina: "290", exercicio: "C26", licenca: "GNU FDL" }}
  solucao={<><Eq>{`\\operatorname{Im}(D) = \\operatorname{span}\\{D(t), D(t^2), D(t^3)\\} = \\operatorname{span}\\{1, 2t, 3t^2\\} = \\mathcal{P}_2`}</Eq>. Dimension 3.</>}
>
Determine the image of $D: \mathcal{P}_3 \to \mathcal{P}_3$, $D(p) = p'$. (Ans: $\mathcal{P}_2$.)
</Exercicio>

<Exercicio numero="113.9" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNNM.html", secao: "§RNNM", pagina: "340", exercicio: "T10", licenca: "GNU FDL" }}
  solucao={<>Nullity $= 1$ (constants), rank $= 3$ ($\mathcal{P}_2$). Sum $1 + 3 = 4 = \dim \mathcal{P}_3$. ✓</>}
>
Verify the rank-nullity theorem for $D: \mathcal{P}_3 \to \mathcal{P}_3$.
</Exercicio>

<Exercicio numero="113.10" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "ch. 3, §II.1", pagina: "207", exercicio: "ex. 1.18", licenca: "CC-BY-SA" }}
  solucao={<><Eq>{`T(x,y,z) = (0,0) \\iff x=y`}</Eq> and <Eq>{`y=z`}</Eq>. Kernel: <Eq>{`\\{(t,t,t) : t \\in \\mathbb{R}\\}`}</Eq>, dim 1. Image: $\mathbb{R}^2$ (rank 2).</>}
>
For $T: \mathbb{R}^3 \to \mathbb{R}^2$, $T(x,y,z) = (x-y,\; y-z)$, determine kernel and image.
</Exercicio>

<Exercicio numero="113.11" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "ch. 3, §II.2", pagina: "219", exercicio: "ex. 2.19", licenca: "CC-BY-SA" }}
  solucao={<>The Vandermonde matrix with distinct nodes is always invertible. Rank $= 3$.</>}
>
Determine the rank of the Vandermonde matrix $\begin{pmatrix} 1 & 1 & 1 \\ 1 & 2 & 4 \\ 1 & 3 & 9 \end{pmatrix}$.
</Exercicio>

<Exercicio numero="113.12" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-ILT.html", secao: "§ILT", pagina: "310", exercicio: "C21", licenca: "GNU FDL" }}
  solucao={<>Projection onto $y=x$: <Eq>{`P(x,y) = \\frac{x+y}{2}(1,1)`}</Eq>. Kernel: vectors orthogonal to $(1,1)$, i.e., <Eq>{`\\operatorname{span}\\{(1,-1)\\}`}</Eq>. Image: <Eq>{`\\operatorname{span}\\{(1,1)\\}`}</Eq>.</>}
>
Determine the kernel and image of the orthogonal projection of $\mathbb{R}^2$ onto the line $y = x$.
</Exercicio>

<Exercicio numero="113.13" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNNM.html", secao: "§RNNM", pagina: "340", exercicio: "C33", licenca: "GNU FDL" }}
  solucao={<>By rank-nullity: nullity $= 5 - 3 = 2$.</>}
>
$A$ is a $3 \times 5$ matrix with rank 3. What is the dimension of the kernel? (Ans: 2.)
</Exercicio>

<Exercicio numero="113.14" dificuldade="compreensao"
  opcoes={[
    { texto: "A) Yes, can have unique solution when b is in the image", correta: false },
    { texto: "B) No — det = 0 implies ker A nontrivial, so if there is a solution, there are infinitely many", correta: true },
    { texto: "C) Depends on whether b is the zero vector or not", correta: false },
    { texto: "D) Yes, if the rows are independent even with det = 0", correta: false },
  ]}
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-ILT.html", secao: "§ILT", pagina: "312", exercicio: "T15", licenca: "GNU FDL" }}
  solucao={<>If <Eq>{`\\det A = 0`}</Eq>, then <Eq>{`A`}</Eq> is singular, so <Eq>{`\\ker A \\neq \\{0\\}`}</Eq>. Any solution <Eq>{`x_p`}</Eq> generates infinitely many solutions <Eq>{`x_p + h`}</Eq> with <Eq>{`h \\in \\ker A`}</Eq>. Unique solution is impossible.</>}
>
$4 \times 4$ system with $\det A = 0$. Can it have a unique solution?
</Exercicio>

<Exercicio numero="113.15" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "ch. 3, §II.1", pagina: "208", exercicio: "ex. 1.22", licenca: "CC-BY-SA" }}
  solucao={<>Row-reducing: <Eq>{`\\begin{pmatrix}1&2\\\\0&0\\end{pmatrix}x = \\begin{pmatrix}3\\\\0\\end{pmatrix}`}</Eq>. Particular solution <Eq>{`x_p = (3,0)^T`}</Eq>. Family: <Eq>{`x = (3,0)^T + t(-2,1)^T`}</Eq>, $t \in \mathbb{R}$.</>}
  passos={<>
    <ol>
      <li>Check consistency: <Eq>{`b = (3,6)^T`}</Eq> and <Eq>{`(3,6) = 3 \\cdot (1,2)`}</Eq>, which is a column of <Eq>{`A`}</Eq>. System is consistent.</li>
      <li>Row-reduce: <Eq>{`L_2 \\leftarrow L_2 - 2L_1`}</Eq> zeros the 2nd row. Free variable <Eq>{`x_2`}</Eq>.</li>
      <li>Particular solution: <Eq>{`x_2 = 0`}</Eq>, <Eq>{`x_1 = 3`}</Eq>.</li>
      <li>Kernel basis: <Eq>{`(-2, 1)^T`}</Eq>. General solution: <Eq>{`(3,0)^T + t(-2,1)^T`}</Eq>.</li>
    </ol>
    <p><em>Shortcut:</em> general solution = particular solution + any vector in the kernel.</p>
  </>}
>
Solve $\begin{pmatrix} 1 & 2 \\ 2 & 4 \end{pmatrix} x = \begin{pmatrix} 3 \\ 6 \end{pmatrix}$ and describe the family of solutions.
</Exercicio>

<Exercicio numero="113.16" dificuldade="compreensao"
  opcoes={[
    { texto: "A) Yes, because b = (1,0) and the rows of A are multiples", correta: false },
    { texto: "B) No — Im(A) = span{(1,2)}, and (1,0) is not a multiple of (1,2)", correta: true },
    { texto: "C) Depends on the value of the second column of A", correta: false },
    { texto: "D) Yes, because the system has infinitely many solutions", correta: false },
  ]}
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-SLT.html", secao: "§SLT", pagina: "320", exercicio: "C20", licenca: "GNU FDL" }}
  solucao={<><Eq>{`\\operatorname{Im}(A) = \\operatorname{span}\\{(1,2)\\}`}</Eq>. For <Eq>{`(1,0) \\in \\operatorname{Im}(A)`}</Eq> we would need <Eq>{`c(1,2) = (1,0)`}</Eq>, impossible. Hence <Eq>{`b \\notin \\operatorname{Im}(A)`}</Eq> — system is inconsistent.</>}
>
For $A = \begin{pmatrix} 1 & 2 \\ 2 & 4 \end{pmatrix}$, is the vector $b = (1, 0)^T$ in the image of $A$? Justify.
</Exercicio>

<Exercicio numero="113.17" dificuldade="compreensao"
  opcoes={[
    { texto: "A) Always has unique solution (more equations restrict more)", correta: false },
    { texto: "B) Never has solution (overdetermined is always inconsistent)", correta: false },
    { texto: "C) $5 \\times 3$ system with rank 3 has unique solution when consistent; rank < 3 has infinitely many", correta: true },
    { texto: "D) Always has infinitely many solutions since there are more equations than unknowns", correta: false },
  ]}
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "ch. 3, §II", pagina: "215", exercicio: "ex. 2.10", licenca: "CC-BY-SA" }}
  solucao={<>$5 \times 3$ system: nullity $= 3 - \operatorname{rank}$. If rank $= 3$: nullity $= 0$, unique solution (when it exists). If rank $< 3$: infinitely many solutions when consistent. Existence of a solution depends on whether <Eq>{`b \\in \\operatorname{Im}(A)`}</Eq>.</>}
>
$5 \times 3$ system with more equations than unknowns: does it have more unique solutions or infinitely many solutions? Discuss the cases.
</Exercicio>

<Exercicio numero="113.18" dificuldade="demonstracao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-SLT.html", secao: "§SLT", pagina: "320", exercicio: "T20", licenca: "GNU FDL" }}
  solucao={<>Let $x_p$ be a particular solution to $Ax=b$. If $x$ is any solution: $A(x - x_p) = Ax - Ax_p = b - b = 0$, so $h = x - x_p \in \ker A$, i.e., $x = x_p + h$. Conversely, if $h \in \ker A$: $A(x_p + h) = Ax_p + Ah = b + 0 = b$. Hence the set of solutions is exactly $x_p + \ker A$.</>}
>
Show that if $Ax = b$ has a particular solution $x_p$, then the complete set of solutions is $\{x_p + h : h \in \ker A\}$.
</Exercicio>

<Exercicio numero="113.19" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "ch. 3, §II.1", pagina: "208", exercicio: "ex. 1.24", licenca: "CC-BY-SA" }}
  solucao={<>The matrix has all rows equal to $(1,1)$, so $\operatorname{Im}(A) = \operatorname{span}\{(1,1,1,1)^T\}$. Any vector $v \in \mathbb{R}^4$ with components not all equal, such as $v = (1,0,0,0)^T$, is not in the image.</>}
>
In $\mathbb{R}^4$, find a vector $v$ that does not belong to the image of $A = \begin{pmatrix} 1 & 1 \\ 1 & 1 \\ 1 & 1 \\ 1 & 1 \end{pmatrix}$.
</Exercicio>

<Exercicio numero="113.20" dificuldade="compreensao"
  opcoes={[
    { texto: "A) When A has nonzero determinant", correta: false },
    { texto: "B) When ker(A) = {0}, equivalently when rank(A) = n", correta: true },
    { texto: "C) When A is a square matrix", correta: false },
    { texto: "D) Whenever A is rectangular with more rows than columns", correta: false },
  ]}
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-MISLE.html", secao: "§MISLE", pagina: "178", exercicio: "C20", licenca: "GNU FDL" }}
  solucao={<><Eq>{`A^T A`}</Eq> is invertible <Eq>{`\\iff \\ker(A^T A) = \\{0\\} \\iff \\ker(A) = \\{0\\}`}</Eq> (since <Eq>{`A^T A x = 0 \\Rightarrow x^T A^T A x = 0 \\Rightarrow \\|Ax\\|^2 = 0 \\Rightarrow Ax = 0`}</Eq>). Hence condition: rank $= n$ (columns linearly independent).</>}
>
When is $A^T A$ invertible?
</Exercicio>

<Exercicio numero="113.21" dificuldade="demonstracao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-NME.html", secao: "§NME", pagina: "200", exercicio: "T10", licenca: "GNU FDL" }}
  solucao={<>(<Eq>{`A`}</Eq> invertible $\Rightarrow$ rank $= n$): <Eq>{`A`}</Eq> invertible $\Rightarrow Ax=0$ has unique solution $x=0$ (multiply by <Eq>{`A^{-1}`}</Eq>), so <Eq>{`\\ker A = \\{0\\}`}</Eq>, nullity $= 0$, rank $= n$. (Rank $= n \\Rightarrow \\ker A = \\{0\\}`)</Eq>: rank-nullity gives nullity $= n - n = 0$, so $\ker A = \{0\}$, so $T$ is injective and surjective (equal dims), so bijective, so <Eq>{`A`}</Eq> has an inverse.</>}
>
Prove: $A$ square $n \times n$ is invertible $\iff$ $\operatorname{rank}(A) = n$ $\iff$ $\ker A = \{0\}$.
</Exercicio>

<Exercicio numero="113.22" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "ch. 3, §II.1", pagina: "207", exercicio: "ex. 1.16", licenca: "CC-BY-SA" }}
  solucao={<><Eq>{`A = \\begin{pmatrix}1&1\\\\2&2\\end{pmatrix}`}</Eq>, <Eq>{`b=(1,3)^T`}</Eq>. <Eq>{`\\operatorname{Im}(A) = \\operatorname{span}\\{(1,2)\\}`}</Eq>. For <Eq>{`b \\in \\operatorname{Im}(A)`}</Eq> we would need <Eq>{`c(1,2)=(1,3)`}</Eq>, impossible. Hence <Eq>{`b \\notin \\operatorname{Im}(A)`}</Eq> and the system is inconsistent.</>}
>
Show that the system $\begin{cases} x + y = 1 \\ 2x + 2y = 3 \end{cases}$ has no solution, verifying that $b = (1,3)^T \notin \operatorname{Im}(A)$.
</Exercicio>

<Exercicio numero="113.23" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNNM.html", secao: "§RNNM", pagina: "341", exercicio: "C40", licenca: "GNU FDL" }}
  solucao={<>Row-reducing: pivots in columns 1 and 3. Rank $= 2$. Nullity $= 4 - 2 = 2$. Image has dim 2. $T$ is neither injective nor surjective.</>}
>
For $T: \mathbb{R}^4 \to \mathbb{R}^3$ with $A = \begin{pmatrix} 1 & 0 & -1 & 2 \\ 0 & 1 & 2 & -1 \\ 1 & 1 & 1 & 1 \end{pmatrix}$, determine rank, nullity and classify the injectivity and surjectivity of $T$.
</Exercicio>

<Exercicio numero="113.24" dificuldade="modelagem"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "ch. 3, §II", pagina: "220", exercicio: "ex. 2.22", licenca: "CC-BY-SA" }}
  solucao={<>Maximum rank of $X$ ($5 \times 50$) is $\min(5,50) = 5$. $X^T X$ ($50 \times 50$) has rank $\leq 5$ — singular. Multicollinearity: many correlated features cause high nullity. Regularization (Ridge, Lasso) or PCA preprocess to reduce the kernel.</>}
>
In ML, regression with 50 features and 5 samples: $X$ is $5 \times 50$. What is the maximum rank of $X$? Discuss the consequence for $X^T X$ and the least-squares solution.
</Exercicio>

<Exercicio numero="113.25" dificuldade="modelagem"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "ch. 3, §II.2", pagina: "219", exercicio: "ex. 2.20", licenca: "CC-BY-SA" }}
  solucao={<>Projection $\mathbb{R}^3 \to \mathbb{R}^2$: kernel is the $z$ axis (dim 1). Objects at different depths map to the same point on screen — z-fighting occurs when two objects have very close depth and the rasterizer cannot decide which is in front.</>}
>
In computer graphics, the perspective projection $\mathbb{R}^3 \to \mathbb{R}^2$ discards the depth coordinate. What is the kernel? Why is this related to the z-fighting problem?
</Exercicio>

<Exercicio numero="113.26" dificuldade="modelagem"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNNM.html", secao: "§RNNM", pagina: "341", exercicio: "T20", licenca: "GNU FDL" }}
  solucao={<>Controllability matrix <Eq>{`\\mathcal{C} = [B, AB, \\ldots, A^{n-1}B]`}</Eq>: system is controllable <Eq>{`\\iff \\operatorname{rank}(\\mathcal{C}) = n \\iff \\ker(\\mathcal{C}) = \\{0\\}`}</Eq>. When rank $< n$: there exist states unreachable from the origin. Kernel of $\mathcal{C}$ indicates the uncontrollable directions.</>}
>
In control, the system $\dot{x} = Ax + Bu$ is controllable if and only if the controllability matrix $\mathcal{C} = [B, AB, \ldots, A^{n-1}B]$ has rank $n$. Interpret this in terms of kernel and image.
</Exercicio>

<Exercicio numero="113.27" dificuldade="modelagem"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "ch. 3, §II", pagina: "218", exercicio: "ex. 2.15", licenca: "CC-BY-SA" }}
  solucao={<>Portfolio with perfectly correlated assets: covariance matrix $\Sigma$ has linearly dependent columns. Rank $< n$ — singular. Risk: risk decomposition via $\Sigma$ becomes ill-conditioned; VaR (Value at Risk) depends on kernel directions that have no volatility, creating false sense of diversification.</>}
>
In finance, a portfolio of perfectly correlated assets leads to a reduced-rank covariance matrix. What are the practical consequences for risk analysis?
</Exercicio>

<Exercicio numero="113.28" dificuldade="demonstracao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-ILT.html", secao: "§ILT", pagina: "312", exercicio: "T11", licenca: "GNU FDL" }}
  solucao={<>Suppose $v_1, \ldots, v_k$ are eigenvectors of distinct eigenvalues and dependent: $c_1 v_1 + \cdots + c_k v_k = 0$. Apply $T$: $c_1 \lambda_1 v_1 + \cdots + c_k \lambda_k v_k = 0$. Subtract $\lambda_k$ times the first: $c_1(\lambda_1 - \lambda_k)v_1 + \cdots + c_{k-1}(\lambda_{k-1}-\lambda_k)v_{k-1} = 0$. By induction, all $c_i = 0$ — contradiction.</>}
>
Prove that eigenvectors corresponding to distinct eigenvalues of $T: V \to V$ are linearly independent. (Preview of Lesson 114.)
</Exercicio>

<Exercicio numero="113.29" dificuldade="demonstracao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNNM.html", secao: "§RNNM", pagina: "340", exercicio: "T30", licenca: "GNU FDL" }}
  solucao={<><Eq>{`\\operatorname{rank}(A) = \\operatorname{rank}(A^T)`}</Eq>: elementary row operations do not change the row space; transpose exchanges row space and column space. Formally: <Eq>{`A = U\\Sigma V^T`}</Eq> (SVD) — rank is the number of nonzero singulars, invariant under transpose (<Eq>{`A^T = V\\Sigma U^T`}</Eq>).</>}
>
Prove: $\operatorname{rank}(A) = \operatorname{rank}(A^T)$.
</Exercicio>

<Exercicio numero="113.30" dificuldade="desafio"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNNM.html", secao: "§RNNM", pagina: "342", exercicio: "C50", licenca: "GNU FDL" }}
  solucao={<>Take <Eq>{`A = \\begin{pmatrix}1&0&0\\\\0&1&0\\\\1&1&0\\end{pmatrix}`}</Eq>: columns 1 and 2 form a basis for $\operatorname{Im}(A)$ (dim 2), column 3 is in the span of the others. Choose $b = (1,2,3)^T$. System $Ax=b$: $x_1 = 1$, $x_2 = 2$, $x_3$ is free. Infinitely many solutions since nullity $= 1$.</>}
>
Construct a $3 \times 3$ matrix $A$ with rank exactly 2 and a vector $b$ such that $Ax = b$ has infinitely many solutions.
</Exercicio>

<Exercicio numero="113.31" dificuldade="demonstracao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-KER.html", secao: "§KER", pagina: "325", exercicio: "T10", licenca: "GNU FDL" }}
  solucao={<>If $u, v \in \ker T$ and $\alpha, \beta \in K$: $T(\alpha u + \beta v) = \alpha T(u) + \beta T(v) = \alpha \cdot 0 + \beta \cdot 0 = 0$. Hence $\alpha u + \beta v \in \ker T$. Closure plus $0 \in \ker T$ (since $T(0) = 0$) prove it is a subspace.</>}
>
Prove that $\ker T$ is a subspace of the domain $V$.
</Exercicio>

<Exercicio numero="113.32" dificuldade="demonstracao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNG.html", secao: "§RNG", pagina: "333", exercicio: "T10", licenca: "GNU FDL" }}
  solucao={<>If $w_1, w_2 \in \operatorname{Im}(T)$, there exist $v_1, v_2 \in V$ with $T(v_i) = w_i$. Then $\alpha w_1 + \beta w_2 = \alpha T(v_1) + \beta T(v_2) = T(\alpha v_1 + \beta v_2) \in \operatorname{Im}(T)$. Hence $\operatorname{Im}(T)$ is closed under linear combinations; $0 \in \operatorname{Im}(T)$ since $T(0) = 0$.</>}
>
Prove that $\operatorname{Im}(T)$ is a subspace of the codomain $W$.
</Exercicio>

<Exercicio numero="113.33" dificuldade="demonstracao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-RNNM.html", secao: "§RNNM", pagina: "340", exercicio: "T50", licenca: "GNU FDL" }}
  solucao={<><Eq>{`\\operatorname{rank}(AB) \\leq \\operatorname{rank}(A)`}</Eq>: <Eq>{`\\operatorname{Im}(AB) = \\{ABx\\} = A(\\{Bx\\}) = A(\\operatorname{Im}(B)) \\subseteq \\operatorname{Im}(A)`}</Eq>. Hence <Eq>{`\\dim \\operatorname{Im}(AB) \\leq \\dim \\operatorname{Im}(A)`}</Eq>. Similarly <Eq>{`\\operatorname{rank}(AB) \\leq \\operatorname{rank}(B)`}</Eq> via row space.</>}
>
Show that $\operatorname{rank}(AB) \leq \min(\operatorname{rank}(A), \operatorname{rank}(B))$.
</Exercicio>

<Exercicio numero="113.34" dificuldade="demonstracao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.ups.edu/html/section-SLT.html", secao: "§SLT", pagina: "320", exercicio: "T30", licenca: "GNU FDL" }}
  solucao={<><Eq>{`\\operatorname{Col}(A) \\perp \\ker(A^T)`}</Eq>: take $y \in \operatorname{Col}(A)$ (i.e., $y = Ax$) and $z \in \ker(A^T)$ (i.e., $A^T z = 0$). Then <Eq>{`\\langle y, z \\rangle = y^T z = (Ax)^T z = x^T A^T z = x^T \\cdot 0 = 0`}</Eq>. Hence $y \perp z$.</>}
>
Prove: $\operatorname{Col}(A) \perp \ker(A^T)$.
</Exercicio>

</ListaExercicios>

## Sources

- [**A First Course in Linear Algebra**](http://linear.ups.edu/) — Robert A. Beezer · 3rd ed. · EN · GNU FDL · §ILT, §KER, §RNG, §SLT, §RNNM, §NME. **Primary source for exercises.**
- [**Linear Algebra**](https://hefferon.net/linearalgebra/) — Jim Hefferon · 4th ed. · EN · CC-BY-SA · ch. 3, §II. Source of worked examples and modeling exercises.
- [**Linear Algebra Done Right**](https://linear.axler.net/) — Sheldon Axler · 4th ed. · EN · CC-BY-NC · §3D. Abstract perspective without determinants.
