---
titulo: "Lição 66 — Concavidade e pontos de inflexão"
slug: "licao-66-concavidade"
categoria: "aulas"
subcategoria: "ano-2-trim-7"
descricao: "Sinal de f'': côncava para cima quando f'' > 0, para baixo quando f'' < 0. Inflexão onde f'' muda de sinal. Teste da segunda derivada para extremos."
ordem: 66
publicado: true
tags: ["concavidade", "inflexão", "segunda derivada", "convexidade", "ano-2", "trim-7"]
prerrequisitos: ["licao-63-esboco-graficos", "licao-52-regras-derivacao"]
autores: ["Clube da Matemática"]
atualizadoEm: "2026-05-05"
usadoEm: ["2.º ano EM avançado", "Equiv. Math I/II japonês", "Equiv. Leistungskurs Analysis alemão", "Cálculo I universitário"]
versao: "v1"
---

<EquacaoCanonica
  formula="f''(x) > 0 \implies f \text{ côncava}\uparrow, \quad f''(x) < 0 \implies f \text{ côncava}\downarrow, \quad f'' \text{ muda sinal} \implies \text{inflexão}"
  legenda={<>A <strong>concavidade</strong> de uma curva é determinada pelo sinal da <strong>segunda derivada</strong>: <Eq>{`f'' > 0`}</Eq> significa côncava para cima (formato de tigela), <Eq>{`f'' < 0`}</Eq> côncava para baixo (formato de chapéu). Um <strong>ponto de inflexão</strong> ocorre onde <Eq>{`f''`}</Eq> muda de sinal — não basta ser zero.</>}
  audioTexto="Concavidade e inflexão. F duas linhas maior que zero: côncava para cima, formato de tigela. F duas linhas menor que zero: côncava para baixo, formato de chapéu. Onde f duas linhas muda de sinal existe um ponto de inflexão."
/>

<aside className="not-prose my-6 rounded-xl border border-clube-mist-soft/40 bg-clube-cream-soft p-5 text-clube-ink">
  <h3 className="!mt-0 mb-3 text-sm font-semibold uppercase tracking-wider text-clube-mist">
    Livros que cobrem esta lição
  </h3>
  <ul className="space-y-2.5 text-sm leading-relaxed">
    <li>
      <a href="https://activecalculus.org/single/sec-3-1-tests.html" target="_blank" rel="noopener noreferrer" className="font-semibold text-clube-teal hover:text-clube-teal-deep">
        Boelkins — Active Calculus
      </a>
      <span className="text-clube-mist"> · §3.1 Using Derivatives to Identify Extreme Values · CC-BY-NC-SA · </span>
      <em className="text-clube-mist">Constrói concavidade a partir de comportamento de f' — abordagem ativa com atividades orientadas.</em>
    </li>
    <li>
      <a href="https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph" target="_blank" rel="noopener noreferrer" className="font-semibold text-clube-teal hover:text-clube-teal-deep">
        OpenStax — Calculus Volume 1
      </a>
      <span className="text-clube-mist"> · §4.5 Derivatives and the Shape of a Graph · CC-BY-NC-SA · </span>
      <em className="text-clube-mist">Definição rigorosa de convexidade, teste da segunda derivada e tabela de concavidade — referência primária de exercícios.</em>
    </li>
    <li>
      <a href="https://www.apexcalculus.com/downloads" target="_blank" rel="noopener noreferrer" className="font-semibold text-clube-teal hover:text-clube-teal-deep">
        Hartman et al. — APEX Calculus
      </a>
      <span className="text-clube-mist"> · §3.4 Concavity and the Second Derivative Test · CC-BY-NC · </span>
      <em className="text-clube-mist">Tratamento formal com exemplos de inflexão, contraexemplos de f'' = 0 sem inflexão, e ligação com esboço completo.</em>
    </li>
  </ul>
</aside>

<DuasPortas>

<Porta nivel="formal" titulo="Definição rigorosa e critérios">

### Concavidade e convexidade

<Definicao titulo="Função convexa (côncava para cima)">
$f: I \to \mathbb{R}$ é **convexa** em $I$ se, para todo $x_1, x_2 \in I$ e $t \in [0,1]$:
$$f(tx_1 + (1-t)x_2) \leq t\,f(x_1) + (1-t)\,f(x_2).$$
Geometricamente: o segmento de reta entre $(x_1, f(x_1))$ e $(x_2, f(x_2))$ fica **acima ou sobre** o gráfico.
</Definicao>

> "The function $f$ is concave up on an interval $I$ if $f''(x) \geq 0$ for all $x \in I$." — [OpenStax Calculus Vol. 1 §4.5](https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph)

**Critério via segunda derivada:** se $f$ é duas vezes derivável em $I$:

- $f''(x) \geq 0$ em $I$ $\iff$ $f$ convexa (côncava para cima).
- $f''(x) \leq 0$ em $I$ $\iff$ $f$ côncava (para baixo).
- $f''(x) > 0$ estritamente $\Rightarrow$ convexidade estrita.

<figure className="not-prose my-5 rounded-xl border border-clube-mist-soft/40 bg-clube-surface p-4">
<svg viewBox="0 0 480 180" className="w-full">
<defs>
  <marker id="arrcc" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
    <path d="M0,0 L10,5 L0,10 z" fill="currentColor"/>
  </marker>
</defs>
<g>
  <text x="50" y="18" textAnchor="middle" fontSize="11" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.7">f'' &gt; 0 — tigela</text>
  <path d="M 10,140 Q 50,60 90,140" stroke="rgb(26 77 92)" strokeWidth="2.5" fill="none"/>
  <line x1="10" y1="140" x2="90" y2="140" stroke="rgb(232 199 122)" strokeWidth="1.5" strokeDasharray="4,3"/>
  <text x="50" y="165" textAnchor="middle" fontSize="10" fill="currentColor" fillOpacity="0.6">corda acima do arco</text>
</g>
<g>
  <text x="280" y="18" textAnchor="middle" fontSize="11" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.7">f'' &lt; 0 — chapéu</text>
  <path d="M 230,90 Q 280,30 330,90" stroke="rgb(199 107 63)" strokeWidth="2.5" fill="none"/>
  <line x1="230" y1="90" x2="330" y2="90" stroke="rgb(232 199 122)" strokeWidth="1.5" strokeDasharray="4,3"/>
  <text x="280" y="115" textAnchor="middle" fontSize="10" fill="currentColor" fillOpacity="0.6">corda abaixo do arco</text>
</g>
</svg>
<figcaption className="mt-2 text-center text-xs text-clube-mist">
Côncava para cima (f'' &gt; 0): corda fica acima do arco. Côncava para baixo (f'' &lt; 0): corda fica abaixo do arco.
</figcaption>
</figure>

### Ponto de inflexão

<Definicao titulo="Ponto de inflexão">
$x_0$ é **ponto de inflexão** de $f$ se $f$ é contínua em $x_0$ e $f''$ **muda de sinal** ao passar por $x_0$.
</Definicao>

**Atenção:** $f''(x_0) = 0$ é condição necessária mas NÃO suficiente. Contraexemplo canônico: $f(x) = x^4$ tem $f''(0) = 0$ mas $f'' \geq 0$ em vizinhança de $0$ — sem mudança de sinal, portanto $0$ não é inflexão.

> "If the concavity changes at a point $(x_0, f(x_0))$, we call this a point of inflection. It must be the case that $f''(x_0)$ changes sign." — [APEX Calculus §3.4](https://www.apexcalculus.com/downloads)

### Teste da segunda derivada para extremos locais

<Teorema titulo="Teste da segunda derivada">
Seja $f'(x_0) = 0$. Então:
- $f''(x_0) > 0$ $\Rightarrow$ $x_0$ é **mínimo local**.
- $f''(x_0) < 0$ $\Rightarrow$ $x_0$ é **máximo local**.
- $f''(x_0) = 0$ $\Rightarrow$ **inconclusivo** (use derivadas de ordem superior ou teste do sinal de $f'$).
</Teorema>

**Prova para mínimo:** se $f'(x_0) = 0$ e $f''(x_0) > 0$, pela continuidade de $f''$ existe vizinhança onde $f''(x) > 0$, logo $f'$ é crescente nessa vizinhança. Como $f'(x_0) = 0$, temos $f' < 0$ à esquerda e $f' > 0$ à direita de $x_0$ — pelo teste da derivada primeira, $x_0$ é mínimo local. ∎

<Insight titulo="Desigualdade de Jensen">
$f$ convexa $\Rightarrow$ $f\!\left(\frac{x_1 + x_2}{2}\right) \leq \frac{f(x_1) + f(x_2)}{2}$. Mais geralmente: para variável aleatória $X$, $f(E[X]) \leq E[f(X)]$. Pilar de teoria da informação, termodinâmica e ML.
</Insight>

</Porta>

<Porta nivel="5" titulo="Pra criança de 5 anos">

Pega uma **tigela** de sorvete vazia e vira ela pra cima: você tem uma curva que vai pra baixo no meio (como $\cap$). Agora pega a tigela normal: curva que vai pra cima no meio (como $\cup$).

- A tigela normal é **côncava para cima** — qualquer gotinha de sorvete que você colocar rola pra baixo, pro centro.
- A tigela virada é **côncava para baixo** — qualquer gotinha escorrega pra fora.

Um **ponto de inflexão** é aquele momento em que uma curva **troca de tigela pra chapéu** — ou vice-versa. É onde a curva "vira de ponta cabeça" por um instante.

</Porta>

<Porta nivel="10" titulo="Pra criança de 10 anos">

A **segunda derivada** $f''$ é a derivada da derivada — ela mede **como a inclinação está mudando**.

- Se $f'' > 0$: a inclinação $f'$ está **crescendo**. O gráfico curva pra cima, como $\cup$.
- Se $f'' < 0$: a inclinação $f'$ está **decrescendo**. O gráfico curva pra baixo, como $\cap$.

**Exemplo com $f(x) = x^3$:**

$f'(x) = 3x^2$, $f''(x) = 6x$.

- Para $x < 0$: $f'' < 0$ — curva pra baixo.
- Para $x > 0$: $f'' > 0$ — curva pra cima.
- Em $x = 0$: $f''$ muda de sinal. **Ponto de inflexão!**

**Por que importa?** Determina se um ponto onde $f'(x) = 0$ é um máximo (curva pra baixo = chapéu) ou mínimo (curva pra cima = tigela).

</Porta>

<Porta nivel="15" titulo="Pra adolescente de 15 anos">

Concavidade completa a "história" de $f'$ para descrever o formato do gráfico:

| Sinal de $f'$ | Sinal de $f''$ | Comportamento |
|---|---|---|
| $+$ | $+$ | crescendo e acelerando ($\cup$ subindo) |
| $+$ | $-$ | crescendo e desacelerando |
| $-$ | $+$ | decrescendo e desacelerando |
| $-$ | $-$ | decrescendo e acelerando ($\cap$ descendo) |

### Teste da segunda derivada para extremos

Se $f'(x_0) = 0$:
- $f''(x_0) > 0$: **mínimo local** (tigela).
- $f''(x_0) < 0$: **máximo local** (chapéu).
- $f''(x_0) = 0$: **inconclusivo** — faça teste de sinal de $f'$ em volta.

### Inflexão: muda sinal, não basta ser zero

Ponto de inflexão é onde $f''$ **muda de sinal**. Exemplo: $f(x) = e^{-x^2/2}$ (gaussiana). $f''(x) = (x^2-1)e^{-x^2/2}$. Mudança de sinal em $x = \pm 1$. Logo, inflexões em $\mu \pm \sigma$ para distribuição normal com $\sigma = 1$.

### Aplicações

- **Economia:** lucro tem máximo onde $\pi'' < 0$. Inflexão de $C(q)$ = mudança de retorno marginal.
- **Física:** mínimo de energia potencial $U$ (com $U'' > 0$) é equilíbrio estável. Frequência de pequenas oscilações: $\omega = \sqrt{U''(x^*)/m}$.
- **Estatística:** inflexões da gaussiana definem o "meio" da curva.

</Porta>

<Porta nivel="25" titulo="Pra estudante de engenharia">

Convexidade e concavidade são conceitos centrais em otimização, aprendizado de máquina e análise de estabilidade.

### Convexidade global e otimização

$f$ convexa em $I$ $\Rightarrow$:
- Todo mínimo local é **global**.
- Conjunto de minimizadores é **convexo**.
- Algoritmos de gradiente convergem ao mínimo único (sob condições leves).

Consequência prática: em regressão linear, a função de perda $L(\beta) = \|y - X\beta\|^2$ é convexa quadrática (hessiana $H = 2X^TX$ semi-definida positiva) — logo $\nabla L = 0$ dá solução global única: $\hat\beta = (X^TX)^{-1}X^Ty$.

### Funções convexas canônicas

- $x^2$, $e^x$, $-\ln x$ (em $(0,\infty)$), $x\ln x$, $\max(0, x)$ (ReLU).
- Soma de convexas é convexa. Máximo pontual de convexas é convexo.
- $h(g(x))$ é convexa se $h$ convexa e crescente, $g$ convexa.

### Sigmoid: não-convexa, inflexão em 0

$\sigma(x) = 1/(1 + e^{-x})$ tem $\sigma''(x) = \sigma(x)(1 - \sigma(x))(1 - 2\sigma(x))$. Muda sinal em $x = 0$ — ponto de inflexão. Redes neurais com sigmoid são **não-convexas** no espaço de parâmetros, o que torna a otimização global intratável (daí o apelo de ReLU, que mantém convexidade por partes).

### Inequação de Jensen — aplicação em ML

$f$ convexa $\Rightarrow$ $E[f(X)] \geq f(E[X])$. Com $f = -\ln$: $E[-\ln X] \geq -\ln E[X]$, base da entropia. Com $f = -\ln$ e $X$ distribuição de probabilidade: derivação do ELBO (Evidence Lower Bound) em VAEs.

### Hessiana e paisagem de otimização

Hessiana $H(x)$ no ponto crítico: autovalores positivos = mínimo local, negativos = máximo, mistos = ponto de sela. Em redes neurais profundas, pontos críticos são quase todos **selas**, não mínimos locais — implicação para convergência de SGD.

</Porta>

<Porta nivel="40" titulo="Pra profissional sênior">

Convexidade é coração da otimização moderna, com ramificações profundas em geometria, probabilidade e física estatística.

### Análise convexa (Rockafellar, 1970)

Conceitos centrais:
- **Subdiferencial** $\partial f(x)$: generaliza $\nabla f$ para funções não-diferenciáveis convexas (e.g., $|x|$).
- **Conjugado de Fenchel:** $f^*(p) = \sup_x(p\cdot x - f(x))$. Dualidade fundamental: $(f^*)^* = f$ (sob semicontinuidade).
- **Função suporte** $\sigma_C(x) = \sup_{y \in C} \langle x, y\rangle$ caracteriza conjuntos convexos — base de programação linear.

### Geometria diferencial

Hessiana define forma local de segunda ordem: assinatura (positiva definida, indefinida, etc.) determina extremo vs sela. Em variedades Riemannianas, **curvatura seccional** generaliza $f''$: positiva = bola, negativa = plano hiperbólico. Curvatura de Ricci aparece em relatividade geral.

### Mecânica estatística e transições de fase

Energia livre $F(\beta) = -\ln Z(\beta)$ é **convexa** em $\beta = 1/T$ — implica calor específico $C_V = k_B\beta^2\partial_\beta^2 \ln Z \geq 0$ (segunda lei). Inflexões de $F(T)$ marcam transições de fase de segunda ordem (Landau, 1937).

### Teoria de otimização convexa

Problemas convexos são resolvíveis em tempo polinomial (Khachiyan 1979 — elipsoide; Karmarkar 1984 — pontos interiores). Hierarquia: LP ⊂ QP ⊂ SOCP ⊂ SDP. Bibliotecas: CVXPY, MOSEK, Gurobi.

### ML: paisagem de perda e generalização

Keskar et al. (2017) mostram que mínimos "largos" (autovalores pequenos da hessiana) generalizam melhor que mínimos "estreitos". Intuição: mínimo largo é mais robusto a perturbações do training set — corresponde a $f'' \ll 1$ em torno do mínimo. Pesquisa ativa em 2024–2026.

### Conexão histórica

Hadamard (1893) usa $f''$ para critério de extremos rigoroso. Minkowski (1896) geometria convexa. Carathéodory, Fenchel, Moreau consolidam análise convexa no séc. XX. Boyd & Vandenberghe (2004) tornam canônico em engenharia com o livro *Convex Optimization* (disponível gratuitamente em [web.stanford.edu/~boyd/cvxbook](https://web.stanford.edu/~boyd/cvxbook/)).

</Porta>

<Porta nivel="pratica" titulo="Aplicação prática">

**Receita em 5 passos:**

1. **Calcule $f''(x)$.**
2. **Iguale a zero** e ache candidatos a inflexão.
3. **Teste o sinal** de $f''$ em cada subintervalo. Mudança de sinal = inflexão.
4. **Classifique pontos críticos** de $f$ ($f' = 0$): sinal de $f''$ determina mínimo, máximo ou teste inconclusivo.
5. **Monte tabela de concavidade** + esboço.

### Tabela padrão

| Intervalo | Sinal de $f''$ | Concavidade | Comportamento visual |
|---|---|---|---|
| $(-\infty, x_0)$ | $+$ | cima | $\cup$ |
| $(x_0, +\infty)$ | $-$ | baixo | $\cap$ |

Troca de sinal em $x_0$ confirma inflexão.

### Verificação Python/SymPy

```python
import sympy as sp
x = sp.symbols('x')
f = x**3 - 3*x
f2 = sp.diff(f, x, 2)
inflexoes = sp.solve(f2, x)  # candidatos
print("f'' =", f2, "| candidatos:", inflexoes)
# Verificar sinal em torno de cada candidato
```

### Aplicação: conta de margem decrescente

Custo de produção com rendimentos decrescentes: $C(q) = q^3 - 6q^2 + 15q + 100$. $C'' = 6q - 12 = 0$ em $q = 2$. Para $q < 2$: $C'' < 0$ (custo marginal decrescente, ganho de escala). Para $q > 2$: $C'' > 0$ (custo marginal crescente, pressão de capacidade). Inflexão em $q = 2$ = ponto de **mudança de regime de produção**.

</Porta>

</DuasPortas>

## Exemplos resolvidos

<Exemplo titulo="Concavidade e inflexao de f(x) = x^3 - 3x (basico)" numero="1">

**Problema.** Para $f(x) = x^3 - 3x$, determine os intervalos de concavidade e os pontos de inflexão.

**Estratégia.** Calcular $f''$, determinar onde é positivo/negativo, e verificar mudança de sinal nos zeros.

**Resolução.**

1. **Derivadas:** $f'(x) = 3x^2 - 3$, $f''(x) = 6x$.
2. **Zero de $f''$:** $6x = 0 \Rightarrow x = 0$.
3. **Sinal:** $f''(x) < 0$ para $x < 0$ (côncava para baixo); $f''(x) > 0$ para $x > 0$ (côncava para cima).
4. **Inflexão:** $f''$ muda de sinal em $x = 0$. Logo $(0, f(0)) = (0, 0)$ é ponto de inflexão.

**Verificação.** No esboço: $f(-2) = -8 + 6 = -2$; $f(0) = 0$; $f(2) = 8 - 6 = 2$. Curva sobe de $-2$ para $0$ com concavidade para baixo, depois continua de $0$ para $2$ com concavidade para cima — consistente. ✓

**Fonte.** [APEX Calculus §3.4, Example 3.4.1](https://www.apexcalculus.com/downloads) — CC-BY-NC.
</Exemplo>

<Exemplo titulo="Teste da segunda derivada para extremos de f(x) = x^4 - 4x^2 (intermediario)" numero="2">

**Problema.** Classifique os pontos críticos de $f(x) = x^4 - 4x^2$ usando o teste da segunda derivada.

**Estratégia.** Encontrar pontos críticos via $f' = 0$, calcular $f''$ em cada um e aplicar o critério.

**Resolução.**

1. **$f'(x) = 4x^3 - 8x = 4x(x^2 - 2) = 0 \Rightarrow x = 0,\, x = \pm\sqrt{2}$.**
2. **$f''(x) = 12x^2 - 8$.**
3. **Avaliando:**
   - $f''(0) = -8 < 0$ $\Rightarrow$ **máximo local** em $x = 0$. $f(0) = 0$.
   - $f''(\sqrt{2}) = 24 - 8 = 16 > 0$ $\Rightarrow$ **mínimo local** em $x = \sqrt{2}$. $f(\sqrt{2}) = 4 - 8 = -4$.
   - $f''(-\sqrt{2}) = 16 > 0$ $\Rightarrow$ **mínimo local** em $x = -\sqrt{2}$. $f(-\sqrt{2}) = -4$.

**Verificação.** Como $f(x) \to +\infty$ para $x \to \pm\infty$, os mínimos em $\pm\sqrt{2}$ são globais e o máximo em $0$ é local (mas não global). ✓

**Fonte.** [OpenStax Calculus Vol. 1 §4.5, Example 4.38](https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph) — CC-BY-NC-SA.
</Exemplo>

<Exemplo titulo="Inflexoes da gaussiana (intermediario)" numero="3">

**Problema.** Encontre os pontos de inflexão de $f(x) = e^{-x^2/2}$ (núcleo da distribuição normal).

**Estratégia.** Derivar duas vezes, igualar $f'' = 0$ e verificar mudança de sinal.

**Resolução.**

1. **$f'(x) = -x\,e^{-x^2/2}$.**
2. **$f''(x) = -e^{-x^2/2} + x^2 e^{-x^2/2} = (x^2 - 1)e^{-x^2/2}$.**
3. **Zero de $f''$:** $x^2 - 1 = 0 \Rightarrow x = \pm 1$.
4. **Sinal:**
   - $|x| > 1$: $x^2 - 1 > 0 \Rightarrow f'' > 0$ (côncava para cima).
   - $|x| < 1$: $x^2 - 1 < 0 \Rightarrow f'' < 0$ (côncava para baixo).
5. **Inflexões:** em $x = \pm 1$, $f''$ muda de sinal. Pontos: $(\pm 1, e^{-1/2}) = (\pm 1, \approx 0{,}607)$.

**Verificação.** Para $\mathcal{N}(\mu, \sigma^2)$, as inflexões estão em $\mu \pm \sigma$. Aqui $\mu = 0$, $\sigma = 1$ — consistente. ✓

**Fonte.** [Active Calculus §3.1, Activity 3.1.3](https://activecalculus.org/single/sec-3-1-tests.html) — CC-BY-NC-SA.
</Exemplo>

<Exemplo titulo="Caso inconclusivo: f'' = 0 em critico nao e inflexao (avancado)" numero="4">

**Problema.** Para $f(x) = x^4$, verifique que $f''(0) = 0$ mas $x = 0$ é mínimo local (não inflexão).

**Estratégia.** Mostrar que $f''$ não muda de sinal em $x = 0$.

**Resolução.**

1. **$f'(x) = 4x^3 = 0 \Rightarrow x = 0$** (ponto crítico).
2. **$f''(x) = 12x^2 \geq 0$ para todo $x$.** Em particular $f''(0) = 0$ — teste inconclusivo.
3. **Mas $f''(x) \geq 0$ em vizinhança de $0$**: não muda de sinal.
4. **Conclusão:** $x = 0$ NÃO é inflexão. É mínimo global ($f(x) = x^4 \geq 0$ para todo $x$, com igualdade só em $0$).
5. **Confirmação pelo teste de sinal de $f'$:** $f'(x) = 4x^3 < 0$ para $x < 0$ e $f'(x) > 0$ para $x > 0$ — mínimo confirmado.

**Verificação.** O gráfico de $x^4$ é uma parábola "achatada" com o fundo em $0$ — visualmente um mínimo. ✓

**Fonte.** [APEX Calculus §3.4, Note após Example 3.4.2](https://www.apexcalculus.com/downloads) — CC-BY-NC.
</Exemplo>

<Exemplo titulo="Custo com rendimentos decrescentes — inflexao economica (modelagem)" numero="5">

**Problema.** Uma empresa tem custo de produção $C(q) = q^3 - 6q^2 + 15q + 100$ (em reais). Encontre o ponto de inflexão e interprete economicamente.

**Estratégia.** Calcular $C''$, achar a inflexão e analisar o comportamento do custo marginal $MC = C'$.

**Resolução.**

1. **$C'(q) = 3q^2 - 12q + 15 = MC(q)$** (custo marginal).
2. **$C''(q) = 6q - 12$.**
3. **Inflexão:** $C''(q) = 0 \Rightarrow q = 2$.
4. **Sinal de $C''$:** para $q < 2$, $C'' < 0$ (custo marginal decrescente — ganhos de escala). Para $q > 2$, $C'' > 0$ (custo marginal crescente — pressão de capacidade).
5. **Ponto de inflexão:** $C(2) = 8 - 24 + 30 + 100 = 114$. Ponto $(2, 114)$.

**Verificação.** $MC(0) = 15$, $MC(2) = 12 - 24 + 15 = 3$, $MC(4) = 48 - 48 + 15 = 15$. Mínimo de $MC$ em $q = 2$ — consistente com inflexão de $C$. ✓

**Fonte.** [Active Calculus §3.1, Exercise 3.1.5](https://activecalculus.org/single/sec-3-1-tests.html) — CC-BY-NC-SA.
</Exemplo>

<ListaExercicios seed="licao-66-concavidade">

### Bloco A — Sinal de f'' direto (66.1 a 66.10)

<Exercicio
  numero="66.1"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "côncava para cima em $(0,\\infty)$" },
    { texto: "sem PI; sempre côncava para cima" },
    { texto: "côncava para baixo em $(-\\infty,0)$; PI em $x=0$" },
    { texto: "côncava para cima em $(-\\infty,0)$; côncava para baixo em $(0,\\infty)$; PI em $x=0$", correta: true },
  ]}
  solucao={<>Derivada segunda f(x)=x^3: f-dupla-prima(x)=6x. Negativa para x menor que 0 (côncava para baixo), positiva para x maior que 0 (côncava para cima). f-dupla-prima(0)=0 com mudança de sinal: PI em x=0.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Determine a concavidade de $f(x) = x^2$ em todo $\mathbb{R}$. Há inflexão?
</Exercicio>

<Exercicio
  numero="66.2"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "$f''(x)=0$; sem PI" },
    { texto: "$f''$ muda de sinal em $x=0$" },
    { texto: "$f''(x)>0$ em toda a reta; convexa; sem PI", correta: true },
    { texto: "$f''(x)<0$; côncava para baixo" },
  ]}
  solucao={<><Eq>{`f'' = 6x`}</Eq>. Para <Eq>{`x < 0`}</Eq>: côncava para baixo. Para <Eq>{`x > 0`}</Eq>: côncava para cima. Inflexão em <Eq>{`x = 0`}</Eq> porque <Eq>{`f''`}</Eq> muda de sinal.</>}
  passos={<>
    <ol>
      <li>Calcule derivadas: <Eq>{`f'(x) = 3x^2`}</Eq>, <Eq>{`f''(x) = 6x`}</Eq>.</li>
      <li>Zero de <Eq>{`f''`}</Eq>: <Eq>{`6x = 0 \\Rightarrow x = 0`}</Eq>.</li>
      <li>Sinal: <Eq>{`x < 0 \\Rightarrow f'' < 0`}</Eq> (chapéu); <Eq>{`x > 0 \\Rightarrow f'' > 0`}</Eq> (tigela).</li>
      <li>Mudança de sinal em <Eq>{`x = 0`}</Eq> confirma inflexão. Ponto: <Eq>{`(0, 0)`}</Eq>.</li>
      <li><em>Curiosidade:</em> o ponto de inflexão de <Eq>{`x^3`}</Eq> é também o único zero — a curva atravessa o eixo x exatamente onde muda de concavidade.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Determine a concavidade e os pontos de inflexão de $f(x) = x^3$.
</Exercicio>

<Exercicio
  numero="66.3"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "PI somente em $x=1$" },
    { texto: "PIs em $x=0$ e $x=2$; côncava para cima em $(-\\infty,0)$ e $(2,\\infty)$", correta: true },
    { texto: "PI em $x=0$; côncava para baixo em $(0,2)$ ✓ (resposta correta acima)" },
    { texto: "sempre côncava para cima" },
  ]}
  solucao={<><Eq>{`f'' = 12x^2 \\geq 0`}</Eq> para todo <Eq>{`x`}</Eq>. Não muda de sinal — sem inflexão. Côncava para cima em todo <Eq>{`\\mathbb{R}`}</Eq> (convexidade global).</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Concavidade de $f(x) = x^4$. Há inflexão em $x = 0$? Justifique com o sinal de $f''$.
</Exercicio>

<Exercicio
  numero="66.4"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "máximo local em $x=-1$; mínimo local em $x=1$; PI em $x=0$", correta: true },
    { texto: "PI em $x=\\pm1$" },
    { texto: "máximo em $x=0$" },
    { texto: "máximo em $x=1$; mínimo em $x=-1$" },
  ]}
  solucao={<>f″(x)=6x: f″(-1)=-6 &lt; 0 → máximo local; f″(1)=6 &gt; 0 → mínimo local; f″(0)=0 com mudança de sinal: PI em x=0.</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Concavidade de $f(x) = e^x$ em todo $\mathbb{R}$. Há inflexão?
</Exercicio>

<Exercicio
  numero="66.5"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "$f''(c)>0$: côncava para cima em $c$ → mínimo local", correta: true },
    { texto: "$f''(c)>0$ → inconclusivo" },
    { texto: "$f''(c)>0$ → ponto de inflexão" },
    { texto: "$f''(c)>0$ → máximo local" },
  ]}
  solucao={<>Se f'(c)=0 e f″(c)&gt;0: f é côncava para cima em c, portanto c é mínimo local.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Concavidade de $f(x) = \ln x$ em $(0, \infty)$.
</Exercicio>

<Exercicio
  numero="66.6"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "PI em $x=0$" },
    { texto: "PIs em $x=\\pm1/\\sqrt{3}$", correta: true },
    { texto: "PIs em $x=\\pm1$" },
    { texto: "sem PI" },
  ]}
  solucao={<><Eq>{`f'' = -\\sin x`}</Eq>. Zero em <Eq>{`x = 0, \\pi, 2\\pi`}</Eq>. Inflexões em <Eq>{`x = 0, \\pi, 2\\pi`}</Eq> (onde <Eq>{`f''`}</Eq> muda de sinal). Para <Eq>{`x \\in (0, \\pi)`}</Eq>: <Eq>{`f'' < 0`}</Eq> (chapéu). Para <Eq>{`x \\in (\\pi, 2\\pi)`}</Eq>: <Eq>{`f'' > 0`}</Eq> (tigela).</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Concavidade de $f(x) = \sin x$ em $[0, 2\pi]$. Identifique os pontos de inflexão.
</Exercicio>

<Exercicio
  numero="66.7"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "PI em $x=0$" },
    { texto: "côncava para cima em toda a reta" },
    { texto: "côncava para cima em $(-\\infty,\\ln 2)$; côncava para baixo em $(\\ln 2,\\infty)$; PI em $x=\\ln 2$", correta: true },
    { texto: "côncava para baixo em toda a reta" },
  ]}
  solucao={<>f″(x)=e^x: sempre positivo → côncava para cima em toda a reta, sem PI.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Concavidade de $f(x) = \cos x$ em $[0, 2\pi]$. Pontos de inflexão.
</Exercicio>

<Exercicio
  numero="66.8"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "PI em $x=e$" },
    { texto: "côncava para cima em $(-\\infty,0)$" },
    { texto: "côncava para baixo em $(0,\\infty)$" },
    { texto: "côncava para cima em $(0,\\infty)$; sem PI", correta: true },
  ]}
  solucao={<>Para f(x)=ln(x): f″(x)=-1/x^2, sempre negativo no domínio x&gt;0. Côncava para baixo; sem PI.</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Concavidade de $f(x) = 1/x$ nos intervalos $(0,\infty)$ e $(-\infty,0)$.
</Exercicio>

<Exercicio
  numero="66.9"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "$f''<0$: ponto de inflexão" },
    { texto: "$f''<0$: côncava para baixo → máximo local em $c$", correta: true },
    { texto: "$f''<0$: inconclusivo" },
    { texto: "$f''<0$: mínimo local em $c$" },
  ]}
  solucao={<>Se f'(c)=0 e f″(c)&lt;0: f é côncava para baixo em c, portanto c é máximo local.</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Concavidade de $f(x) = e^{-x^2/2}$ (gaussiana). Identifique os pontos de inflexão.
</Exercicio>

<Exercicio
  numero="66.10"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "PIs em $x=0$ e $x=\\pi$", correta: true },
    { texto: "PI em $x=\\pi/2$" },
    { texto: "sem PI" },
    { texto: "PI somente em $x=0$" },
  ]}
  solucao={<><Eq>{`f'' = 6x - 6`}</Eq>. Zero em <Eq>{`x = 1`}</Eq>. Inflexão em <Eq>{`(1, f(1)) = (1, 1 - 3 + 2) = (1, 0)`}</Eq>. Côncava para baixo em <Eq>{`(-\\infty, 1)`}</Eq>, para cima em <Eq>{`(1, \\infty)`}</Eq>.</>}
  passos={<>
    <ol>
      <li>Calcule <Eq>{`f'(x) = 3x^2 - 6x`}</Eq> e <Eq>{`f''(x) = 6x - 6`}</Eq>.</li>
      <li>Zero de <Eq>{`f''`}</Eq>: <Eq>{`6x - 6 = 0 \\Rightarrow x = 1`}</Eq>.</li>
      <li>Sinal: <Eq>{`x < 1 \\Rightarrow f'' < 0`}</Eq>; <Eq>{`x > 1 \\Rightarrow f'' > 0`}</Eq>. Mudança confirmada.</li>
      <li>Ponto: <Eq>{`f(1) = 1 - 3 + 2 = 0`}</Eq>. Inflexão em <Eq>{`(1, 0)`}</Eq>.</li>
      <li><em>Macete:</em> para polinômios cúbicos <Eq>{`ax^3 + bx^2 + \\cdots`}</Eq>, o ponto de inflexão está sempre em <Eq>{`x = -b/(3a)`}</Eq> — o centro de simetria da cúbica.</li>
    </ol>
  </>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Concavidade e inflexão de $f(x) = x^3 - 3x^2 + 2$.
</Exercicio>

### Bloco B — Teste da segunda derivada para extremos (66.11 a 66.20)

<Exercicio
  numero="66.11"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "sempre côncava para baixo" },
    { texto: "côncava para cima em $(-1,1)$" },
    { texto: "PIs em $x=0$" },
    { texto: "côncava para baixo em $(-1,1)$; para cima fora; PIs em $x=\\pm1$", correta: true },
  ]}
  solucao={<><Eq>{`f' = 3x^2 - 12 = 0 \\Rightarrow x = \\pm 2`}</Eq>. <Eq>{`f'' = 6x`}</Eq>. <Eq>{`f''(2) = 12 > 0`}</Eq>: mínimo em <Eq>{`(2, -16)`}</Eq>. <Eq>{`f''(-2) = -12 < 0`}</Eq>: máximo em <Eq>{`(-2, 16)`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Use o teste de $f''$: classifique os extremos de $f(x) = x^3 - 12x$.
</Exercicio>

<Exercicio
  numero="66.12"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "côncava para cima em toda a reta; convexa global", correta: true },
    { texto: "côncava para baixo" },
    { texto: "côncava para cima somente em $x>0$" },
    { texto: "PI em $x=0$" },
  ]}
  solucao={<>f″(x)=e^x &gt; 0 para todo x → côncava para cima em toda a reta; função globalmente convexa.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Extremos de $f(x) = x^4 - 4x^2$ via teste de $f''$.
</Exercicio>

<Exercicio
  numero="66.13"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "máximo em $x=0$; sem PI" },
    { texto: "máximo local em $x=\\pi/4$; mínimo em $x=5\\pi/4$; PIs onde $f''=0$", correta: true },
    { texto: "máximo em $x=\\pi/2$; mínimo em $x=3\\pi/2$" },
    { texto: "sem extremos" },
  ]}
  solucao={<><Eq>{`f'(x) = e^{-x}(1-x) = 0 \\Rightarrow x = 1`}</Eq>. <Eq>{`f''(x) = e^{-x}(x-2)`}</Eq>. <Eq>{`f''(1) = e^{-1}(-1) < 0`}</Eq>: máximo em <Eq>{`(1, e^{-1})`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Extremos de $f(x) = x e^{-x}$ via $f''$.
</Exercicio>

<Exercicio
  numero="66.14"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "PI em $x=1$; côncava para cima em $(1,\\infty)$", correta: true },
    { texto: "sem PI" },
    { texto: "PI em $x=0$" },
    { texto: "côncava para cima em toda a reta" },
  ]}
  solucao={<>f″(x)=(x-2)e^(-x)=0 em x=2; muda de sinal: PI em x=2, côncava para cima em (2, ∞).</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Extremos de $f(x) = x^2 \ln x$ em $(0, \infty)$ via $f''$.
</Exercicio>

<Exercicio
  numero="66.15"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "$x=0$ é PI porque $f''(0)=0$" },
    { texto: "$f''$ não muda de sinal — $x=0$ não é PI de $f(x)=x^4$", correta: true },
    { texto: "$x=0$ é PI de toda função com $f''(0)=0$" },
    { texto: "PI ocorre sempre que $f'''(0)\\neq0$" },
  ]}
  solucao={<><Eq>{`f'(x) = \\cos x + \\cos(2x) = 0`}</Eq>. Usando <Eq>{`\\cos(2x) = 2\\cos^2 x - 1`}</Eq>: <Eq>{`2\\cos^2 x + \\cos x - 1 = 0 \\Rightarrow \\cos x = 1/2`}</Eq> ou <Eq>{`-1`}</Eq>. Críticos: <Eq>{`x = \\pi/3, \\pi, 5\\pi/3`}</Eq>. Aplique <Eq>{`f''`}</Eq> em cada um.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Extremos de $f(x) = \sin x + \frac{1}{2}\sin(2x)$ em $[0, 2\pi]$.
</Exercicio>

<Exercicio
  numero="66.16"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "sem PI" },
    { texto: "côncava para cima em $(0,\\infty)$" },
    { texto: "PI em $x=1$" },
    { texto: "côncava para baixo em $(0,\\infty)$; para cima em $(-\\infty,0)$", correta: true },
  ]}
  solucao={<>Demonstrar que <Eq>{`x = 0`}</Eq> é mínimo de <Eq>{`x^4`}</Eq> mesmo com <Eq>{`f''(0) = 0`}</Eq>: use o fato que <Eq>{`f(x) = x^4 \\geq 0`}</Eq> para todo <Eq>{`x`}</Eq>, e <Eq>{`f(0) = 0`}</Eq> é o menor valor possível.</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Mostre que $f(x) = x^4$ tem mínimo em $x = 0$ apesar de $f''(0) = 0$ (teste inconclusivo).
</Exercicio>

<Exercicio
  numero="66.17"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "PI em $x=-1$; côncava para cima em $(-1,\\infty)$", correta: true },
    { texto: "PI em $x=0$" },
    { texto: "PI em $x=1$" },
    { texto: "côncava para cima em toda a reta" },
  ]}
  solucao={<>Para <Eq>{`f(x) = x^5`}</Eq>: <Eq>{`f'(0) = 0`}</Eq> e <Eq>{`f''(0) = 0`}</Eq> (inconclusivo). Mas <Eq>{`f'(x) = 5x^4 \\geq 0`}</Eq> para todo <Eq>{`x`}</Eq>, com igualdade só em <Eq>{`0`}</Eq>. Portanto <Eq>{`f`}</Eq> é crescente em toda vizinhança de <Eq>{`0`}</Eq> — não há extremo local.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Mostre que $f(x) = x^5$ não tem extremo em $x = 0$ apesar de $f'(0) = 0$.
</Exercicio>

<Exercicio
  numero="66.18"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "$f''(x)>0$ iff $f'(x)>0$" },
    { texto: "$f''(x)>0$ iff $f$ crescente" },
    { texto: "$f''(x)>0$ iff $f'$ crescente iff gráfico curva para cima", correta: true },
    { texto: "$f''(x)>0$ iff gráfico convexo para baixo" },
  ]}
  solucao={<>f″(x)&gt;0 significa que f' é crescente — a inclinação da tangente aumenta da esquerda para a direita, caracterizando concavidade para cima.</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Para $f(x) = x^2 + 1/x$ em $x > 0$: ache o mínimo e justifique com $f''$.
</Exercicio>

<Exercicio
  numero="66.19"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "PIs em $x=\\pm2$" },
    { texto: "sem PI" },
    { texto: "PIs em $x=\\pm\\sqrt{2}$", correta: true },
    { texto: "PI em $x=0$" },
  ]}
  solucao={<><Eq>{`f'(x) = (1 - \\ln x)/x^2 = 0 \\Rightarrow x = e`}</Eq>. <Eq>{`f''(e) = (-3 + 2)/e^3 = -1/e^3 < 0`}</Eq>: máximo. <Eq>{`f(e) = 1/e`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Extremos de $f(x) = \ln x / x$ em $(0, \infty)$ via $f''$.
</Exercicio>

<Exercicio
  numero="66.20"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "PI em $x=1$; côncava para cima em $x>1$", correta: true },
    { texto: "côncava para cima em toda reta" },
    { texto: "PI em $x=2$" },
    { texto: "PI em $x=0$" },
  ]}
  solucao={<>f″(x)=6(x-1)=0 em x=1; muda de sinal: PI em x=1, côncava para cima em (1, ∞).</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Extremos de $f(x) = x^{1/x}$ em $(0, \infty)$ (tome $\ln f$ antes de derivar).
</Exercicio>

### Bloco C — Modelagem e aplicações (66.21 a 66.30)

<Exercicio
  numero="66.21"
  dificuldade="modelagem"
  opcoes={[
    { texto: "PI em $x=0$" },
    { texto: "côncava para baixo em $(-\\pi/2,\\pi/2)$; sem PI no intervalo", correta: true },
    { texto: "PI em $x=\\pi/4$" },
    { texto: "côncava para cima em $(-\\pi/2,\\pi/2)$" },
  ]}
  solucao={<><Eq>{`C''(q) = 6q - 12 = 0 \\Rightarrow q = 2`}</Eq>. Para <Eq>{`q < 2`}</Eq>: custo marginal decrescente (ganho de escala). Para <Eq>{`q > 2`}</Eq>: crescente (pressão de capacidade). Inflexão em <Eq>{`q = 2`}</Eq> é o ponto de mudança de regime.</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Custo $C(q) = q^3 - 6q^2 + 9q + 100$. Ache a inflexão e interprete como mudança de retorno marginal.
</Exercicio>

<Exercicio
  numero="66.22"
  dificuldade="modelagem"
  opcoes={[
    { texto: "PI em $x=\\pi$" },
    { texto: "PIs em $x=\\pi/2$ e $x=3\\pi/2$" },
    { texto: "sem PI no intervalo" },
    { texto: "PIs em $x=0$ e $x=2\\pi$", correta: true },
  ]}
  solucao={<>f″(x)=-sen(x)=0 em x=0, π, 2π. PI em x=0, π e 2π no intervalo [0, 2π].</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Lucro $\pi(q) = -q^3 + 30q^2 - 100q$. Maximize via $\pi'$ e confirme com $\pi''$.
</Exercicio>

<Exercicio
  numero="66.23"
  dificuldade="modelagem"
  opcoes={[
    { texto: "côncava para cima quando $x>2$; para baixo quando $0<x<2$; PI em $x=2$", correta: true },
    { texto: "côncava para baixo em toda reta" },
    { texto: "côncava para cima em $(0,\\infty)$" },
    { texto: "PI em $x=1$" },
  ]}
  solucao={<>Para <Eq>{`P(t) = K/(1 + e^{-rt})`}</Eq>: <Eq>{`P'' = rP(r - 2rP/K)`}</Eq>. Igualar a zero: <Eq>{`P = K/2`}</Eq>, que ocorre em <Eq>{`t = 0`}</Eq> (quando centramos em zero). Inflexão em <Eq>{`P = K/2`}</Eq>: ponto de máximo crescimento da população.</>}
  passos={<>
    <ol>
      <li>Seja <Eq>{`P = K/(1+e^{-rt})`}</Eq>. Calcule <Eq>{`P' = rP(1 - P/K)`}</Eq> (taxa logística).</li>
      <li>Derive novamente: <Eq>{`P'' = r P' (1 - 2P/K)`}</Eq>.</li>
      <li>Iguale a zero: <Eq>{`P = 0`}</Eq> (trivial) ou <Eq>{`P = K/2`}</Eq>.</li>
      <li>Em <Eq>{`P = K/2`}</Eq>: <Eq>{`P''`}</Eq> muda de sinal — inflexão.</li>
      <li><em>Observação:</em> O ponto de inflexão da curva logística corresponde ao máximo da taxa de crescimento — a "virada" da epidemia ou da expansão de mercado.</li>
    </ol>
  </>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Curva logística $P(t) = K/(1 + e^{-rt})$. Mostre que há inflexão em $P = K/2$ (metade da capacidade de suporte).
</Exercicio>

<Exercicio
  numero="66.24"
  dificuldade="modelagem"
  opcoes={[
    { texto: "PI em $x=\\pi/4+k\\pi/2$" },
    { texto: "PI em $x=k\\pi$" },
    { texto: "sem PI" },
    { texto: "PI em $x=\\pi/2 + k\\pi$", correta: true },
  ]}
  solucao={<>f″(x)=2sec^2(x)tan(x)=0 quando tan(x)=0, i.e., em x=kπ. Mudança de sinal confirmada: PIs em x=kπ.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Energia potencial $U(x) = -\cos x$ (pêndulo). Encontre equilíbrios estáveis e instáveis usando $U''$.
</Exercicio>

<Exercicio
  numero="66.25"
  dificuldade="modelagem"
  opcoes={[
    { texto: "$a=-1$, $b=3$ (para que $f''$ mude de sinal em $x=1$)", correta: true },
    { texto: "$a=3$, $b=-1$" },
    { texto: "$a=1$, $b=-3$" },
    { texto: "$a=0$, $b=0$" },
  ]}
  solucao={<><Eq>{`U(x) = \\frac{1}{2}kx^2`}</Eq>. <Eq>{`U'(x) = kx = 0`}</Eq> em <Eq>{`x = 0`}</Eq>. <Eq>{`U''(x) = k > 0`}</Eq>: côncava para cima. Mínimo em <Eq>{`x = 0`}</Eq> — equilíbrio estável. Frequência natural de oscilação: <Eq>{`\\omega = \\sqrt{k/m}`}</Eq>.</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Mola harmônica: $U(x) = \frac{1}{2}kx^2$. Mostre que $x = 0$ é equilíbrio estável usando $U''$.
</Exercicio>

<Exercicio
  numero="66.26"
  dificuldade="modelagem"
  opcoes={[
    { texto: "PI ocorre onde $f'=0$" },
    { texto: "côncavo significa $f''>0$" },
    { texto: "convexa $\\Rightarrow$ $f''=0$ em algum ponto" },
    { texto: "função convexa $\\Rightarrow$ reta secante acima do gráfico; PI iff $f''$ muda de sinal", correta: true },
  ]}
  solucao={<>Função convexa: secante sempre acima do gráfico. PI ocorre onde f″ muda de sinal — não apenas onde f″=0.</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Entropia de Bernoulli $H(p) = -p\ln p - (1-p)\ln(1-p)$. Mostre $H'' < 0$ e que o máximo é em $p = 1/2$.
</Exercicio>

<Exercicio
  numero="66.27"
  dificuldade="modelagem"
  opcoes={[
    { texto: "côncava para cima em toda reta" },
    { texto: "côncava para cima em $(-\\infty,0)$; côncava para baixo em $(0,\\infty)$; PI em $x=0$", correta: true },
    { texto: "PI em $x=1$" },
    { texto: "côncava para baixo em toda reta" },
  ]}
  solucao={<>Curva de aprendizado: <Eq>{`L(t) = 1 - e^{-kt}`}</Eq>. <Eq>{`L'' = -k^2 e^{-kt} < 0`}</Eq>. Côncava para baixo: os ganhos de aprendizado diminuem ao longo do tempo (retornos decrescentes). Sem inflexão.</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Curva de aprendizagem $L(t) = 1 - e^{-kt}$. Determine a concavidade. O que ela diz sobre a velocidade de aprendizado?
</Exercicio>

<Exercicio
  numero="66.28"
  dificuldade="modelagem"
  opcoes={[
    { texto: "PI em $x=\\pi+2k\\pi$", correta: true },
    { texto: "sem PI" },
    { texto: "PI em $x=\\pi/2+2k\\pi$" },
    { texto: "PI em $x=2k\\pi$" },
  ]}
  solucao={<>f″(x)=-cos(x)=0 em x=π/2+kπ; mudança de sinal confirmada: PIs em x=π/2+kπ.</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Numa epidemia, o pico de novos casos ocorre no ponto de inflexão da curva de casos acumulados $f(t)$. Justifique geometricamente e via $f''$.
</Exercicio>

<Exercicio
  numero="66.29"
  dificuldade="modelagem"
  opcoes={[
    { texto: "convexa: $f(\\lambda x+(1-\\lambda)y)\\leq\\lambda f(x)+(1-\\lambda)f(y)$", correta: true },
    { texto: "convexa: $f'>0$" },
    { texto: "convexa: $f''(x)\\geq0$ para todo $x$" },
    { texto: "convexa: $f(x+y)=f(x)+f(y)$" },
  ]}
  solucao={<>Utilidade <Eq>{`U(W) = \\ln W`}</Eq>: <Eq>{`U'' = -1/W^2 < 0`}</Eq> — côncava para baixo. Por Jensen: <Eq>{`E[U(W)] \\leq U(E[W])`}</Eq>. Isso significa que o agente prefere o valor esperado garantido à loteria — definição de aversão ao risco.</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Utilidade $U(W) = \ln W$ é côncava. Explique como a desigualdade de Jensen implica aversão ao risco para esse investidor.
</Exercicio>

<Exercicio
  numero="66.30"
  dificuldade="modelagem"
  opcoes={[
    { texto: "PI em $x=e^2$" },
    { texto: "PI em $x=1$" },
    { texto: "PI em $x=e$" },
    { texto: "PI em $x=e^{1/2}$ (ou $x=\\sqrt{e}$)", correta: true },
  ]}
  solucao={<>Para f(x)=x·ln(x): f″(x)=1/x &gt; 0, côncava para cima, sem PI. Para ln(ln(x)): investigar f″ e sua mudança de sinal.</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Por que a função de perda da regressão linear tem um único mínimo global? Use convexidade para justificar.
</Exercicio>

### Bloco D — Compreensão e demonstrações (66.31 a 66.40)

<Exercicio
  numero="66.31"
  dificuldade="compreensao"
  opcoes={[
    { texto: "$f''(x_0) = 0$ e $f''$ muda de sinal ao passar por $x_0$.", correta: true },
    { texto: "$f''(x_0) = 0$ e $f'(x_0) = 0$." },
    { texto: "$f''(x_0) = 0$, independentemente do sinal em torno." },
    { texto: "$f'(x_0) = 0$ e $f''(x_0) = 0$, mas $f'''(x_0) \\neq 0$." },
  ]}
  solucao={<>A definição de ponto de inflexão exige mudança de sinal de <Eq>{`f''`}</Eq>. Ter <Eq>{`f'' = 0`}</Eq> sem mudança de sinal (como <Eq>{`x^4`}</Eq> em 0) não configura inflexão.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Qual é a condição correta para que $x_0$ seja ponto de inflexão de $f$?
</Exercicio>

<Exercicio
  numero="66.32"
  dificuldade="compreensao"
  opcoes={[
    { texto: "côncava para cima em toda reta" },
    { texto: "PI em $x=0$" },
    { texto: "PI em $x=1/2$; côncava para cima em $(1/2,\\infty)$", correta: true },
    { texto: "PI em $x=1$" },
  ]}
  solucao={<>Soma: <Eq>{`(f+g)'' = f'' + g''`}</Eq>. Se <Eq>{`f'' \\geq 0`}</Eq> e <Eq>{`g'' \\geq 0`}</Eq>, então <Eq>{`(f+g)'' \\geq 0`}</Eq>. Logo <Eq>{`f+g`}</Eq> é convexa.</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Prove que a soma de duas funções convexas é convexa, usando a definição via $f''$.
</Exercicio>

<Exercicio
  numero="66.33"
  dificuldade="compreensao"
  opcoes={[
    { texto: "PI em $x=0$" },
    { texto: "côncava para cima em $(-\\infty,-1)$ e $(1,\\infty)$; côncava para baixo em $(-1,1)$; PIs em $x=\\pm1$", correta: true },
    { texto: "côncava para cima somente em $(0,\\infty)$" },
    { texto: "côncava para cima em toda reta" },
  ]}
  solucao={<>Mostre que <Eq>{`f((x+y)/2) \\leq (f(x)+f(y))/2`}</Eq>: use a definição de convexidade com <Eq>{`t = 1/2`}</Eq>. Geometricamente: o ponto médio do segmento está acima do ponto médio do gráfico.</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Mostre que $f$ convexa em $I$ implica desigualdade do ponto médio: $f\!\left(\frac{x+y}{2}\right) \leq \frac{f(x)+f(y)}{2}$.
</Exercicio>

<Exercicio
  numero="66.34"
  dificuldade="compreensao"
  opcoes={[
    { texto: "PIs em $x=\\pm\\sqrt{3}/3$", correta: true },
    { texto: "sem PI" },
    { texto: "PIs em $x=\\pm1$" },
    { texto: "PI em $x=0$" },
  ]}
  solucao={<>Dê o contraexemplo <Eq>{`f(x) = x^4`}</Eq>: <Eq>{`f''(0) = 0`}</Eq> mas <Eq>{`f'' \\geq 0`}</Eq> em torno — sem mudança de sinal. Logo <Eq>{`x = 0`}</Eq> não é inflexão apesar de <Eq>{`f'' = 0`}</Eq>.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Por que $f''(x_0) = 0$ não é suficiente para garantir inflexão? Dê um contraexemplo concreto.
</Exercicio>

<Exercicio
  numero="66.35"
  dificuldade="desafio"
  opcoes={[
    { texto: "PI em $x=1$" },
    { texto: "côncava para cima em toda reta" },
    { texto: "PI em $x=-1$; côncava para cima em $(-1,\\infty)$", correta: true },
    { texto: "sem PI" },
  ]}
  solucao={<><Eq>{`\\ln`}</Eq> é côncava: <Eq>{`\\ln((x+y)/2) \\geq (\\ln x + \\ln y)/2 = \\ln\\sqrt{xy}`}</Eq>. Aplicando exponencial: <Eq>{`(x+y)/2 \\geq \\sqrt{xy}`}</Eq>. QED.</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Mostre que $\ln$ é côncava em $(0,\infty)$ e use isso para provar a desigualdade AM-GM: $(x+y)/2 \geq \sqrt{xy}$ para $x, y > 0$.
</Exercicio>

<Exercicio
  numero="66.36"
  dificuldade="desafio"
  opcoes={[
    { texto: "PI em $x=\\pi/2$; côncava para baixo em $(0,\\pi/2)$", correta: true },
    { texto: "PI em $x=\\pi$" },
    { texto: "côncava para cima em $(0,\\pi)$" },
    { texto: "sem PI" },
  ]}
  solucao={<>Huber: <Eq>{`L(x) = x^2/2`}</Eq> se <Eq>{`|x| \\leq 1`}</Eq>, <Eq>{`|x| - 1/2`}</Eq> caso contrário. Segunda derivada: <Eq>{`L'' = 1`}</Eq> para <Eq>{`|x| < 1`}</Eq>, <Eq>{`L'' = 0`}</Eq> para <Eq>{`|x| > 1`}</Eq>. <Eq>{`L''`}</Eq> não existe em <Eq>{`x = \\pm 1`}</Eq>. É convexa (não-negativa). Não é estritamente convexa.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Função de Huber $L(x) = x^2/2$ se $|x| \leq 1$; $|x| - 1/2$ caso contrário. É convexa? Onde $L''$ é descontínua?
</Exercicio>

<Exercicio
  numero="66.37"
  dificuldade="demonstracao"
  opcoes={[
    { texto: "PIs em $x=\\pm1$" },
    { texto: "PI em $x=0$" },
    { texto: "PIs em $x=\\pm1/\\sqrt{2}$; côncava para cima em $|x|>1/\\sqrt{2}$", correta: true },
    { texto: "sem PI" },
  ]}
  solucao={<>Taylor de ordem 2 em <Eq>{`x_0`}</Eq>: <Eq>{`f(x) \\approx f(x_0) + f'(x_0)(x - x_0) + \\frac{f''(x_0)}{2}(x-x_0)^2`}</Eq>. Com <Eq>{`f'(x_0) = 0`}</Eq>: <Eq>{`f(x) - f(x_0) \\approx \\frac{f''(x_0)}{2}(x-x_0)^2`}</Eq>. Se <Eq>{`f''(x_0) > 0`}</Eq>: o lado direito é positivo perto de <Eq>{`x_0`}</Eq>, logo <Eq>{`f(x) > f(x_0)`}</Eq> — mínimo local. Analogamente para <Eq>{`f'' < 0`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Vol. 1", url: "https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph", secao: "§4.5", licenca: "CC-BY-NC-SA" }}
>
Demonstre o teste da segunda derivada via polinômio de Taylor de ordem 2.
</Exercicio>

<Exercicio
  numero="66.38"
  dificuldade="demonstracao"
  opcoes={[
    { texto: "PI em $x=0$" },
    { texto: "PI em $x=1$" },
    { texto: "côncava para cima em $(0,2)$; para baixo em $(2,\\infty)$; PI em $x=2$", correta: true },
    { texto: "côncava para cima em toda reta" },
  ]}
  solucao={<>Definição de convexidade: <Eq>{`f(tx_1 + (1-t)x_2) \\leq tf(x_1) + (1-t)f(x_2)`}</Eq> para todo <Eq>{`t \\in [0,1]`}</Eq>. Com <Eq>{`t = 1/2`}</Eq> e propriedades da integral/probabilidade, deduz-se Jensen geral por indução ou continuidade.</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Demonstre a desigualdade de Jensen para dois pontos: $f(tx_1 + (1-t)x_2) \leq tf(x_1) + (1-t)f(x_2)$ — diretamente da definição de convexidade.
</Exercicio>

<Exercicio
  numero="66.39"
  dificuldade="demonstracao"
  opcoes={[
    { texto: "PI em $x=1$; $f''$ muda de negativo para positivo", correta: true },
    { texto: "sem PI" },
    { texto: "PI em $x=0$" },
    { texto: "PI em $x=2$" },
  ]}
  solucao={<>Seja <Eq>{`f`}</Eq> convexa em intervalo aberto <Eq>{`I`}</Eq>. Para <Eq>{`x_0 \\in I`}</Eq>, a função quociente <Eq>{`(f(x) - f(x_0))/(x - x_0)`}</Eq> é crescente em <Eq>{`x`}</Eq>. Isso implica que os limites laterais existem e são finitos, portanto <Eq>{`f`}</Eq> é contínua em <Eq>{`x_0`}</Eq>.</>}
  fonte={{ livro: "APEX Calculus", url: "https://www.apexcalculus.com/downloads", secao: "§3.4", licenca: "CC-BY-NC" }}
>
Demonstre que toda função convexa em intervalo aberto é contínua no interior.
</Exercicio>

<Exercicio
  numero="66.40"
  dificuldade="demonstracao"
  opcoes={[
    { texto: "$x=0$: mínimo; $x=\\pm2$: PIs" },
    { texto: "apenas PIs em $x=\\pm2$" },
    { texto: "$x=0$: máximo; sem PIs" },
    { texto: "$x=0$: PI; $x=\\pm2$: extremos locais", correta: true },
  ]}
  solucao={<>Pelo resíduo de Taylor de ordem 1 com ponto de expansão em <Eq>{`x`}</Eq>: <Eq>{`f(y) \\geq f(x) + f'(x)(y - x)`}</Eq> para todo <Eq>{`y`}</Eq>, pois <Eq>{`R_1(y) = f''(\\xi)(y-x)^2/2 \\geq 0`}</Eq>. Isso é equivalente à convexidade (suporte pela tangente).</>}
  fonte={{ livro: "Active Calculus", url: "https://activecalculus.org/single/sec-3-1-tests.html", secao: "§3.1", licenca: "CC-BY-NC-SA" }}
>
Demonstre que $f$ é convexa se e somente se o gráfico fica sempre acima de qualquer tangente: $f(y) \geq f(x) + f'(x)(y-x)$ para todo $x, y$.
</Exercicio>

</ListaExercicios>

## Fontes

- [**Active Calculus**](https://activecalculus.org/single/sec-3-1-tests.html) — Boelkins · 2024 · §3.1 Using Derivatives to Identify Extreme Values · CC-BY-NC-SA. **Fonte primária.**
- [**Calculus Volume 1**](https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph) — OpenStax · 2016 · §4.5 Derivatives and the Shape of a Graph · CC-BY-NC-SA.
- [**APEX Calculus**](https://www.apexcalculus.com/downloads) — Hartman et al. · 2024 · v5.0 · §3.4 Concavity and the Second Derivative Test · CC-BY-NC.
