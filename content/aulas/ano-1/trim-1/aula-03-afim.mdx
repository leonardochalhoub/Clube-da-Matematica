---
titulo: "Aula 3 — Funções afins (1.º grau)"
slug: "aula-03-afim"
categoria: "aulas"
subcategoria: "ano-1-trim-1"
descricao: "Função afim f(x) = ax + b. Inclinação como taxa de variação CONSTANTE — ponte conceitual para derivada."
ordem: 3
publicado: true
tags: ["função afim", "linear", "taxa de variação", "ano-1", "trim-1"]
prerrequisitos: ["aula-02-funcoes"]
autores: ["Clube da Matemática"]
atualizadoEm: "2026-04-29"
usadoEm: ["1.º ano EM"]
---

<EquacaoCanonica
  formula="f(x) = ax + b"
  legenda={<><strong>Função afim</strong>: o coeficiente <em>a</em> é a <strong>taxa de variação</strong> (quanto y muda quando x cresce 1 unidade). O <em>b</em> é o valor de <em>y</em> quando <em>x = 0</em>. Quando <em>b = 0</em>, é função <em>linear</em>. Quando <em>a = 0</em>, é função <em>constante</em>.</>}
/>

<DuasPortas>

<Porta nivel="formal" titulo="Definição e propriedades">

<Definicao titulo="Função afim">
$f: \mathbb{R} \to \mathbb{R}$ é **afim** se existem $a, b \in \mathbb{R}$ tais que $f(x) = ax + b$.
</Definicao>

- $a$: **coeficiente angular** (slope, inclinação)
- $b$: **coeficiente linear** (intercepto em y)
- Gráfico: reta. $a > 0$: crescente. $a < 0$: decrescente. $a = 0$: constante.

<Equation explicacao="Taxa de variação entre dois pontos da reta. Para função afim, esse valor é CONSTANTE — não depende de qual par de pontos você escolha.">
{`\\frac{f(x_2) - f(x_1)}{x_2 - x_1} = a`}
</Equation>

### Zero da função

$f(x) = 0 \iff x = -b/a$ (quando $a \neq 0$).

<figure className="not-prose my-5 rounded-xl border border-clube-mist-soft/40 bg-clube-surface p-4">
<svg viewBox="-10 -10 240 240" className="w-full max-w-[420px] mx-auto block">
<line x1="0" y1="120" x2="220" y2="120" stroke="currentColor" strokeOpacity="0.3"/>
<line x1="100" y1="0" x2="100" y2="220" stroke="currentColor" strokeOpacity="0.3"/>
<text x="215" y="115" fontSize="9" fontFamily="serif" fontStyle="italic" fill="currentColor">x</text>
<text x="105" y="9" fontSize="9" fontFamily="serif" fontStyle="italic" fill="currentColor">y</text>
{[-3,-2,-1,1,2,3].map(t=><g key={t}><line x1={100+t*30} x2={100+t*30} y1="118" y2="122" stroke="currentColor"/><text x={100+t*30} y="135" textAnchor="middle" fontSize="8" fill="currentColor" fillOpacity="0.5">{t}</text></g>)}
{/* y = 2x + 1, slope 2 */}
<line x1="40" y1="180" x2="190" y2="30" stroke="rgb(26 77 92)" strokeWidth="2"/>
<text x="160" y="50" fontSize="10" fontFamily="serif" fill="rgb(26 77 92)">y = 2x + 1</text>
{/* y = -x + 3, slope -1 */}
<line x1="40" y1="60" x2="190" y2="210" stroke="rgb(199 107 63)" strokeWidth="2"/>
<text x="155" y="200" fontSize="10" fontFamily="serif" fill="rgb(199 107 63)">y = −x + 3</text>
{/* triangle showing slope = Δy/Δx */}
<line x1="130" y1="60" x2="160" y2="60" stroke="rgb(232 199 122)" strokeWidth="1.5" strokeDasharray="3 2"/>
<line x1="160" y1="60" x2="160" y2="0" stroke="rgb(232 199 122)" strokeWidth="1.5" strokeDasharray="3 2"/>
<text x="143" y="55" fontSize="9" fill="rgb(201 163 95)">Δx = 1</text>
<text x="165" y="35" fontSize="9" fill="rgb(201 163 95)">Δy = 2</text>
</svg>
<figcaption className="mt-2 text-center text-xs text-clube-mist">
Funções afins. Inclinação <em>a = Δy/Δx</em> é o quanto <em>y</em> sobe quando <em>x</em> avança 1 unidade.
</figcaption>
</figure>

</Porta>

<Porta nivel="15" titulo="Pra adolescente de 15 anos">

Função afim é o tipo mais simples de função "interessante" que existe — e é a que abre a porta pro cálculo.

A regra é: $f(x) = ax + b$. Coloca um valor de $x$, multiplica por $a$, soma $b$, sai $f(x)$. Simples assim.

**Por que é importante?** Porque o gráfico é uma **reta**, e a inclinação dessa reta é exatamente $a$. Quando você anda **1 passo pra direita** no eixo $x$, sobe **a passos no eixo y**. Sempre. Em qualquer ponto da reta. Essa **constância** é o que torna a função afim "linear" no comportamento.

Daqui a algumas aulas você vai estudar **taxa instantânea de variação** (a derivada). E vai ver que: para uma função afim, a derivada é constante e igual a $a$. Para outras funções (parábolas, exponenciais), a "inclinação" muda com $x$ — e é exatamente aí que o cálculo aparece. Mas pra reta, é fácil: a inclinação é $a$, fim de papo.

**Exemplo concreto.** Você anda de carro a velocidade constante de 60 km/h. A distância percorrida é $d(t) = 60t$ (com $t$ em horas). A "inclinação" $a = 60$ é a velocidade. Faz sentido — quanto mais inclinada a reta, mais rápido você vai.

</Porta>

<Porta nivel="25" titulo="Pra estudante de engenharia">

Função afim é o ponto onde a maioria dos modelos físicos é **linearizada localmente**. Mesmo fenômenos não-lineares (queda livre com resistência do ar, dinâmica não-newtoniana) são, em primeira ordem, aproximados por afins — é o que a derivada formaliza.

### Forma reduzida vs. forma geral

- Reduzida: $y = ax + b$
- Geral: $Ax + By + C = 0$
- Paramétrica: $\{(x_0 + t\vec{u}_x, y_0 + t\vec{u}_y) : t \in \mathbb{R}\}$

Cada uma é útil em contextos diferentes. Em CAD, paramétrica. Em otimização linear, geral. Em estatística (regressão), reduzida.

### Ajuste de retas (preview de regressão linear)

Dados $(x_1, y_1), \ldots, (x_n, y_n)$, qual a reta que melhor passa por eles? **Mínimos quadrados**:

$$a = \frac{n \sum x_i y_i - \sum x_i \sum y_i}{n \sum x_i^2 - (\sum x_i)^2}, \quad b = \bar{y} - a \bar{x}$$

Você verá em detalhe no Trim 11 (estatística inferencial).

### Aplicações (lista parcial)

- **Hooke**: $F = kx$ — força elástica em função do alongamento (afim com $b = 0$, ou seja, linear)
- **Ohm**: $V = RI$ — tensão em função da corrente (linear)
- **Conversão Celsius → Fahrenheit**: $F = 1{,}8C + 32$ (afim, $a = 1{,}8$, $b = 32$)
- **Equação horária movimento uniforme**: $s(t) = s_0 + vt$ (afim em $t$, $b = s_0$, $a = v$)

</Porta>

</DuasPortas>

<ListaExercicios seed="aula-03">

<Exercicio numero="3.1" dificuldade="aplicacao" resposta="$f(2) = 7$">
Para $f(x) = 3x + 1$, calcule $f(2)$.
</Exercicio>

<Exercicio numero="3.2" dificuldade="aplicacao" resposta="2">
Qual o coeficiente angular de $f(x) = 2x - 5$?
</Exercicio>

<Exercicio numero="3.3" dificuldade="aplicacao" resposta="−5">
Qual o coeficiente linear de $f(x) = 2x - 5$?
</Exercicio>

<Exercicio numero="3.4" dificuldade="aplicacao" resposta="$x = 5/2$">
Encontre o zero de $f(x) = 2x - 5$.
</Exercicio>

<Exercicio numero="3.5" dificuldade="aplicacao" resposta="crescente">
$f(x) = 4x + 3$ é crescente, decrescente ou constante?
</Exercicio>

<Exercicio numero="3.6" dificuldade="aplicacao" resposta="decrescente">
$g(x) = -2x + 7$ é crescente, decrescente ou constante?
</Exercicio>

<Exercicio
  numero="3.7"
  dificuldade="aplicacao"
  resposta="$y = 2x + 1$"
  solucao={<>Coef. angular: <Eq>{`a = (5-1)/(2-0) = 2`}</Eq>. Linear: <Eq>{`b = 1`}</Eq> (passa por <Eq>{`(0,1)`}</Eq>). Logo <Eq>{`y = 2x + 1`}</Eq>.</>}
>
Determine a equação da reta que passa pelos pontos $(0, 1)$ e $(2, 5)$.
</Exercicio>

<Exercicio numero="3.8" dificuldade="aplicacao" resposta="$y = -3x + 7$">
Determine a equação da reta que passa por $(1, 4)$ e $(3, -2)$.
</Exercicio>

<Exercicio numero="3.9" dificuldade="compreensao">
Mostre que a taxa de variação $(f(x_2) - f(x_1))/(x_2 - x_1)$ de $f(x) = ax + b$ é constante e igual a $a$, para quaisquer $x_1 \neq x_2$.
</Exercicio>

<Exercicio numero="3.10" dificuldade="compreensao">
Duas retas $y = a_1 x + b_1$ e $y = a_2 x + b_2$ são paralelas se $a_1 = a_2$. São perpendiculares se $a_1 \cdot a_2 = -1$. Encontre uma reta perpendicular a $y = 3x + 1$ que passe por $(0, 0)$.
</Exercicio>

<Exercicio numero="3.11" dificuldade="modelagem">
A conta de luz tem cobrança fixa de R\$15,00 + R\$0,80 por kWh consumido. (a) Modele $C(k)$. (b) Quanto custa consumir 250 kWh? (c) Para qual consumo a conta atinge R\$200?
</Exercicio>

<Exercicio numero="3.12" dificuldade="modelagem">
A temperatura em Celsius e Fahrenheit relaciona-se por $F = 1{,}8C + 32$. (a) A 20°C, quanto é em °F? (b) A 100°F, quanto é em °C? (c) Existe alguma temperatura na qual $C = F$?
</Exercicio>

<Exercicio
  numero="3.13"
  dificuldade="desafio"
  referencia="ENEM-style"
  resposta="$P(t) = 1500 + 200t$"
>
Uma cidade tinha 1500 habitantes em 2020 e cresceu linearmente até 2500 em 2025. (a) Modele a população $P(t)$ tomando $t = 0$ em 2020. (b) Em qual ano a população atingirá 4000 (mantendo a taxa)?
</Exercicio>

<Exercicio numero="3.14" dificuldade="desafio">
Em uma corrida, um carro A parte da posição 0 m com velocidade constante de 30 m/s. Um carro B parte simultaneamente da posição 200 m com velocidade constante de 25 m/s, na mesma direção. Em que instante e posição os carros se encontram?
</Exercicio>

<Exercicio
  numero="3.15"
  dificuldade="demonstracao"
  solucao={<>Sejam <Eq>{`f(x) = ax+b`}</Eq> e <Eq>{`g(x) = cx+d`}</Eq>. Então <Eq>{`(f \\circ g)(x) = a(cx+d) + b = acx + (ad+b)`}</Eq>, que é afim com coeficientes <Eq>{`ac`}</Eq> e <Eq>{`ad+b`}</Eq>. ∎</>}
>
Mostre que a composição de duas funções afins é também afim.
</Exercicio>

</ListaExercicios>

<Leituras>
- **Active Calculus** §1.2-1.3 — funções lineares e taxa de variação
- **OpenStax College Algebra** §2 — equação da reta, ajustes
- **Wikilivros — Cálculo Vol 1** §3 (PT-BR)
</Leituras>
