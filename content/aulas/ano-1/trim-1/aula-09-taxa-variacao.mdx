---
titulo: "Aula 9 — TAXA DE VARIAÇÃO MÉDIA — porta de entrada do cálculo"
slug: "aula-09-taxa-variacao"
categoria: "aulas"
subcategoria: "ano-1-trim-1"
descricao: "Δy/Δx como conceito central que precede a derivada. Interpretação geométrica (inclinação) e física (velocidade média). A pergunta que abre o cálculo: 'e se Δx for muito pequeno?'"
ordem: 9
publicado: true
tags: ["taxa de variação", "derivada introdução", "Bruner", "ano-1", "trim-1"]
prerrequisitos: ["aula-08-crescimento"]
autores: ["Clube da Matemática"]
atualizadoEm: "2026-04-29"
usadoEm: ["1.º ano EM", "porta de entrada para Cálculo (Trim 5-6)"]
---

<EquacaoCanonica
  formula="\text{Taxa média} = \frac{\Delta y}{\Delta x} = \frac{f(b) - f(a)}{b - a}"
  legenda={<><strong>Taxa de variação média</strong>: o quanto <em>y</em> mudou dividido pelo quanto <em>x</em> mudou. Geometricamente, é a inclinação da <strong>reta secante</strong> que passa pelos pontos <em>(a, f(a))</em> e <em>(b, f(b))</em> do gráfico.</>}
/>

<DuasPortas>

<Porta nivel="formal" titulo="Definição e interpretação">

<Definicao titulo="Taxa de variação média de f em [a, b]">

Para $f$ definida em $[a, b]$ com $a < b$:

$$\text{TVM}(f, [a, b]) = \frac{f(b) - f(a)}{b - a}$$

</Definicao>

### Interpretação geométrica

É a **inclinação da reta secante** ao gráfico de $f$ pelos pontos $(a, f(a))$ e $(b, f(b))$.

<figure className="not-prose my-5 rounded-xl border border-clube-mist-soft/40 bg-clube-surface p-4">
<svg viewBox="-10 -10 280 240" className="w-full max-w-[460px] mx-auto block">
<line x1="0" y1="180" x2="260" y2="180" stroke="currentColor" strokeOpacity="0.3"/>
<line x1="40" y1="0" x2="40" y2="220" stroke="currentColor" strokeOpacity="0.3"/>
{/* parabola */}
{(()=>{const pts=[];for(let x=0;x<=4.5;x+=0.05){const y=x*x*0.5;pts.push(`${40+x*45},${180-y*15}`);}return <polyline points={pts.join(' ')} fill="none" stroke="rgb(26 77 92)" strokeWidth="2"/>})()}
{/* points (1, 0.5) and (3, 4.5) */}
<circle cx="85" cy="172" r="4" fill="rgb(199 107 63)"/>
<circle cx="175" cy="113" r="4" fill="rgb(199 107 63)"/>
<text x="80" y="200" fontSize="10" fontFamily="serif">a</text>
<text x="172" y="200" fontSize="10" fontFamily="serif">b</text>
<text x="20" y="175" fontSize="10" fontFamily="serif">f(a)</text>
<text x="0" y="115" fontSize="10" fontFamily="serif">f(b)</text>
{/* secante */}
<line x1="60" y1="190" x2="220" y2="85" stroke="rgb(232 199 122)" strokeWidth="2.5"/>
{/* delta */}
<line x1="85" y1="172" x2="175" y2="172" stroke="currentColor" strokeOpacity="0.5" strokeDasharray="3 3"/>
<line x1="175" y1="172" x2="175" y2="113" stroke="currentColor" strokeOpacity="0.5" strokeDasharray="3 3"/>
<text x="120" y="167" fontSize="11" fontStyle="italic" fill="currentColor" fillOpacity="0.7">Δx = b − a</text>
<text x="180" y="148" fontSize="11" fontStyle="italic" fill="currentColor" fillOpacity="0.7">Δy</text>
<text x="200" y="60" fontSize="11" fontStyle="italic" fill="rgb(201 163 95)" fontWeight="bold">secante</text>
</svg>
<figcaption className="mt-2 text-center text-xs text-clube-mist">
Reta <strong>secante</strong> (laranja) pelos pontos <em>(a, f(a))</em> e <em>(b, f(b))</em>. Sua inclinação é exatamente <em>Δy / Δx</em>, a taxa de variação média de <em>f</em> em <em>[a, b]</em>.
</figcaption>
</figure>

### Casos especiais

- $f$ afim ($f(x) = mx + n$): TVM é constante e igual a $m$, qualquer que seja o intervalo.
- $f$ quadrática: TVM varia conforme o intervalo.

### A pergunta que abre o cálculo

**E se $\Delta x \to 0$?** A reta secante "vai virando" reta tangente, e a TVM converge para a **taxa instantânea de variação** — que é exatamente a **derivada** $f'(a)$.

Isso é o tema dos Trim 5 (limites) e Trim 6 (derivadas). Esta aula é a antessala.

</Porta>

<Porta nivel="15" titulo="Pra adolescente de 15 anos">

Esta aula é diferente. **Aqui é onde o cálculo começa.**

A taxa de variação média responde à pergunta: "**em quanto $y$ mudou em relação a $x$, em média, neste intervalo?**".

Velocidade é o exemplo perfeito. Se você anda de carro de São Paulo a Campinas (98 km) em 1h20min ($= 1{,}33$ h), sua **velocidade média** é:

$$v_{\text{média}} = \frac{\Delta s}{\Delta t} = \frac{98 \text{ km}}{1{,}33 \text{ h}} \approx 73{,}5 \text{ km/h}$$

Mas durante o trajeto você passou por trechos a 110 km/h e por trechos a 30 km/h. A velocidade não foi 73,5 o tempo todo — foi 73,5 **na média**. A **velocidade instantânea** num momento qualquer pode ser bem diferente.

Aqui está a ponte que abre o cálculo:

> **E se eu pegar intervalos $[t, t + \Delta t]$ cada vez menores? A velocidade média se aproxima da velocidade instantânea?**

Resposta: SIM. E este "tomar intervalos cada vez menores" é o que se chama **limite**. E o resultado é a **derivada** — o conceito central do cálculo.

Você não precisa entender derivada agora. Mas guarde: **a derivada é a taxa de variação instantânea**. Tudo que você faz nesta aula com $\Delta y / \Delta x$ vai se tornar $dy/dx$ daqui a alguns meses.

</Porta>

<Porta nivel="25" titulo="Pra estudante de engenharia">

Esta aula é o cumprimento prático do princípio de **Bruner — currículo em espiral** que orienta o programa do Clube. A taxa de variação aparece aqui em forma "média" (algébrica, sem limite); reaparece no Trim 5 como limite formal; volta no Trim 6 como derivada; ressurge no Trim 9 como integral (TFC) e no Trim 11 como regressão linear (estimativa estatística da derivada). É **um conceito, cinco encarnações**.

### Conexão Cauchy

Augustin-Louis Cauchy (1821) definiu derivada como limite da razão incremental:

$$f'(a) = \lim_{h \to 0} \frac{f(a+h) - f(a)}{h}$$

Esta razão $\frac{f(a+h) - f(a)}{h}$ é exatamente a TVM no intervalo $[a, a+h]$. Cauchy formaliza o "$h \to 0$" via $\eps$-$\delta$ — no Trim 5 vocês veem.

### TVM e o Teorema do Valor Médio (TVM teórico — não confundir)

Para $f$ contínua em $[a,b]$ e diferenciável em $(a,b)$, existe $c \in (a,b)$ com:

$$f'(c) = \frac{f(b) - f(a)}{b - a}$$

Ou seja: a derivada em algum ponto interior é exatamente igual à TVM. Resultado **não-trivial** que você prova no Trim 6 ou Análise. Note: TVM (teorema) e TVM (taxa de variação média) compartilham acrônimo; o teorema garante a existência de um ponto onde a tangente é paralela à secante.

</Porta>

</DuasPortas>

<ListaExercicios seed="aula-09">

<Exercicio numero="9.1" dificuldade="aplicacao" resposta="2">
Calcule a TVM de $f(x) = x^2$ em $[1, 3]$.
</Exercicio>

<Exercicio numero="9.2" dificuldade="aplicacao" resposta="4">
Calcule a TVM de $f(x) = x^2$ em $[1, 5]$.
</Exercicio>

<Exercicio numero="9.3" dificuldade="aplicacao" resposta="6">
Calcule a TVM de $f(x) = x^2$ em $[2, 4]$.
</Exercicio>

<Exercicio numero="9.4" dificuldade="aplicacao" resposta="3">
Calcule a TVM de $g(x) = 3x - 5$ em $[0, 10]$. (Verifique que é igual à inclinação $a = 3$ da reta — conforme aula 3.)
</Exercicio>

<Exercicio numero="9.5" dificuldade="aplicacao" resposta="2">
Calcule a TVM de $g(x) = 2x + 7$ em $[10, 20]$. Compare com o item anterior — TVM de função afim independe do intervalo.
</Exercicio>

<Exercicio
  numero="9.6"
  dificuldade="compreensao"
  resposta="$2a + h$"
  solucao={<><Eq>{`f(a+h) - f(a) = (a+h)^2 - a^2 = a^2 + 2ah + h^2 - a^2 = 2ah + h^2`}</Eq>. Dividindo por <Eq>{`h`}</Eq>: <Eq>{`2a + h`}</Eq>. Quando <Eq>{`h \\to 0`}</Eq>, isso tende a <strong><Eq>{`2a`}</Eq></strong> — que é a derivada de <Eq>{`f`}</Eq> em <Eq>{`a`}</Eq>. <strong>Foi assim que Cauchy provou que <Eq>{`(x^2)' = 2x`}</Eq>.</strong></>}
>
**Pré-derivada.** Para $f(x) = x^2$, encontre a TVM no intervalo $[a, a+h]$ em função de $a$ e $h$.
</Exercicio>

<Exercicio numero="9.7" dificuldade="compreensao">
Para $f(x) = x^3$, encontre a TVM no intervalo $[a, a+h]$. (O que ocorre quando $h \to 0$?)
</Exercicio>

<Exercicio numero="9.8" dificuldade="compreensao">
Mostre que a TVM de $f(x) = c$ (constante) em qualquer intervalo é $0$. Por que?
</Exercicio>

<Exercicio numero="9.9" dificuldade="modelagem">
Um carro percorre $s(t) = 4t^2$ metros em $t$ segundos. Calcule a velocidade média entre $t = 1$ e $t = 3$.
</Exercicio>

<Exercicio numero="9.10" dificuldade="modelagem">
A altura de uma bola lançada vertical é $h(t) = 20t - 5t^2$ (m, s). (a) Velocidade média entre $t = 0$ e $t = 2$. (b) Entre $t = 0$ e $t = 4$. (c) O que acontece com a "média" se a bola voltou ao chão?
</Exercicio>

<Exercicio numero="9.11" dificuldade="modelagem">
A população $P(t) = 1000 e^{0{,}03t}$ representa habitantes em função do tempo (anos). Qual a TVM (crescimento médio) entre $t = 0$ e $t = 10$? E entre $t = 10$ e $t = 20$?
</Exercicio>

<Exercicio numero="9.12" dificuldade="desafio">
Para $f(x) = 1/x$, calcule a TVM em $[1, 1+h]$ e simplifique. O que ocorre quando $h \to 0$?
</Exercicio>

<Exercicio
  numero="9.13"
  dificuldade="desafio"
  resposta="velocidade média não atende ao limite de velocidade"
  solucao={<>Velocidade média = <Eq>{`100\\,\\text{km}/(0{,}9\\,\\text{h}) \\approx 111\\,\\text{km/h}`}</Eq>. Como o limite é 110 km/h, em algum ponto da rodovia (intervalo) a velocidade média foi maior. Pelo TVM teorema, existe pelo menos um instante <Eq>{`c`}</Eq> em que a velocidade instantânea foi exatamente 111 km/h. Logo houve infração.</>}
>
**ENEM-style.** Um carro percorre 100 km em 54 minutos numa rodovia com limite de 110 km/h. Justifique por que o motorista necessariamente excedeu o limite em algum instante.
</Exercicio>

<Exercicio numero="9.14" dificuldade="demonstracao">
**Demonstre**: a TVM de $f(x) = ax^2 + bx + c$ em $[p, q]$ é $a(p+q) + b$. (Use a fórmula direta.)
</Exercicio>

<Exercicio numero="9.15" dificuldade="demonstracao">
Mostre que a TVM de qualquer função afim em qualquer intervalo é igual ao seu coeficiente angular. (Generaliza os exercícios 9.4 e 9.5.)
</Exercicio>

</ListaExercicios>

<Leituras>
- **Active Calculus** §1.5 — taxa de variação como motivação para derivada
- **OpenStax Calc Vol 1** §2.1 — taxa de variação e secantes
- **Wikilivros — Cálculo Vol 1** — capítulo introdutório
</Leituras>
