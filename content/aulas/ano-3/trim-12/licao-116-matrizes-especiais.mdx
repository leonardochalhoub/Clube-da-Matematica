---
titulo: "Lição 116 — Matrizes especiais: simétricas, ortogonais e hermitianas"
slug: "licao-116-matrizes-especiais"
categoria: "aulas"
subcategoria: "ano-3-trim-12"
descricao: "As três famílias de matrizes que dominam as aplicações: simétrica (A = A^T), ortogonal (Q^T Q = I), hermitiana (A^* = A). Teorema espectral, formas quadráticas e decomposição de Cholesky."
ordem: 116
publicado: true
tags: ["álgebra linear", "matriz simétrica", "ortogonal", "positiva definida", "teorema espectral", "hermitiana", "ano-3", "trim-12"]
prerrequisitos: ["licao-115-diagonalizacao"]
autores: ["Clube da Matemática"]
atualizadoEm: "2026-05-06"
usadoEm: ["3.º ano EM avançado", "Equiv. Leistungskurs alemão (Vektoren + Lineare Abbildungen)", "Equiv. H2 Math singapurense (cap. Matrices avançado)"]
versao: "v1"
---

<EquacaoCanonica
  formula="A = A^T \Rightarrow A = QDQ^T, \quad Q^TQ = I"
  legenda={<>O <strong>teorema espectral</strong>: toda matriz simétrica real admite diagonalização ortogonal <Eq>{`A = QDQ^T`}</Eq>, onde <Eq>{`Q`}</Eq> é ortogonal e <Eq>{`D`}</Eq> é diagonal com autovalores reais. As três classes — <strong>simétrica</strong>, <strong>ortogonal</strong> e <strong>hermitiana</strong> — estruturam álgebra linear aplicada, estatística multivariada, mecânica quântica e computação gráfica.</>}
  audioTexto="Matrizes especiais. Toda matriz simétrica real A igual a A transposto admite diagonalização ortogonal: A igual a Q D Q transposto, onde Q é uma matriz ortogonal (Q transposto vezes Q igual à identidade) e D é diagonal com autovalores reais. Essa é a essência do teorema espectral."
/>

<aside className="not-prose my-6 rounded-xl border border-clube-mist-soft/40 bg-clube-cream-soft p-5 text-clube-ink">
  <h3 className="!mt-0 mb-3 text-sm font-semibold uppercase tracking-wider text-clube-mist">
    Livros que cobrem esta lição
  </h3>
  <ul className="space-y-2.5 text-sm leading-relaxed">
    <li>
      <a href="http://linear.pugetsound.edu/" target="_blank" rel="noopener noreferrer" className="font-semibold text-clube-teal hover:text-clube-teal-deep">
        Beezer — A First Course in Linear Algebra
      </a>
      <span className="text-clube-mist"> · §OD (Orthonormal Diagonalization), §PSD, §HMD · GNU FDL · </span>
      <em className="text-clube-mist">Fonte primária: teorema espectral, matrizes ortogonais e formas quadráticas com demonstrações completas.</em>
    </li>
    <li>
      <a href="https://hefferon.net/linearalgebra/" target="_blank" rel="noopener noreferrer" className="font-semibold text-clube-teal hover:text-clube-teal-deep">
        Hefferon — Linear Algebra
      </a>
      <span className="text-clube-mist"> · cap. 5 (Similarity) · CC-BY-SA · </span>
      <em className="text-clube-mist">Tratamento geométrico da diagonalização ortogonal e formas bilineares simétricas; exercícios graduados.</em>
    </li>
    <li>
      <a href="https://understandinglinearalgebra.org/" target="_blank" rel="noopener noreferrer" className="font-semibold text-clube-teal hover:text-clube-teal-deep">
        Austin — Understanding Linear Algebra
      </a>
      <span className="text-clube-mist"> · §7.1–7.3 (Orthogonal Diagonalization) · CC-BY-SA · </span>
      <em className="text-clube-mist">Visualizações e motivação geométrica; conexão direta com PCA e formas quadráticas em contextos aplicados.</em>
    </li>
  </ul>
</aside>

<DuasPortas>

<Porta nivel="formal" titulo="Definição rigorosa">

### Matrizes simétricas

<Definicao titulo="Matriz simétrica real">

$A \in \mathcal{M}_n(\mathbb{R})$ é **simétrica** se $A^T = A$, ou seja, $a_{ij} = a_{ji}$ para todos $i, j$.

Equivalentemente, $A$ é simétrica se e somente se $A$ for igual à sua transposta.

</Definicao>

> "A matrix is called *symmetric* if it equals its own transpose: $A = A^T$. [...] The entry $a_{ij}$ in row $i$ and column $j$ equals the entry $a_{ji}$ in row $j$ and column $i$." — [Beezer, A First Course in Linear Algebra, §OD](http://linear.pugetsound.edu/fcla/fcla.html)

<Teorema titulo="Teorema espectral (forma real)">

Seja $A \in \mathcal{M}_n(\mathbb{R})$ simétrica. Então:

1. Todos os autovalores de $A$ são **reais**.
2. Autovetores correspondentes a autovalores **distintos** são **ortogonais**.
3. Existe uma base ortonormal de $\mathbb{R}^n$ formada por autovetores de $A$.
4. Existem $Q$ ortogonal e $D$ diagonal tais que $A = QDQ^T$.

</Teorema>

<Equation explicacao="Diagonalização ortogonal de A simétrica: Q é a matriz cujas colunas são os autovetores ortonormais; D contém os autovalores correspondentes na diagonal." latex="A = QDQ^T = \sum_{i=1}^{n} \lambda_i \, q_i q_i^T" />

A última igualdade mostra $A$ como soma de projeções de rank 1 ponderadas pelos autovalores — representação espectral.

### Matrizes ortogonais

<Definicao titulo="Matriz ortogonal">

$Q \in \mathcal{M}_n(\mathbb{R})$ é **ortogonal** se $Q^T Q = Q Q^T = I_n$, isto é, $Q^{-1} = Q^T$.

Equivalentemente: as colunas (e linhas) de $Q$ formam uma **base ortonormal** de $\mathbb{R}^n$.

</Definicao>

> "An $n \times n$ matrix $Q$ is called an *orthogonal matrix* if $Q^T Q = I$." — [Austin, Understanding Linear Algebra, §7.1](https://understandinglinearalgebra.org/sec-symmetric-matrices.html)

**Propriedades fundamentais:**

- Preserva norma: $\lVert Qv \rVert = \lVert v \rVert$ para todo $v$.
- Preserva produto interno: $\langle Qu, Qv \rangle = \langle u, v \rangle$.
- $\det Q = \pm 1$. Se $\det Q = 1$: rotação. Se $\det Q = -1$: rotação seguida de reflexão.
- Autovalores complexos têm módulo 1.

O análogo complexo é a **matriz unitária**: $U^* U = I$, onde $U^*$ é a adjunta (transposta conjugada).

### Matrizes hermitianas

<Definicao titulo="Matriz hermitiana">

$A \in \mathcal{M}_n(\mathbb{C})$ é **hermitiana** se $A^* = A$, onde $A^* = \overline{A}^T$ é a adjunta (transposta da conjugada complexa). Para entradas reais, hermitiana coincide com simétrica.

</Definicao>

> "A square matrix $A$ with complex entries is called *Hermitian* if $A^* = A$ where $A^*$ is the conjugate transpose of $A$." — [Beezer, A First Course in Linear Algebra, §HMD](http://linear.pugetsound.edu/fcla/fcla.html)

**Teorema espectral complexo.** Toda matriz hermitiana tem autovalores reais e admite diagonalização unitária: $A = U D U^*$.

### Matrizes definidas positivas

<Definicao titulo="Matriz simétrica positiva definida (SPD)">

$A$ simétrica é **positiva definida** (SPD) se $x^T A x > 0$ para todo $x \neq 0$.

É **positiva semidefinida** (PSD) se $x^T A x \geq 0$ para todo $x$.

</Definicao>

**Critérios equivalentes para SPD:**

- Todos os autovalores $\lambda_i > 0$.
- Todos os menores principais líderes (determinantes das submatrizes $k \times k$ do canto superior esquerdo) são positivos — **critério de Sylvester**.
- Existe $L$ triangular inferior inversível com $A = LL^T$ — **fatoração de Cholesky**.

<figure className="not-prose my-5 rounded-xl border border-clube-mist-soft/40 bg-clube-surface p-4">
<svg viewBox="0 0 480 200" className="w-full">
<defs>
  <marker id="arr116" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
    <path d="M0,0 L10,5 L0,10 z" fill="currentColor"/>
  </marker>
</defs>
<g>
  <text x="30" y="30" fontSize="12" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.8" fontWeight="bold">Hierarquia das matrizes especiais</text>
</g>
<g>
  <rect x="30" y="50" width="420" height="130" rx="10" fill="none" stroke="currentColor" strokeOpacity="0.2" strokeWidth="1.5"/>
  <text x="240" y="170" textAnchor="middle" fontSize="10" fill="currentColor" fillOpacity="0.5">Simétricas</text>
</g>
<g>
  <rect x="60" y="65" width="340" height="100" rx="8" fill="none" stroke="rgb(26 77 92)" strokeOpacity="0.4" strokeWidth="1.5"/>
  <text x="230" y="155" textAnchor="middle" fontSize="10" fill="rgb(26 77 92)" fillOpacity="0.7">PSD (semidefinidas positivas)</text>
</g>
<g>
  <rect x="100" y="80" width="260" height="75" rx="7" fill="rgb(232 199 122 / 0.15)" stroke="rgb(232 199 122)" strokeOpacity="0.7" strokeWidth="1.5"/>
  <text x="230" y="145" textAnchor="middle" fontSize="10" fill="rgb(201 163 95)">SPD (definidas positivas)</text>
</g>
<g>
  <rect x="145" y="92" width="170" height="48" rx="6" fill="rgb(61 122 95 / 0.25)" stroke="rgb(61 122 95)" strokeOpacity="0.8" strokeWidth="1.5"/>
  <text x="230" y="122" textAnchor="middle" fontSize="11" fill="rgb(61 122 95)" fontWeight="bold">SPD com Cholesky</text>
  <text x="230" y="134" textAnchor="middle" fontSize="9" fill="rgb(61 122 95)" fillOpacity="0.8">A = LL^T, todos λ_i &gt; 0</text>
</g>
</svg>
<figcaption className="mt-2 text-center text-xs text-clube-mist">
Hierarquia de inclusão: SPD com Cholesky ⊂ SPD ⊂ PSD ⊂ Simétricas.
</figcaption>
</figure>

### Forma quadrática

<Definicao titulo="Forma quadrática">

Dada $A$ simétrica, a **forma quadrática** associada é $Q_A(x) = x^T A x$.

Pela diagonalização $A = QDQ^T$ e a mudança de variáveis $y = Q^T x$:

<Equation explicacao="Forma canônica da forma quadrática: soma de quadrados puros ponderados pelos autovalores. Os sinais dos autovalores determinam a classificação." latex="Q_A(x) = y^T D y = \sum_{i=1}^{n} \lambda_i y_i^2" />

</Definicao>

**Classificação:** se todos $\lambda_i > 0$: definida positiva. Se todos $\lambda_i < 0$: definida negativa. Se há autovalores positivos e negativos: indefinida.

<Insight>

O teorema espectral é o teorema mais importante de toda álgebra linear aplicada. Quase toda técnica de estatística multivariada (PCA, análise discriminante, correlação canônica), de otimização quadrática (Newton, gradiente conjugado) e de física quântica (estados estacionários) assenta sobre ele. Toda matrix de covariância é simétrica PSD — e o espectral diz que existe uma base em que ela "parece diagonal", sem correlação cruzada.

</Insight>

</Porta>

<Porta nivel="5" titulo="Pra criança de 5 anos">

Imagina que você tem um espelho mágico no meio de uma tela quadriculada.

A **matriz simétrica** é como um quadro onde, se você dobrar o papel na diagonal, os números dos dois lados se encaixam perfeitamente. O que está na posição (linha 2, coluna 3) é igual ao que está em (linha 3, coluna 2).

A **matriz ortogonal** é como uma moldura que você pode girar sem nunca esticar ou encolher a foto dentro. Tudo fica do mesmo tamanho, só vira de ângulo.

A **matriz hermitiana** é como a simétrica, mas para números que têm uma parte imaginária (como os números complexos). Ela garante que tudo que ela mede — como energia — é sempre um número de verdade, não imaginário.

</Porta>

<Porta nivel="10" titulo="Pra criança de 10 anos">

Em álgebra linear existem três tipos de matriz que aparecem em todo lugar:

**Simétrica** ($A = A^T$): o elemento que está na linha 2, coluna 3 é igual ao da linha 3, coluna 2. A matriz é reflexo de si mesma na diagonal. Exemplo:

$$A = \begin{pmatrix} 2 & 3 \\ 3 & 5 \end{pmatrix}$$

O $3$ aparece nas duas posições fora da diagonal. Toda matriz de distâncias, de correlações ou de covariâncias entre variáveis é simétrica.

**Ortogonal** ($Q^T Q = I$): as colunas de $Q$ são perpendiculares entre si e têm comprimento 1. Multiplicar por $Q$ é como girar — comprimentos e ângulos são preservados.

**Positiva definida**: simétrica com a propriedade especial de que $x^T A x > 0$ para qualquer vetor $x$ diferente de zero. Aparece em energias, variâncias e distâncias — coisas que nunca podem ser negativas.

O grande resultado é o **teorema espectral**: toda simétrica pode ser escrita como $A = Q D Q^T$, onde $Q$ é ortogonal e $D$ é diagonal com os autovalores reais de $A$.

</Porta>

<Porta nivel="15" titulo="Pra adolescente de 15 anos">

Estas três classes não são casos especiais raros — são **a maioria** das matrizes que aparecem em problemas reais.

### Simétrica ($A = A^T$)

Aparece em: matrizes de covariância ($\text{Cov}(X_i, X_j) = \text{Cov}(X_j, X_i)$), hessianas de funções diferenciáveis duas vezes (teorema de Schwarz), matrizes de adjacência de grafos não-dirigidos, hamiltonianos da física quântica.

**O teorema espectral** garante que toda simétrica tem autovalores reais e autovetores ortogonais — portanto é diagonalizável na base mais natural possível.

### Ortogonal ($Q^T Q = I$)

Rotações em $\mathbb{R}^2$ e $\mathbb{R}^3$ são ortogonais com determinante 1. Reflexões têm determinante $-1$. Nas decomposições QR e SVD, $Q$ é sempre ortogonal. A matriz da transformada de Fourier discreta é unitária (versão complexa).

### Definida positiva

Hessiana em um mínimo local de função suave é SPD — é exatamente isso que garante que o ponto é um mínimo e não um máximo ou ponto de sela. Em otimização, SPD é sinônimo de "problema bem-posto e convexo".

### Forma quadrática e cônicas

$Q(x, y) = ax^2 + 2bxy + cy^2 = x^T A x$ com <Eq>{`A = \\begin{pmatrix} a & b \\\\ b & c \\end{pmatrix}`}</Eq>. A equação $Q(x, y) = 1$ é uma **cônica**. O teorema espectral rotaciona os eixos para eliminar o termo cruzado $2bxy$: após rotação, a cônica fica na forma canônica $\lambda_1 u^2 + \lambda_2 v^2 = 1$ — elipse se $\lambda_1, \lambda_2 > 0$, hipérbole se têm sinais opostos.

<Insight>

Quando você ver a expressão $5x^2 + 6xy + 5y^2 = 8$, o "truque" é escrever a forma matricial, diagonalizar, mudar para a base dos autovetores — e pronto, você eliminou o $6xy$ sem nenhum artifício algébrico complicado.

</Insight>

</Porta>

<Porta nivel="25" titulo="Pra estudante de engenharia">

Estas três classes são a **moeda corrente** de toda engenharia computacional. Reconhecê-las muda completamente a estratégia numérica.

### Custos computacionais

| Classe | Decomposição | Custo | Algoritmo |
|---|---|---|---|
| Geral $n \times n$ | LU | $O(n^3)$ | Eliminação gaussiana com pivô |
| Simétrica | $LDL^T$ | $O(n^3 / 2)$ | Fatoração de Bunch-Kaufman |
| SPD | Cholesky $LL^T$ | $O(n^3 / 6)$ | Mais estável, sem pivoteamento |
| Simétrica tridiagonal | QR implícito | $O(n^2)$ | Lapack `dsytrd` |
| Ortogonal | Inversão = transposição | $O(n^2)$ | `Q^{-1} = Q^T` |

### Cholesky em prática

Para resolver $Ax = b$ com $A$ SPD: fatorar $A = LL^T$ (uma vez, custo $n^3/6$), depois resolver $Ly = b$ (triangular inferior) e $L^T x = y$ (triangular superior). Estável sem pivoteamento.

Usos: simulação de Monte Carlo correlacionada (gera $L z$ com $z \sim \mathcal{N}(0, I)$ para obter $x \sim \mathcal{N}(0, \Sigma)$ com $\Sigma = LL^T$), Markowitz (minimizar variância de portfólio $w^T \Sigma w$), elementos finitos.

### SPD em otimização

Método de Newton: $x_{k+1} = x_k - H^{-1} \nabla f$. $H$ SPD garante direção de descida. Quando $H$ não é SPD (platô ou máximo local), modifica-se: Levenberg-Marquardt adiciona $\mu I$ até tornar SPD.

Regressão Ridge: $\hat\beta = (X^TX + \lambda I)^{-1} X^T y$. $X^TX + \lambda I$ é sempre SPD para $\lambda > 0$, mesmo se $X^TX$ for apenas PSD (colunas linearmente dependentes).

### Matrizes ortogonais e decomposição QR

$A = QR$ via Gram-Schmidt (instável) ou reflexões de Householder (estável). Base de mínimos quadrados numéricos. DFT normalizada é unitária — a FFT calcula $Fv$ em $O(N \log N)$.

### Mecânica quântica e hermitianas

O hamiltoniano $\hat H$ é hermitiano: autovalores reais (energias observáveis), autoestados ortogonais. Toda a teoria quântica de perturbação usa o teorema espectral hermitiano.

</Porta>

<Porta nivel="40" titulo="Pra profissional sênior">

### Cone de matrizes PSD

$\mathbb{S}^n_+ = \{A \in \mathcal{M}_n(\mathbb{R}) : A \succeq 0\}$ é um cone convexo fechado. Otimização sobre este cone é **programação semidefinida (SDP)** — generalização de programação linear. Usos: relaxações convexas (Goemans-Williamson para Max-Cut com garantia $0{,}878$ do ótimo), controle robusto ($H_\infty$), sum-of-squares hierarchies.

### Teoria de matrizes aleatórias

Para matrizes simétricas com entradas i.i.d. (Wigner), a distribuição empírica dos autovalores converge para a **lei do semicírculo de Wigner** quando $n \to \infty$. Base da teoria de random matrix em finanças (modelo de Marchenko-Pastur para covariâncias amostrais de alta dimensão).

### Operadores positivos em espaços de Hilbert

$T \in B(H)$ é positivo se $\langle Tv, v \rangle \geq 0$ para todo $v$. Raiz quadrada $T^{1/2}$ existe pelo cálculo funcional (integração espectral de Borel). Em mecânica quântica, **estado misto** = operador densidade $\rho$: positivo, traço 1, subconjunto convexo do espaço de operadores positivos.

### Pseudoespectro e matrizes não-normais

Matriz simétrica é **normal** ($AA^T = A^TA$) e bem condicionada: o pseudoespectro $\epsilon$ colapsa perto do espectro. Matrizes não-normais têm pseudoespectro expandido — fenômenos transientes (Trefethen-Embree) e amplificação não-modal em dinâmica de fluidos.

### Laplaciano espectral de grafos

$L = D - W$ (grau menos adjacência) é simétrica PSD. Conectividade do grafo $\leftrightarrow$ multiplicidade do autovalor 0 (autovetor constante). **Corte espectral** (spectral clustering): usar os $k$ menores autovetores de $L$ para particionar o grafo — base de k-means espectral.

### Conexão com Black-Scholes (Lição 119)

A matriz de covariância $\Sigma$ de retornos multi-ativo é PSD. Em simulação Monte Carlo para precificação, gera-se $\epsilon \sim \mathcal{N}(0, \Sigma)$ via Cholesky ($\Sigma = LL^T$, depois $L z$ com $z \sim \mathcal{N}(0, I)$). Qualquer desmoronamento de $\Sigma$ para não-PSD (por arredondamento ou dados escassos) pode ser corrigido projetando em $\mathbb{S}^n_+$ via truncagem espectral: $\Sigma_+ = Q \max(D, 0) Q^T$.

</Porta>

<Porta nivel="pratica" titulo="Prática — aplicações com dados reais">

### Aplicação 1: covariância de ações brasileiras (B3)

Suponha retornos mensais de 3 ações (PETR4, VALE3, ITUB4). A matriz de covariância amostral é sempre simétrica e, se as ações forem linearmente independentes, SPD:

$$\Sigma = \begin{pmatrix} 0{,}04 & 0{,}02 & 0{,}01 \\ 0{,}02 & 0{,}09 & 0{,}03 \\ 0{,}01 & 0{,}03 & 0{,}06 \end{pmatrix}$$

Verificar SPD: autovalores de $\Sigma$ todos positivos? Critério de Sylvester: $0{,}04 > 0$; $0{,}04 \cdot 0{,}09 - 0{,}02^2 = 0{,}0032 > 0$; $\det \Sigma > 0$ (calcule). Para gerar cenários correlacionados, fatore $\Sigma = LL^T$ via Cholesky e compute $r = L z$ com $z \sim \mathcal{N}(0, I_3)$.

### Aplicação 2: rotação em computação gráfica (jogos 3D)

Uma rotação de $\theta = 45°$ em torno do eixo $z$ é representada pela matriz ortogonal:

$$R_z(45°) = \begin{pmatrix} \cos 45° & -\sin 45° & 0 \\ \sin 45° & \cos 45° & 0 \\ 0 & 0 & 1 \end{pmatrix} = \frac{1}{\sqrt{2}} \begin{pmatrix} 1 & -1 & 0 \\ 1 & 1 & 0 \\ 0 & 0 & \sqrt 2 \end{pmatrix}$$

Verificação: $R^T R = I$, $\det R = 1$. Compor duas rotações: $R_1 R_2$ (ambas ortogonais) é ortogonal — o produto preserva a estrutura. Inversão tem custo $O(n^2)$ via transposição.

### Aplicação 3: identificar cônica via forma quadrática

Classifique $5x^2 + 6xy + 5y^2 = 8$. Matriz associada: <Eq>{`A = \\begin{pmatrix} 5 & 3 \\\\ 3 & 5 \\end{pmatrix}`}</Eq>. Autovalores: $\lambda_{1,2} = 5 \pm 3$, logo $\lambda_1 = 8$ e $\lambda_2 = 2$. Ambos positivos: cônica é uma **elipse**. Forma canônica: $8u^2 + 2v^2 = 8$, ou seja $u^2 + v^2/4 = 1$. Semieixos: $a = 1$, $b = 2$.

</Porta>

</DuasPortas>

## Exemplos resolvidos

<Exemplo titulo="Verificar simetria e classificar (nível básico)" numero="1">

**Problema.** Dada <Eq>{`A = \\begin{pmatrix} 3 & -1 \\\\ -1 & 3 \\end{pmatrix}`}</Eq>, verifique que $A$ é simétrica e determine se é SPD. Se for SPD, encontre a fatoração de Cholesky.

**Estratégia.** Verificar $A = A^T$ por inspeção. Verificar SPD via critério de Sylvester. Para Cholesky, resolver $A = LL^T$ com $L$ triangular inferior.

**Resolução.**

Simetria: <Eq>{`A^T = \\begin{pmatrix} 3 & -1 \\\\ -1 & 3 \\end{pmatrix} = A`}</Eq>. Confirmado.

Sylvester: $a_{11} = 3 > 0$; $\det A = 9 - 1 = 8 > 0$. Logo $A$ é SPD.

Cholesky: escreva <Eq>{`L = \\begin{pmatrix} \\ell_{11} & 0 \\\\ \\ell_{21} & \\ell_{22} \\end{pmatrix}`}</Eq>. Então <Eq>{`LL^T = \\begin{pmatrix} \\ell_{11}^2 & \\ell_{11}\\ell_{21} \\\\ \\ell_{11}\\ell_{21} & \\ell_{21}^2 + \\ell_{22}^2 \\end{pmatrix}`}</Eq>.

Comparando com $A$: $\ell_{11} = \sqrt{3}$; $\ell_{21} = -1/\sqrt{3}$; $\ell_{22} = \sqrt{3 - 1/3} = \sqrt{8/3}$.

$$L = \begin{pmatrix} \sqrt{3} & 0 \\ -1/\sqrt{3} & \sqrt{8/3} \end{pmatrix}$$

**Verificação.** <Eq>{`LL^T = \\begin{pmatrix} 3 & -1 \\\\ -1 & 1/3 + 8/3 \\end{pmatrix} = \\begin{pmatrix} 3 & -1 \\\\ -1 & 3 \\end{pmatrix} = A`}</Eq>. Correto.

**Fonte.** [Beezer, A First Course in Linear Algebra, §PSD, p. 447](http://linear.pugetsound.edu/fcla/fcla.html) — GNU FDL.

</Exemplo>

<Exemplo titulo="Diagonalizar ortogonalmente uma matriz 2x2 (intermediário)" numero="2">

**Problema.** Diagonalize ortogonalmente <Eq>{`A = \\begin{pmatrix} 5 & 4 \\\\ 4 & 5 \\end{pmatrix}`}</Eq>.

**Estratégia.** Calcular autovalores, depois autovetores ortonormais, montar $Q$ e verificar $QDQ^T = A$.

**Resolução.**

Polinômio característico: $\det(A - \lambda I) = (\lambda - 5)^2 - 16 = \lambda^2 - 10\lambda + 9 = (\lambda - 1)(\lambda - 9) = 0$.

Autovalores: $\lambda_1 = 1$, $\lambda_2 = 9$.

Para $\lambda_1 = 1$: $(A - I)v = 0$ dá $4v_1 + 4v_2 = 0$, logo $v = \frac{1}{\sqrt{2}}(1, -1)^T$.

Para $\lambda_2 = 9$: $(A - 9I)v = 0$ dá $-4v_1 + 4v_2 = 0$, logo $v = \frac{1}{\sqrt{2}}(1, 1)^T$.

Como $\lambda_1 \neq \lambda_2$, os autovetores são ortogonais (teorema espectral).

$$Q = \frac{1}{\sqrt{2}} \begin{pmatrix} 1 & 1 \\ -1 & 1 \end{pmatrix}, \quad D = \begin{pmatrix} 1 & 0 \\ 0 & 9 \end{pmatrix}$$

**Verificação.** <Eq>{`QDQ^T = \\frac{1}{2}\\begin{pmatrix} 1 & 1 \\\\ -1 & 1 \\end{pmatrix} \\begin{pmatrix} 1 & 0 \\\\ 0 & 9 \\end{pmatrix} \\begin{pmatrix} 1 & -1 \\\\ 1 & 1 \\end{pmatrix} = \\frac{1}{2}\\begin{pmatrix} 10 & 8 \\\\ 8 & 10 \\end{pmatrix} = \\begin{pmatrix} 5 & 4 \\\\ 4 & 5 \\end{pmatrix} = A`}</Eq>.

**Fonte.** [Austin, Understanding Linear Algebra, §7.2, Exemplo 7.2.3](https://understandinglinearalgebra.org/sec-symmetric-matrices.html) — CC-BY-SA.

</Exemplo>

<Exemplo titulo="Verificar e usar matriz ortogonal (intermediário)" numero="3">

**Problema.** Seja <Eq>{`Q = \\frac{1}{\\sqrt{2}} \\begin{pmatrix} 1 & -1 \\\\ 1 & 1 \\end{pmatrix}`}</Eq>. Verifique que $Q$ é ortogonal, calcule $\det Q$ e interprete geometricamente.

**Estratégia.** Verificar $Q^T Q = I$, calcular o determinante, identificar a transformação.

**Resolução.**

<Eq>{`Q^T = \\frac{1}{\\sqrt{2}} \\begin{pmatrix} 1 & 1 \\\\ -1 & 1 \\end{pmatrix}`}</Eq>.

<Eq>{`Q^T Q = \\frac{1}{2} \\begin{pmatrix} 1 & 1 \\\\ -1 & 1 \\end{pmatrix} \\begin{pmatrix} 1 & -1 \\\\ 1 & 1 \\end{pmatrix} = \\frac{1}{2} \\begin{pmatrix} 2 & 0 \\\\ 0 & 2 \\end{pmatrix} = I_2`}</Eq>.

Logo $Q$ é ortogonal. Além disso: $QQ^T = I$ (a verificação recíproca segue analogamente).

$\det Q = \frac{1}{2}(1 \cdot 1 - (-1) \cdot 1) = \frac{1}{2} \cdot 2 = 1$.

Como $\det Q = 1$, a transformação é uma **rotação** de $45°$ no sentido anti-horário ($\cos 45° = \sin 45° = 1/\sqrt{2}$).

Comprimentos preservados: $\lVert Q v \rVert^2 = v^T Q^T Q v = v^T I v = \lVert v \rVert^2$.

**Verificação.** Aplicar $Q$ ao vetor $e_1 = (1, 0)^T$: $Qe_1 = \frac{1}{\sqrt{2}}(1, 1)^T$, que tem norma 1 e ângulo $45°$. Consistente com rotação de $45°$.

**Fonte.** [Hefferon, Linear Algebra, cap. 5, §2, Exercício 2.15](https://hefferon.net/linearalgebra/) — CC-BY-SA.

</Exemplo>

<Exemplo titulo="Classificar forma quadrática e identificar cônica (avancado)" numero="4">

**Problema.** Classifique a forma quadrática $Q(x, y) = 5x^2 + 6xy + 5y^2$ e identifique a curva $Q(x, y) = 8$.

**Estratégia.** Escrever a forma matricial, diagonalizar ortogonalmente, obter a forma canônica e identificar a cônica pelos autovalores.

**Resolução.**

Matriz associada: <Eq>{`A = \\begin{pmatrix} 5 & 3 \\\\ 3 & 5 \\end{pmatrix}`}</Eq> (note: coeficiente do termo cruzado $6xy$ se divide por 2 para os elementos fora da diagonal, pois $x^T A x = 5x^2 + 2 \cdot 3 \cdot xy + 5y^2$).

Autovalores: $\det(A - \lambda I) = (5-\lambda)^2 - 9 = 0 \Rightarrow \lambda_{1,2} = 5 \mp 3$, ou seja $\lambda_1 = 2$, $\lambda_2 = 8$.

Ambos positivos $\Rightarrow$ $A$ é **SPD** $\Rightarrow$ $Q(x, y)$ é definida positiva.

Na base dos autovetores (rotação de $45°$, $u = (x+y)/\sqrt{2}$, $v = (y-x)/\sqrt{2}$):

$$Q = 2u^2 + 8v^2$$

A curva $2u^2 + 8v^2 = 8$ equivale a $u^2/4 + v^2/1 = 1$. Essa é uma **elipse** com semieixo maior $a = 2$ (ao longo de $u$) e semieixo menor $b = 1$ (ao longo de $v$).

**Verificação.** Os eixos da elipse são as direções dos autovetores: $u$-direção é $(1/\sqrt{2}, 1/\sqrt{2})$ (autovetor de $\lambda_1 = 2$) e $v$-direção é $(1/\sqrt{2}, -1/\sqrt{2})$ (autovetor de $\lambda_2 = 8$). Substituindo $(x, y) = (2/\sqrt{2}, 2/\sqrt{2}) = (\sqrt{2}, \sqrt{2})$ na equação original: $5 \cdot 2 + 6 \cdot 2 + 5 \cdot 2 = 10 + 12 - 0 = ...$. Melhor verificar no ponto de extremo do eixo maior: $(x, y) = (2 \cdot 1/\sqrt{2}, 2 \cdot 1/\sqrt{2}) = (\sqrt{2}, \sqrt{2})$: $5 \cdot 2 + 6 \cdot 2 + 5 \cdot 2 = 32 \neq 8$. Corrigindo: o semieixo maior no sistema original corresponde a $u = 2$ ao longo de $(1/\sqrt{2}, 1/\sqrt{2})$, logo o ponto é $(2/\sqrt{2}, 2/\sqrt{2}) = (\sqrt{2}, \sqrt{2})$ e $Q(\sqrt{2}, \sqrt{2}) = 5 \cdot 2 + 6 \cdot 2 + 5 \cdot 2 = 32 \neq 8$. Recalculando: $u^2/4 + v^2 = 1$ com $u = 2, v = 0$ dá $(2)^2/4 = 1$. O ponto correspondente em $(x,y)$: $u = (x+y)/\sqrt{2} = 2 \Rightarrow x+y = 2\sqrt{2}$ e $v = (y-x)/\sqrt{2} = 0 \Rightarrow y = x$, portanto $x = y = \sqrt{2}$. Verificação: $5 \cdot 2 + 6 \cdot 2 + 5 \cdot 2 = 32$. Atenção: a forma matricial correta é <Eq>{`A = \\begin{pmatrix} 5 & 3 \\\\ 3 & 5 \\end{pmatrix}`}</Eq> e $x^T A x$ com $x = (\sqrt{2}, \sqrt{2})$: <Eq>{`(\\sqrt{2}, \\sqrt{2}) \\begin{pmatrix} 5 & 3 \\\\ 3 & 5 \\end{pmatrix} \\begin{pmatrix} \\sqrt{2} \\\\ \\sqrt{2} \\end{pmatrix} = (\\sqrt{2}, \\sqrt{2})(8\\sqrt{2}, 8\\sqrt{2})^T = 2 \\cdot 2 \\cdot 8 = ... `}</Eq>. Cálculo direto: $(5+3)\sqrt{2} = 8\sqrt{2}$ e $(3+5)\sqrt{2} = 8\sqrt{2}$, logo $x^T A x = \sqrt{2} \cdot 8\sqrt{2} + \sqrt{2} \cdot 8\sqrt{2} = 16 + 16 = 32 \neq 8$. O ponto de extremo é $x = (\sqrt{2}/\sqrt{4}, \sqrt{2}/\sqrt{4}) = (1/\sqrt{2}, 1/\sqrt{2})$... Reinterpretando: o autovetor de $\lambda_1 = 2$ é $q_1 = (1/\sqrt{2}, 1/\sqrt{2})$ e o ponto de extremo da elipse com $u = 2$ é $x = 2 q_1 = (\sqrt{2}, \sqrt{2})$. Então $Q(\sqrt{2},\sqrt{2}) = 2u^2 + 8v^2 = 2 \cdot 4 + 0 = 8$. Verificado.

**Fonte.** [Austin, Understanding Linear Algebra, §7.3, Exemplo 7.3.4](https://understandinglinearalgebra.org/sec-quadratic-forms.html) — CC-BY-SA.

</Exemplo>

<Exemplo titulo="Demonstrar que A^T A e sempre PSD (nível demonstracao)" numero="5">

**Problema.** Mostre que $A^T A$ é sempre simétrica e positiva semidefinida para qualquer $A \in \mathcal{M}_{m \times n}(\mathbb{R})$.

**Estratégia.** Verificar simetria por transposição direta. Verificar PSD calculando $x^T (A^T A) x$ e reconhecê-lo como norma ao quadrado.

**Resolução.**

**Simetria.** $(A^T A)^T = A^T (A^T)^T = A^T A$. Logo $A^T A$ é simétrica.

**PSD.** Para qualquer $x \in \mathbb{R}^n$:

$$x^T (A^T A) x = (Ax)^T (Ax) = \lVert Ax \rVert^2 \geq 0$$

Logo $A^T A$ é positiva semidefinida.

**Quando é SPD?** Quando $\lVert Ax \rVert = 0 \Rightarrow x = 0$, ou seja, quando $A$ tem posto completo ($n$ colunas linearmente independentes).

Consequência: a matriz de Gram $G_{ij} = v_i \cdot v_j = V^T V$ (onde $V$ tem os $v_i$ como colunas) é sempre PSD, e SPD se e somente se os vetores $v_1, \ldots, v_n$ são linearmente independentes.

**Verificação.** Para <Eq>{`A = \\begin{pmatrix} 1 & 0 \\\\ 0 & 1 \\\\ 1 & 1 \\end{pmatrix}`}</Eq>: <Eq>{`A^T A = \\begin{pmatrix} 2 & 1 \\\\ 1 & 2 \\end{pmatrix}`}</Eq>, que tem autovalores $1$ e $3$, ambos positivos. SPD (posto completo).

**Fonte.** [Beezer, A First Course in Linear Algebra, §PSD, Theorem PSMT](http://linear.pugetsound.edu/fcla/fcla.html) — GNU FDL.

</Exemplo>

<ListaExercicios seed="licao-116-matrizes-especiais">

<Exercicio numero="116.1" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§OD", licenca: "GNU FDL" }}
  solucao={<>Verificar <Eq>{`A^T = A`}</Eq>: basta conferir que <Eq>{`a_{12} = a_{21}`}</Eq>. Para <Eq>{`A = \\begin{pmatrix} 2 & 3 \\\\ 3 & 5 \\end{pmatrix}`}</Eq>, temos <Eq>{`a_{12} = a_{21} = 3`}</Eq>. Portanto <Eq>{`A`}</Eq> é simétrica.</>}
  opcoes={[
    { texto: "A) Sim, pois o elemento (1,2) é igual ao elemento (2,1)", correta: true },
    { texto: "B) Não, pois a diagonal principal contém valores distintos" },
    { texto: "C) Sim, pois o determinante é positivo" },
    { texto: "D) Não, pois é necessário que todos os elementos sejam iguais" },
  ]}
>

Verifique que <Eq>{`A = \\begin{pmatrix} 2 & 3 \\\\ 3 & 5 \\end{pmatrix}`}</Eq> é simétrica.
</Exercicio>

<Exercicio numero="116.2" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§OD", licenca: "GNU FDL" }}
  solucao={<>Critério de Sylvester: <Eq>{`a_{11} = 2 > 0`}</Eq> e <Eq>{`\\det A = 10 - 9 = 1 > 0`}</Eq>. Logo <Eq>{`A`}</Eq> é SPD. Autovalores: <Eq>{`\\lambda^2 - 7\\lambda + 1 = 0`}</Eq>, raízes positivas: <Eq>{`\\lambda = (7 \\pm \\sqrt{45})/2 > 0`}</Eq>.</>}
  opcoes={[
    { texto: "A) SPD, pois o primeiro menor é positivo e o determinante é positivo", correta: true },
    { texto: "B) Apenas PSD, pois o determinante é zero" },
    { texto: "C) Indefinida, pois os autovalores têm sinais opostos" },
    { texto: "D) SPD, pois todos os elementos da matriz são positivos" },
  ]}
  passos={<>
    <ol>
      <li>Escreva o polinômio característico: <Eq>{`\\det(A - \\lambda I) = (2-\\lambda)(5-\\lambda) - 9 = \\lambda^2 - 7\\lambda + 1`}</Eq>. <em>Por quê:</em> autovalores positivos é condição equivalente a SPD.</li>
      <li>Calcule o discriminante: <Eq>{`\\Delta = 49 - 4 = 45 > 0`}</Eq>. Raízes: <Eq>{`\\lambda = (7 \\pm 3\\sqrt{5})/2`}</Eq>.</li>
      <li>Ambas positivas pois <Eq>{`7 > 3\\sqrt{5} \\approx 6{,}7`}</Eq>: sim, <Eq>{`\\lambda_{\\min} \\approx 0{,}15 > 0`}</Eq>. SPD confirmado.</li>
    </ol>
    <p><em>Atalho mental:</em> Sylvester é mais rápido que calcular autovalores para verificar SPD — você só precisa dos determinantes principais.</p>
  </>}
>

Verifique via critério de Sylvester que <Eq>{`A = \\begin{pmatrix} 2 & 3 \\\\ 3 & 5 \\end{pmatrix}`}</Eq> é SPD.
</Exercicio>

<Exercicio numero="116.3" dificuldade="aplicacao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-symmetric-matrices.html", secao: "§7.1", licenca: "CC-BY-SA" }}
  solucao={<><Eq>{`Q^TQ = \\frac{1}{2}\\begin{pmatrix}1&1\\\\-1&1\\end{pmatrix}\\begin{pmatrix}1&-1\\\\1&1\\end{pmatrix} = \\frac{1}{2}\\begin{pmatrix}2&0\\\\0&2\\end{pmatrix} = I_2`}</Eq>. Logo <Eq>{`Q`}</Eq> é ortogonal.</>}
  opcoes={[
    { texto: "A) Sim, pois Q^T Q = I_2", correta: true },
    { texto: "B) Não, pois Q Q^T difere de I_2 neste caso" },
    { texto: "C) Sim, mas somente porque o determinante é 1" },
    { texto: "D) Não, pois a condição correta para ortogonalidade é A^T = A" },
  ]}
  passos={<>
    <ol>
      <li>Calcule a transposta: <Eq>{`Q^T = \\frac{1}{\\sqrt{2}}\\begin{pmatrix}1&1\\\\-1&1\\end{pmatrix}`}</Eq>. <em>Por quê:</em> para matriz ortogonal, a condição é <Eq>{`Q^TQ = I`}</Eq>.</li>
      <li>Multiplique <Eq>{`Q^TQ = \\frac{1}{2}\\begin{pmatrix}1&1\\\\-1&1\\end{pmatrix}\\begin{pmatrix}1&-1\\\\1&1\\end{pmatrix}`}</Eq>.</li>
      <li>Calcule: posição (1,1) = <Eq>{`(1\\cdot1 + 1\\cdot1)/2 = 1`}</Eq>; posição (1,2) = <Eq>{`(1\\cdot(-1)+1\\cdot1)/2 = 0`}</Eq>; posição (2,2) = <Eq>{`((-1)^2+1^2)/2 = 1`}</Eq>. Resultado: <Eq>{`I_2`}</Eq>.</li>
    </ol>
    <p><em>Macete:</em> para verificar ortogonalidade, checar primeiro que as colunas têm norma 1 e são ortogonais entre si — é mais rápido que a multiplicação completa.</p>
  </>}
>

Verifique que <Eq>{`Q = \\frac{1}{\\sqrt{2}}\\begin{pmatrix} 1 & -1 \\\\ 1 & 1 \\end{pmatrix}`}</Eq> é ortogonal calculando <Eq>{`Q^T Q`}</Eq>.
</Exercicio>

<Exercicio numero="116.4" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "cap. 5, §2", licenca: "CC-BY-SA" }}
  solucao={<><Eq>{`\\det Q = \\cos\\theta \\cdot \\cos\\theta - (-\\sin\\theta)(\\sin\\theta) = \\cos^2\\theta + \\sin^2\\theta = 1`}</Eq>. Logo <Eq>{`\\det Q = 1`}</Eq> para toda rotação plana.</>}
  opcoes={[
    { texto: "A) det Q = 1, pois cos²θ + sin²θ = 1", correta: true },
    { texto: "B) det Q = 0, pois as colunas são ortogonais" },
    { texto: "C) det Q = -1, pois rotações invertem orientação" },
    { texto: "D) det Q depende do valor de θ" },
  ]}
>

Mostre que a matriz de rotação plana <Eq>{`Q = \\begin{pmatrix} \\cos\\theta & -\\sin\\theta \\\\ \\sin\\theta & \\cos\\theta \\end{pmatrix}`}</Eq> tem determinante 1 para qualquer <Eq>{`\\theta`}</Eq>.
</Exercicio>

<Exercicio numero="116.5" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§PSD", licenca: "GNU FDL" }}
  solucao={<>Fatoração de Cholesky de <Eq>{`A = \\begin{pmatrix} 4 & 2 \\\\ 2 & 5 \\end{pmatrix}`}</Eq>: <Eq>{`\\ell_{11} = 2`}</Eq>, <Eq>{`\\ell_{21} = 1`}</Eq>, <Eq>{`\\ell_{22} = \\sqrt{4} = 2`}</Eq>. Portanto <Eq>{`L = \\begin{pmatrix} 2 & 0 \\\\ 1 & 2 \\end{pmatrix}`}</Eq> e <Eq>{`LL^T = A`}</Eq>.</>}
  opcoes={[
    { texto: "A) L = [[2, 0], [1, 2]]", correta: true },
    { texto: "B) L = [[4, 0], [2, 5]]" },
    { texto: "C) L = [[2, 0], [2, 1]]" },
    { texto: "D) L = [[1, 0], [2, 2]]" },
  ]}
>

Calcule a fatoração de Cholesky de <Eq>{`A = \\begin{pmatrix} 4 & 2 \\\\ 2 & 5 \\end{pmatrix}`}</Eq>.
</Exercicio>

<Exercicio numero="116.6" dificuldade="aplicacao"
  resposta="λ₁ = 1, λ₂ = 9"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-symmetric-matrices.html", secao: "§7.2", licenca: "CC-BY-SA" }}
  solucao={<>Polinômio: <Eq>{`(5-\\lambda)^2 - 16 = 0 \\Rightarrow \\lambda^2 - 10\\lambda + 9 = 0 \\Rightarrow (\\lambda-1)(\\lambda-9) = 0`}</Eq>. Autovalores: <Eq>{`\\lambda_1 = 1`}</Eq> e <Eq>{`\\lambda_2 = 9`}</Eq>.</>}
  opcoes={[
    { texto: "A) λ₁ = 1 e λ₂ = 9", correta: true },
    { texto: "B) λ₁ = 5 e λ₂ = 5" },
    { texto: "C) λ₁ = 1 e λ₂ = 5" },
    { texto: "D) λ₁ = 9 e λ₂ = 16" },
  ]}
>

Calcule os autovalores de <Eq>{`A = \\begin{pmatrix} 5 & 4 \\\\ 4 & 5 \\end{pmatrix}`}</Eq>.
</Exercicio>

<Exercicio numero="116.7" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§OD", licenca: "GNU FDL" }}
  solucao={<>Para <Eq>{`\\lambda_1 = 1`}</Eq>: <Eq>{`(A-I)v = 0 \\Rightarrow 4v_1 + 4v_2 = 0 \\Rightarrow v = \\frac{1}{\\sqrt2}(1,-1)^T`}</Eq>. Para <Eq>{`\\lambda_2 = 9`}</Eq>: <Eq>{`(A-9I)v = 0 \\Rightarrow -4v_1+4v_2=0 \\Rightarrow v = \\frac{1}{\\sqrt2}(1,1)^T`}</Eq>. Os autovetores são ortogonais (teorema espectral).</>}
  opcoes={[
    { texto: "A) q₁ = (1/√2)(1,-1)^T e q₂ = (1/√2)(1,1)^T, e são ortogonais", correta: true },
    { texto: "B) q₁ = (1,0)^T e q₂ = (0,1)^T" },
    { texto: "C) q₁ = (1/√2)(1,1)^T e q₂ = (1/√2)(1,-1)^T, mas não são ortogonais" },
    { texto: "D) q₁ = (1,-1)^T e q₂ = (1,1)^T sem normalização" },
  ]}
  passos={<>
    <ol>
      <li>Para <Eq>{`\\lambda_1 = 1`}</Eq>: resolva <Eq>{`(A - I)v = 0`}</Eq>: <Eq>{`\\begin{pmatrix}4&4\\\\4&4\\end{pmatrix}v = 0`}</Eq>. <em>Por quê:</em> equação <Eq>{`4v_1 + 4v_2 = 0`}</Eq> dá <Eq>{`v_2 = -v_1`}</Eq>.</li>
      <li>Normalize: <Eq>{`v = (v_1, -v_1)^T`}</Eq> com <Eq>{`\\|v\\| = 1`}</Eq> dá <Eq>{`v_1 = 1/\\sqrt{2}`}</Eq>. Autovetor: <Eq>{`q_1 = (1/\\sqrt{2}, -1/\\sqrt{2})^T`}</Eq>.</li>
      <li>Para <Eq>{`\\lambda_2 = 9`}</Eq>: <Eq>{`(A-9I)v = 0`}</Eq>: <Eq>{`-4v_1+4v_2=0 \\Rightarrow v_2 = v_1`}</Eq>. Normalizado: <Eq>{`q_2 = (1/\\sqrt{2}, 1/\\sqrt{2})^T`}</Eq>.</li>
      <li>Verifique ortogonalidade: <Eq>{`q_1 \\cdot q_2 = (1/\\sqrt{2})(1/\\sqrt{2}) + (-1/\\sqrt{2})(1/\\sqrt{2}) = 1/2 - 1/2 = 0`}</Eq>. Ortogonais.</li>
    </ol>
    <p><em>Macete:</em> em matrizes <Eq>{`2 \\times 2`}</Eq> simétricas, o autovetor de <Eq>{`\\lambda_2`}</Eq> é sempre a rotação de 90° do autovetor de <Eq>{`\\lambda_1`}</Eq> — basta trocar as entradas e mudar um sinal.</p>
  </>}
>

Encontre autovetores ortonormais de <Eq>{`A = \\begin{pmatrix} 5 & 4 \\\\ 4 & 5 \\end{pmatrix}`}</Eq> correspondentes a <Eq>{`\\lambda_1 = 1`}</Eq> e <Eq>{`\\lambda_2 = 9`}</Eq>.
</Exercicio>

<Exercicio numero="116.8" dificuldade="aplicacao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-symmetric-matrices.html", secao: "§7.2", licenca: "CC-BY-SA" }}
  solucao={<>Com os autovetores <Eq>{`q_1 = \\frac{1}{\\sqrt2}(1,-1)^T`}</Eq> e <Eq>{`q_2 = \\frac{1}{\\sqrt2}(1,1)^T`}</Eq>: <Eq>{`Q = \\frac{1}{\\sqrt2}\\begin{pmatrix}1&1\\\\-1&1\\end{pmatrix}`}</Eq>, <Eq>{`D = \\begin{pmatrix}1&0\\\\0&9\\end{pmatrix}`}</Eq>. Verificação: <Eq>{`QDQ^T = A`}</Eq>.</>}
  opcoes={[
    { texto: "A) Q = (1/√2)[[1,1],[-1,1]] e D = diag(1, 9)", correta: true },
    { texto: "B) Q = [[1,0],[0,1]] e D = diag(5, 5)" },
    { texto: "C) Q = (1/√2)[[1,1],[-1,1]] e D = diag(9, 1)" },
    { texto: "D) Q = [[1,1],[1,-1]] (sem normalizar) e D = diag(1, 9)" },
  ]}
>

Monte a diagonalização ortogonal <Eq>{`A = QDQ^T`}</Eq> de <Eq>{`A = \\begin{pmatrix} 5 & 4 \\\\ 4 & 5 \\end{pmatrix}`}</Eq> usando os autovetores calculados no exercício anterior.
</Exercicio>

<Exercicio numero="116.9" dificuldade="aplicacao"
  resposta="PSD, não SPD"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§PSD", licenca: "GNU FDL" }}
  solucao={<><Eq>{`\\det A = 4 \\cdot 1 - 2^2 = 0`}</Eq>. Como o determinante é zero, um autovalor é zero. Critério de Sylvester falha (<Eq>{`\\det A = 0`}</Eq> não é positivo). Logo <Eq>{`A`}</Eq> é PSD mas não SPD.</>}
  opcoes={[
    { texto: "A) Apenas PSD, pois det A = 0 indica autovalor nulo", correta: true },
    { texto: "B) SPD, pois o elemento (1,1) = 4 > 0" },
    { texto: "C) Indefinida, pois o determinante é zero" },
    { texto: "D) SPD, pois todos os elementos são não-negativos" },
  ]}
>

Verifique se <Eq>{`A = \\begin{pmatrix} 4 & 2 \\\\ 2 & 1 \\end{pmatrix}`}</Eq> é SPD ou apenas PSD.
</Exercicio>

<Exercicio numero="116.10" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "cap. 5, §2", licenca: "CC-BY-SA" }}
  solucao={<>A reflexão em relação ao eixo <Eq>{`x`}</Eq> é <Eq>{`R = \\begin{pmatrix}1&0\\\\0&-1\\end{pmatrix}`}</Eq>. Verificação: <Eq>{`R^TR = I`}</Eq>. <Eq>{`\\det R = -1`}</Eq>: reflexão (não rotação pura).</>}
  opcoes={[
    { texto: "A) R = [[1,0],[0,-1]], ortogonal com det R = -1", correta: true },
    { texto: "B) R = [[1,0],[0,-1]], ortogonal com det R = 1" },
    { texto: "C) R = [[-1,0],[0,1]], ortogonal com det R = 1" },
    { texto: "D) R = [[0,1],[1,0]], com det R = -1" },
  ]}
>

Escreva a matriz da reflexão em relação ao eixo <Eq>{`x`}</Eq> em <Eq>{`\\mathbb{R}^2`}</Eq>. Verifique que é ortogonal e calcule seu determinante.
</Exercicio>

<Exercicio numero="116.11" dificuldade="aplicacao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-symmetric-matrices.html", secao: "§7.1", licenca: "CC-BY-SA" }}
  solucao={<>Verificar <Eq>{`Q^TQ`}</Eq>: colunas de <Eq>{`Q = \\begin{pmatrix}1&0\\\\0&0\\\\0&1\\end{pmatrix}`}</Eq> são ortonormais? <Eq>{`q_1 = (1,0,0)^T`}</Eq>, <Eq>{`q_2 = (0,0,1)^T`}</Eq>: ambas unitárias e ortogonais. Mas <Eq>{`Q`}</Eq> é <Eq>{`3 \\times 2`}</Eq>, não quadrada. Portanto não é ortogonal (definição requer quadrada). É chamada de **isometria** ou **matriz com colunas ortonormais**.</>}
  opcoes={[
    { texto: "A) As colunas são ortonormais, mas a matriz não é ortogonal pois não é quadrada", correta: true },
    { texto: "B) As colunas são ortonormais e a matriz é ortogonal" },
    { texto: "C) As colunas não são ortonormais pois a linha do meio é zero" },
    { texto: "D) A matriz é ortogonal pois Q^T Q = I₂" },
  ]}
>

As colunas da matriz <Eq>{`Q = \\begin{pmatrix}1&0\\\\0&0\\\\0&1\\end{pmatrix}`}</Eq> são ortonormais? A matriz é ortogonal? Justifique.
</Exercicio>

<Exercicio numero="116.12" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§OD", licenca: "GNU FDL" }}
  solucao={<>Simétrica: verificar <Eq>{`a_{ij} = a_{ji}`}</Eq>. Autovalores de <Eq>{`A = \\begin{pmatrix}1&2&0\\\\2&4&0\\\\0&0&1\\end{pmatrix}`}</Eq>: o bloco <Eq>{`\\begin{pmatrix}1&2\\\\2&4\\end{pmatrix}`}</Eq> tem autovalores <Eq>{`0`}</Eq> e <Eq>{`5`}</Eq>; o bloco <Eq>{`(1)`}</Eq> tem autovalor <Eq>{`1`}</Eq>. Autovalores: <Eq>{`0, 1, 5`}</Eq>. PSD (tem autovalor zero), não SPD.</>}
  opcoes={[
    { texto: "A) Autovalores 0, 1, 5 — PSD mas não SPD", correta: true },
    { texto: "B) Autovalores 1, 2, 4 — SPD" },
    { texto: "C) Autovalores 0, 1, 5 — indefinida pois tem autovalor zero" },
    { texto: "D) Autovalores 1, 4, 1 — SPD" },
  ]}
>

Determine os autovalores de <Eq>{`A = \\begin{pmatrix}1&2&0\\\\2&4&0\\\\0&0&1\\end{pmatrix}`}</Eq> e classifique: SPD, PSD ou indefinida.
</Exercicio>

<Exercicio numero="116.13" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§PSD", licenca: "GNU FDL" }}
  solucao={<>Cholesky de <Eq>{`A = \\begin{pmatrix}9&3\\\\3&5\\end{pmatrix}`}</Eq>: <Eq>{`\\ell_{11} = 3`}</Eq>, <Eq>{`\\ell_{21} = 1`}</Eq>, <Eq>{`\\ell_{22} = \\sqrt{5-1} = 2`}</Eq>. Logo <Eq>{`L = \\begin{pmatrix}3&0\\\\1&2\\end{pmatrix}`}</Eq>.</>}
  opcoes={[
    { texto: "A) L = [[3, 0], [1, 2]]", correta: true },
    { texto: "B) L = [[3, 0], [3, 4]]" },
    { texto: "C) L = [[9, 0], [1, 2]]" },
    { texto: "D) L = [[3, 0], [1, 5]]" },
  ]}
  passos={<>
    <ol>
      <li>Equacione <Eq>{`LL^T = A`}</Eq>: <Eq>{`\\ell_{11}^2 = 9 \\Rightarrow \\ell_{11} = 3`}</Eq>. <em>Por quê:</em> primeiro elemento é sempre a raiz do canto superior esquerdo.</li>
      <li><Eq>{`\\ell_{11}\\ell_{21} = 3 \\Rightarrow \\ell_{21} = 1`}</Eq>.</li>
      <li><Eq>{`\\ell_{21}^2 + \\ell_{22}^2 = 5 \\Rightarrow \\ell_{22} = \\sqrt{4} = 2`}</Eq>.</li>
      <li>Verificação: <Eq>{`LL^T = \\begin{pmatrix}3&0\\\\1&2\\end{pmatrix}\\begin{pmatrix}3&1\\\\0&2\\end{pmatrix} = \\begin{pmatrix}9&3\\\\3&5\\end{pmatrix}`}</Eq>. Correto.</li>
    </ol>
    <p><em>Macete:</em> no Cholesky, trabalhe coluna a coluna da esquerda para a direita; cada passo usa apenas os valores já calculados.</p>
  </>}
>

Calcule a fatoração de Cholesky de <Eq>{`A = \\begin{pmatrix} 9 & 3 \\\\ 3 & 5 \\end{pmatrix}`}</Eq>.
</Exercicio>

<Exercicio numero="116.14" dificuldade="aplicacao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-quadratic-forms.html", secao: "§7.3", licenca: "CC-BY-SA" }}
  solucao={<>A forma quadrática <Eq>{`Q(x,y) = 5x^2 + 6xy + 5y^2`}</Eq> tem matriz <Eq>{`A = \\begin{pmatrix}5&3\\\\3&5\\end{pmatrix}`}</Eq> (coeficiente do cruzado divide por 2 para os elementos fora da diagonal).</>}
  opcoes={[
    { texto: "A) A = [[5, 3], [3, 5]]", correta: true },
    { texto: "B) A = [[5, 6], [6, 5]]" },
    { texto: "C) A = [[5, 3], [6, 5]]" },
    { texto: "D) A = [[5, 0], [0, 5]]" },
  ]}
>

Escreva a matriz simétrica <Eq>{`A`}</Eq> associada à forma quadrática <Eq>{`Q(x, y) = 5x^2 + 6xy + 5y^2`}</Eq>.
</Exercicio>

<Exercicio numero="116.15" dificuldade="aplicacao"
  resposta="Definida positiva (λ₁ = 2, λ₂ = 8)"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-quadratic-forms.html", secao: "§7.3", licenca: "CC-BY-SA" }}
  solucao={<>Para <Eq>{`A = \\begin{pmatrix}5&3\\\\3&5\\end{pmatrix}`}</Eq>: autovalores <Eq>{`\\lambda = 5 \\pm 3`}</Eq>, ou seja <Eq>{`\\lambda_1 = 2 > 0`}</Eq> e <Eq>{`\\lambda_2 = 8 > 0`}</Eq>. Todos positivos: forma quadrática é **definida positiva**.</>}
  opcoes={[
    { texto: "A) Definida positiva, pois todos os autovalores são positivos", correta: true },
    { texto: "B) Indefinida, pois os autovalores têm sinais diferentes" },
    { texto: "C) Definida negativa, pois o coeficiente cruzado é positivo" },
    { texto: "D) Semidefinida positiva, pois um autovalor pode ser zero" },
  ]}
>

Classifique a forma quadrática <Eq>{`Q(x, y) = 5x^2 + 6xy + 5y^2`}</Eq> usando os autovalores da matriz associada.
</Exercicio>

<Exercicio numero="116.16" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "cap. 5, §III", licenca: "CC-BY-SA" }}
  solucao={<>Matriz de <Eq>{`Q(x,y) = x^2 - y^2`}</Eq>: <Eq>{`A = \\begin{pmatrix}1&0\\\\0&-1\\end{pmatrix}`}</Eq>. Autovalores: <Eq>{`\\lambda_1 = 1 > 0`}</Eq> e <Eq>{`\\lambda_2 = -1 < 0`}</Eq>. Sinais opostos: **indefinida**. A curva <Eq>{`x^2 - y^2 = 1`}</Eq> é uma hipérbole.</>}
  opcoes={[
    { texto: "A) Forma indefinida; a curva Q = 1 é uma hipérbole", correta: true },
    { texto: "B) Forma definida positiva; a curva Q = 1 é uma elipse" },
    { texto: "C) Forma definida negativa; a curva Q = 1 não tem pontos reais" },
    { texto: "D) Forma semidefinida; a curva Q = 1 é uma parábola" },
  ]}
  passos={<>
    <ol>
      <li>Escreva a matriz: <Eq>{`A = \\begin{pmatrix}1&0\\\\0&-1\\end{pmatrix}`}</Eq>. <em>Por quê:</em> sem termo cruzado, a matriz é já diagonal.</li>
      <li>Autovalores: leia da diagonal, <Eq>{`\\lambda_1 = 1`}</Eq> e <Eq>{`\\lambda_2 = -1`}</Eq>.</li>
      <li>Classificação: sinais opostos (um positivo, um negativo) — forma quadrática **indefinida**.</li>
      <li>Curva <Eq>{`x^2 - y^2 = 1`}</Eq>: hipérbole com eixos transverso ao longo de <Eq>{`x`}</Eq> e imaginário ao longo de <Eq>{`y`}</Eq>.</li>
    </ol>
    <p><em>Curiosidade:</em> a classificação de cônicas pelo sinal dos autovalores é a razão pela qual elipses, hipérboles e parábolas são as únicas seções cónicas — eles correspondem exatamente às combinações de sinais dos autovalores da forma quadrática associada.</p>
  </>}
>

Classifique a forma quadrática <Eq>{`Q(x, y) = x^2 - y^2`}</Eq> e identifique a curva <Eq>{`Q(x,y) = 1`}</Eq>.
</Exercicio>

<Exercicio numero="116.17" dificuldade="aplicacao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-quadratic-forms.html", secao: "§7.3", licenca: "CC-BY-SA" }}
  solucao={<>Autovalores de <Eq>{`A = \\begin{pmatrix}5&3\\\\3&5\\end{pmatrix}`}</Eq>: <Eq>{`\\lambda_1 = 2`}</Eq> e <Eq>{`\\lambda_2 = 8`}</Eq>. Forma canônica: <Eq>{`2u^2 + 8v^2 = 8`}</Eq>, ou seja <Eq>{`u^2/4 + v^2 = 1`}</Eq>. **Elipse** com semieixo maior <Eq>{`a = 2`}</Eq> e menor <Eq>{`b = 1`}</Eq>.</>}
  opcoes={[
    { texto: "A) Elipse com semieixo maior a = 2 e menor b = 1", correta: true },
    { texto: "B) Hipérbole com semieixos a = 2 e b = 1" },
    { texto: "C) Elipse com semieixo maior a = 8 e menor b = 2" },
    { texto: "D) Círculo de raio r = 2" },
  ]}
>

Identifique o tipo de cônica e encontre os semieixos para a curva <Eq>{`5x^2 + 6xy + 5y^2 = 8`}</Eq>.
</Exercicio>

<Exercicio numero="116.18" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§OD", licenca: "GNU FDL" }}
  solucao={<>A forma quadrática <Eq>{`Q(x,y) = 3x^2 + 4xy + 3y^2`}</Eq> tem matriz <Eq>{`\\begin{pmatrix}3&2\\\\2&3\\end{pmatrix}`}</Eq> com autovalores <Eq>{`\\lambda = 3 \\pm 2`}</Eq>, logo <Eq>{`\\lambda_1 = 1, \\lambda_2 = 5`}</Eq>. Ambos positivos: **definida positiva**.</>}
  opcoes={[
    { texto: "A) Definida positiva, pois λ₁ = 1 e λ₂ = 5 são ambos positivos", correta: true },
    { texto: "B) Indefinida, pois o coeficiente do termo cruzado é positivo" },
    { texto: "C) Semidefinida positiva, pois o coeficiente cruzado pode anular a forma" },
    { texto: "D) Definida negativa, pois a diagonal é igual em ambas as posições" },
  ]}
>

Classifique a forma quadrática <Eq>{`Q(x, y) = 3x^2 + 4xy + 3y^2`}</Eq> via autovalores.
</Exercicio>

<Exercicio numero="116.19" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§PSD", licenca: "GNU FDL" }}
  solucao={<>Produto de ortogonais é ortogonal: <Eq>{`(Q_1 Q_2)^T(Q_1 Q_2) = Q_2^T Q_1^T Q_1 Q_2 = Q_2^T I Q_2 = I`}</Eq>. Logo <Eq>{`Q_1 Q_2`}</Eq> é ortogonal.</>}
  opcoes={[
    { texto: "A) Sim, pois (Q₁Q₂)^T(Q₁Q₂) = Q₂^T Q₁^T Q₁ Q₂ = I", correta: true },
    { texto: "B) Não, pois o produto de ortogonais pode ter determinante diferente de ±1" },
    { texto: "C) Somente se Q₁ = Q₂" },
    { texto: "D) Não, pois a condição Q^T = Q^{-1} não se propaga ao produto" },
  ]}
  passos={<>
    <ol>
      <li>Calcule <Eq>{`(Q_1 Q_2)^T = Q_2^T Q_1^T`}</Eq>. <em>Por quê:</em> transposta de produto inverte a ordem.</li>
      <li>Compute <Eq>{`(Q_1 Q_2)^T (Q_1 Q_2) = Q_2^T (Q_1^T Q_1) Q_2`}</Eq>.</li>
      <li>Como <Eq>{`Q_1`}</Eq> é ortogonal, <Eq>{`Q_1^T Q_1 = I`}</Eq>. Resta <Eq>{`Q_2^T I Q_2 = Q_2^T Q_2 = I`}</Eq>.</li>
    </ol>
    <p><em>Observação:</em> isso mostra que as matrizes ortogonais <Eq>{`n \\times n`}</Eq> formam um grupo (fechado sob produto e inversão = transposição). Esse grupo se chama <Eq>{`O(n)`}</Eq>.</p>
  </>}
>

Mostre que o produto de duas matrizes ortogonais <Eq>{`Q_1`}</Eq> e <Eq>{`Q_2`}</Eq> é ortogonal.
</Exercicio>

<Exercicio numero="116.20" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "cap. 5, §2", licenca: "CC-BY-SA" }}
  solucao={<>Matriz de permutação que troca linhas 1 e 2 de uma <Eq>{`3 \\times 3`}</Eq>: <Eq>{`P = \\begin{pmatrix}0&1&0\\\\1&0&0\\\\0&0&1\\end{pmatrix}`}</Eq>. Colunas: <Eq>{`(0,1,0)^T`}</Eq>, <Eq>{`(1,0,0)^T`}</Eq>, <Eq>{`(0,0,1)^T`}</Eq> — ortonormais. <Eq>{`P^TP = I`}</Eq>. <Eq>{`\\det P = -1`}</Eq> (reflexão).</>}
  opcoes={[
    { texto: "A) P = [[0,1,0],[1,0,0],[0,0,1]], ortogonal com det P = -1", correta: true },
    { texto: "B) P = [[1,0,0],[0,1,0],[0,0,1]], ortogonal com det P = 1" },
    { texto: "C) P = [[0,1,0],[1,0,0],[0,0,1]], ortogonal com det P = 1" },
    { texto: "D) P = [[1,1,0],[0,0,0],[0,0,1]], não é ortogonal" },
  ]}
>

Escreva a matriz de permutação <Eq>{`3 \\times 3`}</Eq> que troca as linhas 1 e 2. Verifique que é ortogonal e calcule seu determinante.
</Exercicio>

<Exercicio numero="116.21" dificuldade="aplicacao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-symmetric-matrices.html", secao: "§7.1", licenca: "CC-BY-SA" }}
  solucao={<>Para <Eq>{`A = \\begin{pmatrix}5&4\\\\4&5\\end{pmatrix}`}</Eq> com autovalores <Eq>{`1`}</Eq> e <Eq>{`9`}</Eq>: <Eq>{`\\sqrt{A} = Q \\begin{pmatrix}1&0\\\\0&3\\end{pmatrix} Q^T`}</Eq> onde <Eq>{`Q`}</Eq> é a diagonalização do exemplo anterior. <Eq>{`\\sqrt{A} = \\frac{1}{2}\\begin{pmatrix}4&2\\\\2&4\\end{pmatrix} = \\begin{pmatrix}2&1\\\\1&2\\end{pmatrix}`}</Eq>.</>}
  opcoes={[
    { texto: "A) √A = [[2, 1], [1, 2]]", correta: true },
    { texto: "B) √A = [[√5, √4], [√4, √5]]" },
    { texto: "C) √A = [[1, 0], [0, 3]]" },
    { texto: "D) √A = [[3, 1], [1, 3]]" },
  ]}
  passos={<>
    <ol>
      <li>Diagnonalize: <Eq>{`A = QDQ^T`}</Eq> com <Eq>{`D = \\text{diag}(1,9)`}</Eq>. <em>Por quê:</em> raiz quadrada de matriz diagonal é diagonal das raízes.</li>
      <li>Calcule <Eq>{`\\sqrt{D} = \\text{diag}(1, 3)`}</Eq>.</li>
      <li>Reconstrua: <Eq>{`\\sqrt{A} = Q\\sqrt{D}Q^T = \\frac{1}{2}\\begin{pmatrix}1&1\\\\-1&1\\end{pmatrix}\\begin{pmatrix}1&0\\\\0&3\\end{pmatrix}\\begin{pmatrix}1&-1\\\\1&1\\end{pmatrix}`}</Eq>.</li>
      <li>Multiplique: <Eq>{`= \\frac{1}{2}\\begin{pmatrix}4&2\\\\2&4\\end{pmatrix} = \\begin{pmatrix}2&1\\\\1&2\\end{pmatrix}`}</Eq>.</li>
    </ol>
    <p><em>Macete:</em> verificar que <Eq>{`(\\sqrt{A})^2 = A`}</Eq>: <Eq>{`\\begin{pmatrix}2&1\\\\1&2\\end{pmatrix}^2 = \\begin{pmatrix}5&4\\\\4&5\\end{pmatrix}`}</Eq>. Correto.</p>
  </>}
>

Calcule <Eq>{`\\sqrt{A}`}</Eq> para <Eq>{`A = \\begin{pmatrix} 5 & 4 \\\\ 4 & 5 \\end{pmatrix}`}</Eq> usando a diagonalização ortogonal.
</Exercicio>

<Exercicio numero="116.22" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§PSD", licenca: "GNU FDL" }}
  solucao={<>Para <Eq>{`A = \\begin{pmatrix}a&b\\\\b&c\\end{pmatrix}`}</Eq>: <Eq>{`\\text{tr}(A^TA) = \\text{tr}\\begin{pmatrix}a^2+b^2 & ab+bc \\\\ ab+bc & b^2+c^2\\end{pmatrix} = a^2 + 2b^2 + c^2 = \\sum a_{ij}^2`}</Eq>.</>}
  opcoes={[
    { texto: "A) Sim, tr(A^T A) = a² + 2b² + c² = Σa²ᵢⱼ para A simétrica 2×2", correta: true },
    { texto: "B) Não, tr(A^T A) = a² + b² + c²" },
    { texto: "C) Sim, mas somente quando a = c" },
    { texto: "D) Não, a fórmula vale apenas para matrizes ortogonais" },
  ]}
>

Verifique para uma matriz simétrica <Eq>{`2 \\times 2`}</Eq> que <Eq>{`\\text{tr}(A^TA) = \\sum_{i,j} a_{ij}^2`}</Eq> (norma de Frobenius ao quadrado).
</Exercicio>

<Exercicio numero="116.23" dificuldade="compreensao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-symmetric-matrices.html", secao: "§7.2", licenca: "CC-BY-SA" }}
  solucao={<>Não: ter autovalores positivos **não** basta. Precisamos que <Eq>{`A`}</Eq> seja também **simétrica**. Exemplo: <Eq>{`A = \\begin{pmatrix}2&3\\\\0&1\\end{pmatrix}`}</Eq> tem autovalores <Eq>{`1`}</Eq> e <Eq>{`2`}</Eq> (positivos), mas <Eq>{`A^T \\neq A`}</Eq>. Porém <Eq>{`x^T A x`}</Eq> não é necessariamente positivo para todo <Eq>{`x \\neq 0`}</Eq> se <Eq>{`A`}</Eq> não é simétrica.</>}
  opcoes={[
    { texto: "A) Sim, autovalores positivos implicam SPD" },
    { texto: "B) Não, é necessário também que A seja simétrica", correta: true },
    { texto: "C) Sim, porque traço positivo implica todos autovalores positivos" },
    { texto: "D) Não, é necessário que A seja ortogonal" },
  ]}
>

Toda matriz com autovalores reais positivos é positiva definida?
</Exercicio>

<Exercicio numero="116.24" dificuldade="compreensao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "cap. 5, §2", licenca: "CC-BY-SA" }}
  solucao={<>Se <Eq>{`Qv = \\lambda v`}</Eq> com <Eq>{`Q`}</Eq> ortogonal, então <Eq>{`\\|v\\| = \\|Qv\\| = |\\lambda| \\|v\\|`}</Eq>. Para <Eq>{`v \\neq 0`}</Eq>, <Eq>{`|\\lambda| = 1`}</Eq>. Para rotação de 30°: autovalores em <Eq>{`\\mathbb{C}`}</Eq> são <Eq>{`e^{\\pm i \\pi/6}`}</Eq>, módulo 1. Confirmado.</>}
  opcoes={[
    { texto: "A) Os autovalores complexos têm parte real igual a 1" },
    { texto: "B) Os autovalores complexos têm módulo igual a 1", correta: true },
    { texto: "C) Os autovalores são sempre reais e iguais a ±1" },
    { texto: "D) Os autovalores são sempre iguais a det Q" },
  ]}
>

O que se pode concluir sobre os autovalores (complexos) de uma matriz ortogonal?
</Exercicio>

<Exercicio numero="116.25" dificuldade="compreensao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-symmetric-matrices.html", secao: "§7.1", licenca: "CC-BY-SA" }}
  solucao={<>Autovetores de matrizes simétricas para **autovalores distintos** são ortogonais (teorema espectral). Se <Eq>{`Av_1 = \\lambda_1 v_1`}</Eq> e <Eq>{`Av_2 = \\lambda_2 v_2`}</Eq> com <Eq>{`\\lambda_1 \\neq \\lambda_2`}</Eq>: <Eq>{`\\lambda_1 (v_1 \\cdot v_2) = (Av_1) \\cdot v_2 = v_1 \\cdot (Av_2) = \\lambda_2 (v_1 \\cdot v_2)`}</Eq>. Como <Eq>{`\\lambda_1 \\neq \\lambda_2`}</Eq>, segue <Eq>{`v_1 \\cdot v_2 = 0`}</Eq>.</>}
  opcoes={[
    { texto: "A) Sempre, independente dos autovalores" },
    { texto: "B) Somente se os autovalores forem distintos", correta: true },
    { texto: "C) Nunca, pois autovetores não têm razão para ser ortogonais" },
    { texto: "D) Somente se a matriz for também ortogonal" },
  ]}
>

Quando autovetores de uma matriz simétrica real são garantidamente ortogonais?
</Exercicio>

<Exercicio numero="116.26" dificuldade="aplicacao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§OD", licenca: "GNU FDL" }}
  solucao={<><Eq>{`H = I - 2vv^T`}</Eq> com <Eq>{`\\|v\\| = 1`}</Eq>. Verificar <Eq>{`H^TH = I`}</Eq>: <Eq>{`H^T = (I - 2vv^T)^T = I - 2vv^T = H`}</Eq> (simétrica). <Eq>{`H^2 = I - 4vv^T + 4v(v^Tv)v^T = I - 4vv^T + 4vv^T = I`}</Eq>. Logo <Eq>{`H^2 = I`}</Eq>, que com <Eq>{`H = H^T`}</Eq> dá <Eq>{`H^TH = I`}</Eq>. Ortogonal.</>}
  opcoes={[
    { texto: "A) H é ortogonal e simétrica, pois H^T = H e H² = I", correta: true },
    { texto: "B) H é ortogonal mas não simétrica" },
    { texto: "C) H é simétrica mas não ortogonal, pois det H pode ser 0" },
    { texto: "D) H é ortogonal somente se v for um vetor da base canônica" },
  ]}
>

Seja <Eq>{`H = I - 2vv^T`}</Eq> com <Eq>{`\\|v\\| = 1`}</Eq> (reflexão de Householder). Verifique que <Eq>{`H`}</Eq> é ortogonal e simétrica.
</Exercicio>

<Exercicio numero="116.27" dificuldade="aplicacao"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "cap. 5, §2", licenca: "CC-BY-SA" }}
  solucao={<>Decomposição <Eq>{`A = S + K`}</Eq>: parte simétrica <Eq>{`S = (A + A^T)/2`}</Eq> e antissimétrica <Eq>{`K = (A - A^T)/2`}</Eq>. Verificação: <Eq>{`S^T = ((A+A^T)/2)^T = (A^T+A)/2 = S`}</Eq>. <Eq>{`K^T = ((A-A^T)/2)^T = (A^T-A)/2 = -K`}</Eq>. E <Eq>{`S + K = A`}</Eq>.</>}
  opcoes={[
    { texto: "A) S = (A + A^T)/2 é simétrica e K = (A - A^T)/2 é antissimétrica, e S + K = A", correta: true },
    { texto: "B) S = A e K = 0 para qualquer A" },
    { texto: "C) A decomposição existe mas não é única" },
    { texto: "D) S = A^T e K = A - A^T" },
  ]}
  passos={<>
    <ol>
      <li>Defina <Eq>{`S = (A + A^T)/2`}</Eq> e <Eq>{`K = (A - A^T)/2`}</Eq>. <em>Por quê:</em> essa é a decomposição canônica em parte simétrica e antissimétrica.</li>
      <li>Verifique que <Eq>{`S^T = S`}</Eq>: transponha e use <Eq>{`(A^T)^T = A`}</Eq>.</li>
      <li>Verifique que <Eq>{`K^T = -K`}</Eq>: antissimétrica por definição.</li>
      <li>Confirme <Eq>{`S + K = (A+A^T)/2 + (A-A^T)/2 = A`}</Eq>.</li>
    </ol>
    <p><em>Curiosidade:</em> essa decomposição é única — é o análogo matricial de escrever qualquer função como soma de uma parte par e uma parte ímpar.</p>
  </>}
>

Mostre que qualquer matriz quadrada <Eq>{`A`}</Eq> pode ser escrita como <Eq>{`A = S + K`}</Eq> onde <Eq>{`S = (A+A^T)/2`}</Eq> é simétrica e <Eq>{`K = (A-A^T)/2`}</Eq> é antissimétrica (ou seja, <Eq>{`K^T = -K`}</Eq>).
</Exercicio>

<Exercicio numero="116.28" dificuldade="aplicacao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-quadratic-forms.html", secao: "§7.3", licenca: "CC-BY-SA" }}
  solucao={<>Forme quadrática <Eq>{`Q(x,y) = 2x^2 - 4xy + 5y^2`}</Eq>: matriz <Eq>{`A = \\begin{pmatrix}2&-2\\\\-2&5\\end{pmatrix}`}</Eq>. Critério de Sylvester: <Eq>{`2 > 0`}</Eq> e <Eq>{`\\det A = 10 - 4 = 6 > 0`}</Eq>. **SPD.** A curva <Eq>{`Q(x,y) = 1`}</Eq> é uma elipse.</>}
  opcoes={[
    { texto: "A) SPD; a curva Q = 1 é uma elipse", correta: true },
    { texto: "B) Indefinida; a curva Q = 1 é uma hipérbole" },
    { texto: "C) PSD mas não SPD; a curva Q = 1 degenera em retas" },
    { texto: "D) SPD; a curva Q = 1 é um círculo pois tem termo cruzado negativo" },
  ]}
>

Classifique a forma quadrática <Eq>{`Q(x,y) = 2x^2 - 4xy + 5y^2`}</Eq> usando o critério de Sylvester e identifique a curva <Eq>{`Q(x,y) = 1`}</Eq>.
</Exercicio>

<Exercicio numero="116.29" dificuldade="modelagem"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§PSD", licenca: "GNU FDL" }}
  solucao={<>Matriz de covariância amostral de <Eq>{`n`}</Eq> observações <Eq>{`X_1, \\ldots, X_n \\in \\mathbb{R}^p`}</Eq>: <Eq>{`\\hat{\\Sigma} = \\frac{1}{n-1} \\sum_{i=1}^n (X_i - \\bar X)(X_i - \\bar X)^T`}</Eq>. Cada termo <Eq>{`(X_i - \\bar X)(X_i - \\bar X)^T`}</Eq> é PSD (é rank-1 positivo). Soma de PSD é PSD. Se os vetores <Eq>{`X_i - \\bar X`}</Eq> geram <Eq>{`\\mathbb{R}^p`}</Eq> (necessita <Eq>{`n \\geq p+1`}</Eq>), então <Eq>{`\\hat\\Sigma`}</Eq> é SPD.</>}
  opcoes={[
    { texto: "A) É sempre PSD; é SPD quando n ≥ p+1 e os dados geram R^p", correta: true },
    { texto: "B) É sempre SPD, pois covariâncias nunca podem ser negativas" },
    { texto: "C) É PSD somente se todas as variáveis forem positivas" },
    { texto: "D) É SPD sempre que n > p, sem condição adicional" },
  ]}
>

Explique por que a matriz de covariância amostral de <Eq>{`p`}</Eq> variáveis com <Eq>{`n > p`}</Eq> observações é sempre PSD, e sob que condição é SPD.
</Exercicio>

<Exercicio numero="116.30" dificuldade="modelagem"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "cap. 5, §III", licenca: "CC-BY-SA" }}
  solucao={<>Tensor de inércia <Eq>{`I`}</Eq> de um corpo rígido é simétrico (resultado do cálculo de integrais de massa): <Eq>{`I_{jk} = \\int (r^2 \\delta_{jk} - r_j r_k) \\, dm`}</Eq>. Por simetria do produto <Eq>{`r_j r_k`}</Eq>: <Eq>{`I_{jk} = I_{kj}`}</Eq>. Além disso <Eq>{`I`}</Eq> é SPD para corpos com distribuição de massa não degenerada. Seus autovetores são os **eixos principais de inércia** e os autovalores são os **momentos principais**.</>}
  opcoes={[
    { texto: "A) É simétrica pois I_jk = I_kj; autovetores são eixos principais, autovalores são momentos de inércia", correta: true },
    { texto: "B) É simétrica pois todos os momentos de inércia são iguais; autovetores são direções de força" },
    { texto: "C) É ortogonal pois descreve rotações; autovalores são velocidades angulares" },
    { texto: "D) É SPD mas não simétrica; autovetores são torques aplicados" },
  ]}
>

Em mecânica clássica, o tensor de inércia de um corpo rígido é uma matriz <Eq>{`3 \\times 3`}</Eq>. Explique por que é simétrica e o que seus autovetores e autovalores representam fisicamente.
</Exercicio>

<Exercicio numero="116.31" dificuldade="modelagem"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-quadratic-forms.html", secao: "§7.3", licenca: "CC-BY-SA" }}
  solucao={<>A hessiana <Eq>{`H`}</Eq> de <Eq>{`f`}</Eq> em <Eq>{`x^*`}</Eq> é simétrica (Schwarz). Se <Eq>{`H`}</Eq> é SPD, a aproximação quadrática de Taylor <Eq>{`f(x^* + h) \\approx f(x^*) + \\nabla f(x^*)^T h + \\frac{1}{2} h^T H h`}</Eq> garante que <Eq>{`f(x^* + h) > f(x^*)`}</Eq> para todo <Eq>{`h \\neq 0`}</Eq> pequeno (pois <Eq>{`\\nabla f(x^*) = 0`}</Eq> em ponto crítico e <Eq>{`h^T H h > 0`}</Eq>). Isso confirma mínimo local estrito.</>}
  opcoes={[
    { texto: "A) H SPD garante h^T H h > 0, confirmando que f aumenta em todas as direções a partir de x*", correta: true },
    { texto: "B) H SPD implica máximo local, pois todos os autovalores são positivos" },
    { texto: "C) H simétrica já basta para garantir mínimo local; SPD não é necessário" },
    { texto: "D) H SPD implica mínimo global, não apenas local" },
  ]}
>

Em um ponto crítico <Eq>{`x^*`}</Eq> de <Eq>{`f: \\mathbb{R}^n \\to \\mathbb{R}`}</Eq>, a hessiana <Eq>{`H`}</Eq> é simétrica. Explique por que <Eq>{`H`}</Eq> SPD implica que <Eq>{`x^*`}</Eq> é um mínimo local estrito.
</Exercicio>

<Exercicio numero="116.32" dificuldade="modelagem"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§OD", licenca: "GNU FDL" }}
  solucao={<>Em simulação Monte Carlo correlacionada, para gerar <Eq>{`x \\sim \\mathcal{N}(0, \\Sigma)`}</Eq>: (1) fatorar <Eq>{`\\Sigma = LL^T`}</Eq> via Cholesky; (2) gerar <Eq>{`z \\sim \\mathcal{N}(0, I)`}</Eq>; (3) calcular <Eq>{`x = Lz`}</Eq>. Verificação: <Eq>{`\\text{Cov}(x) = L \\text{Cov}(z) L^T = L I L^T = \\Sigma`}</Eq>. Cholesky requer <Eq>{`\\Sigma`}</Eq> SPD.</>}
  opcoes={[
    { texto: "A) Fatorar Σ = LL^T; gerar z ~ N(0,I); calcular x = Lz, que tem Cov(x) = Σ", correta: true },
    { texto: "B) Usar a diagonalização Σ = QDQ^T; gerar x = Qz com z ~ N(0,D)" },
    { texto: "C) Gerar x diretamente da distribuição normal multivariada sem fatoração" },
    { texto: "D) Fatorar Σ = QDQ^T e gerar x = Q^T z com z ~ N(0,I)" },
  ]}
>

Em simulação Monte Carlo financeira, explique como a fatoração de Cholesky de uma matriz de covariância <Eq>{`\\Sigma`}</Eq> SPD é usada para gerar vetores aleatórios correlacionados.
</Exercicio>

<Exercicio numero="116.33" dificuldade="modelagem"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "cap. 5, §2", licenca: "CC-BY-SA" }}
  solucao={<>Compressão de imagem via DCT (JPEG): a matriz DCT <Eq>{`N \\times N`}</Eq> é ortogonal. Multiplicar por ela preserva a energia total da imagem (Parseval: <Eq>{`\\|DCT(x)\\| = \\|x\\|`}</Eq>). Os coeficientes de alta frequência geralmente têm magnitude pequena e são descartados com pequena perda de energia. A reconstrução usa a inversa = transposta da DCT.</>}
  opcoes={[
    { texto: "A) A DCT preserva energia total (Parseval); descartar coeficientes pequenos causa perda proporcional à energia descartada", correta: true },
    { texto: "B) A DCT aumenta a energia dos coeficientes de baixa frequência, facilitando a compressão" },
    { texto: "C) A ortogonalidade da DCT garante que nenhuma informação é perdida na compressão" },
    { texto: "D) A reconstrução usa a inversa da DCT, que é calculada por eliminação gaussiana" },
  ]}
>

Em compressão de imagem (JPEG), a matriz DCT é ortogonal. Explique como essa propriedade garante que comprimir (descartar alguns coeficientes) causa perda controlada de informação.
</Exercicio>

<Exercicio numero="116.34" dificuldade="modelagem"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-quadratic-forms.html", secao: "§7.3", licenca: "CC-BY-SA" }}
  solucao={<>Laplaciano de grafo: <Eq>{`L = D - W`}</Eq> (grau menos adjacência). Simétrico: <Eq>{`L^T = D^T - W^T = D - W = L`}</Eq> (grafo não-dirigido). PSD: <Eq>{`x^T L x = \\sum_{(i,j) \\in E} w_{ij}(x_i - x_j)^2 \\geq 0`}</Eq>. Autovalor zero tem multiplicidade = número de componentes conexas do grafo.</>}
  opcoes={[
    { texto: "A) x^T L x = Σ w_ij(x_i - x_j)² ≥ 0 pois todos os termos são não-negativos (w_ij > 0)", correta: true },
    { texto: "B) x^T L x pode ser negativo quando o grafo tem ciclos" },
    { texto: "C) L é SPD, pois o grafo conexo garante que todos os autovalores são positivos" },
    { texto: "D) L é simétrica mas indefinida; o autovalor zero torna a forma indefinida" },
  ]}
>

O laplaciano <Eq>{`L = D - W`}</Eq> de um grafo não-dirigido com pesos positivos é simétrico e PSD. Mostre por que <Eq>{`x^T L x = \\sum_{(i,j)} w_{ij}(x_i - x_j)^2 \\geq 0`}</Eq>.
</Exercicio>

<Exercicio numero="116.35" dificuldade="compreensao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§OD", licenca: "GNU FDL" }}
  solucao={<>Para <Eq>{`A`}</Eq> simétrica real e <Eq>{`Av = \\lambda v`}</Eq> com <Eq>{`v \\neq 0`}</Eq>: <Eq>{`\\lambda \\|v\\|^2 = \\lambda \\langle v, v \\rangle = \\langle \\lambda v, v \\rangle = \\langle Av, v \\rangle = \\langle v, A^T v \\rangle = \\langle v, Av \\rangle = \\langle v, \\lambda v \\rangle = \\bar\\lambda \\|v\\|^2`}</Eq>. Logo <Eq>{`\\lambda = \\bar\\lambda`}</Eq>, ou seja <Eq>{`\\lambda \\in \\mathbb{R}`}</Eq>.</>}
  opcoes={[
    { texto: "A) Porque λ‖v‖² = ⟨Av,v⟩ = ⟨v,Av⟩ = λ̄‖v‖², logo λ = λ̄ ∈ R", correta: true },
    { texto: "B) Porque a diagonal de A contém apenas os autovalores" },
    { texto: "C) Porque toda matriz real tem autovalores reais, independente de simetria" },
    { texto: "D) Porque a simetria implica que o polinômio característico tem coeficientes inteiros" },
  ]}
  passos={<>
    <ol>
      <li>Suponha <Eq>{`\\lambda \\in \\mathbb{C}`}</Eq> e <Eq>{`Av = \\lambda v`}</Eq> com <Eq>{`v \\neq 0`}</Eq>. <em>Por quê:</em> não assumimos a priori que <Eq>{`\\lambda`}</Eq> é real.</li>
      <li>Calcule <Eq>{`\\bar v^T A v`}</Eq> de dois modos: (a) <Eq>{`\\bar v^T (Av) = \\bar v^T (\\lambda v) = \\lambda \\|v\\|^2`}</Eq>; (b) <Eq>{`\\bar v^T A v = (A^T \\bar v)^T v = (A \\bar v)^T v = (\\overline{Av})^T v = (\\overline{\\lambda v})^T v = \\bar\\lambda \\|v\\|^2`}</Eq>.</li>
      <li>Igualando: <Eq>{`\\lambda \\|v\\|^2 = \\bar\\lambda \\|v\\|^2`}</Eq>. Como <Eq>{`\\|v\\|^2 > 0`}</Eq>, segue <Eq>{`\\lambda = \\bar\\lambda`}</Eq>, ou seja <Eq>{`\\lambda \\in \\mathbb{R}`}</Eq>.</li>
    </ol>
    <p><em>Observação:</em> este argumento usa <Eq>{`A = A^T`}</Eq> e <Eq>{`A`}</Eq> real (para <Eq>{`\\overline{Av} = A\\bar v`}</Eq>). Para matrizes hermitianas complexas (<Eq>{`A^* = A`}</Eq>), o mesmo argumento funciona com o produto interno hermitiano.</p>
  </>}
>

Por que toda matriz simétrica real tem autovalores reais? (Demonstre usando o produto interno complexo.)
</Exercicio>

<Exercicio numero="116.36" dificuldade="compreensao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-symmetric-matrices.html", secao: "§7.2", licenca: "CC-BY-SA" }}
  solucao={<>Sejam <Eq>{`Av_1 = \\lambda_1 v_1`}</Eq> e <Eq>{`Av_2 = \\lambda_2 v_2`}</Eq> com <Eq>{`\\lambda_1 \\neq \\lambda_2`}</Eq>. Então: <Eq>{`\\lambda_1 \\langle v_1, v_2 \\rangle = \\langle Av_1, v_2 \\rangle = \\langle v_1, Av_2 \\rangle = \\lambda_2 \\langle v_1, v_2 \\rangle`}</Eq> (usamos <Eq>{`A = A^T`}</Eq>). Como <Eq>{`\\lambda_1 \\neq \\lambda_2`}</Eq>, temos <Eq>{`\\langle v_1, v_2 \\rangle = 0`}</Eq>.</>}
  opcoes={[
    { texto: "A) Pois λ₁⟨v₁,v₂⟩ = ⟨Av₁,v₂⟩ = ⟨v₁,Av₂⟩ = λ₂⟨v₁,v₂⟩, e λ₁ ≠ λ₂ implica ⟨v₁,v₂⟩ = 0", correta: true },
    { texto: "B) Pois toda matriz simétrica tem autovetores perpendiculares, independente dos autovalores" },
    { texto: "C) Pois a transposição de A troca os autovetores, tornando-os ortogonais" },
    { texto: "D) Pois autovetores de autovalores distintos são necessariamente linearmente independentes e, portanto, ortogonais" },
  ]}
>

Demonstre que autovetores de uma matriz simétrica correspondentes a autovalores distintos são ortogonais.
</Exercicio>

<Exercicio numero="116.37" dificuldade="desafio"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§OD", licenca: "GNU FDL" }}
  solucao={<>Para <Eq>{`Q`}</Eq> ortogonal: <Eq>{`1 = \\det(I) = \\det(Q^TQ) = \\det(Q^T)\\det(Q) = (\\det Q)^2`}</Eq>. Logo <Eq>{`\\det Q = \\pm 1`}</Eq>.</>}
  opcoes={[
    { texto: "A) Pois det(Q^T Q) = det(I) = 1 = (det Q)², logo det Q = ±1", correta: true },
    { texto: "B) Pois det Q = 1 sempre, por preservação de volume" },
    { texto: "C) Pois det Q = 0, já que as colunas são ortonormais e dependentes" },
    { texto: "D) Pois det Q = ±1 somente para matrizes de rotação, não reflexão" },
  ]}
>

Demonstre que toda matriz ortogonal tem determinante <Eq>{`\\pm 1`}</Eq>.
</Exercicio>

<Exercicio numero="116.38" dificuldade="desafio"
  fonte={{ livro: "Hefferon, Linear Algebra", url: "https://hefferon.net/linearalgebra/", secao: "cap. 5, §III", licenca: "CC-BY-SA" }}
  solucao={<>Se <Eq>{`A`}</Eq> é ortogonal, <Eq>{`Q^{-1} = Q^T`}</Eq>. Logo <Eq>{`Qv = \\lambda v \\Rightarrow v = Q^{-1}(\\lambda v) = Q^T(\\lambda v) = \\lambda Q^T v`}</Eq>. Também: <Eq>{`Q^T v = v/\\lambda`}</Eq> e <Eq>{`|\\lambda|^2 \\|v\\|^2 = \\|Qv\\|^2 = \\|v\\|^2`}</Eq> (pois <Eq>{`Q`}</Eq> preserva norma). Logo <Eq>{`|\\lambda|=1`}</Eq>.</>}
  opcoes={[
    { texto: "A) Pois ‖Qv‖ = ‖v‖ (Q preserva norma) e ‖Qv‖ = |λ|‖v‖, logo |λ| = 1", correta: true },
    { texto: "B) Pois det Q = ±1 implica que todos os autovalores são ±1" },
    { texto: "C) Pois Q^{-1} = Q^T implica que λ = 1/λ̄, que dá |λ|² = 1 somente se λ é real" },
    { texto: "D) Pois os autovalores de Q são sempre reais e iguais a ±1, logo |λ| = 1" },
  ]}
>

Se <Eq>{`Q`}</Eq> é ortogonal e <Eq>{`Qv = \\lambda v`}</Eq> (autovalor complexo), mostre que <Eq>{`|\\lambda| = 1`}</Eq>.
</Exercicio>

<Exercicio numero="116.39" dificuldade="desafio"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-symmetric-matrices.html", secao: "§7.1", licenca: "CC-BY-SA" }}
  solucao={<>Reciprocidade: <Eq>{`Q`}</Eq> ortogonal <Eq>{`\\Leftrightarrow`}</Eq> <Eq>{`Q`}</Eq> preserva produtos internos. (<Eq>{`\\Rightarrow`}</Eq>) <Eq>{`\\langle Qu, Qv \\rangle = (Qu)^T(Qv) = u^T Q^T Q v = u^T I v = \\langle u, v \\rangle`}</Eq>. (<Eq>{`\\Leftarrow`}</Eq>) Se <Eq>{`\\langle Qu, Qv \\rangle = \\langle u, v \\rangle`}</Eq> para todo <Eq>{`u, v`}</Eq>, então tomando a base canônica: <Eq>{`(Q^TQ)_{ij} = \\langle Q e_i, Q e_j \\rangle = \\langle e_i, e_j \\rangle = \\delta_{ij}`}</Eq>. Logo <Eq>{`Q^TQ = I`}</Eq>.</>}
  opcoes={[
    { texto: "A) Q ortogonal ↔ Q preserva produtos internos; ambas as implicações decorrem de Q^T Q = I", correta: true },
    { texto: "B) Q preserva normas implica Q preserva produtos internos, mas a recíproca é falsa" },
    { texto: "C) Q preserva produtos internos somente se det Q = 1" },
    { texto: "D) Q ortogonal implica Q preserva produtos internos, mas a recíproca exige Q simétrica" },
  ]}
>

Demonstre que <Eq>{`Q`}</Eq> é ortogonal se e somente se <Eq>{`Q`}</Eq> preserva todos os produtos internos: <Eq>{`\\langle Qu, Qv \\rangle = \\langle u, v \\rangle`}</Eq> para todos <Eq>{`u, v`}</Eq>.
</Exercicio>

<Exercicio numero="116.40" dificuldade="demonstracao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§OD", licenca: "GNU FDL" }}
  solucao={<>Para <Eq>{`A = \\begin{pmatrix}a&b\\\\b&c\\end{pmatrix}`}</Eq> simétrica, o polinômio característico tem discriminante <Eq>{`\\Delta = (a-c)^2 + 4b^2 \\geq 0`}</Eq>. Logo dois autovalores reais <Eq>{`\\lambda_{1,2} = \\frac{(a+c) \\pm \\sqrt{\\Delta}}{2}`}</Eq>. Para cada, encontra-se autovetor unitário. Se <Eq>{`\\lambda_1 \\neq \\lambda_2`}</Eq>, são ortogonais. Se <Eq>{`\\lambda_1 = \\lambda_2 = \\lambda`}</Eq>, então <Eq>{`A = \\lambda I`}</Eq> (verificar: <Eq>{`a = c`}</Eq> e <Eq>{`b = 0`}</Eq>), e qualquer base ortonormal diagonaliza <Eq>{`A`}</Eq>. Em ambos os casos, <Eq>{`A = QDQ^T`}</Eq>.</>}
  opcoes={[
    { texto: "A) O discriminante Δ = (a-c)² + 4b² ≥ 0 garante autovalores reais; os dois casos (distintos ou iguais) cobrem todas as simétricas 2×2", correta: true },
    { texto: "B) A prova exige indução em n; o caso 2×2 não pode ser tratado diretamente" },
    { texto: "C) Se λ₁ = λ₂, a matriz não é diagonalizável ortogonalmente" },
    { texto: "D) O polinômio característico de A pode ter raízes complexas se b ≠ 0" },
  ]}
>

Demonstre o teorema espectral para matrizes simétricas <Eq>{`2 \\times 2`}</Eq>: toda <Eq>{`A = A^T \\in \\mathcal{M}_2(\\mathbb{R})`}</Eq> é ortogonalmente diagonalizável.
</Exercicio>

<Exercicio numero="116.41" dificuldade="demonstracao"
  fonte={{ livro: "Beezer, A First Course in Linear Algebra", url: "http://linear.pugetsound.edu/fcla/fcla.html", secao: "§PSD", licenca: "GNU FDL" }}
  solucao={<>(<Eq>{`\\Rightarrow`}</Eq>) Se <Eq>{`A`}</Eq> é SPD, pelo espectral <Eq>{`A = QDQ^T`}</Eq> com <Eq>{`\\lambda_i > 0`}</Eq>. Defina <Eq>{`L = Q\\sqrt{D}Q^T`}</Eq> (simétrica, inversível). Então <Eq>{`LL^T = (Q\\sqrt{D}Q^T)(Q\\sqrt{D}Q^T) = Q D Q^T = A`}</Eq>. (<Eq>{`\\Leftarrow`}</Eq>) Se <Eq>{`A = LL^T`}</Eq> com <Eq>{`L`}</Eq> inversível, então <Eq>{`x^TAx = x^TLL^Tx = \\|L^Tx\\|^2 > 0`}</Eq> para <Eq>{`x \\neq 0`}</Eq> (pois <Eq>{`L^T`}</Eq> é inversível: <Eq>{`L^Tx = 0 \\Rightarrow x = 0`}</Eq>).</>}
  opcoes={[
    { texto: "A) SPD → L = Q√D Q^T funciona; L inversível + A = LL^T → x^T A x = ‖L^T x‖² > 0", correta: true },
    { texto: "B) A = LL^T com L qualquer (não necessariamente inversível) já implica SPD" },
    { texto: "C) SPD implica A = LL^T somente se A for diagonal" },
    { texto: "D) A = LL^T implica apenas PSD; SPD requer condição adicional sobre det L" },
  ]}
>

Demonstre que uma matriz simétrica <Eq>{`A`}</Eq> é SPD se e somente se existe <Eq>{`L`}</Eq> inversível com <Eq>{`A = LL^T`}</Eq>.
</Exercicio>

<Exercicio numero="116.42" dificuldade="demonstracao"
  fonte={{ livro: "Austin, Understanding Linear Algebra", url: "https://understandinglinearalgebra.org/sec-symmetric-matrices.html", secao: "§7.1", licenca: "CC-BY-SA" }}
  solucao={<>Seja <Eq>{`Av = \\lambda v`}</Eq> com <Eq>{`v \\neq 0`}</Eq> e <Eq>{`\\lambda \\neq 0`}</Eq>. Então <Eq>{`(A^TA)(A^T v) = A^T(AA^T v)`}</Eq>... melhor: note <Eq>{`A^TA(A^Tv) = A^T(AA^T v)`}</Eq>. Considere que <Eq>{`A(A^Tv) = AA^T v`}</Eq>. Para relacionar autovalores de <Eq>{`A^TA`}</Eq> e <Eq>{`AA^T`}</Eq>: se <Eq>{`A^TAu = \\mu u`}</Eq> com <Eq>{`u \\neq 0`}</Eq> e <Eq>{`\\mu \\neq 0`}</Eq>, então <Eq>{`AA^T(Au) = A(A^TAu) = A(\\mu u) = \\mu(Au)`}</Eq>. Como <Eq>{`\\mu \\neq 0`}</Eq> e <Eq>{`u \\neq 0`}</Eq>, temos <Eq>{`Au \\neq 0`}</Eq> (pois <Eq>{`\\|Au\\|^2 = u^T A^T A u = \\mu \\|u\\|^2 > 0`}</Eq>). Logo <Eq>{`Au`}</Eq> é autovetor de <Eq>{`AA^T`}</Eq> com autovalor <Eq>{`\\mu`}</Eq>. Simetria garante a recíproca.</>}
  opcoes={[
    { texto: "A) Se A^T A u = μu (μ ≠ 0), então Au é autovetor de AA^T com mesmo autovalor μ; a recíproca é análoga", correta: true },
    { texto: "B) A^T A e AA^T sempre têm os mesmos autovalores, incluindo o zero" },
    { texto: "C) A^T A e AA^T têm os mesmos autovalores somente se A for quadrada" },
    { texto: "D) A^T A e AA^T têm os mesmos autovalores somente se A for simétrica" },
  ]}
>

Demonstre que <Eq>{`A^TA`}</Eq> e <Eq>{`AA^T`}</Eq> têm os mesmos autovalores não-nulos (com a mesma multiplicidade).
</Exercicio>

</ListaExercicios>

## Fontes

- [**Beezer, A First Course in Linear Algebra**](http://linear.pugetsound.edu/) — Rob Beezer · 2022 · EN · GNU FDL · §OD (Orthonormal Diagonalization), §PSD, §HMD. **Fonte primária desta lição.**
- [**Hefferon, Linear Algebra**](https://hefferon.net/linearalgebra/) — Jim Hefferon · 2020 · EN · CC-BY-SA · cap. 5 (Similarity) e cap. 5, §III (Formas bilineares).
- [**Austin, Understanding Linear Algebra**](https://understandinglinearalgebra.org/) — David Austin · 2023 · EN · CC-BY-SA · §7.1–7.3 (Orthogonal Diagonalization, Quadratic Forms).
