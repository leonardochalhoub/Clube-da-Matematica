---
titulo: "Lição 106 — Regressão múltipla"
slug: "licao-106-regressao-multipla"
categoria: "aulas"
subcategoria: "ano-3-trim-11"
descricao: "Modelo com p preditores, solução matricial OLS, R² ajustado, multicolinearidade, seleção de variáveis e diagnóstico de pressupostos."
ordem: 106
publicado: true
tags: ["estatística inferencial", "regressão múltipla", "OLS", "ano-3", "trim-11"]
prerrequisitos: ["licao-105-regressao-simples"]
autores: ["Clube da Matemática"]
atualizadoEm: "2026-05-06"
usadoEm: ["Stochastik LK alemão (Klasse 12)", "H2 Mathematics Singapura (§15)", "econometria introdutória"]
versao: "v1"
---

<EquacaoCanonica
  formula="\hat{\boldsymbol\beta} = (X^TX)^{-1}X^T\mathbf{y}"
  legenda={<>Na <strong>regressão múltipla</strong> com <Eq>{`p`}</Eq> preditores, o estimador OLS é a solução matricial das equações normais. Cada coeficiente <Eq>{`\\hat\\beta_j`}</Eq> mede o efeito de <Eq>{`X_j`}</Eq> sobre <Eq>{`Y`}</Eq> mantendo os demais preditores fixos — o chamado <em>efeito parcial</em>.</>}
  audioTexto="Regressão múltipla. Beta chapéu vetor igual à inversa de equis transposto equis, multiplicada por equis transposto ípsilon. Cada coeficiente beta jota chapéu é o efeito parcial do preditor jota, controlando os demais. Generalização matricial da regressão simples."
/>

<aside>

**Livros que cobrem esta lição**

- [**OpenIntro Statistics** (4ª ed.)](https://www.openintro.org/book/os/) · §8.1–8.4 · CC-BY-SA · <em>Tratamento pedagógico de regressão múltipla com interpretação de coeficientes parciais, R² ajustado e seleção de modelos; datasets disponíveis online para reprodução.</em>
- [**Statistics** — OpenStax (Illowsky, Dean)](https://openstax.org/details/books/statistics) · §13.1–13.5 · CC-BY · <em>Abordagem aplicada com tabelas ANOVA de regressão múltipla, testes F global e parcial, e exemplos com dados reais.</em>
- [**Probabilidade e Estatística** — Wikilivros](https://pt.wikibooks.org/wiki/Probabilidade_e_Estat%C3%ADstica) · Regressão múltipla · CC-BY-SA · <em>Referência em PT-BR com notação matricial compatível com o currículo de engenharia brasileira.</em>

</aside>

<DuasPortas>

<Porta nivel="formal" titulo="Definição rigorosa">

### Modelo de regressão linear múltipla

<Definicao titulo="Modelo e notacao matricial">

Para $n$ observações e $p$ preditores:

$$Y_i = \beta_0 + \beta_1 X_{i1} + \beta_2 X_{i2} + \cdots + \beta_p X_{ip} + \varepsilon_i, \quad \varepsilon_i \stackrel{i.i.d.}{\sim} \mathcal{N}(0,\sigma^2)$$

Em forma matricial: $\mathbf{Y} = X\boldsymbol\beta + \boldsymbol\varepsilon$, onde:

- $\mathbf{Y} \in \mathbb{R}^n$: vetor de respostas.
- $X \in \mathbb{R}^{n\times(p+1)}$: matriz de design (primeira coluna de 1s para o intercepto).
- $\boldsymbol\beta \in \mathbb{R}^{p+1}$: vetor de coeficientes.
- $\boldsymbol\varepsilon \in \mathbb{R}^n$: vetor de erros.

</Definicao>

> "The multiple regression model is $y = \beta_0 + \beta_1 x_1 + \beta_2 x_2 + \cdots + \beta_k x_k + \varepsilon$. The coefficient $\beta_i$ measures the expected change in $y$ per unit change in $x_i$ when all other predictors are held constant." — [OpenIntro Statistics, §8.1](https://www.openintro.org/book/os/), p. 362

<Definicao titulo="Estimador OLS e equacoes normais">

Minimizar $SSE = \|\mathbf{Y} - X\boldsymbol\beta\|^2$ leva às equações normais:

$$X^TX\hat{\boldsymbol\beta} = X^T\mathbf{Y}$$

cuja solução (quando $X^TX$ é invertível) é:

<Equation explicacao="Estimador OLS matricial: existe quando os preditores são linearmente independentes." latex="\hat{\boldsymbol\beta} = (X^TX)^{-1}X^T\mathbf{Y}" />

A matriz de variância-covariância dos estimadores é:

<Equation explicacao="Cov do estimador: inversamente proporcional a X'X. Mais dados ou mais variação em X reduz a variância." latex="\text{Cov}(\hat{\boldsymbol\beta}) = \sigma^2 (X^TX)^{-1}" />

</Definicao>

### Métricas de ajuste

<Definicao titulo="R² ajustado">

O $R^2$ cresce com qualquer preditor adicionado — mesmo irrelevante. O $R^2$ ajustado penaliza pelo número de parâmetros:

<Equation explicacao="R² ajustado: penaliza pela complexidade. Pode cair ao adicionar preditor fraco." latex="\bar R^2 = 1 - \frac{SSE/(n-p-1)}{SST/(n-1)}" />

Se $\bar R^2$ aumenta com novo preditor, o preditor melhora o modelo; se cai, prejudica.

</Definicao>

### Inferência

<Definicao titulo="Teste t parcial e teste F global">

**Teste $t$ parcial** para $H_0: \beta_j = 0$ (com os demais preditores no modelo):

<Equation explicacao="Testa se o preditor Xj tem efeito significativo dado que os demais já estão no modelo." latex="T_j = \frac{\hat\beta_j}{SE(\hat\beta_j)} \sim t_{n-p-1}" />

**Teste $F$ global** para $H_0: \beta_1 = \cdots = \beta_p = 0$:

<Equation explicacao="Testa se o modelo como um todo explica mais do que a média de Y." latex="F = \frac{SSR/p}{SSE/(n-p-1)} \sim F_{p,\,n-p-1}" />

</Definicao>

<Definicao titulo="Multicolinearidade e VIF">

Quando dois ou mais preditores são altamente correlacionados, $X^TX$ fica quase-singular e os $SE(\hat\beta_j)$ incham — coeficientes instáveis e difíceis de interpretar.

**Variance Inflation Factor:**

<Equation explicacao="VIF mede quanto a variância de beta_j aumenta pela correlação com os outros preditores. VIF > 10 é preocupante." latex="VIF_j = \frac{1}{1 - R_j^2}" />

onde $R_j^2$ é o $R^2$ da regressão de $X_j$ sobre os demais preditores.

</Definicao>

<Insight>

Em regressão simples, $\hat\beta_1$ e o coeficiente de correlação têm sempre o mesmo sinal. Em regressão múltipla, a correlação entre $X_j$ e $Y$ pode ter sinal oposto ao de $\hat\beta_j$ (efeito de supressão). Isso ocorre porque o coeficiente parcial controla para os outros preditores.

</Insight>

<figure className="not-prose my-5 rounded-xl border border-clube-mist-soft/40 bg-clube-surface p-4">
<svg viewBox="0 0 520 160" className="w-full">
<g>
  <text x="10" y="20" fontSize="11" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.8" fontWeight="bold">Matriz de design X (n=4, p=2)</text>
  <rect x="10" y="30" width="200" height="110" rx="4" fill="none" stroke="currentColor" strokeOpacity="0.3" strokeWidth="1"/>
  <text x="20" y="50" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">1   X₁₁   X₁₂</text>
  <text x="20" y="70" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">1   X₂₁   X₂₂</text>
  <text x="20" y="90" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">1   X₃₁   X₃₂</text>
  <text x="20" y="110" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">1   X₄₁   X₄₂</text>
  <text x="60" y="135" fontSize="9" fill="currentColor" fillOpacity="0.5" textAnchor="middle">n×(p+1)</text>
</g>
<g>
  <text x="230" y="82" fontSize="16" fill="rgb(232 199 122)" textAnchor="middle">×</text>
</g>
<g>
  <rect x="250" y="30" width="60" height="110" rx="4" fill="none" stroke="currentColor" strokeOpacity="0.3" strokeWidth="1"/>
  <text x="270" y="55" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">β₀</text>
  <text x="270" y="75" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">β₁</text>
  <text x="270" y="95" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">β₂</text>
  <text x="280" y="135" fontSize="9" fill="currentColor" fillOpacity="0.5" textAnchor="middle">(p+1)×1</text>
</g>
<g>
  <text x="335" y="82" fontSize="16" fill="rgb(232 199 122)" textAnchor="middle">=</text>
</g>
<g>
  <rect x="355" y="30" width="50" height="110" rx="4" fill="none" stroke="rgb(26 77 92)" strokeOpacity="0.5" strokeWidth="1.5"/>
  <text x="375" y="55" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(26 77 92)" fillOpacity="0.9">Ŷ₁</text>
  <text x="375" y="75" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(26 77 92)" fillOpacity="0.9">Ŷ₂</text>
  <text x="375" y="95" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(26 77 92)" fillOpacity="0.9">Ŷ₃</text>
  <text x="375" y="115" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(26 77 92)" fillOpacity="0.9">Ŷ₄</text>
  <text x="380" y="135" fontSize="9" fill="currentColor" fillOpacity="0.5" textAnchor="middle">n×1</text>
</g>
<g>
  <text x="425" y="82" fontSize="16" fill="rgb(232 199 122)" textAnchor="middle">+</text>
</g>
<g>
  <rect x="445" y="30" width="60" height="110" rx="4" fill="none" stroke="rgb(199 107 63)" strokeOpacity="0.5" strokeWidth="1.5"/>
  <text x="465" y="55" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(199 107 63)" fillOpacity="0.9">ε₁</text>
  <text x="465" y="75" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(199 107 63)" fillOpacity="0.9">ε₂</text>
  <text x="465" y="95" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(199 107 63)" fillOpacity="0.9">ε₃</text>
  <text x="465" y="115" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(199 107 63)" fillOpacity="0.9">ε₄</text>
  <text x="475" y="135" fontSize="9" fill="currentColor" fillOpacity="0.5" textAnchor="middle">n×1</text>
</g>
</svg>
<figcaption className="mt-2 text-center text-xs text-clube-mist">
Representação matricial do modelo: $\mathbf{Y} = X\boldsymbol\beta + \boldsymbol\varepsilon$. A primeira coluna de 1s em $X$ gera o intercepto $\beta_0$.
</figcaption>
</figure>

</Porta>

<Porta nivel="5" titulo="Pra criança de 5 anos">

Na aula anterior, você usou **uma régua** para prever uma coisa com outra: altura para prever peso.

Agora imagine que você quer prever o peso de uma pessoa e tem **duas informações**: altura E idade. Com duas informações juntas, sua previsão fica melhor!

Regressão múltipla é como ter **várias réguas ao mesmo tempo** — cada uma mede uma coisa diferente — e todas juntas fazem uma previsão mais certeira.

</Porta>

<Porta nivel="10" titulo="Pra criança de 10 anos">

Na regressão simples, você usava só **uma variável** ($X$) para prever ($Y$). Mas quase sempre temos mais informações disponíveis.

Por exemplo, para prever o **preço de uma casa** você pode usar:
- $X_1$ = área em m²
- $X_2$ = número de quartos
- $X_3$ = distância do centro (em km)

A regressão múltipla ajusta uma fórmula do tipo:

$$\hat Y = \hat\beta_0 + \hat\beta_1 X_1 + \hat\beta_2 X_2 + \hat\beta_3 X_3$$

Cada $\hat\beta_j$ diz: "se eu aumentar $X_j$ em uma unidade, mantendo os outros fixos, quanto $Y$ muda em média?"

</Porta>

<Porta nivel="15" titulo="Pra adolescente de 15 anos">

A regressão múltipla generaliza a simples incluindo $p$ preditores. O modelo é:

$$Y = \beta_0 + \beta_1 X_1 + \cdots + \beta_p X_p + \varepsilon$$

**Interpretação dos coeficientes:** $\hat\beta_j$ é o efeito de $X_j$ sobre $Y$, **mantendo todos os outros preditores constantes** (efeito parcial ou "ceteris paribus").

**Por que isso importa?** Imagine prever o salário de um funcionário usando anos de experiência ($X_1$) e nível de escolaridade ($X_2$). O efeito de experiência no salário é diferente quando você controla por escolaridade do que quando você ignora escolaridade.

**R² vs. R² ajustado:** adicionar qualquer variável aumenta $R^2$, mesmo que ela seja irrelevante. O $\bar R^2$ (ajustado) penaliza por variáveis extras — use-o para comparar modelos com número diferente de preditores.

</Porta>

<Porta nivel="25" titulo="Para estudante de engenharia">

**Álgebra matricial do modelo**

Dado $X \in \mathbb{R}^{n\times(p+1)}$ com posto coluna $p+1$ (preditores LI), o OLS resolve:

$$\min_{\boldsymbol\beta} \|\mathbf{Y} - X\boldsymbol\beta\|^2 \implies \hat{\boldsymbol\beta} = (X^TX)^{-1}X^T\mathbf{Y}$$

O vetor de valores ajustados é $\hat{\mathbf{Y}} = H\mathbf{Y}$, onde $H = X(X^TX)^{-1}X^T$ é o **hat matrix** (projeção ortogonal sobre o espaço coluna de $X$). Os resíduos $\mathbf{e} = (I-H)\mathbf{Y}$ são ortogonais a $\hat{\mathbf{Y}}$.

**Tabela ANOVA múltipla**

| Fonte | SS | df | MS | F |
|---|---|---|---|---|
| Regressão | $SSR$ | $p$ | $SSR/p$ | $MSR/MSE$ |
| Erro | $SSE$ | $n-p-1$ | $SSE/(n-p-1)$ | — |
| Total | $SST$ | $n-1$ | — | — |

**Seleção de variáveis:** AIC, BIC, $\bar R^2$, validação cruzada. Métodos automáticos: stepwise (forward, backward, both) — úteis mas não substituem raciocínio substantivo.

**Multicolinearidade:** $\text{det}(X^TX) \to 0$ implica inversão instável. Ridge regression ($L_2$): $\hat{\boldsymbol\beta}_{ridge} = (X^TX + \lambda I)^{-1}X^T\mathbf{Y}$ regulariza sem seleção; Lasso ($L_1$) seleciona variáveis automaticamente.

</Porta>

<Porta nivel="40" titulo="Para profissional sênior">

A regressão múltipla domina a análise empírica em ciências sociais, epidemiologia e finanças. Os debates metodológicos centrais:

**Identificação causal:** OLS com controles não resolve endogeneidade. O "modelo de resultado potencial" (Rubin) distingue efeito causal de associação condicional. Design baseado em variação exógena (experimentos naturais, RDD, DiD, IV) é a linguagem moderna da causalidade em economia — reconhecida pelo Nobel de Economia 2021 (Card, Angrist, Imbens). Veja [nobelprize.org/prizes/economic-sciences/2021/summary/](https://www.nobelprize.org/prizes/economic-sciences/2021/summary/).

**Overfitting e validação:** em amostras pequenas com muitos preditores ($p \approx n$), OLS overfitta. Regularização (ridge, lasso, elastic net) e validação cruzada $k$-fold são o padrão em ML; em econometria, o uso de dados de teste separados é ainda a norma.

**Erros padrão robustos:** heterocedasticidade e correlação serial violam $\text{Cov}(\boldsymbol\varepsilon) = \sigma^2 I$. Os erros de White (HC) e Newey-West (HAC) corrigem a matriz de variância sem exigir forma funcional para a heterocedast.

**Regressão em alta dimensão ($p > n$):** OLS não tem solução única. Lasso via coordenada descida, PCR, PLS. Inferência pós-seleção (debiased Lasso) é área ativa de pesquisa.

</Porta>

<Porta nivel="pratica" titulo="Aplicação brasileira">

**Preço de imóveis em São Paulo (dados simulados conforme padrão IBGE FipeZap)**

Regressão do preço de venda $Y$ (em R\$ mil) sobre: área $X_1$ (m²), número de quartos $X_2$ e distância do metro $X_3$ (km). Com $n=150$ transações:

$$\hat Y = 180 + 4{,}2 X_1 + 35 X_2 - 12 X_3$$

- $hatbeta_1 = 4{,}2$: cada m² adicional vale, em média, R\$ 4.200 (controlando quartos e metro).
- $hatbeta_2 = 35$: cada quarto extra adiciona R\$ 35.000 (controlando área e metro).
- $hatbeta_3 = -12$: cada km a mais do metro reduz R\$ 12.000 (controlando área e quartos).
- $R^2 = 0{,}78$, $\bar R^2 = 0{,}77$: o modelo explica 77% da variabilidade de preços ajustando pela complexidade.

**Teste F global:** $F = 172{,}4$ com $p$-valor menor que 0,001 — os três preditores juntos são altamente significativos.

**Cuidado:** multicolinearidade entre área e quartos ($r = 0{,}69$) infla os $SE$ — VIF calculados: $VIF_1 = 2{,}3$, $VIF_2 = 2{,}1$, $VIF_3 = 1{,}1$. Todos abaixo de 10: multicolinearidade aceitável.

</Porta>

</DuasPortas>

## Exemplos resolvidos

<Exemplo numero="106.1" titulo="Ajuste e interpretacao com dois preditores">

**Problema.** Regressão do salário anual $Y$ (R\$ mil) sobre experiência $X_1$ (anos) e escolaridade $X_2$ (anos de estudo) produziu: $hat Y = -12 + 2{,}8 X_1 + 5{,}2 X_2$, com $n=50$, $R^2=0{,}74$, $bar R^2=0{,}73$.

Interprete cada coeficiente e calcule a previsão para um profissional com 10 anos de experiência e 16 anos de estudo.

**Estratégia.** Interpretar cada $\hat\beta_j$ como efeito parcial (ceteris paribus). Substituir os valores na equação ajustada.

**Resolução.**

- $\hat\beta_0 = -12$: intercepto (sem interpretação substantiva isolada — nenhum profissional tem zero anos de experiência e estudo).
- $hatbeta_1 = 2{,}8$: controlando escolaridade, cada ano adicional de experiência está associado a R\$ 2.800/ano a mais, em média.
- $hatbeta_2 = 5{,}2$: controlando experiência, cada ano adicional de estudo está associado a R\$ 5.200/ano a mais.

Previsão: $hat Y = -12 + 2{,}8times10 + 5{,}2times16 = -12+28+83{,}2 = 99{,}2$ mil R\$/ano.

**Verificação.** $\bar R^2 = 0{,}73$ próximo de $R^2 = 0{,}74$: apenas 2 preditores, pouca penalização — modelo parsimonioso.

**Fonte.** [OpenIntro Statistics, §8.1, Exemplo 8.1](https://www.openintro.org/book/os/) — CC-BY-SA

</Exemplo>

<Exemplo numero="106.2" titulo="R² ajustado na selecao entre modelos">

**Problema.** Três modelos para prever o consumo de energia elétrica domiciliar ($n=80$):

| Modelo | Preditores | $R^2$ | $\bar R^2$ |
|---|---|---|---|
| M1 | área | 0,61 | 0,61 |
| M2 | área + nº moradores | 0,71 | 0,70 |
| M3 | área + nº moradores + renda + temperatura + dia semana | 0,74 | 0,70 |

Qual modelo preferir?

**Estratégia.** Comparar $\bar R^2$ — penaliza por número de preditores.

**Resolução.**

M2 e M3 têm o mesmo $\bar R^2 = 0{,}70$, mas M3 usa 5 preditores contra 2 de M2. Pelo princípio da parcimônia (navalha de Occam), **M2 é preferido**: adicionar 3 preditores não melhora o $\bar R^2$ — eles não contribuem informação líquida.

Note que $R^2$ de M3 (0,74) supera M2 (0,71) — mas $R^2$ sempre cresce com preditores adicionais, mesmo irrelevantes.

**Verificação.** Poderia confirmar com AIC/BIC: $AIC = n\ln(SSE/n) + 2(p+1)$ — menor é melhor.

**Fonte.** [OpenIntro Statistics, §8.3, Tabela 8.7](https://www.openintro.org/book/os/) — CC-BY-SA

</Exemplo>

<Exemplo numero="106.3" titulo="Teste F global e tabela ANOVA">

**Problema.** Regressão com $p=3$ preditores, $n=40$: $SST=2400$, $SSE=720$. Monte a tabela ANOVA e teste $H_0: \beta_1=\beta_2=\beta_3=0$ ao nível 5%.

**Estratégia.** Calcular SSR, df, MS, F; comparar com valor crítico $F_{3,36;\,0{,}05}$.

**Resolução.**

$SSR = 2400-720 = 1680$; $R^2 = 1680/2400 = 0{,}70$.

| Fonte | SS | df | MS | F |
|---|---|---|---|---|
| Regressão | 1680 | 3 | 560 | 28,0 |
| Erro | 720 | 36 | 20 | — |
| Total | 2400 | 39 | — | — |

$F_{3,36;\,0{,}05} \approx 2{,}87$. Como $F = 28{,}0 \gg 2{,}87$, rejeitamos $H_0$: o modelo como um todo é altamente significativo.

**Verificação.** $MSE = 20 \implies \hat\sigma = \sqrt{20} \approx 4{,}47$ — desvio padrão residual em unidades de $Y$.

**Fonte.** [OpenStax Statistics, §13.4](https://openstax.org/books/statistics/pages/13-introduction) — CC-BY

</Exemplo>

<Exemplo numero="106.4" titulo="Detectar multicolinearidade via VIF">

**Problema.** Regressão do preço de um veículo ($Y$) sobre potência ($X_1$), cilindrada ($X_2$) e peso ($X_3$). Regressões auxiliares: $R_1^2=0{,}89$ ($X_1$ sobre $X_2, X_3$), $R_2^2=0{,}91$, $R_3^2=0{,}85$. Calcule os VIFs e avalie.

**Estratégia.** $VIF_j = 1/(1-R_j^2)$.

**Resolução.**

$VIF_1 = 1/(1-0{,}89) = 9{,}09$; $VIF_2 = 1/(1-0{,}91) = 11{,}1$; $VIF_3 = 1/(1-0{,}85) = 6{,}67$.

$VIF_2 = 11{,}1 > 10$: cilindrada apresenta multicolinearidade severa com as demais variáveis.

**Ação:** remover $X_2$ do modelo ou combinar $X_1$ e $X_2$ em um índice composto (análise de componentes principais).

**Verificação.** Após remover $X_2$, recalcular VIFs para os preditores restantes.

**Fonte.** [OpenIntro Statistics, §8.4](https://www.openintro.org/book/os/) — CC-BY-SA

</Exemplo>

<Exemplo numero="106.5" titulo="Variavel dummy e interacao">

**Problema.** Prever salário $Y$ (R\$ mil/ano) usando experiência $X_1$ (anos) e gênero $D$ (1=feminino, 0=masculino). Resultado: $hat Y = 45 + 2{,}5X_1 - 8D$.

Interprete o coeficiente de $D$ e calcule os salários previstos para: (a) homem com 10 anos de experiência; (b) mulher com 10 anos de experiência.

**Estratégia.** A variável dummy cria dois interceptos diferentes (um por gênero), mantendo inclinação igual.

**Resolução.**

$hatbeta_D = -8$: controlando experiência, mulheres ganham em média R\$ 8.000/ano a menos do que homens — diferença bruta (não causal, pois não controlamos setor, cargo, jornada).

(a) Homem ($D=0$), $X_1=10$: $hat Y = 45 + 25 - 0 = 70$ mil R\$/ano.

(b) Mulher ($D=1$), $X_1=10$: $hat Y = 45 + 25 - 8 = 62$ mil R\$/ano.

**Verificação.** A diferença é constante ($-8$) para qualquer nível de experiência — modelo sem interação assume efeito aditivo. Para permitir inclinações diferentes, adicionar $X_1 \times D$.

**Fonte.** [OpenIntro Statistics, §8.2, Exemplo 8.4](https://www.openintro.org/book/os/) — CC-BY-SA

</Exemplo>

<ListaExercicios seed="licao-106-regressao-multipla">

<Exercicio
  numero="106.1"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "hat beta_1 = 3,2 mede o efeito total da area sobre o preco, ignorando quartos e andar" },
    { texto: "hat beta_1 = 3,2 indica que cada m² adicional esta associado a R$ 3.200 a mais no preco, mantendo quartos e andar fixos", correta: true },
    { texto: "hat beta_2 = 28 e o preco medio de um apartamento de zero quartos" },
    { texto: "hat beta_0 = 50 mede o efeito medio do andar sobre o preco" }
  ]}
  solucao={<>Veja a referência indicada em <em>fonte</em> para a resolução detalhada.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.1", licenca: "CC-BY-SA" }}
>

Regressão: $hat Y = 50 + 3{,}2X_1 + 28X_2 + 5{,}5X_3$ (preço em R\$ mil, área m², quartos, andar). Interprete cada coeficiente.
</Exercicio>

<Exercicio
  numero="106.2"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "hat Y = 450 e residuo = 0" },
    { texto: "hat Y = 459 e residuo = -9" },
    { texto: "hat Y = 50 + 256 + 84 + 27,5 = 417,5 e residuo = 32,5", correta: true },
    { texto: "hat Y = 50 + 3,2 + 28 + 5,5 = 86,7 e residuo = 363,3" }
  ]}
  passos={<>Veja a referência indicada em <em>fonte</em> para o passo a passo.</>}
  solucao={<>Veja a referência indicada em <em>fonte</em> para a resolução detalhada.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.1", licenca: "CC-BY-SA" }}
>

Usando $hat Y = 50 + 3{,}2X_1 + 28X_2 + 5{,}5X_3$, calcule a previsão e o resíduo para um apartamento de 80 m², 3 quartos, 5º andar com preço observado de R\$ 450 mil.
</Exercicio>

<Exercicio
  numero="106.3"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "R² = 0,60 e R² ajustado = 0,60 (iguais pois os graus de liberdade se cancelam)" },
    { texto: "R² = 0,60 e R² ajustado = 0,567", correta: true },
    { texto: "R² = 0,567 e R² ajustado = 0,60 (o ajustado e sempre maior)" },
    { texto: "R² = 0,40 e R² ajustado = 0,433 (complement de SSE/SST)" }
  ]}
  solucao={<><Eq>{`\\bar R^2 = 1 - \\frac{SSE/(n-p-1)}{SST/(n-1)} = 1 - \\frac{800/36}{2000/39} = 1 - \\frac{22{,}2}{51{,}3} = 1 - 0{,}433 = 0{,}567`}</Eq>. Comparar com <Eq>{`R^2 = 1-800/2000 = 0{,}60`}</Eq>: o ajuste penalizado cai levemente.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.3", licenca: "CC-BY-SA" }}
>

$n=40$, $p=3$ preditores, $SST=2000$, $SSE=800$. Calcule $R^2$ e $\bar R^2$.
</Exercicio>

<Exercicio
  numero="106.4"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "M1 e preferivel pois tem menos preditores e portanto maior R² ajustado" },
    { texto: "M3 e preferivel pois tem o menor SSE/SST e portanto maior R² ajustado", correta: true },
    { texto: "M2 e preferivel pois tem o melhor equilibrio entre R² e numero de preditores" },
    { texto: "Todos os tres modelos tem o mesmo R² ajustado pois n e o mesmo" }
  ]}
  solucao={<>M1: <Eq>{`\\bar R^2_1 = 1-(1-0{,}60)\\times(n-1)/(n-2)`}</Eq>. Com <Eq>{`n=50`}</Eq>: <Eq>{`\\bar R^2_1 = 1-0{,}40\\times49/48 = 1-0{,}408 = 0{,}592`}</Eq>. M2: <Eq>{`\\bar R^2_2 = 1-0{,}35\\times49/46 = 1-0{,}373 = 0{,}627`}</Eq>. M3: <Eq>{`\\bar R^2_3 = 1-0{,}32\\times49/43 = 1-0{,}365 = 0{,}635`}</Eq>. M3 tem maior <Eq>{`\\bar R^2`}</Eq>: preferível entre os três.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.3", licenca: "CC-BY-SA" }}
>

$n=50$. Três modelos com $p=1,3,6$ preditores e $SSE/SST = 0{,}40$; $0{,}35$; $0{,}32$. Calcule $\bar R^2$ de cada e aponte o preferível.
</Exercicio>

<Exercicio
  numero="106.5"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "F = 18,3 com df1 = 55; nao rejeita H0" },
    { texto: "F = 35,4 com df1 = 4 e df2 = 55; rejeita H0", correta: true },
    { texto: "F = 35,4 com df1 = 4 e df2 = 59; rejeita H0 (erro de df2)" },
    { texto: "F = 648 com df1 = 1; o modelo nao e globalmente significativo" }
  ]}
  solucao={<><Eq>{`SSR = 0{,}72 \\times 3600 = 2592`}</Eq>; <Eq>{`SSE = 3600 - 2592 = 1008`}</Eq>; <Eq>{`MSR = 2592/4 = 648`}</Eq>; <Eq>{`MSE = 1008/55 = 18{,}3`}</Eq>; <Eq>{`F = 648/18{,}3 = 35{,}4`}</Eq>. Valor crítico <Eq>{`F_{4,55;\\,0{,}05} \\approx 2{,}54`}</Eq>: rejeita <Eq>{`H_0`}</Eq>.</>}
  fonte={{ livro: "OpenStax Statistics", url: "https://openstax.org/books/statistics/pages/13-introduction", secao: "§13.4", licenca: "CC-BY" }}
>

$n=60$, $p=4$, $SST=3600$, $R^2=0{,}72$. Monte a tabela ANOVA e teste o modelo ao nível 5%.
</Exercicio>

<Exercicio
  numero="106.6"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "VIF_1 = 1,43; VIF_2 = 3,33; VIF_3 = 12,5 — preditor 3 tem multicolinearidade severa", correta: true },
    { texto: "VIF_1 = 0,70; VIF_2 = 0,30; VIF_3 = 0,08 — todos abaixo de 1, sem problema" },
    { texto: "VIF_1 = 1,43; VIF_2 = 3,33; VIF_3 = 12,5 — preditor 2 tem multicolinearidade severa pois VIF > 3" },
    { texto: "VIF_3 = 1/(1-0,92) = 12,5 mas o limiar e VIF > 5, entao preditores 2 e 3 tem problema" }
  ]}
  solucao={<><Eq>{`VIF_j = 1/(1-R_j^2)`}</Eq>. <Eq>{`VIF_1 = 1/(1-0{,}30) = 1{,}43`}</Eq>; <Eq>{`VIF_2 = 1/(1-0{,}70) = 3{,}33`}</Eq>; <Eq>{`VIF_3 = 1/(1-0{,}92) = 12{,}5`}</Eq>. <Eq>{`VIF_3 > 10`}</Eq>: preditor 3 apresenta multicolinearidade severa.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.4", licenca: "CC-BY-SA" }}
>

Regressões auxiliares para 3 preditores: $R_1^2=0{,}30$, $R_2^2=0{,}70$, $R_3^2=0{,}92$. Calcule os VIFs e identifique multicolinearidade severa.
</Exercicio>

<Exercicio
  numero="106.7"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "O programa eleva a nota em 12,4 pontos para qualquer aluno (efeito causal confirmado)" },
    { texto: "Controlando por renda familiar, participar do programa esta associado a 12,4 pontos a mais na nota, em media", correta: true },
    { texto: "O coeficiente 12,4 e o efeito total do programa, sem controlar para renda" },
    { texto: "O coeficiente 12,4 representa a media das notas dos participantes do programa" }
  ]}
  passos={<>
    <ol>
      <li>Modelo com dummy: <Eq>{`\\hat Y = \\hat\\beta_0 + \\hat\\beta_1 X_1 + \\hat\\beta_2 D`}</Eq> onde <Eq>{`D=1`}</Eq> se tratado, <Eq>{`D=0`}</Eq> se controle.</li>
      <li><Eq>{`\\hat\\beta_2`}</Eq> é a diferença média de <Eq>{`Y`}</Eq> entre tratados e controles, controlando por <Eq>{`X_1`}</Eq>.</li>
      <li>Com <Eq>{`\\hat\\beta_2 = 12{,}4`}</Eq>: o programa está associado a um aumento médio de 12,4 pontos na nota, dado o mesmo nível de renda familiar.</li>
      <li><em>Observação:</em> isso ainda não é efeito causal — precisa de aleatorização ou design quase-experimental.</li>
    </ol>
  </>}
  solucao={<>O coeficiente da dummy indica que participar do programa está associado a 12,4 pontos a mais na nota, controlando por renda familiar. Efeito causal requer desenho experimental adequado.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.2", licenca: "CC-BY-SA" }}
>

Regressão da nota do ENEM em renda familiar ($X_1$) e participação em programa de reforço ($D$: 1=sim, 0=não): $\hat Y = 480 + 8{,}2X_1 + 12{,}4D$, $n=200$. Interprete $\hat\beta_D = 12{,}4$.
</Exercicio>

<Exercicio
  numero="106.8"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "T = 2,8/0,7 = 4,0; df = n-p = 47; rejeita H0" },
    { texto: "T = 2,8/0,7 = 4,0; df = n-p-1 = 46; rejeita H0", correta: true },
    { texto: "T = 0,7/2,8 = 0,25; df = 46; nao rejeita H0" },
    { texto: "T = 4,0; df = n-1 = 49; o teste F deve ser usado em vez do teste t" }
  ]}
  solucao={<>Teste <Eq>{`t`}</Eq>: <Eq>{`T = 2{,}8/0{,}7 = 4{,}0`}</Eq>. Com <Eq>{`df = n-p-1 = 50-3-1 = 46`}</Eq>, <Eq>{`t_{46;\\,0{,}025} \\approx 2{,}013`}</Eq>. Como <Eq>{`|T| = 4{,}0 > 2{,}013`}</Eq>, rejeitamos <Eq>{`H_0: \\beta_1 = 0`}</Eq>. O preditor <Eq>{`X_1`}</Eq> é significativo ao nível 5%.</>}
  fonte={{ livro: "OpenStax Statistics", url: "https://openstax.org/books/statistics/pages/13-introduction", secao: "§13.3", licenca: "CC-BY" }}
>

$n=50$, $p=3$, $\hat\beta_1=2{,}8$, $SE(\hat\beta_1)=0{,}7$. Teste $H_0:\beta_1=0$ ao nível 5% (bicaudal).
</Exercicio>

<Exercicio
  numero="106.9"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "IC = (5,2 - 1,8; 5,2 + 1,8) = (3,4; 7,0) — usa SE diretamente sem multiplicar pelo t" },
    { texto: "IC = (1,58; 8,82) — beta_2 e significativo pois o IC nao contem 0", correta: true },
    { texto: "IC = (5,2 - 1,96 x 1,8; 5,2 + 1,96 x 1,8) = (1,67; 8,73) — usa z ao inves de t" },
    { texto: "IC = (5,2 - 2,013 x 1,8; 5,2 + 2,013 x 1,8) mas contem 0, logo nao significativo" }
  ]}
  solucao={<>IC 95% para <Eq>{`\\beta_2`}</Eq>: <Eq>{`\\hat\\beta_2 \\pm t_{46;\\,0{,}025} \\times SE(\\hat\\beta_2) = 5{,}2 \\pm 2{,}013 \\times 1{,}8 = 5{,}2 \\pm 3{,}62 = (1{,}58;\\; 8{,}82)`}</Eq>. O IC não contém 0: <Eq>{`\\beta_2`}</Eq> é significativo ao nível 5%.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.1", licenca: "CC-BY-SA" }}
>

$n=50$, $p=3$, $\hat\beta_2=5{,}2$, $SE(\hat\beta_2)=1{,}8$. Construa IC 95% para $\beta_2$. Use $t_{46;\,0{,}025}\approx 2{,}013$.
</Exercicio>

<Exercicio
  numero="106.10"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "-3,0 (media dos quatro residuos fornecidos)" },
    { texto: "-5,0", correta: true },
    { texto: "3,0 (soma dos residuos pode ser qualquer valor)" },
    { texto: "0,0 (o quinto residuo e sempre zero em regressao com intercepto)" }
  ]}
  solucao={<>Soma dos resíduos é sempre zero em qualquer regressão com intercepto: <Eq>{`\\sum e_i = 0`}</Eq>. Portanto o quinto resíduo é <Eq>{`e_5 = -(3{,}2 - 1{,}5 - 0{,}8 + 2{,}1) = -5{,}0`}</Eq>. Verificar: soma <Eq>{`3{,}2-1{,}5-0{,}8+2{,}1-5{,}0 = 0`}</Eq>. Correto.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.1", licenca: "CC-BY-SA" }}
>

Quatro dos cinco resíduos de uma regressão são: $3{,}2$; $-1{,}5$; $-0{,}8$; $2{,}1$. Qual é o quinto resíduo?
</Exercicio>

<Exercicio
  numero="106.11"
  dificuldade="compreensao"
  opcoes={[
    { texto: "R² sempre cai quando adicionamos um preditor irrelevante", correta: false },
    { texto: "R² ajustado pode cair quando adicionamos um preditor irrelevante", correta: true },
    { texto: "R² e R² ajustado são sempre iguais", correta: false },
    { texto: "R² ajustado é sempre maior que R²", correta: false }
  ]}
  solucao={<><Eq>{`R^2`}</Eq> nunca cai ao adicionar preditores (propriedade matemática do OLS). <Eq>{`\\bar R^2`}</Eq> penaliza pelo número de preditores e pode cair quando o preditor adicionado é fraco. Por isso <Eq>{`\\bar R^2`}</Eq> é o critério adequado para comparar modelos com diferentes números de preditores.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.3", licenca: "CC-BY-SA" }}
>

Qual afirmação sobre $R^2$ e $R^2$ ajustado é CORRETA?
</Exercicio>

<Exercicio
  numero="106.12"
  dificuldade="compreensao"
  opcoes={[
    { texto: "Multicolinearidade torna os estimadores viesados", correta: false },
    { texto: "Multicolinearidade infla os erros padrão dos coeficientes", correta: true },
    { texto: "Multicolinearidade invalida o teste F global", correta: false },
    { texto: "Multicolinearidade só ocorre quando VIF = 0", correta: false }
  ]}
  solucao={<>Multicolinearidade não vicia os estimadores (eles continuam BLUE), mas torna <Eq>{`X^TX`}</Eq> quase-singular, inflando <Eq>{`(X^TX)^{-1}`}</Eq> e portanto os <Eq>{`SE`}</Eq>. Testes <Eq>{`t`}</Eq> individuais perdem poder, mas o teste <Eq>{`F`}</Eq> global e as previsões continuam confiáveis.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.4", licenca: "CC-BY-SA" }}
>

Qual é o principal efeito prático da multicolinearidade em regressão múltipla?
</Exercicio>

<Exercicio
  numero="106.13"
  dificuldade="compreensao"
  opcoes={[
    { texto: "O coeficiente de X₁ na regressão múltipla é igual ao da regressão simples de Y em X₁", correta: false },
    { texto: "Adicionar X₂ ao modelo pode mudar o sinal de X₁", correta: true },
    { texto: "Os coeficientes parciais são sempre menores que os simples", correta: false },
    { texto: "Na regressão múltipla, β₁ mede a correlação marginal entre X₁ e Y", correta: false }
  ]}
  solucao={<>O fenômeno é chamado de "inversão de sinal" ou efeito de supressão. Ocorre quando <Eq>{`X_2`}</Eq> está correlacionado com <Eq>{`X_1`}</Eq> de forma que controlar por <Eq>{`X_2`}</Eq> revela a verdadeira relação de <Eq>{`X_1`}</Eq> com <Eq>{`Y`}</Eq>. Exemplo clássico: correlação positiva entre pompas de incêndio e dano — ao controlar pelo tamanho do incêndio, o efeito pode mudar.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.2", licenca: "CC-BY-SA" }}
>

Qual afirmação sobre coeficientes parciais em regressão múltipla é CORRETA?
</Exercicio>

<Exercicio
  numero="106.14"
  dificuldade="modelagem"
  opcoes={[
    { texto: "MSE = 5400/80 = 67,5; hat sigma = 8,22; bar R^2 = 0,70" },
    { texto: "MSE = 5400/75 = 72,0; hat sigma = 8,49; bar R^2 = 0,692", correta: true },
    { texto: "MSE = 5400/76 = 71,1; hat sigma = 8,43; bar R^2 = 0,70 (confunde df com n-p)" },
    { texto: "MSE = 18000/79 = 227,8; hat sigma = 15,1 (usa SST em vez de SSE)" }
  ]}
  solucao={<>Veja a referência indicada em <em>fonte</em> para a resolução detalhada.</>}
  fonte={{ livro: "OpenStax Statistics", url: "https://openstax.org/books/statistics/pages/13-introduction", secao: "§13.3", licenca: "CC-BY" }}
>

Regressão do gasto mensal familiar (R\$ mil) sobre 4 preditores socioeconômicos: $n=80$, $SST=18000$, $SSE=5400$. Calcule $MSE$, $hatsigma$ e $bar R^2$.
</Exercicio>

<Exercicio
  numero="106.15"
  dificuldade="modelagem"
  opcoes={[
    { texto: "Homem: hat Y = 70 mil; Mulher: hat Y = 62 mil; gap de R$ 8 mil constante para qualquer experiencia", correta: true },
    { texto: "Homem: hat Y = 70 mil; Mulher: hat Y = 70 - 8 x 10 = -10 mil (multiplica D pela experiencia)" },
    { texto: "Homem: hat Y = 62 mil; Mulher: hat Y = 70 mil (inverte o sinal da dummy)" },
    { texto: "Homem: hat Y = 45 mil; Mulher: hat Y = 37 mil (ignora o efeito de X1)" }
  ]}
  passos={<>
    <ol>
      <li>Modelo sem interação: uma reta de inclinação igual para homens e mulheres.</li>
      <li>Para homens (<Eq>{`D=0`}</Eq>): <Eq>{`\\hat Y = 45 + 2{,}5X_1`}</Eq>. Para mulheres (<Eq>{`D=1`}</Eq>): <Eq>{`\\hat Y = 45 + 2{,}5X_1 - 8 = 37 + 2{,}5X_1`}</Eq>.</li>
      <li>Diferença salarial entre gêneros é constante em R\$ 8 mil para qualquer nível de experiência.</li>
      <li>Para testar se a diferença varia com experiência, adicione o termo de interação <Eq>{`X_1 \\times D`}</Eq>.</li>
      <li><em>Observação:</em> o modelo com interação é <Eq>{`\\hat Y = \\beta_0 + \\beta_1 X_1 + \\beta_2 D + \\beta_3(X_1 \\cdot D)`}</Eq>. Se <Eq>{`\\hat\\beta_3`}</Eq> for significativo, a diferença de gênero varia com experiência.</li>
    </ol>
  </>}
  solucao={<>Veja a referência indicada em <em>fonte</em> para a resolução detalhada.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.2", licenca: "CC-BY-SA" }}
>

Modelo: $hat Y = 45 + 2{,}5X_1 - 8D$ (salário em R\$ mil, experiência em anos, $D$=1 se mulher). Calcule salários para (a) homem, 10 anos; (b) mulher, 10 anos. Como incluir interação para verificar se o gap varia com experiência?
</Exercicio>

<Exercicio
  numero="106.16"
  dificuldade="modelagem"
  opcoes={[
    { texto: "Incluir X3 se o R² do novo modelo for maior que 0,65 (comparar R² bruto)" },
    { texto: "Incluir X3 se bar R² subir e/ou o teste t parcial de hat beta_3 tiver p-valor < 0,05", correta: true },
    { texto: "Incluir X3 se VIF > 10, pois isso indica que X3 contribui nova informacao" },
    { texto: "Incluir X3 se SSE do novo modelo for maior que SSE do modelo original" }
  ]}
  solucao={<>Comparar modelos com e sem <Eq>{`X_3`}</Eq>: se <Eq>{`\\bar R^2`}</Eq> sobe ao incluir <Eq>{`X_3`}</Eq>, manter. Alternativa: fazer teste <Eq>{`t`}</Eq> parcial para <Eq>{`\\hat\\beta_3`}</Eq>. Se p-valor menor que 0,05, <Eq>{`X_3`}</Eq> é significativo dado os demais. Critério AIC: <Eq>{`AIC = n\\ln(SSE/n) + 2(p+1)`}</Eq>, menor é melhor. Usar VIF para verificar se <Eq>{`X_3`}</Eq> introduz multicolinearidade severa.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.3", licenca: "CC-BY-SA" }}
>

Um pesquisador tem modelo de regressão com 2 preditores ($R^2=0{,}65$, $\bar R^2=0{,}64$) e considera adicionar um terceiro preditor. Descreva dois critérios para decidir se deve incluí-lo.
</Exercicio>

<Exercicio
  numero="106.17"
  dificuldade="desafio"
  opcoes={[
    { texto: "H^2 = H porque H e diagonal" },
    { texto: "H^2 = H porque X^TX(X^TX)^{-1} = I, cancelando o fator central", correta: true },
    { texto: "H^2 = 2H porque cada projecao dobra a norma" },
    { texto: "H^2 = H^T pois a matriz e simetrica mas nao idempotente" }
  ]}
  solucao={<>O hat matrix <Eq>{`H = X(X^TX)^{-1}X^T`}</Eq> é simétrico (<Eq>{`H^T=H`}</Eq>) e idempotente (<Eq>{`H^2=H`}</Eq>). Idempotência: <Eq>{`H^2 = X(X^TX)^{-1}X^TX(X^TX)^{-1}X^T = X(X^TX)^{-1}X^T = H`}</Eq>. Isso confirma que <Eq>{`H`}</Eq> é uma projeção ortogonal: aplicá-la duas vezes dá o mesmo resultado.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.1", licenca: "CC-BY-SA" }}
>

Prove que o hat matrix $H = X(X^TX)^{-1}X^T$ é idempotente: $H^2 = H$.
</Exercicio>

<Exercicio
  numero="106.18"
  dificuldade="desafio"
  opcoes={[
    { texto: "X e uma matriz 6x2 (n=6, p=2 colunas para X1 e X2)" },
    { texto: "X e uma matriz 6x3 com primeira coluna de 1s, segunda X1 e terceira X2", correta: true },
    { texto: "X e uma matriz 3x6 pois temos p+1=3 parametros e n=6 observacoes" },
    { texto: "X e uma matriz 6x6 pois a matriz de design e sempre quadrada" }
  ]}
  passos={<>
    <ol>
      <li>Com <Eq>{`n=6`}</Eq>, <Eq>{`p=2`}</Eq>: montar <Eq>{`X`}</Eq> (coluna de 1s, coluna <Eq>{`X_1`}</Eq>, coluna <Eq>{`X_2`}</Eq>).</li>
      <li>Calcular <Eq>{`X^TX`}</Eq> (matriz 3×3).</li>
      <li>Calcular <Eq>{`(X^TX)^{-1}`}</Eq>.</li>
      <li>Calcular <Eq>{`X^T\\mathbf{y}`}</Eq> (vetor 3×1).</li>
      <li>Multiplicar: <Eq>{`\\hat{\\boldsymbol\\beta} = (X^TX)^{-1}X^T\\mathbf{y}`}</Eq>.</li>
      <li><em>Curiosidade:</em> em aplicações reais, <Eq>{`(X^TX)^{-1}`}</Eq> é calculado numericamente via decomposição QR, não pela inversa direta (mais estável numericamente).</li>
    </ol>
  </>}
  solucao={<>Montar as matrizes com os dados fornecidos, calcular o produto e obter <Eq>{`\\hat{\\boldsymbol\\beta}`}</Eq>. Este exercício é melhor resolvido com calculadora matricial ou R/Python.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.1", licenca: "CC-BY-SA" }}
>

Dados: $n=6$ observações com $Y=(10,14,18,12,16,20)$, $X_1=(1,2,3,2,3,4)$, $X_2=(5,4,3,6,5,4)$. Escreva a matriz de design $X$ e enuncie o procedimento para calcular $\hat{\boldsymbol\beta} = (X^TX)^{-1}X^T\mathbf{y}$ (não é necessário executar a inversão a mão — descreva os passos).
</Exercicio>

<Exercicio
  numero="106.19"
  dificuldade="demonstracao"
  opcoes={[
    { texto: "A prova usa que X^T e = 0 implica que a primeira linha (vetor de 1s) multiplicada por e e zero", correta: true },
    { texto: "A prova usa que H e diagonal, logo X^T H = X^T" },
    { texto: "sum e_i = 0 so vale para regressao simples, nao para multipla" },
    { texto: "X^T e = 0 implica sum e_i = 0 apenas quando p = 1" }
  ]}
  solucao={<>Devemos mostrar que <Eq>{`\\sum e_i = 0`}</Eq>. Como <Eq>{`\\mathbf{e} = (I-H)\\mathbf{Y}`}</Eq> e a primeira coluna de <Eq>{`X`}</Eq> é um vetor de 1s (<Eq>{`\\mathbf{1}`}</Eq>), temos <Eq>{`X^T\\mathbf{e} = X^T(I-H)\\mathbf{Y} = X^T\\mathbf{Y} - X^TX(X^TX)^{-1}X^T\\mathbf{Y} = \\mathbf{0}`}</Eq>. A primeira linha de <Eq>{`X^T`}</Eq> é <Eq>{`\\mathbf{1}^T`}</Eq>, portanto <Eq>{`\\mathbf{1}^T\\mathbf{e} = \\sum e_i = 0`}</Eq>.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.1", licenca: "CC-BY-SA" }}
>

Prove que em qualquer regressão com intercepto, $\sum_{i=1}^n e_i = 0$, usando a ortogonalidade $X^T\mathbf{e} = \mathbf{0}$.
</Exercicio>

<Exercicio
  numero="106.20"
  dificuldade="desafio"
  opcoes={[
    { texto: "bar R^2 aumenta se e somente se |T_j| > t_{n-p-1; 0,025} (limiar do teste t ao nivel 5%)" },
    { texto: "bar R^2 aumenta se e somente se |T_j| > 1 (limiar menos conservador que o teste t a 5%)", correta: true },
    { texto: "bar R^2 aumenta se e somente se T_j^2 > F_{1,n-p-1; 0,05} (limiar do teste F)" },
    { texto: "bar R^2 sempre aumenta ao adicionar qualquer preditor, pois SSE nao pode subir" }
  ]}
  solucao={<>Fórmula da diferença de <Eq>{`\\bar R^2`}</Eq> ao adicionar um preditor: <Eq>{`\\bar R^2`}</Eq> aumenta se e somente se o valor <Eq>{`F`}</Eq> parcial do novo preditor for maior que 1. Equivalentemente: <Eq>{`\\bar R^2`}</Eq> do modelo ampliado <Eq>{`>`}</Eq> <Eq>{`\\bar R^2`}</Eq> do modelo reduzido <Eq>{`\\Leftrightarrow`}</Eq> <Eq>{`T_j^2 > 1`}</Eq>. Isso mostra que o critério <Eq>{`\\bar R^2`}</Eq> é menos conservador do que o teste <Eq>{`t`}</Eq> ao nível 5% (que exigiria <Eq>{`|T_j| > t_{n-p-1;\\,0{,}025} \\approx 2`}</Eq>).</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§8.3", licenca: "CC-BY-SA" }}
>

Mostre que adicionar um preditor ao modelo aumenta $\bar R^2$ se e somente se a estatística $|T|$ do novo preditor é maior que 1.
</Exercicio>

<Exercicio
  numero="106.21"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Fumar cigarros: coeficiente mede o efeito de fumar ignorando a paridade" },
    { texto: "Fumar cigarros: coeficiente mede o efeito de fumar controlando para paridade", correta: true },
    { texto: "Paridade: coeficiente mede o efeito total do numero de filhos sobre o peso" },
    { texto: "Ambos os coeficientes medem efeitos marginais sem controle mutuo" }
  ]}
  solucao={<>Na regressão múltipla, o coeficiente de "fumar" mede o efeito parcial — a mudança esperada no peso do bebê por unidade de mudança no tabagismo, mantendo a paridade constante. Cada coeficiente é um efeito condicional nos demais preditores do modelo.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

Um modelo múltiplo prevê peso de bebês ($Y$) com preditores: fumar cigarros ($X_1$) e paridade ($X_2$, número de filhos anteriores). O coeficiente de $X_1$ mede qual efeito?
</Exercicio>

<Exercicio
  numero="106.22"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "GPA depende apenas de horas de estudo; as demais variaveis sao irrelevantes" },
    { texto: "O coeficiente de horas de estudo controla para numero de aulas perdidas e sexo", correta: true },
    { texto: "O coeficiente de sexo mede o efeito total do sexo sobre o GPA" },
    { texto: "R^2 ajustado diminui quando preditores desnecessarios sao incluidos" }
  ]}
  solucao={<>Em um modelo com múltiplos preditores (GPA, horas de estudo, aulas perdidas, sexo), o coeficiente de cada variável controla as demais. O coeficiente de horas de estudo representa o efeito parcial de uma hora adicional de estudo mantendo as demais variáveis (aulas perdidas, sexo) constantes.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

Pesquisa com 55 alunos da Universidade Duke modela GPA em função de: horas de estudo por noite, número de aulas perdidas e sexo. O que o coeficiente de "horas de estudo" representa nesse modelo múltiplo?
</Exercicio>

<Exercicio
  numero="106.23"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "X3 = diametro x altura e uma variavel de controle para remover o efeito da correlacao entre X1 e X2" },
    { texto: "X3 e um termo de interacao: o efeito do diametro sobre o volume varia conforme a altura", correta: true },
    { texto: "X3 transforma o modelo de linear para quadratico em X1 apenas" },
    { texto: "X3 e necessario para evitar multicolinearidade entre X1 e X2" }
  ]}
  passos={<>
<ol>
<li>Modelo: <Eq>{`\\hat Y = \\hat\\beta_0 + \\hat\\beta_1 X_1 + \\hat\\beta_2 X_2 + \\hat\\beta_3 X_3`}</Eq> onde <Eq>{`X_1`}</Eq> = diâmetro, <Eq>{`X_2`}</Eq> = altura, <Eq>{`X_3`}</Eq> = diâmetro × altura.</li>
<li>Volume de madeira é a variável resposta <Eq>{`Y`}</Eq>.</li>
<li>O produto <Eq>{`X_3 = X_1 \\times X_2`}</Eq> é um termo de interação que permite que o efeito do diâmetro sobre o volume dependa da altura.</li>
<li>Com todos os três preditores, o modelo captura melhor a relação não-linear entre dimensões e volume.</li>
</ol>
</>}
  solucao={<>Modelo com 3 preditores: diâmetro (<Eq>{`X_1`}</Eq>), altura (<Eq>{`X_2`}</Eq>) e interação diâmetro×altura (<Eq>{`X_3`}</Eq>). O <Eq>{`\\bar R^2`}</Eq> ajustado deve ser alto por conta da forte relação geométrica entre essas dimensões e o volume.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

Madeireiras estimam o volume de árvores (em pés cúbicos) a partir do diâmetro e altura. Um modelo de regressão múltipla inclui diâmetro ($X_1$), altura ($X_2$) e o produto diâmetro $\times$ altura ($X_3$). Qual é o papel do termo $X_3$ no modelo?
</Exercicio>

<Exercicio
  numero="106.24"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Nao, pois o R^2 da regressao simples sempre e menor" },
    { texto: "Sim, o R^2 simples pode ser menor pois nao controla para paridade", correta: true },
    { texto: "Nao, o coeficiente de fumo deve ser igual em ambos os modelos" },
    { texto: "Sim, o R^2 da regressao multipla e sempre maior" }
  ]}
  solucao={<>Na regressão simples, o coeficiente de fumo absorve também o efeito confundidor da paridade (mães que fumam tendem a ter mais ou menos filhos). Na múltipla, controlando para paridade, o coeficiente de fumo captura apenas o efeito parcial. Os coeficientes podem diferir por confundimento.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

Comparando o modelo simples (apenas fumo como preditor) com o modelo múltiplo (fumo + paridade) para peso de bebês: o coeficiente de fumo pode ser diferente nos dois modelos? Por quê?
</Exercicio>

<Exercicio
  numero="106.25"
  dificuldade="compreensao"
  opcoes={[
    { texto: "O R^2 ajustado nao pode diminuir quando adicionamos um preditor" },
    { texto: "O R^2 ajustado pode diminuir quando o preditor adicionado nao contribui o suficiente", correta: true },
    { texto: "O R^2 ajustado e sempre maior que o R^2 nao ajustado" },
    { texto: "O R^2 ajustado penaliza por ter poucos preditores" }
  ]}
  passos={<>
<ol>
<li>Fórmula: <Eq>{`\\bar R^2 = 1 - \\frac{\\mathrm{SSE}/(n-p-1)}{\\mathrm{SST}/(n-1)}`}</Eq>.</li>
<li>Ao adicionar um preditor: <Eq>{`p \\to p+1`}</Eq>, então <Eq>{`n-p-1 \\to n-p-2`}</Eq> (denominator decreases).</li>
<li>SSE também decresce (pois mais preditores reduzem erros).</li>
<li>Se a redução em SSE for pequena (preditor pouco informativo), o quociente <Eq>{`\\mathrm{SSE}/(n-p-2)`}</Eq> pode aumentar.</li>
<li>Nesse caso, <Eq>{`\\bar R^2`}</Eq> diminui — penalizando o modelo por incluir um preditor redundante.</li>
</ol>
  </>}
  solucao={<><Eq>{`\\bar R^2 = 1 - (\\mathrm{SSE}/(n-p-1))/(\\mathrm{SST}/(n-1))`}</Eq>. Adicionar um preditor reduz SSE, mas também reduz os graus de liberdade <Eq>{`n-p-1`}</Eq>. Se a redução em SSE for pequena, o quociente <Eq>{`\\mathrm{SSE}/(n-p-1)`}</Eq> pode aumentar, fazendo <Eq>{`\\bar R^2`}</Eq> diminuir.</>}
  fonte={{ livro: "OpenStax Statistics", url: "https://openstax.org/books/statistics/pages/12-homework", secao: "§12.H", licenca: "CC-BY" }}
>

Por que o $\bar R^2$ (R² ajustado) pode diminuir quando adicionamos um novo preditor ao modelo de regressão múltipla?
</Exercicio>

<Exercicio
  numero="106.26"
  dificuldade="modelagem"
  opcoes={[
    { texto: "hat beta_1 mede o efeito total de X1 sobre Y, somando direto e indireto via X2" },
    { texto: "hat beta_1 mede o efeito de X1 sobre Y mantendo X2 (distancia) constante — efeito parcial", correta: true },
    { texto: "hat beta_0 e o absentismo esperado para um aluno com X1 = 1 e X2 = 1" },
    { texto: "hat beta_2 e negativo por definicao pois maior distancia reduz o absentismo" }
  ]}
  solucao={<>Absentismo em função de falta de disciplina e distância da escola. Modelo: <Eq>{`\\hat Y = \\hat\\beta_0 + \\hat\\beta_1 X_1 + \\hat\\beta_2 X_2`}</Eq>. O coeficiente <Eq>{`\\hat\\beta_1`}</Eq> mede o efeito da falta de disciplina sobre o absentismo controlando para a distância. O coeficiente <Eq>{`\\hat\\beta_2`}</Eq> mede o efeito da distância controlando para a disciplina. O intercepto é o absentismo esperado quando ambas as variáveis são zero.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

Pesquisadores investigam o absentismo escolar ($Y$) em função de falta de disciplina ($X_1$) e distância da escola até a residência do aluno ($X_2$, em km). Escreva o modelo de regressão múltipla e interprete cada coeficiente.
</Exercicio>

<Exercicio
  numero="106.27"
  dificuldade="compreensao"
  opcoes={[
    { texto: "Multicolinearidade viola o pressuposto de normalidade dos erros" },
    { texto: "Multicolinearidade aumenta os erros padrao dos coeficientes, tornando o teste t impreciso", correta: true },
    { texto: "Multicolinearidade faz os coeficientes OLS serem viesados" },
    { texto: "Multicolinearidade reduz o R^2 do modelo" }
  ]}
  solucao={<>Quando preditores são altamente correlacionados, a matriz <Eq>{`X^TX`}</Eq> fica quase singular, amplificando os erros padrão dos coeficientes. Os estimadores OLS permanecem não-viesados, mas têm variância muito alta, tornando os testes <Eq>{`t`}</Eq> individuais sem poder para detectar efeitos reais.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", licenca: "CC-BY-SA" }}
>

Qual é o principal efeito prático da multicolinearidade em um modelo de regressão múltipla?
</Exercicio>

<Exercicio
  numero="106.28"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "VIF = 1 indica multicolinearidade perfeita" },
    { texto: "VIF = 5 indica que o erro padrao de beta_j e sqrt(5) vezes maior que sem colinearidade", correta: true },
    { texto: "VIF > 1 sempre indica problema grave de multicolinearidade" },
    { texto: "VIF mede a correlacao entre beta_j e os demais coeficientes" }
  ]}
  solucao={<>O Fator de Inflação da Variância é <Eq>{`\\mathrm{VIF}_j = 1/(1 - R_j^2)`}</Eq>, onde <Eq>{`R_j^2`}</Eq> é o <Eq>{`R^2`}</Eq> da regressão de <Eq>{`X_j`}</Eq> sobre os demais preditores. <Eq>{`\\mathrm{VIF}_j = 5`}</Eq> significa que <Eq>{`\\mathrm{Var}(\\hat\\beta_j)`}</Eq> é 5 vezes maior do que seria sem colinearidade; logo o SE é <Eq>{`\\sqrt{5} \\approx 2{,}24`}</Eq> vezes maior.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", licenca: "CC-BY-SA" }}
>

O VIF (Variance Inflation Factor) de um preditor é 5. O que isso significa para o erro padrão do coeficiente estimado?
</Exercicio>

<Exercicio
  numero="106.29"
  dificuldade="modelagem"
  opcoes={[
    { texto: "X1 e X2 nao podem ser colineares pois renda e escolaridade sao tipos de variaveis diferentes" },
    { texto: "X1 (renda) e X2 (escolaridade dos pais) tendem a ser altamente correlacionadas — VIF pode ser alto para ambas", correta: true },
    { texto: "X4 (dummy escola) nunca causa multicolinearidade pois so tem valores 0 e 1" },
    { texto: "Multicolinearidade e diagnosticada pelo R² do modelo completo, nao pelos VIFs individuais" }
  ]}
  passos={<>
<ol>
<li>Preditores propostos para prever notas no ENEM: renda familiar, escolaridade dos pais, horas de estudo, tipo de escola (pública/privada).</li>
<li>Verificar multicolinearidade: renda e escolaridade dos pais podem ser altamente correlacionadas. Calcular <Eq>{`\\mathrm{VIF}`}</Eq> para cada preditor.</li>
<li>Se <Eq>{`\\mathrm{VIF} > 10`}</Eq> para algum preditor, considerar remover o mais colinear ou usar componentes principais.</li>
<li>Verificar pressupostos: gráfico de resíduos vs. ajustados (homocedasticidade), QQ-plot (normalidade), gráfico de resíduos vs. ordem (independência).</li>
</ol>
  </>}
  solucao={<>Modelo: <Eq>{`\\hat Y = \\hat\\beta_0 + \\hat\\beta_1 \\cdot \\text{renda} + \\hat\\beta_2 \\cdot \\text{escolaridade} + \\hat\\beta_3 \\cdot \\text{horas} + \\hat\\beta_4 \\cdot \\text{escola}`}</Eq>. Verificar multicolinearidade entre renda e escolaridade dos pais via VIF.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", licenca: "CC-BY-SA" }}
>

Um pesquisador quer modelar a nota do ENEM ($Y$) de alunos em função de: renda familiar ($X_1$), escolaridade dos pais ($X_2$), horas de estudo semanais ($X_3$) e tipo de escola ($X_4$: 1=privada, 0=pública). Escreva o modelo, identifique possível multicolinearidade e descreva como verificá-la.
</Exercicio>

<Exercicio
  numero="106.30"
  dificuldade="modelagem"
  opcoes={[
    { texto: "Forward adiciona o preditor menos significativo primeiro; backward remove o mais significativo" },
    { texto: "Forward inclui preditores do mais ao menos significativo; backward remove do menos ao mais significativo; AIC e recomendado com muitos candidatos", correta: true },
    { texto: "Forward e backward sao identicos quando o numero de preditores e pequeno" },
    { texto: "O criterio AIC penaliza modelos com poucos preditores, favorecendo sempre modelos saturados" }
  ]}
  solucao={<>Na seleção stepwise, começa-se sem preditores (ou com todos) e adiciona-se (ou remove-se) preditores um a um pelo critério AIC ou pelo p-valor do teste <Eq>{`F`}</Eq> parcial. Na forward selection, inclui-se primeiro o preditor mais significativo, depois o segundo mais significativo dado o primeiro, etc. Backward elimination começa com todos e remove os menos significativos. O critério AIC balanceia ajuste e parcimônia: <Eq>{`\\mathrm{AIC} = n\\ln(\\mathrm{SSE}/n) + 2(p+1)`}</Eq>.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.3", licenca: "CC-BY-SA" }}
>

Explique as estratégias de seleção de variáveis forward selection e backward elimination. Qual critério é mais recomendado quando se tem muitos candidatos a preditores?
</Exercicio>

<Exercicio
  numero="106.31"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "O coeficiente de X4 mede o efeito de ser escola privada em relacao ao intercepto" },
    { texto: "O coeficiente de X4 mede a diferenca esperada em Y entre escola privada e publica, controlando os demais preditores", correta: true },
    { texto: "O coeficiente de X4 e sempre positivo pois privada > publica" },
    { texto: "Variaveis dummy nao podem ser usadas em regressao multipla" }
  ]}
  solucao={<>Quando <Eq>{`X_4 = 1`}</Eq> (privada), <Eq>{`\\hat Y`}</Eq> aumenta em <Eq>{`\\hat\\beta_4`}</Eq> em relação ao caso <Eq>{`X_4 = 0`}</Eq> (pública), mantendo os demais preditores constantes. <Eq>{`\\hat\\beta_4`}</Eq> é a diferença média esperada entre as duas categorias de escola, parcialmente ajustada para renda, escolaridade dos pais e horas de estudo.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

No modelo do exercício anterior, a variável $X_4$ é uma dummy (1=privada, 0=pública). Como interpretar o coeficiente $\hat\beta_4$?
</Exercicio>

<Exercicio
  numero="106.32"
  dificuldade="compreensao"
  opcoes={[
    { texto: "SSR = 0 indica ajuste perfeito do modelo" },
    { texto: "SST = SSR + SSE, onde SSR e a variacao explicada e SSE e a variacao residual", correta: true },
    { texto: "SSE = SST + SSR pois os residuos contribuem para a variacao total" },
    { texto: "SSR = SSE quando o modelo explica 50% da variacao" }
  ]}
  solucao={<>Na decomposição da variância: <Eq>{`\\mathrm{SST} = \\mathrm{SSR} + \\mathrm{SSE}`}</Eq>, onde SST = soma total dos quadrados, SSR = soma dos quadrados da regressão (explicada) e SSE = soma dos quadrados dos resíduos (não-explicada). Por definição, <Eq>{`R^2 = \\mathrm{SSR}/\\mathrm{SST} = 1 - \\mathrm{SSE}/\\mathrm{SST}`}</Eq>.</>}
  fonte={{ livro: "OpenStax Statistics", url: "https://openstax.org/books/statistics/pages/12-homework", secao: "§12.H", licenca: "CC-BY" }}
>

Qual é a relação entre SST (variação total), SSR (variação explicada pela regressão) e SSE (variação dos resíduos)?
</Exercicio>

<Exercicio
  numero="106.33"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "F = (120/3)/(80/55) = 40/1,45 = 27,6; df2 = n = 55 (erro de df)" },
    { texto: "F = (120/3)/(80/51) = 25,5; rejeita H0 ao nivel 5%", correta: true },
    { texto: "F = (80/3)/(120/51) = 11,3; aceita H0 (inverte SSR e SSE)" },
    { texto: "F = (120/51)/(80/3) = 0,088; nao rejeita H0 (inverte MSR e MSE)" }
  ]}
  passos={<>
<ol>
<li>Dados: <Eq>{`p = 3`}</Eq>, <Eq>{`n = 55`}</Eq>, <Eq>{`\\mathrm{SSR} = 120`}</Eq>, <Eq>{`\\mathrm{SSE} = 80`}</Eq>.</li>
<li>Graus de liberdade: <Eq>{`df_1 = p = 3`}</Eq>, <Eq>{`df_2 = n - p - 1 = 55 - 3 - 1 = 51`}</Eq>.</li>
<li>Estatística F: <Eq>{`F = (\\mathrm{SSR}/df_1)/(\\mathrm{SSE}/df_2) = (120/3)/(80/51) = 40/1{,}569 = 25{,}5`}</Eq>.</li>
<li>Valor crítico: <Eq>{`F_{0{,}05;\\,3;\\,51} \\approx 2{,}79`}</Eq>. Como <Eq>{`25{,}5 \\gg 2{,}79`}</Eq>, rejeitamos <Eq>{`H_0`}</Eq>.</li>
<li>Conclusão: o conjunto de 3 preditores explica significativamente a variação de Y ao nível 5%.</li>
</ol>
  </>}
  solucao={<><Eq>{`F = 25{,}5 > 2{,}79`}</Eq>. Rejeitamos <Eq>{`H_0`}</Eq>: os 3 preditores explicam significativamente a variação de Y. (Resp: <Eq>{`F = 25{,}5`}</Eq>)</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

Um modelo de regressão múltipla com $p = 3$ preditores ajustado a $n = 55$ observações tem $\mathrm{SSR} = 120$ e $\mathrm{SSE} = 80$. Calcule a estatística $F$ global e conclua ao nível 5% (valor crítico $F_{0{,}05; 3; 51} \approx 2{,}79$).
</Exercicio>

<Exercicio
  numero="106.34"
  dificuldade="modelagem"
  opcoes={[
    { texto: "hat beta_1 mede o aumento medio de volume por polegada de diametro, sem controlar a altura; bar R² = 0,944 indica ajuste ruim" },
    { texto: "hat beta_1 mede o aumento medio de volume por polegada de diametro mantendo a altura constante; bar R² = 0,944 indica ajuste excelente", correta: true },
    { texto: "hat beta_1 e o intercepto do modelo quando o diametro e zero" },
    { texto: "bar R² = 0,944 indica que 5,6% da variacao de volume e explicada pelo modelo" }
  ]}
  passos={<>
<ol>
<li>Modelo base: <Eq>{`\\hat\\beta = (X^TX)^{-1}X^T\\mathbf{y}`}</Eq>. Com <Eq>{`p = 2`}</Eq>, <Eq>{`n = 3`}</Eq>: <Eq>{`X^TX`}</Eq> é <Eq>{`3 \\times 3`}</Eq>.</li>
<li>Dados: <Eq>{`X_1 =`}</Eq> diâmetro, <Eq>{`X_2 =`}</Eq> altura (em pés), <Eq>{`Y =`}</Eq> volume (pés cúbicos).</li>
<li>Calcular <Eq>{`\\hat\\beta_0, \\hat\\beta_1, \\hat\\beta_2`}</Eq> numericamente e interpretar: por unidade extra de diâmetro (altura fixada), o volume muda em <Eq>{`\\hat\\beta_1`}</Eq> pés cúbicos.</li>
<li>Verificar <Eq>{`\\bar R^2`}</Eq>. Se alto (&gt;0,90), o modelo ajusta bem. Plotar resíduos vs. ajustados para checar homocedasticidade.</li>
</ol>
  </>}
  solucao={<>Modelo: <Eq>{`\\hat{\\text{vol}} = \\hat\\beta_0 + \\hat\\beta_1 \\cdot \\text{diam} + \\hat\\beta_2 \\cdot \\text{altura}`}</Eq>. Cada coeficiente é o efeito parcial mantendo a outra dimensão constante. Um <Eq>{`\\bar R^2`}</Eq> alto confirma que árvores maiores têm volume maior, como esperado geometricamente.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

O volume de árvores em um pomar (em pés cúbicos) é modelado em função de diâmetro ($X_1$, em polegadas) e altura ($X_2$, em pés). Com base no modelo múltiplo, interprete o coeficiente $\hat\beta_1$. O que um $\bar R^2 = 0{,}944$ indica sobre o ajuste?
</Exercicio>

<Exercicio
  numero="106.35"
  dificuldade="desafio"
  opcoes={[
    { texto: "X^TX tem posto cheio mesmo com X3 = 2X1 + X2, pois a dependencia linear nao afeta a inversao" },
    { texto: "X^TX e singular (det = 0) quando X3 e combinacao linear de X1 e X2, tornando (X^TX)^{-1} indefinida", correta: true },
    { texto: "X^TX e singular mas a solucao OLS ainda existe como um sistema inconsistente" },
    { texto: "O software resolve o problema automaticamente sem remover nenhum preditor" }
  ]}
  solucao={<>Se <Eq>{`X_3 = \\alpha X_1 + \\gamma X_2 + c`}</Eq> (combinação linear exata dos demais), a matriz <Eq>{`X^TX`}</Eq> tem determinante zero e não é invertível. A solução OLS <Eq>{`\\hat\\beta = (X^TX)^{-1}X^T\\mathbf{y}`}</Eq> não existe. No computador, o software detecta multicolinearidade perfeita e descarta a variável redundante automaticamente.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", licenca: "CC-BY-SA" }}
>

Em um modelo com três preditores $X_1, X_2, X_3$ em que $X_3 = 2X_1 + X_2$ (multicolinearidade perfeita), explique por que a solução OLS $\hat\beta = (X^TX)^{-1}X^T\mathbf{y}$ não existe. O que o software estatístico faz nessa situação?
</Exercicio>

<Exercicio
  numero="106.36"
  dificuldade="desafio"
  opcoes={[
    { texto: "R^2 = 0,97 com 15 preditores e n = 20 e confiavel pois o R^2 penaliza pela complexidade" },
    { texto: "Com p = 15 e n = 20, o modelo quase interpola os dados (overfitting); validacao cruzada estima o erro out-of-sample", correta: true },
    { texto: "Validacao cruzada e desnecessaria quando R^2 > 0,90" },
    { texto: "R^2 = 0,97 garante que o modelo generaliza bem para novos dados" }
  ]}
  solucao={<>O RMSE de validação cruzada (leave-one-out ou k-fold) é mais honesto que o <Eq>{`R^2`}</Eq> in-sample porque mede o erro em dados não usados no ajuste. Um modelo com muitos preditores pode ter <Eq>{`R^2 \\approx 1`}</Eq> (overfitting) mas alto RMSE out-of-sample. O <Eq>{`\\bar R^2`}</Eq> e o AIC penalizam a complexidade, mas apenas a validação cruzada estima diretamente o erro de previsão.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.3", licenca: "CC-BY-SA" }}
>

Um modelo de regressão múltipla com 15 preditores e $n = 20$ observações tem $R^2 = 0{,}97$. Explique por que esse alto $R^2$ pode ser enganoso e como a validação cruzada fornece uma medida mais honesta do poder preditivo.
</Exercicio>

<Exercicio
  numero="106.37"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "O intercepto e a predicao de Y quando todas as variaveis sao zero, mesmo sem sentido pratico", correta: true },
    { texto: "O intercepto mede o efeito de X1 quando os demais preditores sao zero" },
    { texto: "O intercepto so e interpretavel quando X = 0 esta dentro do intervalo amostrado" },
    { texto: "O intercepto deve ser removido se nao for significativo ao nivel 5%" }
  ]}
  solucao={<>O intercepto <Eq>{`\\hat\\beta_0`}</Eq> é o valor predito de <Eq>{`Y`}</Eq> quando todos os preditores são zero. Em muitos contextos práticos, isso não tem sentido (altura = 0, renda = 0). Ainda assim, o intercepto é necessário para centrar a reta corretamente e deve ser mantido mesmo quando não é significativo, para evitar viés nos demais coeficientes.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

Em um modelo múltiplo, o intercepto $\hat\beta_0$ não é significativo ao nível 5%. Deve-se removê-lo do modelo? Qual é a interpretação do intercepto?
</Exercicio>

<Exercicio
  numero="106.38"
  dificuldade="demonstracao"
  opcoes={[
    { texto: "A derivada de ||y - X beta||^2 em relacao a beta e 2X(y - Xbeta), igualada a zero resulta em X^TXbeta = X^Ty", correta: true },
    { texto: "A derivada de ||y - X beta||^2 e 2||y - Xbeta||, que nao tem forma matricial fechada" },
    { texto: "As equacoes normais sao X X^T beta = X y, nao X^TX beta = X^Ty" },
    { texto: "X^T e = 0 implica que os residuos sao ortogonais apenas ao vetor y, nao as colunas de X" }
  ]}
  solucao={<>As equações normais são <Eq>{`X^TX\\hat\\beta = X^T\\mathbf{y}`}</Eq>. Multiplicando à esquerda por <Eq>{`(X^TX)^{-1}`}</Eq>: <Eq>{`\\hat\\beta = (X^TX)^{-1}X^T\\mathbf{y}`}</Eq>. Para verificar que <Eq>{`X^T\\mathbf{e} = 0`}</Eq>: <Eq>{`\\mathbf{e} = \\mathbf{y} - X\\hat\\beta`}</Eq>, então <Eq>{`X^T\\mathbf{e} = X^T\\mathbf{y} - X^TX\\hat\\beta = X^T\\mathbf{y} - X^T\\mathbf{y} = 0`}</Eq>. Isso implica <Eq>{`\\sum e_i = 0`}</Eq> (primeira linha de <Eq>{`X`}</Eq> é de 1s).</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

Derive as equações normais $X^TX\hat\beta = X^T\mathbf{y}$ a partir da minimização de $\|\mathbf{y} - X\boldsymbol\beta\|^2$ em relação a $\boldsymbol\beta$. Use o resultado para mostrar que os resíduos são ortogonais às colunas de $X$.
</Exercicio>

<Exercicio
  numero="106.39"
  dificuldade="demonstracao"
  opcoes={[
    { texto: "tr(H) = n pois H e uma matriz n x n" },
    { texto: "tr(H) = p+1 (numero de parametros estimados), pois os autovalores de H sao 0 e 1", correta: true },
    { texto: "tr(H) = 0 pois a soma dos autovalores de uma projecao e zero" },
    { texto: "tr(H) = n - p - 1 pois esse e o numero de graus de liberdade dos residuos" }
  ]}
  solucao={<>A matriz chapéu (hat matrix) é <Eq>{`H = X(X^TX)^{-1}X^T`}</Eq>. Os valores ajustados são <Eq>{`\\hat{\\mathbf{y}} = H\\mathbf{y}`}</Eq> e os resíduos <Eq>{`\\mathbf{e} = (I-H)\\mathbf{y}`}</Eq>. Propriedades: (1) <Eq>{`H`}</Eq> é simétrica: <Eq>{`H^T = H`}</Eq>; (2) <Eq>{`H`}</Eq> é idempotente: <Eq>{`H^2 = H`}</Eq> (projeção ortogonal); (3) os autovalores de <Eq>{`H`}</Eq> são 0 e 1; (4) <Eq>{`\\mathrm{tr}(H) = p+1`}</Eq> (número de parâmetros estimados).</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", licenca: "CC-BY-SA" }}
>

Defina a matriz chapéu $H = X(X^TX)^{-1}X^T$. Prove que $H$ é idempotente ($H^2 = H$) e simétrica. Qual é o traço de $H$ e o que ele representa geometricamente?
</Exercicio>

<Exercicio
  numero="106.40"
  dificuldade="compreensao"
  opcoes={[
    { texto: "Os residuos devem ter media zero e variancias constantes ao longo dos ajustados" },
    { texto: "O grafico de residuos vs. ajustados deve mostrar um padrao em forma de leque (heteroscedasticidade)" },
    { texto: "O QQ-plot dos residuos deve seguir uma reta, indicando normalidade", correta: true },
    { texto: "Os residuos devem ser positivos para que o modelo seja valido" }
  ]}
  solucao={<>O QQ-plot (gráfico quantil-quantil) dos resíduos versus quantis de uma distribuição normal padrão deve mostrar os pontos sobre uma reta diagonal se os erros forem normalmente distribuídos. Desvios nas caudas indicam assimetria ou caudas pesadas. É um dos diagnósticos padrão do pressuposto de normalidade (N do mnemônico LINE).</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", licenca: "CC-BY-SA" }}
>

Qual gráfico de diagnóstico verifica especificamente o pressuposto de normalidade dos erros em um modelo de regressão múltipla? O que se observa nesse gráfico quando a normalidade é satisfeita?
</Exercicio>

<Exercicio
  numero="106.41"
  dificuldade="modelagem"
  opcoes={[
    { texto: "R^2 = 0,60 e bar R^2 = 0,576 — bar R^2 e menor pois penaliza pelo numero de preditores", correta: true },
    { texto: "R^2 = 0,576 e bar R^2 = 0,60 — o ajustado e sempre maior que o nao-ajustado" },
    { texto: "R^2 = 0,60 e bar R^2 = 0,60 — sao identicos pois n e grande" },
    { texto: "R^2 = 120/80 = 1,5 — SSR dividido por SSE" }
  ]}
  solucao={<>Para a regressão com <Eq>{`n = 55`}</Eq>, <Eq>{`p = 3`}</Eq>, <Eq>{`\\mathrm{SSR} = 120`}</Eq>, <Eq>{`\\mathrm{SSE} = 80`}</Eq>: <Eq>{`\\mathrm{SST} = 200`}</Eq>. <Eq>{`R^2 = 120/200 = 0{,}60`}</Eq>. <Eq>{`\\bar R^2 = 1 - (80/51)/(200/54) = 1 - 1{,}569/3{,}704 = 1 - 0{,}424 = 0{,}576`}</Eq>. O modelo explica 57,6% da variância de Y após penalizar pela complexidade.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.3", licenca: "CC-BY-SA" }}
>

Para o modelo do exercício 106.33 ($p = 3$, $n = 55$, $\mathrm{SSR} = 120$, $\mathrm{SSE} = 80$): calcule o $R^2$ e o $\bar R^2$ (ajustado). Compare os dois e interprete a diferença.
</Exercicio>

<Exercicio
  numero="106.42"
  dificuldade="desafio"
  opcoes={[
    { texto: "P(pelo menos um falso positivo) = 10 x 0,05 = 0,50; Bonferroni usa alpha* = alpha x p" },
    { texto: "P(pelo menos um falso positivo) = 1 - 0,95^10 ≈ 0,40; Bonferroni usa alpha* = alpha/p = 0,005", correta: true },
    { texto: "P(pelo menos um falso positivo) = 0,05 pois os testes sao independentes" },
    { texto: "P(pelo menos um falso positivo) = 0,95^10 ≈ 0,60; Bonferroni aumenta alpha para compensar" }
  ]}
  solucao={<>Com <Eq>{`p`}</Eq> preditores, o teste <Eq>{`t`}</Eq> individual de <Eq>{`H_0: \\beta_j = 0`}</Eq> ao nível <Eq>{`\\alpha`}</Eq> tem probabilidade <Eq>{`\\alpha`}</Eq> de falso positivo por preditor. Com <Eq>{`p = 10`}</Eq> testes independentes e <Eq>{`\\alpha = 0{,}05`}</Eq>: probabilidade de pelo menos um falso positivo <Eq>{`= 1 - (1-0{,}05)^{10} \\approx 0{,}40`}</Eq>. Correções: Bonferroni (<Eq>{`\\alpha^* = \\alpha/p`}</Eq>), Holm (procedimento sequencial), ou controle da taxa de falsas descobertas (FDR de Benjamini-Hochberg).</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.3", licenca: "CC-BY-SA" }}
>

Em um modelo com $p = 10$ preditores, cada coeficiente é testado ao nível $\alpha = 0{,}05$. Qual é a probabilidade de pelo menos um falso positivo (erro Tipo I) sob $H_0$ global? Como o ajuste de Bonferroni corrige isso?
</Exercicio>

</ListaExercicios>

## Fontes

- [**OpenIntro Statistics** (4ª ed.)](https://www.openintro.org/book/os/) — Diez, Çetinkaya-Rundel, Barr · CC-BY-SA · Capítulo 8 (Multiple and logistic regression). Fonte primária para interpretação de coeficientes, $\bar R^2$, multicolinearidade e variáveis dummy.
- [**Statistics** — OpenStax](https://openstax.org/details/books/statistics) — Illowsky, Dean · CC-BY · Capítulo 13 (Linear Regression and Correlation — Multiple). Fonte para tabelas ANOVA de regressão múltipla e teste F global.
- [**Probabilidade e Estatística** — Wikilivros](https://pt.wikibooks.org/wiki/Probabilidade_e_Estat%C3%ADstica) — colaborativo · CC-BY-SA · Seção de regressão múltipla. Referência em PT-BR com notação matricial.
