---
titulo: "Lição 106 — Regressão múltipla"
slug: "licao-106-regressao-multipla"
categoria: "aulas"
subcategoria: "ano-3-trim-11"
descricao: "Modelo com p preditores, solução matricial OLS, R² ajustado, multicolinearidade, seleção de variáveis e diagnóstico de pressupostos."
ordem: 106
publicado: true
tags: ["estatística inferencial", "regressão múltipla", "OLS", "ano-3", "trim-11"]
prerrequisitos: ["licao-105-regressao-simples"]
autores: ["Clube da Matemática"]
atualizadoEm: "2026-05-06"
usadoEm: ["Stochastik LK alemão (Klasse 12)", "H2 Mathematics Singapura (§15)", "econometria introdutória"]
versao: "v1"
---

<EquacaoCanonica
  formula="\hat{\boldsymbol\beta} = (X^TX)^{-1}X^T\mathbf{y}"
  legenda={<>Na <strong>regressão múltipla</strong> com <Eq>{`p`}</Eq> preditores, o estimador OLS é a solução matricial das equações normais. Cada coeficiente <Eq>{`\\hat\\beta_j`}</Eq> mede o efeito de <Eq>{`X_j`}</Eq> sobre <Eq>{`Y`}</Eq> mantendo os demais preditores fixos — o chamado <em>efeito parcial</em>.</>}
  audioTexto="Regressão múltipla. Beta chapéu vetor igual à inversa de equis transposto equis, multiplicada por equis transposto ípsilon. Cada coeficiente beta jota chapéu é o efeito parcial do preditor jota, controlando os demais. Generalização matricial da regressão simples."
/>

<aside>

**Livros que cobrem esta lição**

- [**OpenIntro Statistics** (4ª ed.)](https://www.openintro.org/book/os/) · §8.1–8.4 · CC-BY-SA · <em>Tratamento pedagógico de regressão múltipla com interpretação de coeficientes parciais, R² ajustado e seleção de modelos; datasets disponíveis online para reprodução.</em>
- [**Statistics** — OpenStax (Illowsky, Dean)](https://openstax.org/details/books/statistics) · §13.1–13.5 · CC-BY · <em>Abordagem aplicada com tabelas ANOVA de regressão múltipla, testes F global e parcial, e exemplos com dados reais.</em>
- [**Probabilidade e Estatística** — Wikilivros](https://pt.wikibooks.org/wiki/Probabilidade_e_Estat%C3%ADstica) · Regressão múltipla · CC-BY-SA · <em>Referência em PT-BR com notação matricial compatível com o currículo de engenharia brasileira.</em>

</aside>

<DuasPortas>

<Porta nivel="formal" titulo="Definição rigorosa">

### Modelo de regressão linear múltipla

<Definicao titulo="Modelo e notacao matricial">

Para $n$ observações e $p$ preditores:

$$Y_i = \beta_0 + \beta_1 X_{i1} + \beta_2 X_{i2} + \cdots + \beta_p X_{ip} + \varepsilon_i, \quad \varepsilon_i \stackrel{i.i.d.}{\sim} \mathcal{N}(0,\sigma^2)$$

Em forma matricial: $\mathbf{Y} = X\boldsymbol\beta + \boldsymbol\varepsilon$, onde:

- $\mathbf{Y} \in \mathbb{R}^n$: vetor de respostas.
- $X \in \mathbb{R}^{n\times(p+1)}$: matriz de design (primeira coluna de 1s para o intercepto).
- $\boldsymbol\beta \in \mathbb{R}^{p+1}$: vetor de coeficientes.
- $\boldsymbol\varepsilon \in \mathbb{R}^n$: vetor de erros.

</Definicao>

> "The multiple regression model is $y = \beta_0 + \beta_1 x_1 + \beta_2 x_2 + \cdots + \beta_k x_k + \varepsilon$. The coefficient $\beta_i$ measures the expected change in $y$ per unit change in $x_i$ when all other predictors are held constant." — [OpenIntro Statistics, §8.1](https://www.openintro.org/book/os/), p. 362

<Definicao titulo="Estimador OLS e equacoes normais">

Minimizar $SSE = \|\mathbf{Y} - X\boldsymbol\beta\|^2$ leva às equações normais:

$$X^TX\hat{\boldsymbol\beta} = X^T\mathbf{Y}$$

cuja solução (quando $X^TX$ é invertível) é:

<Equation explicacao="Estimador OLS matricial: existe quando os preditores são linearmente independentes." latex="\hat{\boldsymbol\beta} = (X^TX)^{-1}X^T\mathbf{Y}" />

A matriz de variância-covariância dos estimadores é:

<Equation explicacao="Cov do estimador: inversamente proporcional a X'X. Mais dados ou mais variação em X reduz a variância." latex="\text{Cov}(\hat{\boldsymbol\beta}) = \sigma^2 (X^TX)^{-1}" />

</Definicao>

### Métricas de ajuste

<Definicao titulo="R² ajustado">

O $R^2$ cresce com qualquer preditor adicionado — mesmo irrelevante. O $R^2$ ajustado penaliza pelo número de parâmetros:

<Equation explicacao="R² ajustado: penaliza pela complexidade. Pode cair ao adicionar preditor fraco." latex="\bar R^2 = 1 - \frac{SSE/(n-p-1)}{SST/(n-1)}" />

Se $\bar R^2$ aumenta com novo preditor, o preditor melhora o modelo; se cai, prejudica.

</Definicao>

### Inferência

<Definicao titulo="Teste t parcial e teste F global">

**Teste $t$ parcial** para $H_0: \beta_j = 0$ (com os demais preditores no modelo):

<Equation explicacao="Testa se o preditor Xj tem efeito significativo dado que os demais já estão no modelo." latex="T_j = \frac{\hat\beta_j}{SE(\hat\beta_j)} \sim t_{n-p-1}" />

**Teste $F$ global** para $H_0: \beta_1 = \cdots = \beta_p = 0$:

<Equation explicacao="Testa se o modelo como um todo explica mais do que a média de Y." latex="F = \frac{SSR/p}{SSE/(n-p-1)} \sim F_{p,\,n-p-1}" />

</Definicao>

<Definicao titulo="Multicolinearidade e VIF">

Quando dois ou mais preditores são altamente correlacionados, $X^TX$ fica quase-singular e os $SE(\hat\beta_j)$ incham — coeficientes instáveis e difíceis de interpretar.

**Variance Inflation Factor:**

<Equation explicacao="VIF mede quanto a variância de beta_j aumenta pela correlação com os outros preditores. VIF > 10 é preocupante." latex="VIF_j = \frac{1}{1 - R_j^2}" />

onde $R_j^2$ é o $R^2$ da regressão de $X_j$ sobre os demais preditores.

</Definicao>

<Insight>

Em regressão simples, $\hat\beta_1$ e o coeficiente de correlação têm sempre o mesmo sinal. Em regressão múltipla, a correlação entre $X_j$ e $Y$ pode ter sinal oposto ao de $\hat\beta_j$ (efeito de supressão). Isso ocorre porque o coeficiente parcial controla para os outros preditores.

</Insight>

<figure className="not-prose my-5 rounded-xl border border-clube-mist-soft/40 bg-clube-surface p-4">
<svg viewBox="0 0 520 160" className="w-full">
<g>
  <text x="10" y="20" fontSize="11" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.8" fontWeight="bold">Matriz de design X (n=4, p=2)</text>
  <rect x="10" y="30" width="200" height="110" rx="4" fill="none" stroke="currentColor" strokeOpacity="0.3" strokeWidth="1"/>
  <text x="20" y="50" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">1   X₁₁   X₁₂</text>
  <text x="20" y="70" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">1   X₂₁   X₂₂</text>
  <text x="20" y="90" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">1   X₃₁   X₃₂</text>
  <text x="20" y="110" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">1   X₄₁   X₄₂</text>
  <text x="60" y="135" fontSize="9" fill="currentColor" fillOpacity="0.5" textAnchor="middle">n×(p+1)</text>
</g>
<g>
  <text x="230" y="82" fontSize="16" fill="rgb(232 199 122)" textAnchor="middle">×</text>
</g>
<g>
  <rect x="250" y="30" width="60" height="110" rx="4" fill="none" stroke="currentColor" strokeOpacity="0.3" strokeWidth="1"/>
  <text x="270" y="55" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">β₀</text>
  <text x="270" y="75" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">β₁</text>
  <text x="270" y="95" fontSize="10" fontFamily="ui-monospace,monospace" fill="currentColor" fillOpacity="0.6">β₂</text>
  <text x="280" y="135" fontSize="9" fill="currentColor" fillOpacity="0.5" textAnchor="middle">(p+1)×1</text>
</g>
<g>
  <text x="335" y="82" fontSize="16" fill="rgb(232 199 122)" textAnchor="middle">=</text>
</g>
<g>
  <rect x="355" y="30" width="50" height="110" rx="4" fill="none" stroke="rgb(26 77 92)" strokeOpacity="0.5" strokeWidth="1.5"/>
  <text x="375" y="55" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(26 77 92)" fillOpacity="0.9">Ŷ₁</text>
  <text x="375" y="75" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(26 77 92)" fillOpacity="0.9">Ŷ₂</text>
  <text x="375" y="95" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(26 77 92)" fillOpacity="0.9">Ŷ₃</text>
  <text x="375" y="115" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(26 77 92)" fillOpacity="0.9">Ŷ₄</text>
  <text x="380" y="135" fontSize="9" fill="currentColor" fillOpacity="0.5" textAnchor="middle">n×1</text>
</g>
<g>
  <text x="425" y="82" fontSize="16" fill="rgb(232 199 122)" textAnchor="middle">+</text>
</g>
<g>
  <rect x="445" y="30" width="60" height="110" rx="4" fill="none" stroke="rgb(199 107 63)" strokeOpacity="0.5" strokeWidth="1.5"/>
  <text x="465" y="55" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(199 107 63)" fillOpacity="0.9">ε₁</text>
  <text x="465" y="75" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(199 107 63)" fillOpacity="0.9">ε₂</text>
  <text x="465" y="95" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(199 107 63)" fillOpacity="0.9">ε₃</text>
  <text x="465" y="115" fontSize="10" fontFamily="ui-monospace,monospace" fill="rgb(199 107 63)" fillOpacity="0.9">ε₄</text>
  <text x="475" y="135" fontSize="9" fill="currentColor" fillOpacity="0.5" textAnchor="middle">n×1</text>
</g>
</svg>
<figcaption className="mt-2 text-center text-xs text-clube-mist">
Representação matricial do modelo: $\mathbf{Y} = X\boldsymbol\beta + \boldsymbol\varepsilon$. A primeira coluna de 1s em $X$ gera o intercepto $\beta_0$.
</figcaption>
</figure>

</Porta>

<Porta nivel="5" titulo="Pra criança de 5 anos">

Na aula anterior, você usou **uma régua** para prever uma coisa com outra: altura para prever peso.

Agora imagine que você quer prever o peso de uma pessoa e tem **duas informações**: altura E idade. Com duas informações juntas, sua previsão fica melhor!

Regressão múltipla é como ter **várias réguas ao mesmo tempo** — cada uma mede uma coisa diferente — e todas juntas fazem uma previsão mais certeira.

</Porta>

<Porta nivel="10" titulo="Pra criança de 10 anos">

Na regressão simples, você usava só **uma variável** ($X$) para prever ($Y$). Mas quase sempre temos mais informações disponíveis.

Por exemplo, para prever o **preço de uma casa** você pode usar:
- $X_1$ = área em m²
- $X_2$ = número de quartos
- $X_3$ = distância do centro (em km)

A regressão múltipla ajusta uma fórmula do tipo:

$$\hat Y = \hat\beta_0 + \hat\beta_1 X_1 + \hat\beta_2 X_2 + \hat\beta_3 X_3$$

Cada $\hat\beta_j$ diz: "se eu aumentar $X_j$ em uma unidade, mantendo os outros fixos, quanto $Y$ muda em média?"

</Porta>

<Porta nivel="15" titulo="Pra adolescente de 15 anos">

A regressão múltipla generaliza a simples incluindo $p$ preditores. O modelo é:

$$Y = \beta_0 + \beta_1 X_1 + \cdots + \beta_p X_p + \varepsilon$$

**Interpretação dos coeficientes:** $\hat\beta_j$ é o efeito de $X_j$ sobre $Y$, **mantendo todos os outros preditores constantes** (efeito parcial ou "ceteris paribus").

**Por que isso importa?** Imagine prever o salário de um funcionário usando anos de experiência ($X_1$) e nível de escolaridade ($X_2$). O efeito de experiência no salário é diferente quando você controla por escolaridade do que quando você ignora escolaridade.

**R² vs. R² ajustado:** adicionar qualquer variável aumenta $R^2$, mesmo que ela seja irrelevante. O $\bar R^2$ (ajustado) penaliza por variáveis extras — use-o para comparar modelos com número diferente de preditores.

</Porta>

<Porta nivel="25" titulo="Para estudante de engenharia">

**Álgebra matricial do modelo**

Dado $X \in \mathbb{R}^{n\times(p+1)}$ com posto coluna $p+1$ (preditores LI), o OLS resolve:

$$\min_{\boldsymbol\beta} \|\mathbf{Y} - X\boldsymbol\beta\|^2 \implies \hat{\boldsymbol\beta} = (X^TX)^{-1}X^T\mathbf{Y}$$

O vetor de valores ajustados é $\hat{\mathbf{Y}} = H\mathbf{Y}$, onde $H = X(X^TX)^{-1}X^T$ é o **hat matrix** (projeção ortogonal sobre o espaço coluna de $X$). Os resíduos $\mathbf{e} = (I-H)\mathbf{Y}$ são ortogonais a $\hat{\mathbf{Y}}$.

**Tabela ANOVA múltipla**

| Fonte | SS | df | MS | F |
|---|---|---|---|---|
| Regressão | $SSR$ | $p$ | $SSR/p$ | $MSR/MSE$ |
| Erro | $SSE$ | $n-p-1$ | $SSE/(n-p-1)$ | — |
| Total | $SST$ | $n-1$ | — | — |

**Seleção de variáveis:** AIC, BIC, $\bar R^2$, validação cruzada. Métodos automáticos: stepwise (forward, backward, both) — úteis mas não substituem raciocínio substantivo.

**Multicolinearidade:** $\text{det}(X^TX) \to 0$ implica inversão instável. Ridge regression ($L_2$): $\hat{\boldsymbol\beta}_{ridge} = (X^TX + \lambda I)^{-1}X^T\mathbf{Y}$ regulariza sem seleção; Lasso ($L_1$) seleciona variáveis automaticamente.

</Porta>

<Porta nivel="40" titulo="Para profissional sênior">

A regressão múltipla domina a análise empírica em ciências sociais, epidemiologia e finanças. Os debates metodológicos centrais:

**Identificação causal:** OLS com controles não resolve endogeneidade. O "modelo de resultado potencial" (Rubin) distingue efeito causal de associação condicional. Design baseado em variação exógena (experimentos naturais, RDD, DiD, IV) é a linguagem moderna da causalidade em economia — reconhecida pelo Nobel de Economia 2021 (Card, Angrist, Imbens). Veja [nobelprize.org/prizes/economic-sciences/2021/summary/](https://www.nobelprize.org/prizes/economic-sciences/2021/summary/).

**Overfitting e validação:** em amostras pequenas com muitos preditores ($p \approx n$), OLS overfitta. Regularização (ridge, lasso, elastic net) e validação cruzada $k$-fold são o padrão em ML; em econometria, o uso de dados de teste separados é ainda a norma.

**Erros padrão robustos:** heterocedasticidade e correlação serial violam $\text{Cov}(\boldsymbol\varepsilon) = \sigma^2 I$. Os erros de White (HC) e Newey-West (HAC) corrigem a matriz de variância sem exigir forma funcional para a heterocedast.

**Regressão em alta dimensão ($p > n$):** OLS não tem solução única. Lasso via coordenada descida, PCR, PLS. Inferência pós-seleção (debiased Lasso) é área ativa de pesquisa.

</Porta>

<Porta nivel="pratica" titulo="Aplicação brasileira">

**Preço de imóveis em São Paulo (dados simulados conforme padrão IBGE FipeZap)**

Regressão do preço de venda $Y$ (em R\$ mil) sobre: área $X_1$ (m²), número de quartos $X_2$ e distância do metro $X_3$ (km). Com $n=150$ transações:

$$\hat Y = 180 + 4{,}2 X_1 + 35 X_2 - 12 X_3$$

- $hatbeta_1 = 4{,}2$: cada m² adicional vale, em média, R\$ 4.200 (controlando quartos e metro).
- $hatbeta_2 = 35$: cada quarto extra adiciona R\$ 35.000 (controlando área e metro).
- $hatbeta_3 = -12$: cada km a mais do metro reduz R\$ 12.000 (controlando área e quartos).
- $R^2 = 0{,}78$, $\bar R^2 = 0{,}77$: o modelo explica 77% da variabilidade de preços ajustando pela complexidade.

**Teste F global:** $F = 172{,}4$ com $p$-valor menor que 0,001 — os três preditores juntos são altamente significativos.

**Cuidado:** multicolinearidade entre área e quartos ($r = 0{,}69$) infla os $SE$ — VIF calculados: $VIF_1 = 2{,}3$, $VIF_2 = 2{,}1$, $VIF_3 = 1{,}1$. Todos abaixo de 10: multicolinearidade aceitável.

</Porta>

</DuasPortas>

## Exemplos resolvidos

<Exemplo numero="106.1" titulo="Ajuste e interpretacao com dois preditores">

**Problema.** Regressão do salário anual $Y$ (R\$ mil) sobre experiência $X_1$ (anos) e escolaridade $X_2$ (anos de estudo) produziu: $hat Y = -12 + 2{,}8 X_1 + 5{,}2 X_2$, com $n=50$, $R^2=0{,}74$, $bar R^2=0{,}73$.

Interprete cada coeficiente e calcule a previsão para um profissional com 10 anos de experiência e 16 anos de estudo.

**Estratégia.** Interpretar cada $\hat\beta_j$ como efeito parcial (ceteris paribus). Substituir os valores na equação ajustada.

**Resolução.**

- $\hat\beta_0 = -12$: intercepto (sem interpretação substantiva isolada — nenhum profissional tem zero anos de experiência e estudo).
- $hatbeta_1 = 2{,}8$: controlando escolaridade, cada ano adicional de experiência está associado a R\$ 2.800/ano a mais, em média.
- $hatbeta_2 = 5{,}2$: controlando experiência, cada ano adicional de estudo está associado a R\$ 5.200/ano a mais.

Previsão: $hat Y = -12 + 2{,}8times10 + 5{,}2times16 = -12+28+83{,}2 = 99{,}2$ mil R\$/ano.

**Verificação.** $\bar R^2 = 0{,}73$ próximo de $R^2 = 0{,}74$: apenas 2 preditores, pouca penalização — modelo parsimonioso.

**Fonte.** [OpenIntro Statistics, §8.1, Exemplo 8.1](https://www.openintro.org/book/os/) — CC-BY-SA

</Exemplo>

<Exemplo numero="106.2" titulo="R² ajustado na selecao entre modelos">

**Problema.** Três modelos para prever o consumo de energia elétrica domiciliar ($n=80$):

| Modelo | Preditores | $R^2$ | $\bar R^2$ |
|---|---|---|---|
| M1 | área | 0,61 | 0,61 |
| M2 | área + nº moradores | 0,71 | 0,70 |
| M3 | área + nº moradores + renda + temperatura + dia semana | 0,74 | 0,70 |

Qual modelo preferir?

**Estratégia.** Comparar $\bar R^2$ — penaliza por número de preditores.

**Resolução.**

M2 e M3 têm o mesmo $\bar R^2 = 0{,}70$, mas M3 usa 5 preditores contra 2 de M2. Pelo princípio da parcimônia (navalha de Occam), **M2 é preferido**: adicionar 3 preditores não melhora o $\bar R^2$ — eles não contribuem informação líquida.

Note que $R^2$ de M3 (0,74) supera M2 (0,71) — mas $R^2$ sempre cresce com preditores adicionais, mesmo irrelevantes.

**Verificação.** Poderia confirmar com AIC/BIC: $AIC = n\ln(SSE/n) + 2(p+1)$ — menor é melhor.

**Fonte.** [OpenIntro Statistics, §8.3, Tabela 8.7](https://www.openintro.org/book/os/) — CC-BY-SA

</Exemplo>

<Exemplo numero="106.3" titulo="Teste F global e tabela ANOVA">

**Problema.** Regressão com $p=3$ preditores, $n=40$: $SST=2400$, $SSE=720$. Monte a tabela ANOVA e teste $H_0: \beta_1=\beta_2=\beta_3=0$ ao nível 5%.

**Estratégia.** Calcular SSR, df, MS, F; comparar com valor crítico $F_{3,36;\,0{,}05}$.

**Resolução.**

$SSR = 2400-720 = 1680$; $R^2 = 1680/2400 = 0{,}70$.

| Fonte | SS | df | MS | F |
|---|---|---|---|---|
| Regressão | 1680 | 3 | 560 | 28,0 |
| Erro | 720 | 36 | 20 | — |
| Total | 2400 | 39 | — | — |

$F_{3,36;\,0{,}05} \approx 2{,}87$. Como $F = 28{,}0 \gg 2{,}87$, rejeitamos $H_0$: o modelo como um todo é altamente significativo.

**Verificação.** $MSE = 20 \implies \hat\sigma = \sqrt{20} \approx 4{,}47$ — desvio padrão residual em unidades de $Y$.

**Fonte.** [OpenStax Statistics, §13.4](https://openstax.org/books/statistics/pages/13-introduction) — CC-BY

</Exemplo>

<Exemplo numero="106.4" titulo="Detectar multicolinearidade via VIF">

**Problema.** Regressão do preço de um veículo ($Y$) sobre potência ($X_1$), cilindrada ($X_2$) e peso ($X_3$). Regressões auxiliares: $R_1^2=0{,}89$ ($X_1$ sobre $X_2, X_3$), $R_2^2=0{,}91$, $R_3^2=0{,}85$. Calcule os VIFs e avalie.

**Estratégia.** $VIF_j = 1/(1-R_j^2)$.

**Resolução.**

$VIF_1 = 1/(1-0{,}89) = 9{,}09$; $VIF_2 = 1/(1-0{,}91) = 11{,}1$; $VIF_3 = 1/(1-0{,}85) = 6{,}67$.

$VIF_2 = 11{,}1 > 10$: cilindrada apresenta multicolinearidade severa com as demais variáveis.

**Ação:** remover $X_2$ do modelo ou combinar $X_1$ e $X_2$ em um índice composto (análise de componentes principais).

**Verificação.** Após remover $X_2$, recalcular VIFs para os preditores restantes.

**Fonte.** [OpenIntro Statistics, §8.4](https://www.openintro.org/book/os/) — CC-BY-SA

</Exemplo>

<Exemplo numero="106.5" titulo="Variável dummy e interacao">

**Problema.** Prever salário $Y$ (R\$ mil/ano) usando experiência $X_1$ (anos) e gênero $D$ (1=feminino, 0=masculino). Resultado: $hat Y = 45 + 2{,}5X_1 - 8D$.

Interprete o coeficiente de $D$ e calcule os salários previstos para: (a) homem com 10 anos de experiência; (b) mulher com 10 anos de experiência.

**Estratégia.** A variável dummy cria dois interceptos diferentes (um por gênero), mantendo inclinação igual.

**Resolução.**

$hatbeta_D = -8$: controlando experiência, mulheres ganham em média R\$ 8.000/ano a menos do que homens — diferença bruta (não causal, pois não controlamos setor, cargo, jornada).

(a) Homem ($D=0$), $X_1=10$: $hat Y = 45 + 25 - 0 = 70$ mil R\$/ano.

(b) Mulher ($D=1$), $X_1=10$: $hat Y = 45 + 25 - 8 = 62$ mil R\$/ano.

**Verificação.** A diferença é constante ($-8$) para qualquer nível de experiência — modelo sem interação assume efeito aditivo. Para permitir inclinações diferentes, adicionar $X_1 \times D$.

**Fonte.** [OpenIntro Statistics, §8.2, Exemplo 8.4](https://www.openintro.org/book/os/) — CC-BY-SA

</Exemplo>

<ListaExercicios seed="licao-106-regressao-multipla">

<Exercicio
  numero="106.1"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "O coeficiente de fumar mede o efeito total do tabagismo sobre o peso, ignorando outros fatores" },
    { texto: "O coeficiente de fumar mede o efeito parcial do tabagismo sobre o peso do bebê, controlando para paridade", correta: true },
    { texto: "O intercepto é o peso esperado de bebês de mães fumantes com paridade zero" },
    { texto: "A equação de regressão não pode incluir uma variável binária como fumar" }
  ]}
  solucao={<>Em regressão múltipla, cada coeficiente mede o efeito parcial — a mudança esperada em <Eq>{`Y`}</Eq> por unidade de mudança no preditor, mantendo os demais fixos. O coeficiente de fumar indica quantas onças de diferença há no peso do bebê entre mães fumantes e não-fumantes, controlando para paridade.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", exercicio: "ex. 9.1", licenca: "CC-BY-SA 3.0" }}
>

Um modelo de regressão múltipla prevê o peso médio de bebês ao nascer (em onças) usando como preditores: fumar (1 = fumante, 0 = não fumante) e paridade (número de filhos anteriores). Escreva a equação do modelo e interprete o coeficiente de fumar como efeito parcial.

</Exercicio>

<Exercicio
  numero="106.2"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "A paridade mede o peso do bebê em onças por cada filho adicional, sem controle de fumo" },
    { texto: "Controlando para o hábito de fumar, cada filho anterior está associado a uma mudança no peso, medida pelo coeficiente de paridade", correta: true },
    { texto: "Paridade é irrelevante para o peso e deve ser excluída do modelo" },
    { texto: "O coeficiente de paridade é sempre negativo pois mais filhos implica menor atenção pré-natal" }
  ]}
  passos={<>
    <ol>
      <li>Modelo múltiplo: <Eq>{`\\hat Y = \\hat\\beta_0 + \\hat\\beta_1 \\cdot \\text{fumar} + \\hat\\beta_2 \\cdot \\text{paridade}`}</Eq>.</li>
      <li>O coeficiente <Eq>{`\\hat\\beta_2`}</Eq> é o efeito parcial de paridade, controlando para fumo.</li>
      <li>Compare o coeficiente de fumar no modelo simples (só fumar) com o da regressão múltipla — eles diferem por confundimento.</li>
      <li>Conclusão: na regressão múltipla, cada coeficiente isola o efeito de uma variável mantendo as demais constantes.</li>
    </ol>
  </>}
  solucao={<>O coeficiente de paridade mede o efeito parcial — a mudança esperada no peso do bebê por filho adicional anterior, controlando para o hábito de fumar da mãe. Se fumar e paridade são correlacionadas, os coeficientes na regressão múltipla diferirão dos modelos simples separados.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", exercicio: "ex. 9.2", licenca: "CC-BY-SA 3.0" }}
>

O estudo sobre peso de bebês incluiu um segundo preditor: paridade (0 = primeiro filho, 1 = segundo ou posterior). Interprete o coeficiente de paridade na regressão múltipla e explique por que o coeficiente de fumar pode mudar ao incluir paridade no modelo.

</Exercicio>

<Exercicio
  numero="106.3"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Os coeficientes do modelo múltiplo são iguais aos modelos simples separados, pois OLS é linear" },
    { texto: "O modelo múltiplo com fumar e paridade pode ter coeficientes diferentes dos modelos simples, por confundimento", correta: true },
    { texto: "O R² do modelo múltiplo é sempre menor que os R² dos modelos simples" },
    { texto: "Só é possível comparar modelos com o mesmo número de preditores" }
  ]}
  solucao={<>Quando duas variáveis são correlacionadas entre si (como fumar e paridade), os coeficientes na regressão múltipla diferem dos modelos simples. O modelo múltiplo isola o efeito parcial de cada preditor, eliminando o confundimento mútuo. O <Eq>{`\\bar R^2`}</Eq> permite comparar os modelos penalizando pela complexidade.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", exercicio: "ex. 9.3", licenca: "CC-BY-SA 3.0" }}
>

Considere três modelos: (1) peso ~ fumar; (2) peso ~ paridade; (3) peso ~ fumar + paridade. Os coeficientes de fumar nos modelos 1 e 3 devem ser iguais? Justifique e explique o papel do confundimento.

</Exercicio>

<Exercicio
  numero="106.4"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "O intercepto é o número médio de dias de ausência para um aluno com falta de disciplina zero e distância zero", correta: true },
    { texto: "O intercepto mede o efeito da distância sobre o absentismo" },
    { texto: "O intercepto é a correlação entre falta de disciplina e absentismo" },
    { texto: "O intercepto é sempre zero em modelos de regressão múltipla" }
  ]}
  solucao={<>Em qualquer regressão múltipla com intercepto, <Eq>{`\\hat\\beta_0`}</Eq> é o valor predito de <Eq>{`Y`}</Eq> quando todos os preditores são zero. Para o modelo de absentismo, é o número médio de dias ausentes esperado para um aluno com ausência de disciplina zero e distância zero da escola. Pode não ter interpretação prática útil se zero não está no intervalo amostrado.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", exercicio: "ex. 9.4", licenca: "CC-BY-SA 3.0" }}
>

Pesquisadores modelaram o absentismo escolar ($Y$, dias de ausência) em função de falta de disciplina ($X_1$) e distância da escola ($X_2$, km): <Eq>{`\\hat Y = \\hat\\beta_0 + \\hat\\beta_1 X_1 + \\hat\\beta_2 X_2`}</Eq>. O que representa o intercepto <Eq>{`\\hat\\beta_0`}</Eq> nesse modelo?

</Exercicio>

<Exercicio
  numero="106.5"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "O coeficiente de horas de estudo mede o efeito total de estudar, incluindo efeitos de aulas perdidas e sexo" },
    { texto: "O coeficiente de horas de estudo mede o efeito parcial de uma hora adicional de estudo, controlando para aulas perdidas e sexo", correta: true },
    { texto: "O coeficiente de horas de estudo é a correlação de Pearson entre GPA e horas" },
    { texto: "O coeficiente de horas de estudo e o R² do modelo simples de GPA em horas" }
  ]}
  passos={<>
    <ol>
      <li>Modelo: <Eq>{`\\hat{\\text{GPA}} = \\hat\\beta_0 + \\hat\\beta_1 \\cdot \\text{horas} + \\hat\\beta_2 \\cdot \\text{aulas perdidas} + \\hat\\beta_3 \\cdot \\text{sexo}`}</Eq>.</li>
      <li>O coeficiente <Eq>{`\\hat\\beta_1`}</Eq> é o efeito parcial: mantendo aulas perdidas e sexo constantes, cada hora adicional de estudo está associada a uma mudança de <Eq>{`\\hat\\beta_1`}</Eq> no GPA.</li>
      <li>Se <Eq>{`\\hat\\beta_1 > 0`}</Eq>: estudar mais está associado a GPA maior, dado o mesmo nível de frequência e sexo.</li>
    </ol>
  </>}
  solucao={<>Na pesquisa com 55 alunos da Duke, o modelo múltiplo isola o efeito de cada hora adicional de estudo sobre o GPA, controlando para o número de aulas perdidas e o sexo do estudante. Cada coeficiente é um efeito parcial (ceteris paribus).</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", exercicio: "ex. 9.5", licenca: "CC-BY-SA 3.0" }}
>

Uma pesquisa com 55 alunos da Universidade Duke modelou o GPA em função de horas de estudo por noite, número de aulas perdidas e sexo. O que o coeficiente de horas de estudo representa no modelo múltiplo?

</Exercicio>

<Exercicio
  numero="106.6"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "O modelo com diâmetro, altura e a interação diâmetro×altura captura melhor o volume do que os preditores separados", correta: true },
    { texto: "Incluir a interação diâmetro×altura sempre reduz o R² ajustado" },
    { texto: "O produto diâmetro×altura só é útil se os dois preditores são independentes" },
    { texto: "Usar a interação é equivalente a modelar apenas o diâmetro ao quadrado" }
  ]}
  solucao={<>O volume de madeira tem uma relação geométrica com diâmetro e altura. Incluir o termo de interação <Eq>{`X_3 = X_1 \\times X_2`}</Eq> permite que o efeito do diâmetro sobre o volume varie com a altura — capturando a relação multiplicativa entre as dimensões. O <Eq>{`\\bar R^2`}</Eq> deve ser alto, refletindo a forte relação geométrica.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", exercicio: "ex. 9.6", licenca: "CC-BY-SA 3.0" }}
>

Madeireiras estimam o volume de árvores (pés cúbicos) a partir do diâmetro ($X_1$, polegadas) e altura ($X_2$, pés). Um modelo inclui diâmetro, altura e a interação diâmetro×altura. Por que incluir o termo de interação melhora o ajuste nesse contexto?

</Exercicio>

<Exercicio
  numero="106.7"
  dificuldade="modelagem"
  opcoes={[
    { texto: "Remover o preditor com o maior valor-p no teste t parcial, pois esse é o menos significativo dado os demais", correta: true },
    { texto: "Remover o preditor com o menor coeficiente absoluto, pois é o de menor efeito" },
    { texto: "Remover todos os preditores com valor-p > 0,01 simultaneamente" },
    { texto: "Nunca remover preditores — o modelo completo é sempre o melhor" }
  ]}
  passos={<>
    <ol>
      <li>Verificar a tabela de coeficientes do modelo de peso de bebês com múltiplos preditores.</li>
      <li>Identificar o preditor com maior valor-p no teste <Eq>{`t`}</Eq> parcial — esse preditor explica menos variância dado os demais.</li>
      <li>Remover esse preditor e reajustar o modelo reduzido.</li>
      <li>Verificar se o <Eq>{`\\bar R^2`}</Eq> sobe ou cai ao remover o preditor.</li>
      <li>Repetir o processo até que todos os preditores restantes sejam significativos (backward elimination).</li>
    </ol>
  </>}
  solucao={<>Na seleção backward, o primeiro passo é identificar e remover o preditor menos significativo dado os demais — aquele com o maior valor-p no teste <Eq>{`t`}</Eq> parcial. Isso é o início do processo de eliminação regressiva para obter um modelo parcimonioso.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", exercicio: "ex. 9.7", licenca: "CC-BY-SA 3.0" }}
>

O modelo completo de peso de bebês inclui vários preditores. Usando o critério de eliminação backward (backward elimination), qual seria o primeiro preditor a ser removido e como essa decisão é tomada?

</Exercicio>

<Exercicio
  numero="106.8"
  dificuldade="modelagem"
  opcoes={[
    { texto: "Manter todos os preditores do modelo completo, pois cada um pode contribuir margem mesmo com p-valor alto" },
    { texto: "Aplicar backward elimination: remover iterativamente o preditor menos significativo até que todos tenham p-valor abaixo do limiar", correta: true },
    { texto: "Usar forward selection apenas quando o modelo completo tem multicolinearidade perfeita" },
    { texto: "Selecionar preditores pelo maior coeficiente absoluto, ignorando os erros padrão" }
  ]}
  solucao={<>Na seleção de modelos para absentismo, aplica-se backward elimination: começar com o modelo completo (falta de disciplina, distância, etc.) e remover o preditor menos significativo a cada passo, reajustando o modelo. Parar quando todos os preditores restantes têm valor-p abaixo do limiar (tipicamente 0,05) ou quando o <Eq>{`\\bar R^2`}</Eq> deixa de melhorar.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", exercicio: "ex. 9.8", licenca: "CC-BY-SA 3.0" }}
>

O modelo completo de absentismo escolar inclui falta de disciplina, distância da escola, sexo, número de irmãos e renda familiar. Usando backward elimination, descreva o processo para chegar a um modelo parcimonioso.

</Exercicio>

<Exercicio
  numero="106.9"
  dificuldade="modelagem"
  opcoes={[
    { texto: "O modelo reduzido tem melhor ajuste se o R² bruto for maior que o do modelo completo" },
    { texto: "O modelo reduzido é preferível se seu R² ajustado for maior ou igual ao do modelo completo", correta: true },
    { texto: "O modelo reduzido é sempre preferível ao completo por parcimônia, independentemente do R² ajustado" },
    { texto: "O modelo completo é sempre preferível pois tem menor SSE por definição" }
  ]}
  solucao={<>Após eliminar preditores pouco significativos do modelo de peso de bebês, o modelo reduzido é preferível se tiver <Eq>{`\\bar R^2`}</Eq> igual ou superior ao completo. O <Eq>{`\\bar R^2`}</Eq> penaliza pela complexidade: se cai ao remover um preditor, esse preditor contribui informação real. Se sobe ou mantém, o preditor era redundante.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", exercicio: "ex. 9.9", licenca: "CC-BY-SA 3.0" }}
>

Após backward elimination no modelo de peso de bebês, obteve-se um modelo reduzido. Como usar o $\bar R^2$ para decidir se o modelo reduzido é melhor que o completo?

</Exercicio>

<Exercicio
  numero="106.10"
  dificuldade="modelagem"
  opcoes={[
    { texto: "O modelo reduzido de absentismo é preferível se tiver menor SSE que o completo" },
    { texto: "O modelo reduzido de absentismo é preferível se tiver maior R² ajustado que o completo, indicando melhor parcimônia", correta: true },
    { texto: "O modelo reduzido nunca é comparado ao completo pelo R² ajustado" },
    { texto: "O modelo com AIC maior é sempre o melhor por ter mais informação" }
  ]}
  solucao={<>O critério de comparação adequado entre modelos com diferentes números de preditores é o <Eq>{`\\bar R^2`}</Eq> (ou o AIC). Um <Eq>{`\\bar R^2`}</Eq> maior indica melhor ajuste penalizado pela complexidade. Se o modelo reduzido de absentismo tem <Eq>{`\\bar R^2`}</Eq> maior ou igual ao completo, é preferível por ser mais parcimonioso.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", exercicio: "ex. 9.10", licenca: "CC-BY-SA 3.0" }}
>

Após aplicar backward elimination ao modelo de absentismo, o modelo reduzido manteve apenas falta de disciplina e distância. O $\bar R^2$ do modelo reduzido é 0,56 e o do completo é 0,54. Qual modelo é preferível e por quê?

</Exercicio>

<Exercicio
  numero="106.11"
  dificuldade="compreensao"
  opcoes={[
    { texto: "A pesquisadora deve usar regressão logística pois a variável resposta é binária (nota do crítico sim/não)" },
    { texto: "A pesquisadora pode usar regressão linear múltipla se a nota do crítico for numérica e contínua", correta: true },
    { texto: "Regressão múltipla não pode incluir variáveis categóricas como gênero do diretor" },
    { texto: "Com mais de 3 preditores, a regressão múltipla perde validade estatística" }
  ]}
  solucao={<>Quando a variável resposta é numérica e contínua (como nota do crítico), a regressão linear múltipla é o modelo adequado. Variáveis categóricas (gênero do diretor, gênero do filme) são incluídas como dummies. Se a resposta fosse binária (sucesso/fracasso), usaríamos regressão logística.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", exercicio: "ex. 9.11", licenca: "CC-BY-SA 3.0" }}
>

Uma cientista social quer modelar a nota média de críticos para filmes em função de: orçamento, duração, gênero do filme e gênero do diretor. Que tipo de modelo de regressão é apropriado e como tratar as variáveis categóricas?

</Exercicio>

<Exercicio
  numero="106.12"
  dificuldade="compreensao"
  opcoes={[
    { texto: "Um sistema de recomendação de filmes deve usar regressão simples pois só há uma variável resposta por usuário" },
    { texto: "Regressão múltipla pode ser usada para prever avaliação de filmes com múltiplos preditores; backward elimination remove preditores pouco relevantes", correta: true },
    { texto: "A seleção de variáveis não é necessária em sistemas de recomendação pois todos os preditores são relevantes" },
    { texto: "O R² é o único critério válido para seleção de modelos em sistemas de recomendação" }
  ]}
  solucao={<>Um sistema de recomendação pode usar regressão múltipla para prever a avaliação de um usuário com base em características do filme (gênero, duração, orçamento, nota de críticos). Backward elimination remove preditores irrelevantes, e o <Eq>{`\\bar R^2`}</Eq> ou AIC guia a seleção do modelo final.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", exercicio: "ex. 9.12", licenca: "CC-BY-SA 3.0" }}
>

Uma empresa de streaming quer construir um modelo de regressão para prever a avaliação de usuários de filmes usando como preditores: gênero, duração, nota de críticos, orçamento e ano de lançamento. Como a seleção de variáveis melhora esse sistema?

</Exercicio>

<Exercicio
  numero="106.13"
  dificuldade="compreensao"
  opcoes={[
    { texto: "Verdadeiro: o R² ajustado sempre aumenta com o número de preditores adicionados" },
    { texto: "Falso: o R² ajustado pode diminuir ao adicionar preditores pouco informativos, penalizando a complexidade", correta: true },
    { texto: "Verdadeiro: o R² não ajustado e o ajustado são sempre iguais em modelos com muitos preditores" },
    { texto: "Falso: o R² ajustado não pode ser comparado entre modelos com diferentes tamanhos de amostra" }
  ]}
  solucao={<>O <Eq>{`R^2`}</Eq> não ajustado nunca diminui ao adicionar preditores — é uma propriedade matemática do OLS. O <Eq>{`\\bar R^2`}</Eq> ajustado penaliza pelo número de parâmetros e pode diminuir quando o preditor adicionado contribui pouco. Por isso, <Eq>{`\\bar R^2`}</Eq> é o critério correto para comparar modelos com diferentes números de preditores.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.R", exercicio: "ex. 9.19", licenca: "CC-BY-SA 3.0" }}
>

Verifique se a seguinte afirmação é verdadeira ou falsa: "O R² ajustado pode diminuir ao adicionar um preditor ao modelo de regressão múltipla." Justifique.

</Exercicio>

<Exercicio
  numero="106.14"
  dificuldade="compreensao"
  opcoes={[
    { texto: "Verdadeiro: a regressão logística é usada quando a variável resposta é contínua e positiva" },
    { texto: "Falso: a regressão logística é usada quando a variável resposta é binária (0/1), não contínua", correta: true },
    { texto: "Verdadeiro: o output da regressão logística é a média condicional de Y dado X" },
    { texto: "Falso: a regressão logística só pode ter um único preditor" }
  ]}
  solucao={<>A regressão logística é usada quando a variável resposta é binária (0 ou 1), como aprovação/reprovação, doente/saudável. O modelo prevê a probabilidade de Y=1 como uma função logística dos preditores: <Eq>{`P(Y=1) = e^{\\eta}/(1+e^{\\eta})`}</Eq>, onde <Eq>{`\\eta = \\beta_0 + \\beta_1 X_1 + \\cdots`}</Eq>. A regressão linear múltipla é para Y contínuo.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.R", exercicio: "ex. 9.20", licenca: "CC-BY-SA 3.0" }}
>

Verifique se a afirmação é verdadeira ou falsa: "A regressão logística é apropriada quando a variável resposta é contínua e simétrica." Explique a diferença entre regressão linear e logística quanto à natureza da variável resposta.

</Exercicio>

<Exercicio
  numero="106.15"
  dificuldade="modelagem"
  opcoes={[
    { texto: "Um filtro de spam usa regressão linear múltipla com Y = número de spams por dia" },
    { texto: "Um filtro de spam usa regressão logística para estimar a probabilidade de um e-mail ser spam, com base em palavras-chave e remetente", correta: true },
    { texto: "Filtros de spam não usam modelos estatísticos, apenas regras fixas de palavras proibidas" },
    { texto: "A regressão logística não pode ser usada com preditores categóricos como palavras-chave" }
  ]}
  solucao={<>Filtros de spam são classificadores binários: e-mail é spam (1) ou não (0). A regressão logística modela <Eq>{`P(\\text{spam}=1 | X_1,\\ldots,X_p)`}</Eq> como função logística de preditores (frequência de palavras suspeitas, remetente desconhecido, etc.). Os coeficientes positivos aumentam a probabilidade de classificar como spam.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.R", exercicio: "ex. 9.21", licenca: "CC-BY-SA 3.0" }}
>

Filtros de spam são baseados em princípios similares à regressão logística. Que tipo de variável resposta tem um modelo de filtragem de spam? Por que se usa regressão logística em vez de linear?

</Exercicio>

<Exercicio
  numero="106.16"
  dificuldade="modelagem"
  opcoes={[
    { texto: "R² = 0,60; o modelo linear explica 60% da variação no retorno, portanto é muito confiável" },
    { texto: "Mesmo com R² = 0,60, o modelo pode sofrer overfitting se tiver muitos preditores; validação out-of-sample é necessária", correta: true },
    { texto: "Um R² de 0,60 em regressão múltipla indica que o modelo é inválido e deve ser descartado" },
    { texto: "O R² de 0,60 é suficiente para confirmar causalidade entre os preditores e o retorno" }
  ]}
  solucao={<>Um <Eq>{`R^2 = 0{,}60`}</Eq> indica que o modelo explica 60% da variação no retorno de filmes — razoável, mas não extraordinário. Com muitos preditores em amostra pequena, o modelo pode overfit. Validação cruzada out-of-sample (k-fold) fornece estimativa mais honesta do poder preditivo. Além disso, alto <Eq>{`R^2`}</Eq> não implica causalidade.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.R", exercicio: "ex. 9.22", licenca: "CC-BY-SA 3.0" }}
>

Um estudante analisou o retorno sobre investimento (ROI) de filmes usando regressão múltipla com gênero, orçamento e nota de críticos como preditores, obtendo $R^2 = 0{,}60$. Quais são as limitações de interpretar esse $R^2$ como medida definitiva de ajuste?

</Exercicio>

<Exercicio
  numero="106.17"
  dificuldade="desafio"
  opcoes={[
    { texto: "Backward elimination garante encontrar o modelo ótimo global com menor AIC" },
    { texto: "Backward elimination pode não encontrar o modelo ótimo global, pois é um algoritmo guloso que não reverte decisões anteriores", correta: true },
    { texto: "Forward selection sempre produz modelos com mais preditores que backward elimination" },
    { texto: "AIC e backward elimination sempre concordam sobre qual modelo é o melhor" }
  ]}
  solucao={<>Backward elimination e forward selection são algoritmos gulosos — tomam a melhor decisão local a cada passo sem garantir ótimo global. Podem produzir modelos diferentes e nenhum garante encontrar o menor AIC entre todos os subconjuntos possíveis. A busca exaustiva (best subset) garante o ótimo, mas é computacionalmente inviável com muitos preditores (<Eq>{`2^p`}</Eq> modelos).</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.R", exercicio: "ex. 9.23", licenca: "CC-BY-SA 3.0" }}
>

Um pesquisador aplicou backward elimination ao modelo de filtragem de spam e obteve um modelo final. Esse procedimento garante que o modelo encontrado tem o menor AIC possível entre todos os subconjuntos de preditores? Justifique.

</Exercicio>

<Exercicio
  numero="106.18"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Variável dependente é o número de horas de aluguel; variável independente é a taxa cobrada" },
    { texto: "Variável dependente é a taxa total cobrada; variável independente é o número de horas de aluguel", correta: true },
    { texto: "Ambas as variáveis são dependentes em um modelo de regressão mútua" },
    { texto: "A variável independente deve ser sempre qualitativa em modelos de regressão" }
  ]}
  solucao={<>Em uma relação de aluguel de equipamento, a taxa total cobrada depende do número de horas de aluguel. Portanto: <Eq>{`Y`}</Eq> (dependente) = taxa total cobrada; <Eq>{`X`}</Eq> (independente) = número de horas de aluguel. O modelo é <Eq>{`\\hat Y = \\hat\\beta_0 + \\hat\\beta_1 X`}</Eq>, onde <Eq>{`\\hat\\beta_0`}</Eq> é a taxa base e <Eq>{`\\hat\\beta_1`}</Eq> é o custo por hora.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 1", licenca: "CC-BY 4.0" }}
>

Em uma locadora de equipamentos, a taxa cobrada depende do número de horas de aluguel. Identifique a variável dependente e a variável independente nessa relação.

</Exercicio>

<Exercicio
  numero="106.19"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "A equação é Y = 5X + 20, onde X é o número de horas e a taxa base é R\$ 5" },
    { texto: "A equação é Y = 20 + 5X, onde Y é a taxa total, 20 é a taxa base (intercepto) e 5 é o custo por hora (inclinação)", correta: true },
    { texto: "A equação é Y = 5 + 20X, onde 5 é a taxa base e 20 é o custo por hora" },
    { texto: "A equação não pode ser expressa como função linear pois a taxa varia com o uso" }
  ]}
  solucao={<>Com taxa base de R\$ 20 e custo adicional de R\$ 5 por hora: <Eq>{`\\hat Y = 20 + 5X`}</Eq>, onde <Eq>{`\\hat\\beta_0 = 20`}</Eq> (taxa fixa de locação) e <Eq>{`\\hat\\beta_1 = 5`}</Eq> (custo por hora). Para 3 horas: <Eq>{`\\hat Y = 20 + 5\\times 3 = 35`}</Eq> reais. (Resp: <Eq>{`\\hat Y = 20 + 5X`}</Eq>)</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 2", licenca: "CC-BY 4.0" }}
>

Uma locadora cobra uma taxa base de R\$ 20 mais R\$ 5 por hora de aluguel. Encontre a equação linear que expressa a taxa total $Y$ em função do número de horas $X$.

</Exercicio>

<Exercicio
  numero="106.20"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "A equação Y = 10 + 5x - 3x² é linear porque o coeficiente de x é positivo" },
    { texto: "A equação Y = 10 + 5x - 3x² não é linear porque contém o termo 3x², que é de segundo grau em x", correta: true },
    { texto: "A equação é linear porque tem apenas duas variáveis: x e Y" },
    { texto: "A equação é linear porque os coeficientes 10, 5 e 3 são constantes" }
  ]}
  solucao={<>Um modelo de regressão é linear se for linear nos parâmetros e se cada preditor aparece com expoente 1. A equação <Eq>{`Y = 10 + 5x - 3x^2`}</Eq> não é linear porque contém <Eq>{`x^2`}</Eq> — o preditor <Eq>{`x`}</Eq> aparece com expoente 2. É um modelo de regressão polinomial (grau 2), não linear.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 6", licenca: "CC-BY 4.0" }}
>

A equação $Y = 10 + 5x - 3x^2$ é um modelo de regressão linear? Justifique.

</Exercicio>

<Exercicio
  numero="106.21"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Somente (a) e linear pois tem apenas termos de primeiro grau em x" },
    { texto: "(a) e (b) são lineares; (c) não é linear pois tem x²; (d) é linear pois pode ser escrito Y = 2", correta: true },
    { texto: "Todas as quatro equações são lineares pois têm apenas uma variável x" },
    { texto: "Nenhuma das equações é linear pois a variável Y também aparece no lado esquerdo" }
  ]}
  solucao={<>(a) <Eq>{`y = 6x + 8`}</Eq>: linear (<Eq>{`x`}</Eq> com expoente 1). (b) <Eq>{`y + 7 = 3x`}</Eq> <Eq>{`\\Rightarrow y = 3x - 7`}</Eq>: linear. (c) <Eq>{`y - x = 8x^2`}</Eq> <Eq>{`\\Rightarrow y = x + 8x^2`}</Eq>: não linear (tem <Eq>{`x^2`}</Eq>). (d) <Eq>{`4y = 8`}</Eq> <Eq>{`\\Rightarrow y = 2`}</Eq>: linear (reta horizontal, inclinação zero). (Resp: a, b, d)</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 7", licenca: "CC-BY 4.0" }}
>

Quais das seguintes equações são lineares? (a) $y = 6x + 8$; (b) $y + 7 = 3x$; (c) $y - x = 8x^2$; (d) $4y = 8$.

</Exercicio>

<Exercicio
  numero="106.22"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "O ano é a variável dependente porque ele determina o número de casos de gripe" },
    { texto: "O ano é a variável independente pois o número de casos é consequência do tempo, e não o contrário", correta: true },
    { texto: "Ambas as variáveis são independentes pois o tempo não causa gripe" },
    { texto: "O número de casos de gripe é a variável independente pois é medido diretamente" }
  ]}
  solucao={<>O ano é a variável independente (<Eq>{`X`}</Eq>) pois o número de casos de gripe (<Eq>{`Y`}</Eq>) varia ao longo do tempo — o tempo não é causado pelo número de casos. Em modelos de séries temporais, o tempo é tipicamente o preditor. Isso não implica causalidade direta: outras variáveis (sazonalidade, vacinação) também afetam <Eq>{`Y`}</Eq>.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 9", licenca: "CC-BY 4.0" }}
>

Em um banco de dados com colunas "Ano" e "Número de casos de gripe diagnosticados", por que o ano é a variável independente e o número de casos é a variável dependente?

</Exercicio>

<Exercicio
  numero="106.23"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "O intercepto é zero pois a reta passa pela origem quando x = 0" },
    { texto: "O intercepto é o valor predito de Y quando X = 0, e a inclinação é a mudança esperada em Y por unidade de X", correta: true },
    { texto: "A inclinação é o valor médio de Y em toda a amostra" },
    { texto: "O intercepto e a inclinação têm sempre o mesmo sinal" }
  ]}
  passos={<>
    <ol>
      <li>A equação de regressão simples é <Eq>{`\\hat Y = \\hat\\beta_0 + \\hat\\beta_1 X`}</Eq>.</li>
      <li>O intercepto <Eq>{`\\hat\\beta_0`}</Eq>: valor previsto de <Eq>{`Y`}</Eq> quando <Eq>{`X = 0`}</Eq>.</li>
      <li>A inclinação <Eq>{`\\hat\\beta_1`}</Eq>: mudança média em <Eq>{`Y`}</Eq> para cada unidade adicional de <Eq>{`X`}</Eq>.</li>
      <li>Em regressão múltipla, cada coeficiente é interpretado como efeito parcial (mantendo os demais fixos).</li>
    </ol>
  </>}
  solucao={<>Na equação de regressão <Eq>{`\\hat Y = \\hat\\beta_0 + \\hat\\beta_1 X`}</Eq>: o intercepto <Eq>{`\\hat\\beta_0`}</Eq> é o valor predito quando <Eq>{`X = 0`}</Eq>; a inclinação <Eq>{`\\hat\\beta_1`}</Eq> é a variação média esperada em <Eq>{`Y`}</Eq> por unidade de aumento em <Eq>{`X`}</Eq>.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 11", licenca: "CC-BY 4.0" }}
>

Interprete o intercepto e a inclinação de uma equação de regressão simples $\hat Y = \hat\beta_0 + \hat\beta_1 X$. Como essas interpretações se generalizam para a regressão múltipla?

</Exercicio>

<Exercicio
  numero="106.24"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Uma costa perde 5 toneladas de solo por ano, pois o intercepto é 5" },
    { texto: "A inclinação indica quantas toneladas de solo são perdidas por ano; o intercepto é a quantidade inicial de solo", correta: true },
    { texto: "O intercepto indica a taxa de erosão anual e a inclinação é a perda acumulada total" },
    { texto: "A quantidade de solo perdida não pode ser modelada por regressão linear pois é sempre positiva" }
  ]}
  solucao={<>Em um modelo de erosão costeira, se <Eq>{`Y`}</Eq> = toneladas de solo perdidas e <Eq>{`X`}</Eq> = anos: a inclinação <Eq>{`\\hat\\beta_1`}</Eq> é a perda anual média de solo (toneladas/ano) e o intercepto <Eq>{`\\hat\\beta_0`}</Eq> é a perda estimada no ano zero (início do período de observação).</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 13", licenca: "CC-BY 4.0" }}
>

Um modelo de regressão linear simples descreve a erosão de uma costa em toneladas de solo perdidas por ano. O que representam o intercepto e a inclinação nesse contexto?

</Exercicio>

<Exercicio
  numero="106.25"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Prefiro inclinação negativa pois significa que o preço da ação cai, reduzindo meu investimento" },
    { texto: "Prefiro inclinação positiva pois indica que o preço da ação tende a aumentar ao longo do tempo", correta: true },
    { texto: "A inclinação é irrelevante; o que importa é o intercepto (preço inicial)" },
    { texto: "Inclinação zero é a mais desejável pois indica estabilidade sem risco" }
  ]}
  solucao={<>Em um modelo de regressão onde <Eq>{`Y`}</Eq> é o preço de uma ação e <Eq>{`X`}</Eq> é o tempo, a inclinação positiva indica que o preço tende a aumentar com o tempo — desejável para quem detém a ação. Inclinação negativa indica tendência de queda; inclinação zero indica estagnação. Claro que o modelo linear é uma simplificação da dinâmica real dos mercados.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 16", licenca: "CC-BY 4.0" }}
>

No modelo de regressão do preço de uma ação em função do tempo, o investidor preferiria uma inclinação positiva ou negativa? Por quê?

</Exercicio>

<Exercicio
  numero="106.26"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "r = 0 indica correlação perfeita negativa entre as duas variáveis" },
    { texto: "r = 0 indica ausência de correlação linear entre as duas variáveis; pode ainda existir relação não-linear", correta: true },
    { texto: "r = 0 indica que Y não depende de nenhuma variável" },
    { texto: "r = 0 é impossível em amostras reais pois sempre há alguma correlação" }
  ]}
  solucao={<>O coeficiente de correlação de Pearson <Eq>{`r = 0`}</Eq> indica ausência de relação linear entre as variáveis. Não significa que não há relação — pode existir uma relação não-linear (quadrática, exponencial, etc.) que <Eq>{`r`}</Eq> não detecta. Em regressão múltipla, a correlação entre preditores (<em>multicolinearidade</em>) é distinta de <Eq>{`r`}</Eq> entre preditor e resposta.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 23", licenca: "CC-BY 4.0" }}
>

O que significa um valor de $r = 0$ para o coeficiente de correlação de Pearson? Isso implica que as variáveis são completamente independentes?

</Exercicio>

<Exercicio
  numero="106.27"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Com n = 2 e r = 1, a correlação é significativa pois r = 1 é o máximo possível" },
    { texto: "Com n = 2 e r = 1, os dados nunca são estatisticamente significativos; qualquer duas pontos definem uma reta perfeita", correta: true },
    { texto: "Com n = 2, o teste t tem df = 1 e o valor crítico é 12,7; como r = 1, T é infinito e rejeita-se H0" },
    { texto: "A significância depende apenas de r, não de n" }
  ]}
  solucao={<>Com <Eq>{`n = 2`}</Eq>, dois pontos sempre determinam uma reta perfeita, logo <Eq>{`r = 1`}</Eq> automaticamente — sem exceção. O teste de significância tem <Eq>{`df = n - 2 = 0`}</Eq> graus de liberdade, tornando o teste inválido. Com <Eq>{`n = 2`}</Eq>, a estatística <Eq>{`T`}</Eq> é indeterminada (divisão por zero). Portanto, <Eq>{`r = 1`}</Eq> com <Eq>{`n = 2`}</Eq> não é significativo no sentido estatístico.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 24", licenca: "CC-BY 4.0" }}
>

Quando $n = 2$ e $r = 1$, os dados são estatisticamente significativos? Explique.

</Exercicio>

<Exercicio
  numero="106.28"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Não há correlação significativa pois r = -0,89 é negativo" },
    { texto: "Há correlação significativa negativa pois com n = 100 e |r| = 0,89, o valor crítico é aproximadamente 0,197 < 0,89", correta: true },
    { texto: "Não há correlação significativa pois n = 100 é uma amostra muito grande para confiar em r" },
    { texto: "A correlação é significativa apenas se r > 0" }
  ]}
  solucao={<>Com <Eq>{`n = 100`}</Eq> e <Eq>{`r = -0{,}89`}</Eq>: <Eq>{`T = r\\sqrt{(n-2)/(1-r^2)} = -0{,}89\\sqrt{98/(1-0{,}7921)} = -0{,}89\\sqrt{98/0{,}2079} \\approx -0{,}89 \\times 21{,}7 \\approx -19{,}3`}</Eq>. Com <Eq>{`df = 98`}</Eq>, o valor crítico <Eq>{`t_{98;\\,0{,}025} \\approx 1{,}984`}</Eq>. Como <Eq>{`|T| = 19{,}3 \\gg 1{,}984`}</Eq>, a correlação é altamente significativa.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 25", licenca: "CC-BY 4.0" }}
>

Com $n = 100$ e $r = -0{,}89$, há correlação significativa entre as variáveis? Justifique usando a estatística $T$ do teste de significância. (Resp: sim, correlação altamente significativa)

</Exercicio>

<Exercicio
  numero="106.29"
  dificuldade="compreensao"
  opcoes={[
    { texto: "H0: r = 1 (correlação perfeita entre as variáveis)" },
    { texto: "H0: rho = 0 (ausência de correlação linear na população)", correta: true },
    { texto: "H0: beta_1 = 1 (inclinação unitária na reta de regressão)" },
    { texto: "H0: Y = X (as variáveis são iguais na população)" }
  ]}
  solucao={<>Ao testar a significância do coeficiente de correlação, a hipótese nula é <Eq>{`H_0: \\rho = 0`}</Eq>, onde <Eq>{`\\rho`}</Eq> (rho) é o coeficiente de correlação populacional. Rejeitá-la significa que há evidência de correlação linear entre as variáveis na população. A estatística de teste é <Eq>{`T = r\\sqrt{(n-2)/(1-r^2)} \\sim t_{n-2}`}</Eq> sob <Eq>{`H_0`}</Eq>.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 26", licenca: "CC-BY 4.0" }}
>

Ao testar a significância do coeficiente de correlação, qual é a hipótese nula $H_0$?

</Exercicio>

<Exercicio
  numero="106.30"
  dificuldade="compreensao"
  opcoes={[
    { texto: "H1: rho = 0 (correlação nula como alternativa)" },
    { texto: "H1: rho diferente de 0 (para teste bilateral) ou H1: rho > 0 / rho < 0 (para teste unilateral)", correta: true },
    { texto: "H1: r = 1 (correlação perfeita como alternativa)" },
    { texto: "H1: beta_1 diferente de 0 (inclinação diferente de zero)" }
  ]}
  solucao={<>A hipótese alternativa no teste do coeficiente de correlação é <Eq>{`H_1: \\rho \\neq 0`}</Eq> (bilateral) quando não se tem direção esperada. Se houver razão para esperar correlação positiva: <Eq>{`H_1: \\rho > 0`}</Eq>; correlação negativa: <Eq>{`H_1: \\rho < 0`}</Eq>. Em regressão múltipla, o teste <Eq>{`t`}</Eq> parcial de cada coeficiente tem hipótese alternativa <Eq>{`H_1: \\beta_j \\neq 0`}</Eq>.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 27", licenca: "CC-BY 4.0" }}
>

Ao testar a significância do coeficiente de correlação, qual é a hipótese alternativa $H_1$ para um teste bilateral?

</Exercicio>

<Exercicio
  numero="106.31"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Como p-valor = 0,04 > 0,01, não rejeitamos H0 ao nível 1%" },
    { texto: "Como p-valor = 0,04 < 0,05, rejeitamos H0 ao nível 5% e concluímos que há correlação linear significativa", correta: true },
    { texto: "Como p-valor = 0,04 > 0,05, não rejeitamos H0 ao nível 5% (erro de comparação)" },
    { texto: "O p-valor de 0,04 é inconclusivo pois está muito próximo de 0,05" }
  ]}
  solucao={<>Com nível de significância <Eq>{`\\alpha = 0{,}05`}</Eq> e p-valor <Eq>{`= 0{,}04`}</Eq>: como <Eq>{`0{,}04 < 0{,}05`}</Eq>, rejeitamos <Eq>{`H_0: \\rho = 0`}</Eq>. Há evidência estatística de correlação linear entre as variáveis ao nível 5%. (Nota: ao nível 1%, não rejeitaríamos pois <Eq>{`0{,}04 > 0{,}01`}</Eq>.)</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 28", licenca: "CC-BY 4.0" }}
>

No teste de significância do coeficiente de correlação com nível de significância $\alpha = 0{,}05$ e p-valor $= 0{,}04$, qual é a conclusão?

</Exercicio>

<Exercicio
  numero="106.32"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Vendas no dia 60: hat Y = 50 + 3 * 60 = 230 unidades" },
    { texto: "Vendas no dia 60: hat Y = 200 + 2 * 60 = 320 unidades (usando coeficiente correto)", correta: true },
    { texto: "Vendas no dia 60 não podem ser previstas pois o modelo é extrapolação" },
    { texto: "Vendas no dia 60: hat Y = 60 * 2 = 120 (ignora o intercepto)" }
  ]}
  solucao={<>Com o modelo <Eq>{`\\hat Y = 200 + 2X`}</Eq> (intercepto 200, inclinação 2): no dia 60, <Eq>{`\\hat Y = 200 + 2 \\times 60 = 200 + 120 = 320`}</Eq> unidades. (Resp: <Eq>{`\\hat Y = 320`}</Eq> unidades)</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 29", licenca: "CC-BY 4.0" }}
>

Um modelo de regressão das vendas diárias de um produto em função do dia de operação ($X$) é $\hat Y = 200 + 2X$. Qual seria a previsão de vendas no dia 60?

</Exercicio>

<Exercicio
  numero="106.33"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Vendas no dia 90: hat Y = 200 + 2 * 90 = 380 unidades", correta: true },
    { texto: "Vendas no dia 90: hat Y = 90 * 2 = 180 (ignora o intercepto)" },
    { texto: "Vendas no dia 90: hat Y = 200 + 90 = 290 (ignora a inclinação)" },
    { texto: "Vendas no dia 90 não podem ser previstas pois estão fora do intervalo de confiança" }
  ]}
  solucao={<>Usando <Eq>{`\\hat Y = 200 + 2X`}</Eq>: no dia 90, <Eq>{`\\hat Y = 200 + 2 \\times 90 = 200 + 180 = 380`}</Eq> unidades. Atenção: previsão fora do intervalo amostral é extrapolação e pode ter maior incerteza. (Resp: <Eq>{`\\hat Y = 380`}</Eq> unidades)</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 30", licenca: "CC-BY 4.0" }}
>

Usando o mesmo modelo $\hat Y = 200 + 2X$, qual é a previsão de vendas no dia 90?

</Exercicio>

<Exercicio
  numero="106.34"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Após 20 horas: hat Y = 100 - 2 * 20 = 60 acres restantes; o intercepto é a área inicial", correta: true },
    { texto: "Após 20 horas: hat Y = 100 + 2 * 20 = 140 acres (sinal errado)" },
    { texto: "Após 20 horas: hat Y = 2 * 20 = 40 acres (ignora o intercepto)" },
    { texto: "Após 20 horas não restam acres pois a tosa demora exatamente 100 horas" }
  ]}
  solucao={<>Com o modelo <Eq>{`\\hat Y = 100 - 2X`}</Eq> (área restante em acres, horas trabalhadas como preditor): após 20 horas, <Eq>{`\\hat Y = 100 - 2 \\times 20 = 100 - 40 = 60`}</Eq> acres restantes. O intercepto (100 acres) é a área total inicial. (Resp: 60 acres restantes)</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 31", licenca: "CC-BY 4.0" }}
>

O modelo da área restante a tosar (em acres) em função das horas de trabalho é $\hat Y = 100 - 2X$. Quantos acres restam após 20 horas de trabalho?

</Exercicio>

<Exercicio
  numero="106.35"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Após 100 horas: hat Y = 100 - 2 * 100 = -100 acres (extrapolação inválida)", correta: true },
    { texto: "Após 100 horas: hat Y = 0 acres pois o modelo linear sempre chega a zero" },
    { texto: "Após 100 horas: hat Y = 100 - 100 = 0 acres (erro de cálculo)" },
    { texto: "Após 100 horas: hat Y = 100 - 2 * 100 = -100 acres, o que indica que o modelo linear é válido além do domínio" }
  ]}
  solucao={<>Aplicando <Eq>{`\\hat Y = 100 - 2 \\times 100 = -100`}</Eq> acres — valor negativo sem sentido físico. Isso é extrapolação além do domínio válido. O modelo linear prediz que toda a área é tosada quando <Eq>{`\\hat Y = 0`}</Eq>: <Eq>{`0 = 100 - 2X \\Rightarrow X = 50`}</Eq> horas. Usar o modelo além de 50 horas é extrapolação inválida.</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 32", licenca: "CC-BY 4.0" }}
>

Usando $\hat Y = 100 - 2X$, quantos acres restam após 100 horas? O resultado faz sentido? O que esse resultado revela sobre os limites do modelo linear?

</Exercicio>

<Exercicio
  numero="106.36"
  dificuldade="aplicacao"
  opcoes={[
    { texto: "Toda a área é tosada quando X = 50 horas, pois hat Y = 100 - 2*50 = 0", correta: true },
    { texto: "Toda a área é tosada quando X = 100 horas, pois o intercepto é 100" },
    { texto: "Toda a área é tosada quando X = 200 horas, pois 2*200/100 = 4 ciclos" },
    { texto: "hat Y nunca chega a zero pois a função linear é estritamente positiva" }
  ]}
  solucao={<>Toda a área é tosada quando <Eq>{`\\hat Y = 0`}</Eq>: <Eq>{`0 = 100 - 2X \\Rightarrow 2X = 100 \\Rightarrow X = 50`}</Eq> horas. Portanto, o modelo prediz que toda a grama é tosada em 50 horas de trabalho. (Resp: <Eq>{`X = 50`}</Eq> horas)</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 33", licenca: "CC-BY 4.0" }}
>

Usando $\hat Y = 100 - 2X$, em quantas horas toda a área é tosada (ou seja, quando $\hat Y = 0$)?

</Exercicio>

<Exercicio
  numero="106.37"
  dificuldade="compreensao"
  opcoes={[
    { texto: "O R² sempre indica causalidade entre X e Y quando maior que 0,80" },
    { texto: "O R² mede a proporção da variância de Y explicada pelo modelo, mas não implica causalidade", correta: true },
    { texto: "R² alto garante que as previsões do modelo são precisas fora da amostra" },
    { texto: "R² e coeficiente de correlação r medem a mesma coisa em qualquer modelo" }
  ]}
  solucao={<>O <Eq>{`R^2`}</Eq> mede a proporção da variação total de <Eq>{`Y`}</Eq> que é explicada pelo modelo. Em regressão simples, <Eq>{`R^2 = r^2`}</Eq>. Um <Eq>{`R^2`}</Eq> alto indica bom ajuste linear, mas não implica causalidade — correlação não é causalidade. Além disso, <Eq>{`R^2`}</Eq> alto pode ser enganoso com overfitting (muitos preditores para poucos dados).</>}
  fonte={{ livro: "OpenStax Introductory Statistics", url: "https://openstax.org/books/statistics/pages/12-practice", secao: "§12.P", exercicio: "ex. 21", licenca: "CC-BY 4.0" }}
>

O que o coeficiente de determinação $R^2$ mede em um modelo de regressão? Um $R^2$ alto implica que X causa Y?

</Exercicio>

<Exercicio
  numero="106.38"
  dificuldade="desafio"
  opcoes={[
    { texto: "No modelo simples, hat beta_1 de fumar pode ser mais negativo pois absorve o confundimento positivo com paridade" },
    { texto: "No modelo simples, hat beta_1 de fumar pode diferir do modelo múltiplo por confundimento de paridade — o sinal e magnitude mudam ao controlar paridade", correta: true },
    { texto: "Os coeficientes de fumar são idênticos em ambos os modelos pois OLS é linear" },
    { texto: "A adição de paridade só muda o intercepto, nunca o coeficiente de fumar" }
  ]}
  passos={<>
    <ol>
      <li>Modelo simples: <Eq>{`\\hat Y = \\hat\\beta_0^{(s)} + \\hat\\beta_1^{(s)} \\cdot \\text{fumar}`}</Eq>.</li>
      <li>Modelo múltiplo: <Eq>{`\\hat Y = \\hat\\beta_0^{(m)} + \\hat\\beta_1^{(m)} \\cdot \\text{fumar} + \\hat\\beta_2^{(m)} \\cdot \\text{paridade}`}</Eq>.</li>
      <li>Se fumar e paridade são correlacionados, <Eq>{`\\hat\\beta_1^{(s)} \\neq \\hat\\beta_1^{(m)}`}</Eq>.</li>
      <li>O efeito de confundimento: no modelo simples, <Eq>{`\\hat\\beta_1^{(s)}`}</Eq> captura tanto o efeito de fumar quanto a correlação com paridade.</li>
      <li>O modelo múltiplo isola o efeito parcial de fumar, removendo o confundimento de paridade.</li>
    </ol>
  </>}
  solucao={<>Quando dois preditores são correlacionados, os coeficientes de regressão simples e múltipla diferem — o coeficiente simples captura o efeito total (direto + via confundidor), enquanto o múltiplo isola apenas o efeito parcial. Esse fenômeno é central em epidemiologia e ciências sociais.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", exercicio: "ex. 9.3", licenca: "CC-BY-SA 3.0" }}
>

Compare os coeficientes de fumar no modelo simples (só fumar como preditor) e no modelo múltiplo (fumar + paridade) para peso de bebês. Por que podem diferir? Qual efeito cada um mede?

</Exercicio>

<Exercicio
  numero="106.39"
  dificuldade="desafio"
  opcoes={[
    { texto: "F = SSR/SSE independe dos graus de liberdade; valor crítico é sempre 3,84" },
    { texto: "F = (SSR/p) / (SSE/(n-p-1)); sob H0 segue distribuição F com p e n-p-1 graus de liberdade", correta: true },
    { texto: "F = SST/SSE; quanto mais próximo de 1, melhor o modelo" },
    { texto: "O teste F global testa individualmente cada coeficiente beta_j = 0" }
  ]}
  passos={<>
    <ol>
      <li>Decomposição: <Eq>{`\\mathrm{SST} = \\mathrm{SSR} + \\mathrm{SSE}`}</Eq>.</li>
      <li>Médias quadráticas: <Eq>{`\\mathrm{MSR} = \\mathrm{SSR}/p`}</Eq>, <Eq>{`\\mathrm{MSE} = \\mathrm{SSE}/(n-p-1)`}</Eq>.</li>
      <li>Estatística F: <Eq>{`F = \\mathrm{MSR}/\\mathrm{MSE}`}</Eq>.</li>
      <li>Sob <Eq>{`H_0: \\beta_1 = \\cdots = \\beta_p = 0`}</Eq>: <Eq>{`F \\sim F_{p,\\,n-p-1}`}</Eq>.</li>
      <li>Rejeitar <Eq>{`H_0`}</Eq> se <Eq>{`F > F_{p,\\,n-p-1;\\,\\alpha}`}</Eq>.</li>
    </ol>
  </>}
  solucao={<>O teste F global testa <Eq>{`H_0: \\beta_1 = \\beta_2 = \\cdots = \\beta_p = 0`}</Eq> (nenhum preditor é útil). A estatística <Eq>{`F = (\\mathrm{SSR}/p)/(\\mathrm{SSE}/(n-p-1))`}</Eq> segue distribuição F com <Eq>{`p`}</Eq> e <Eq>{`n-p-1`}</Eq> graus de liberdade sob <Eq>{`H_0`}</Eq>. Rejeitar <Eq>{`H_0`}</Eq> indica que pelo menos um preditor explica variação em <Eq>{`Y`}</Eq>.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", exercicio: "ex. 9.4", licenca: "CC-BY-SA 3.0" }}
>

Descreva a lógica e a fórmula do teste F global em regressão múltipla. Qual hipótese nula ele testa e qual distribuição usa para calcular o p-valor?

</Exercicio>

<Exercicio
  numero="106.40"
  dificuldade="demonstracao"
  opcoes={[
    { texto: "R² = SSR/SST = 1 - SSE/SST; é a proporção da variância de Y explicada pelo modelo", correta: true },
    { texto: "R² = SSE/SST; quanto maior o SSE, maior o R²" },
    { texto: "R² = 1 + SSE/SST; o R² pode ser maior que 1 em modelos com muitos preditores" },
    { texto: "R² = SSR/SSE; a razão entre variância explicada e residual" }
  ]}
  solucao={<>A decomposição ANOVA é <Eq>{`\\mathrm{SST} = \\mathrm{SSR} + \\mathrm{SSE}`}</Eq>. Dividindo ambos os lados por <Eq>{`\\mathrm{SST}`}</Eq>: <Eq>{`1 = \\mathrm{SSR}/\\mathrm{SST} + \\mathrm{SSE}/\\mathrm{SST}`}</Eq>. Portanto <Eq>{`R^2 = \\mathrm{SSR}/\\mathrm{SST} = 1 - \\mathrm{SSE}/\\mathrm{SST}`}</Eq>. Como <Eq>{`\\mathrm{SSR} \\geq 0`}</Eq> e <Eq>{`\\mathrm{SSE} \\leq \\mathrm{SST}`}</Eq>, temos <Eq>{`0 \\leq R^2 \\leq 1`}</Eq>. <Eq>{`R^2`}</Eq> mede a fração da variabilidade total de <Eq>{`Y`}</Eq> explicada pelos preditores.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.1", exercicio: "ex. 9.1", licenca: "CC-BY-SA 3.0" }}
>

A partir da decomposição $\mathrm{SST} = \mathrm{SSR} + \mathrm{SSE}$, derive a fórmula do $R^2$ e mostre que $0 \leq R^2 \leq 1$. O que $R^2$ mede geometricamente?

</Exercicio>

<Exercicio
  numero="106.41"
  dificuldade="desafio"
  opcoes={[
    { texto: "VIF > 10 indica que o preditor deve ser removido imediatamente e sem exceção" },
    { texto: "VIF > 10 sinaliza multicolinearidade severa; ações possíveis incluem remover o preditor, combinar preditores ou usar ridge regression", correta: true },
    { texto: "VIF > 10 indica apenas que o preditor tem inclinação maior que 10 no modelo" },
    { texto: "VIF não pode ser maior que 10 em amostras com n > 100" }
  ]}
  passos={<>
    <ol>
      <li>Calcular <Eq>{`\\mathrm{VIF}_j = 1/(1-R_j^2)`}</Eq> para cada preditor, onde <Eq>{`R_j^2`}</Eq> é o <Eq>{`R^2`}</Eq> da regressão de <Eq>{`X_j`}</Eq> sobre os demais.</li>
      <li>Se <Eq>{`\\mathrm{VIF}_j > 10`}</Eq>: <Eq>{`X_j`}</Eq> tem multicolinearidade severa com os demais preditores.</li>
      <li>Opções: (a) remover o preditor mais colinear; (b) combinar preditores correlacionados via PCA; (c) usar ridge regression (<Eq>{`L_2`}</Eq>) para estabilizar os estimadores.</li>
      <li>Recalcular VIFs após cada ação e verificar se melhorou.</li>
    </ol>
  </>}
  solucao={<><Eq>{`\\mathrm{VIF} > 10`}</Eq> indica que a variância do estimador <Eq>{`\\hat\\beta_j`}</Eq> é mais de 10 vezes maior do que seria sem colinearidade. Ações: remover o preditor colinear, combinar preditores via ACP, ou aplicar ridge regression. A escolha depende do objetivo (previsão vs. interpretação).</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.2", exercicio: "ex. 9.8", licenca: "CC-BY-SA 3.0" }}
>

Um preditor em um modelo de regressão múltipla tem $\mathrm{VIF} = 15$. O que esse valor indica e quais ações o analista pode tomar para remediar o problema?

</Exercicio>

<Exercicio
  numero="106.42"
  dificuldade="desafio"
  opcoes={[
    { texto: "A validação cruzada reduz o R² do modelo, tornando-o menos preciso" },
    { texto: "A validação cruzada estima o erro de previsão out-of-sample, detectando overfitting que o R² in-sample não revela", correta: true },
    { texto: "A validação cruzada só é útil quando n < 30" },
    { texto: "Modelos com R² = 1 nunca precisam de validação cruzada pois têm ajuste perfeito" }
  ]}
  solucao={<>Em modelos com muitos preditores relativos a <Eq>{`n`}</Eq>, o <Eq>{`R^2`}</Eq> in-sample superestima o ajuste por overfitting. A validação cruzada (k-fold ou leave-one-out) divide os dados em treino e teste, estimando o erro de previsão em dados não usados no ajuste. Um <Eq>{`R^2`}</Eq> in-sample alto com RMSE de validação cruzada alto indica overfitting severo. O <Eq>{`\\bar R^2`}</Eq> e o AIC penalizam a complexidade, mas apenas a VC estima diretamente o erro preditivo.</>}
  fonte={{ livro: "OpenIntro Statistics", url: "https://www.openintro.org/book/os/", secao: "§9.3", exercicio: "ex. 9.23", licenca: "CC-BY-SA 3.0" }}
>

Por que a validação cruzada fornece uma estimativa mais honesta do poder preditivo de um modelo de regressão múltipla do que o $R^2$ calculado nos próprios dados de ajuste?

</Exercicio>

</ListaExercicios>

## Fontes

- [**OpenIntro Statistics** (4ª ed.)](https://www.openintro.org/book/os/) — Diez, Çetinkaya-Rundel, Barr · CC-BY-SA · Capítulo 8 (Multiple and logistic regression). Fonte primária para interpretação de coeficientes, $\bar R^2$, multicolinearidade e variáveis dummy.
- [**Statistics** — OpenStax](https://openstax.org/details/books/statistics) — Illowsky, Dean · CC-BY · Capítulo 13 (Linear Regression and Correlation — Multiple). Fonte para tabelas ANOVA de regressão múltipla e teste F global.
- [**Probabilidade e Estatística** — Wikilivros](https://pt.wikibooks.org/wiki/Probabilidade_e_Estat%C3%ADstica) — colaborativo · CC-BY-SA · Seção de regressão múltipla. Referência em PT-BR com notação matricial.
