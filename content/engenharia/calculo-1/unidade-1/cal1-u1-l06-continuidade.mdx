---
titulo: "Lição 6 — Continuidade: definição e descontinuidades"
slug: "cal1-u1-l06-continuidade"
categoria: "calculo-1"
subcategoria: "unidade-1"
descricao: "Continuidade em um ponto via três condições. Classificação das descontinuidades (removível, salto, essencial). Continuidade em intervalos. Álgebra e composição de funções contínuas."
ordem: 6
publicado: true
tags: ["calculo-1", "continuidade", "descontinuidade", "limites", "engenharia"]
prerrequisitos:
  - "Lição 2 — Limite de função: definição ε-δ"
  - "Lição 3 — Técnicas algébricas de cálculo de limites"
autores: ["Clube da Matemática"]
atualizadoEm: "2026-05-20"
usadoEm: ["Cálculo 1 — Unidade 1", "USP MAC0105", "ITA MA-011"]
versao: "v1"
---

<EquacaoCanonica
  formula="f \text{ contínua em } a \;\iff\; \lim_{x \to a} f(x) = f(a)"
  legenda={<>A continuidade em <Eq>{`a`}</Eq> exige três coisas simultâneas: <Eq>{`f(a)`}</Eq> existe, o limite <Eq>{`\\lim_{x\\to a}f(x)`}</Eq> existe e os dois são iguais. Qualquer uma dessas três falhar gera uma descontinuidade, com classificação própria.</>}
  audioTexto="f é contínua em a se, e somente se, o limite de f de x quando x tende a a é igual a f de a."
/>

<DuasPortas>

<Porta nivel="formal" titulo="Definição rigorosa">

### Continuidade: a definição precisa

<Definicao titulo="Continuidade em um ponto (Cauchy-Weierstrass)">

$f$ é **contínua em** $a$ se as três condições abaixo são satisfeitas simultaneamente:

1. $f(a)$ está definida ($a \in \text{Dom}(f)$).
2. $\displaystyle\lim_{x \to a} f(x)$ existe (finito).
3. $\displaystyle\lim_{x \to a} f(x) = f(a)$.

Em linguagem ε-δ: $\forall\,\varepsilon > 0,\;\exists\,\delta > 0: |x - a| < \delta \implies |f(x) - f(a)| < \varepsilon$.

</Definicao>

<Definicao titulo="Tipos de descontinuidade">

Se $f$ não é contínua em $a$, a descontinuidade é classificada em:

| Tipo | Condição |
|------|----------|
| **Removível** | $\lim_{x\to a}f(x)$ existe, mas $\neq f(a)$ (ou $f(a)$ indefinido) |
| **Salto (1ª espécie)** | Limites laterais existem mas $\lim_{x\to a^-}f(x) \neq \lim_{x\to a^+}f(x)$ |
| **Essencial (2ª espécie)** | Pelo menos um limite lateral não existe ou é infinito |

</Definicao>

<Definicao titulo="Continuidade lateral e em intervalos">

- $f$ é **contínua à direita** em $a$ se $\lim_{x\to a^+}f(x) = f(a)$.
- $f$ é **contínua à esquerda** em $a$ se $\lim_{x\to a^-}f(x) = f(a)$.
- $f$ é **contínua em** $[a,b]$ se é contínua em cada $x \in (a,b)$, contínua à direita em $a$ e contínua à esquerda em $b$.

</Definicao>

<Teorema titulo="Álgebra da continuidade">

Se $f$ e $g$ são contínuas em $a$, então também são contínuas em $a$:

$$f + g,\quad f - g,\quad f \cdot g,\quad \frac{f}{g}\;(g(a) \neq 0),\quad |f|.$$

</Teorema>

<Teorema titulo="Continuidade da composição">

Se $g$ é contínua em $a$ e $f$ é contínua em $g(a)$, então $f \circ g$ é contínua em $a$:

$$\lim_{x \to a} f(g(x)) = f\!\left(\lim_{x \to a} g(x)\right) = f(g(a)).$$

</Teorema>

<Teorema titulo="Funções elementares são contínuas">

Polinômios, funções racionais (no domínio), $\sqrt[n]{\cdot}$, $\sin$, $\cos$, $e^x$, $\ln x$ e todas as suas composições são contínuas em todo ponto do domínio.

</Teorema>

<Cuidado>

A função de Dirichlet $f(x) = \mathbf{1}_{\mathbb{Q}}(x)$ (1 em racionais, 0 em irracionais) é descontínua em **todo** ponto de $\mathbb{R}$. Ela mostra que "limite em todo ponto" não implica continuidade sem que o valor seja correto.

</Cuidado>

</Porta>

<Porta nivel="5anos" titulo="Pra criança de 5 anos">

Imagina que você está andando de skate numa rampa. Se a rampa for suave, sem buracos nem degraus, você anda sem travar — isso é continuidade. Se tiver um buraco, você cai de repente — isso é uma descontinuidade!

</Porta>

<Porta nivel="10anos" titulo="Pra quem tem 10 anos">

Uma função é como um caminho num mapa. Se você consegue desenhar o caminho sem tirar o lápis do papel, a função é contínua. Se precisar pular de um ponto para outro, tem uma descontinuidade.

Tem três tipos de "pulos":
- **Buraco preenchível**: o caminho quase chega num ponto, mas alguém colocou o ponto no lugar errado.
- **Degrau**: o caminho vem de baixo de um lado e de cima do outro.
- **Caos total**: o caminho vai para o infinito ou oscila loucamente.

</Porta>

<Porta nivel="15anos" titulo="Pra quem tem 15 anos">

### O que é continuidade?

Uma função $f$ é contínua em $x = a$ quando três coisas acontecem ao mesmo tempo:

1. $f(a)$ existe.
2. $\lim_{x \to a} f(x)$ existe.
3. Os dois são iguais: $\lim_{x \to a} f(x) = f(a)$.

**Tipos de descontinuidade:**

- **Removível**: o limite existe, mas $f(a)$ é diferente ou indefinido. Basta "consertar" o ponto.
  - Exemplo: $f(x) = \dfrac{x^2 - 1}{x - 1}$ em $x = 1$.

- **Salto**: os limites laterais existem mas são diferentes.
  - Exemplo: $f(x) = \begin{cases} 0 & x < 0 \\ 1 & x \geq 0 \end{cases}$ em $x = 0$.

- **Essencial**: o limite não existe de nenhum lado (ou é $\pm\infty$).
  - Exemplo: $f(x) = \sin(1/x)$ em $x = 0$.

</Porta>

<Porta nivel="25anos" titulo="Pra quem tem 25 anos">

### Continuidade com rigor

**Definição ε-δ:** $f$ é contínua em $a$ se

$$\forall\,\varepsilon > 0,\;\exists\,\delta > 0: |x - a| < \delta \implies |f(x) - f(a)| < \varepsilon.$$

Note que — ao contrário da definição de limite — aqui $x = a$ é **permitido** (e obrigatoriamente dá $|f(a)-f(a)| = 0 < \varepsilon$).

**Por que a álgebra funciona?** Se $|f(x)-f(a)| < \varepsilon/2$ e $|g(x)-g(a)| < \varepsilon/2$ para $|x-a|<\delta$, então por desigualdade triangular $|(f+g)(x)-(f+g)(a)| < \varepsilon$. O quociente exige $g(a) \neq 0$ para usar a estimativa $|1/g(x) - 1/g(a)| \leq |g(x)-g(a)|/(|g(a)|/2)^2$ quando $|g(x)| > |g(a)|/2$.

**Composição:** A prova clássica é: dado $\varepsilon > 0$, pela continuidade de $f$ em $g(a)$ existe $\eta > 0$ tal que $|u - g(a)| < \eta \implies |f(u) - f(g(a))| < \varepsilon$. Pela continuidade de $g$ em $a$, existe $\delta > 0$ tal que $|x-a| < \delta \implies |g(x)-g(a)| < \eta$. Combinando: $|x-a|<\delta \implies |f(g(x))-f(g(a))|<\varepsilon$.

**Teorema:** Toda função contínua em $[a,b]$ é uniformemente contínua em $[a,b]$ (Teorema de Heine-Cantor). A recíproca é falsa em domínios não compactos.

</Porta>

<Porta nivel="40anos" titulo="Pra quem tem 40 anos (engenharia)">

### Continuidade na prática da engenharia

Em engenharia, a continuidade aparece como requisito de **regularidade do modelo**:

**Circuitos elétricos:** A tensão $V(t)$ em um capacitor é contínua (pois $V = Q/C$ e a carga não pode saltar instantaneamente). A corrente $I(t)$ pode ter descontinuidades de salto (chaveamento).

**Análise de sinais:** Um sinal $x(t)$ com descontinuidade de salto em $t_0$ tem espectro que decai como $1/\omega$ — lento. Sinais contínuos com derivada contínua têm espectro decaindo como $1/\omega^2$ — muito mais compacto em frequência.

**Elementos finitos (MEF):** A escolha entre funções de forma $C^0$ (contínuas, como elementos lineares) e $C^1$ (com derivada contínua, como Hermite) determina a precisão na aproximação de tensões e campos.

**Critério prático:** Se o modelo prediz uma descontinuidade de salto num fenômeno físico real, isso geralmente indica uma idealização. O fenômeno real tem uma transição contínua (possivelmente muito rápida), e a descontinuidade é apenas uma aproximação conveniente.

</Porta>

<Porta nivel="pratica" titulo="Prática guiada">

### Identificando e classificando descontinuidades

**Estratégia:**

1. Encontre os pontos onde $f$ pode ser descontínua (denominadores zero, raízes de expressões negativas, pontos de definição por partes).
2. Para cada ponto suspeito $a$, calcule $\lim_{x\to a^-}f(x)$ e $\lim_{x\to a^+}f(x)$.
3. Compare com $f(a)$ (se existir).
4. Classifique: removível, salto ou essencial.

**Exemplo:** $f(x) = \dfrac{x^2 - 4}{x - 2}$ em $x = 2$.

- $f(2)$ é indefinido (denominador zero).
- $\lim_{x\to 2}\dfrac{x^2-4}{x-2} = \lim_{x\to 2}(x+2) = 4$.
- Limite existe, $f(2)$ não existe → **descontinuidade removível** em $x=2$.
- Conserto: redefina $f(2) = 4$.

**Exemplo:** $g(x) = \begin{cases} x+1 & x < 1 \\ 3 & x = 1 \\ 2x & x > 1 \end{cases}$

- $\lim_{x\to 1^-}g(x) = 2$, $\lim_{x\to 1^+}g(x) = 2$, $g(1) = 3$.
- Limite bilateral = 2 ≠ 3 = g(1) → **descontinuidade removível** em $x=1$.

</Porta>

</DuasPortas>

## Exemplos resolvidos

<Exemplo titulo="Ex. 1 — Verificação das três condições" numero={1} fonte="Stewart §2.5, Ex. 1–3; Guidorizzi §2.4">

Verifique a continuidade de $f(x) = \dfrac{x^2 - 9}{x - 3}$ em $x = 3$ e em $x = 0$.

**Em $x = 3$:**
- $f(3) = \dfrac{9-9}{3-3} = \dfrac{0}{0}$ — indefinido.
- $\lim_{x\to 3}\dfrac{x^2-9}{x-3} = \lim_{x\to 3}(x+3) = 6$.
- Como $f(3)$ não existe, $f$ é **descontínua em $x = 3$** — descontinuidade **removível** (basta definir $f(3) = 6$).

**Em $x = 0$:**
- $f(0) = \dfrac{-9}{-3} = 3$ — existe.
- $\lim_{x\to 0}\dfrac{x^2-9}{x-3} = \dfrac{-9}{-3} = 3$ — existe e é finito.
- $\lim = f(0) = 3$ — **contínua em $x = 0$**.

</Exemplo>

<Exemplo titulo="Ex. 2 — Função definida por partes" numero={2} fonte="Stewart §2.5, Ex. 5; Active Calculus §1.7">

Para que valor de $c$ é $f$ contínua em $x = 2$?

$$f(x) = \begin{cases} cx^2 + 1 & x \leq 2 \\ x + 5 & x > 2 \end{cases}$$

**Continuidade à esquerda:** $\lim_{x\to 2^-}f(x) = c\cdot 4 + 1 = 4c + 1$.

**Continuidade à direita:** $\lim_{x\to 2^+}f(x) = 2 + 5 = 7$.

**Valor:** $f(2) = 4c + 1$.

Para continuidade: $4c + 1 = 7 \implies c = \dfrac{3}{2}$.

**Verificação:** $f(2) = 4\cdot\tfrac{3}{2} + 1 = 7$ e $\lim_{x\to 2^+}f(x) = 7$. $\checkmark$

</Exemplo>

<Exemplo titulo="Ex. 3 — Descontinuidade essencial" numero={3} fonte="Guidorizzi §2.4, Ex. 3; Apostol I §3.1">

Mostre que $f(x) = \sin\!\left(\dfrac{1}{x}\right)$ é descontínua em $x = 0$.

**$f(0)$:** indefinido (divisão por zero).

**$\lim_{x\to 0}f(x)$:** Tome $x_n = \dfrac{1}{n\pi}$, então $f(x_n) = \sin(n\pi) = 0$. Tome $y_n = \dfrac{1}{(n+1/2)\pi}$, então $f(y_n) = \sin\!\big((n+\tfrac{1}{2})\pi\big) = \pm 1$.

As duas subsequências $x_n, y_n \to 0$ mas $f(x_n) \to 0 \neq \pm 1 \leftarrow f(y_n)$.

Logo o limite não existe → descontinuidade **essencial** em $x = 0$.

</Exemplo>

<Exemplo titulo="Ex. 4 — Continuidade de composição" numero={4} fonte="Stewart §2.5, Ex. 10; OpenStax Calc I §2.4">

Mostre que $h(x) = \cos(x^2 - 1)$ é contínua em todo $\mathbb{R}$.

Escreva $h = f \circ g$ com $g(x) = x^2 - 1$ e $f(u) = \cos u$.

- $g$ é polinômio → contínua em todo $\mathbb{R}$.
- $f$ é cosseno → contínua em todo $\mathbb{R}$.

Pelo Teorema da Composição: $h = f \circ g$ é contínua em todo $\mathbb{R}$.

</Exemplo>

<Exemplo titulo="Ex. 5 — Prova ε-δ de continuidade" numero={5} fonte="Guidorizzi §2.4, Ex. 5; Apostol I §3.2">

Prove que $f(x) = 3x + 1$ é contínua em $x = 2$.

Dado $\varepsilon > 0$, precisamos $|f(x) - f(2)| < \varepsilon$.

$$|f(x) - f(2)| = |3x + 1 - 7| = |3x - 6| = 3|x - 2|.$$

Escolha $\delta = \varepsilon/3$. Se $|x - 2| < \delta$, então:

$$|f(x) - f(2)| = 3|x - 2| < 3\cdot\dfrac{\varepsilon}{3} = \varepsilon. \quad \blacksquare$$

</Exemplo>

---

## Exercícios

### Bloco A — Verificação e classificação

<Exercicio numero={1} fonte="Stewart §2.5, Ex. 3–10">

Determine os pontos de descontinuidade de cada função e classifique (removível, salto ou essencial).

**(a)** $f(x) = \dfrac{x^2 - x - 2}{x - 2}$

**(b)** $g(x) = \begin{cases} x + 1 & x < 1 \\ 2 & x = 1 \\ x^2 & x > 1 \end{cases}$

**(c)** $h(x) = \dfrac{1}{x^2 - 4}$

**(d)** $p(x) = \cos\!\left(\dfrac{\pi}{x}\right)$

</Exercicio>

<Exercicio numero={2} fonte="Guidorizzi §2.4, Exercício 1">

Para cada função, encontre o valor de $k$ que torna $f$ contínua em todo $\mathbb{R}$.

**(a)** $f(x) = \begin{cases} kx^2 & x \leq 1 \\ 2x + 1 & x > 1 \end{cases}$

**(b)** $f(x) = \begin{cases} \dfrac{\sin(3x)}{x} & x \neq 0 \\ k & x = 0 \end{cases}$

**(c)** $f(x) = \begin{cases} \dfrac{x^2 - k^2}{x - k} & x \neq k \\ 4 & x = k \end{cases}$

</Exercicio>

<Exercicio numero={3} fonte="OpenStax Calc I §2.4, Ex. 140–145">

Determine os intervalos de continuidade de cada função.

**(a)** $f(x) = \dfrac{x+1}{x^2 - 3x + 2}$

**(b)** $g(x) = \sqrt{4 - x^2}$

**(c)** $h(x) = \ln(x^2 - 1)$

**(d)** $p(x) = \dfrac{\sin x}{x^2 + 1}$

</Exercicio>

<Exercicio numero={4} fonte="Active Calculus §1.7, Preview Activity 1.7.2">

Esboce o gráfico de uma função que:

**(a)** Tem descontinuidade removível em $x = -1$ e descontinuidade de salto em $x = 2$.

**(b)** É contínua em todo $\mathbb{R}$ exceto em $x = 0$, onde tem descontinuidade essencial.

**(c)** É contínua à direita mas não à esquerda em $x = 3$.

</Exercicio>

<Exercicio numero={5} fonte="Stewart §2.5, Ex. 35–40">

Usando o Teorema da Álgebra da Continuidade, justifique que as seguintes funções são contínuas em seus domínios.

**(a)** $f(x) = \dfrac{x^3 - 2x + 1}{x^2 + 4}$

**(b)** $g(x) = \sqrt{x^2 - 2x + 2}$

**(c)** $h(x) = e^{\sin x} \cdot \cos(x^2)$

</Exercicio>

### Bloco B — Cálculo e análise

<Exercicio numero={6} fonte="Guidorizzi §2.4, Exercício 3">

Prove, usando a definição ε-δ, que as seguintes funções são contínuas no ponto indicado.

**(a)** $f(x) = x^2$ em $x = 3$.

**(b)** $f(x) = \sqrt{x}$ em $x = 4$. *(Dica: rationalize $|\sqrt{x} - 2| = \frac{|x-4|}{\sqrt{x}+2}$.)*

**(c)** $f(x) = \dfrac{1}{x}$ em $x = 2$.

</Exercicio>

<Exercicio numero={7} fonte="Apostol I §3.2, Ex. 1–4">

Seja $f: \mathbb{R} \to \mathbb{R}$ contínua em todo $\mathbb{R}$, com $f(0) = 0$.

**(a)** Mostre que $g(x) = f(x) \cdot \sin x$ é contínua em $x = 0$.

**(b)** Se $f(a) > 0$ para algum $a \in \mathbb{R}$, prove que existe $\delta > 0$ tal que $f(x) > 0$ para todo $x \in (a-\delta, a+\delta)$.

</Exercicio>

<Exercicio numero={8} fonte="Stewart §2.5, Ex. 43–52">

Determine se cada função pode ser estendida a uma função contínua no ponto indicado. Se sim, defina a extensão.

**(a)** $f(x) = \dfrac{x^3 - 8}{x - 2}$ em $x = 2$.

**(b)** $g(x) = \dfrac{1 - \cos x}{x^2}$ em $x = 0$.

**(c)** $h(x) = x \sin\!\left(\dfrac{1}{x}\right)$ em $x = 0$.

</Exercicio>

<Exercicio numero={9} fonte="REAMAT cap. 2, Ex. 2.15–2.18">

Analise a continuidade de $f$ e classifique cada descontinuidade.

$$f(x) = \begin{cases} \dfrac{|x|}{x} & x \neq 0 \\ 0 & x = 0 \end{cases}$$

**(a)** Calcule $\lim_{x\to 0^-}f(x)$ e $\lim_{x\to 0^+}f(x)$.

**(b)** Classifique a descontinuidade em $x = 0$.

**(c)** $f$ é contínua em algum subconjunto de $\mathbb{R}$? Qual?

</Exercicio>

<Exercicio numero={10} fonte="ITA MA-011; Guidorizzi §2.4, Ex. 6">

Seja $f(x) = \lfloor x \rfloor$ (parte inteira de $x$).

**(a)** Prove que $f$ é contínua à direita em todo $n \in \mathbb{Z}$.

**(b)** Prove que $f$ tem descontinuidade de salto em todo $n \in \mathbb{Z}$.

**(c)** Em que pontos $f$ é contínua?

</Exercicio>

### Bloco C — Demonstrações e aplicações

<Exercicio numero={11} fonte="Apostol I §3.3–3.4">

**Teorema.** Se $f$ é contínua em $[a,b]$ e $f(a) \cdot f(b) < 0$, então existe $c \in (a,b)$ com $f(c) = 0$.

Sem usar o TVI (que será provado na Lição 7), demonstre esse resultado para o caso especial $f(x) = x^2 - 2$ em $[1, 2]$ usando apenas a definição ε-δ de continuidade e o axioma da completude de $\mathbb{R}$.

</Exercicio>

<Exercicio numero={12} fonte="Stewart §2.5, Ex. 55; Active Calculus §1.7, Ex. 4">

Uma empresa modela o custo de produção por

$$C(q) = \begin{cases} 100 + 5q & 0 \leq q \leq 50 \\ 350 + 3q & q > 50 \end{cases}$$

**(a)** $C$ é contínua em $q = 50$? Justifique.

**(b)** Se não, qual tipo de descontinuidade ocorre e qual é a interpretação econômica?

**(c)** Redefina $C(50)$ para tornar $C$ contínua à esquerda em $q = 50$.

</Exercicio>

<Exercicio numero={13} fonte="Guidorizzi §2.4, Ex. 8; ITA MA-011">

Seja $f: \mathbb{R} \to \mathbb{R}$ contínua. Mostre que o conjunto $\{x \in \mathbb{R} : f(x) = 0\}$ é fechado.

*(Dica: Mostre que o complemento $\{x : f(x) \neq 0\}$ é aberto usando a definição ε-δ.)*

</Exercicio>

<Exercicio numero={14} fonte="Apostol I §3.5; REAMAT cap. 2">

Prove que $f(x) = x^3 - 3x + 1$ tem pelo menos três zeros reais.

**(a)** Use as três condições de continuidade para justificar que $f$ é contínua em $\mathbb{R}$.

**(b)** Avalie $f$ em $x = -2, -1, 0, 1, 2$ e aplique o princípio da mudança de sinal para localizar os zeros.

**(c)** Estime cada zero com precisão de 0,1 usando bisseção manual.

</Exercicio>

<Exercicio numero={15} fonte="ITA MA-011; Stewart §2.5, Ex. 59">

**(Nível ITA)** Seja $f: \mathbb{R} \to \mathbb{R}$ contínua tal que $f(x + y) = f(x) + f(y)$ para todos $x, y \in \mathbb{R}$.

**(a)** Mostre que $f(0) = 0$.

**(b)** Mostre que $f(nx) = nf(x)$ para todo $n \in \mathbb{Z}$.

**(c)** Mostre que $f(x) = cx$ para todo $x \in \mathbb{Q}$, onde $c = f(1)$.

**(d)** Use a continuidade de $f$ para concluir que $f(x) = cx$ para todo $x \in \mathbb{R}$.

</Exercicio>
