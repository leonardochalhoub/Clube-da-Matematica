---
titulo: "Lição 9 — Limites de sequências: formalização ε-N"
slug: "cal1-u1-l09-limites-sequencias"
categoria: "calculo-1"
subcategoria: "unidade-1"
descricao: "Definição rigorosa ε-N de convergência de sequências. Sequências monótonas e limitadas. Teorema de Bolzano-Weierstrass. Sequências de Cauchy e completude de ℝ."
ordem: 9
publicado: true
tags: ["calculo-1", "sequencias", "convergencia", "epsilon-N", "bolzano-weierstrass", "engenharia"]
prerrequisitos:
  - "Lição 1 — Revisão: números reais, supremo e ínfimo"
  - "Lição 2 — Limite de função: definição ε-δ"
autores: ["Clube da Matemática"]
atualizadoEm: "2026-05-20"
usadoEm: ["Cálculo 1 — Unidade 1", "USP MAC0105", "ITA MA-011"]
versao: "v1"
---

<EquacaoCanonica
  formula="\lim_{n \to \infty} a_n = L \;\iff\; \forall\,\varepsilon > 0,\;\exists\, N \in \mathbb{N}: n > N \;\Rightarrow\; |a_n - L| < \varepsilon"
  legenda={<>Uma sequência <Eq>{`(a_n)`}</Eq> <strong>converge</strong> para <Eq>{`L`}</Eq> se, dado qualquer tolerância <Eq>{`\\varepsilon > 0`}</Eq>, a partir de algum índice <Eq>{`N`}</Eq> todos os termos da sequência ficam dentro do intervalo <Eq>{`(L-\\varepsilon, L+\\varepsilon)`}</Eq>. O papel de <Eq>{`N`}</Eq> em sequências é análogo ao de <Eq>{`M`}</Eq> em limites no infinito de funções.</>}
  audioTexto="O limite de a sub n quando n tende ao infinito é L, se e somente se, para todo epsilon maior que zero, existe N natural tal que: se n é maior que N, então o módulo de a sub n menos L é menor que epsilon."
/>

<DuasPortas>

<Porta nivel="formal" titulo="Definição rigorosa">

### Sequências e convergência

<Definicao titulo="Sequência real">

Uma **sequência** é uma função $a: \mathbb{N} \to \mathbb{R}$, denotada $(a_n)_{n \geq 1}$ ou simplesmente $(a_n)$. O termo $a_n = a(n)$ é o **$n$-ésimo termo**.

</Definicao>

<Definicao titulo="Convergência (definição ε-N)">

$(a_n)$ **converge** para $L \in \mathbb{R}$ — escrevemos $\lim_{n\to\infty} a_n = L$ ou $a_n \to L$ — se

$$\forall\,\varepsilon > 0,\;\exists\, N \in \mathbb{N}: n > N \implies |a_n - L| < \varepsilon.$$

Uma sequência que não converge é **divergente**.

</Definicao>

<Teorema titulo="Unicidade do limite">

Se $a_n \to L$ e $a_n \to M$, então $L = M$.

**Prova:** Para $\varepsilon > 0$ qualquer, existem $N_1, N_2$ com $|a_n - L| < \varepsilon/2$ ($n>N_1$) e $|a_n - M| < \varepsilon/2$ ($n>N_2$). Para $n > \max(N_1,N_2)$: $|L-M| \leq |L-a_n|+|a_n-M| < \varepsilon$. Como $\varepsilon>0$ é arbitrário, $L = M$. $\blacksquare$

</Teorema>

<Teorema titulo="Álgebra dos limites de sequências">

Se $a_n \to L$ e $b_n \to M$, então:

$$a_n + b_n \to L + M,\quad a_n b_n \to LM,\quad \frac{a_n}{b_n} \to \frac{L}{M}\;(M \neq 0).$$

</Teorema>

<Teorema titulo="Sequência monótona limitada converge">

Se $(a_n)$ é **monótona crescente** e **limitada superiormente**, então converge para $\sup_n a_n$.

Se $(a_n)$ é **monótona decrescente** e **limitada inferiormente**, então converge para $\inf_n a_n$.

</Teorema>

<Teorema titulo="Teorema de Bolzano-Weierstrass">

Toda sequência **limitada** (em $\mathbb{R}$) tem pelo menos uma **subsequência convergente**.

</Teorema>

<Definicao titulo="Sequência de Cauchy">

$(a_n)$ é uma **sequência de Cauchy** se

$$\forall\,\varepsilon > 0,\;\exists\,N \in \mathbb{N}: m, n > N \implies |a_m - a_n| < \varepsilon.$$

</Definicao>

<Teorema titulo="Critério de Cauchy para completude de ℝ">

Em $\mathbb{R}$: $(a_n)$ converge $\iff$ $(a_n)$ é de Cauchy.

Este teorema é equivalente ao **axioma da completude** de $\mathbb{R}$ e **falha em $\mathbb{Q}$** (exemplo: $a_n = $ aproximações decimais de $\sqrt{2}$, que é de Cauchy mas não converge em $\mathbb{Q}$).

</Teorema>

<Cuidado>

**Sequência limitada $\not\Rightarrow$ convergente.** Exemplo: $a_n = (-1)^n$ é limitada ($|a_n|=1$) mas diverge — tem subsequências convergindo para $1$ e para $-1$.

</Cuidado>

</Porta>

<Porta nivel="5anos" titulo="Pra criança de 5 anos">

Imagina que você quer se aproximar de um alvo no centro de um alvo de arco. A cada flecha que você lança, você fica mais perto do centro. Se você continuar lançando e chegar cada vez mais perto — tão perto quanto quiser — você está "convergindo" para o centro!

Uma sequência é uma lista de números onde cada número é uma tentativa nova. Se a lista vai chegando cada vez mais perto de um número final, ela converge para esse número.

</Porta>

<Porta nivel="10anos" titulo="Pra quem tem 10 anos">

Você já viu uma calculadora dando $1, 1.4, 1.41, 1.414, 1.4142, \ldots$ para representar $\sqrt{2}$? Cada número se aproxima mais de $\sqrt{2}$ sem nunca chegá-lo exatamente.

Isso é uma sequência: uma lista ordenada de números. E quando a lista "se aproxima" de um valor fixo, dizemos que ela **converge** para esse valor.

Em matemática, "se aproxima" tem uma definição precisa: dado qualquer erro que você aceite (por menor que seja!), a partir de algum ponto da lista, todos os números ficam dentro desse erro.

</Porta>

<Porta nivel="15anos" titulo="Pra quem tem 15 anos">

### Sequências e convergência

Uma **sequência** é simplesmente uma lista: $a_1, a_2, a_3, \ldots$

**Exemplos:**

- $a_n = 1/n$: $1, 1/2, 1/3, 1/4, \ldots \to 0$
- $a_n = (-1)^n$: $-1, 1, -1, 1, \ldots$ — diverge (oscila)
- $a_n = (1 + 1/n)^n$: $2, 2.25, 2.37, \ldots \to e \approx 2.718$

**Definição ε-N:** $a_n \to L$ se dado qualquer "erro permitido" $\varepsilon > 0$, existe um índice $N$ tal que a partir do $N$-ésimo termo, todos os $a_n$ estão a distância menor que $\varepsilon$ de $L$.

**Estratégia para provas:** Para mostrar $a_n \to L$:

1. Calcule $|a_n - L|$.
2. Simplifi-o em algo do tipo $C/n$ ou $C/n^2$.
3. Dado $\varepsilon$, escolha $N$ tal que $C/N < \varepsilon$, ou seja, $N > C/\varepsilon$.

**Exemplo:** Mostre que $\dfrac{3n+1}{n+2} \to 3$.

$$\left|\frac{3n+1}{n+2} - 3\right| = \left|\frac{3n+1-3n-6}{n+2}\right| = \frac{5}{n+2} < \frac{5}{n}.$$

Dado $\varepsilon > 0$, tome $N = \lceil 5/\varepsilon \rceil$. Para $n > N$: $\dfrac{5}{n} < \varepsilon$. $\square$

</Porta>

<Porta nivel="25anos" titulo="Pra quem tem 25 anos">

### Estrutura teórica

**Teorema da sequência monótona:** A prova usa completude: se $(a_n)$ crescente e limitada, então $L = \sup_n a_n$ existe. Para qualquer $\varepsilon > 0$, existe $N$ com $a_N > L - \varepsilon$ (caso contrário $L - \varepsilon$ seria uma cota superior menor). Para $n > N$: $L - \varepsilon < a_N \leq a_n \leq L$, logo $|a_n - L| < \varepsilon$.

**Bolzano-Weierstrass:** $(a_n)$ limitada por $[m, M]$. Bissecta repetidamente $[m, M]$: um dos dois subintervalos contém infinitos termos. Itera-se, gerando sequência de intervalos encaixados com comprimento $\to 0$. O ponto de interseção é o limite da subsequência.

**Sequências de Cauchy e completude:** Em $\mathbb{Q}$, a sequência $a_1 = 1$, $a_{n+1} = \dfrac{a_n + 2/a_n}{2}$ (algoritmo babilônico para $\sqrt{2}$) é de Cauchy mas diverge em $\mathbb{Q}$. Isso motivou a construção dos reais como completamento de $\mathbb{Q}$ (cortes de Dedekind ou sequências de Cauchy com equivalência).

**Conexão com funções:** Se $f$ é contínua em $a$ e $x_n \to a$, então $f(x_n) \to f(a)$. Essa é a caracterização sequential de continuidade — equivalente à definição ε-δ.

</Porta>

<Porta nivel="40anos" titulo="Pra quem tem 40 anos (engenharia)">

### Sequências em algoritmos iterativos

**Algoritmos iterativos** na engenharia produzem sequências $x_0, x_1, x_2, \ldots$ que convergem para uma solução. A teoria de sequências responde:

- **Existe solução?** (Completude de $\mathbb{R}$ — se é Cauchy, converge.)
- **A sequência converge?** (Critério do ponto fixo de Banach.)
- **Com que velocidade?** (Taxa de convergência: linear, quadrática, superlinear.)

**Exemplo — Método de Newton:** $x_{n+1} = x_n - f(x_n)/f'(x_n)$. Quando converge, a convergência é **quadrática**: o número de casas decimais corretas dobra a cada iteração. Isso é formalizado por $|x_{n+1} - c| \leq C |x_n - c|^2$.

**Exemplo — Filtros digitais:** A resposta de um filtro IIR com entrada $u_n$ e saída $y_n$ satisfaz $y_n = a_1 y_{n-1} + b_0 u_n$. A estabilidade do filtro equivale a $|a_1| < 1$, que garante que a sequência de saída converge para zero quando a entrada é removida — diretamente do critério de convergência geométrica.

**Tolerâncias em critérios de parada:** O critério $|x_{n+1} - x_n| < \varepsilon$ em algoritmos iterativos é a versão prática do critério de Cauchy. É mais fácil de implementar que $|x_n - c| < \varepsilon$ (que exigiria conhecer $c$).

</Porta>

<Porta nivel="pratica" titulo="Prática guiada">

### Calculando limites de sequências

**Técnica 1 — Divisão pelo maior grau:**

$$\lim_{n\to\infty} \frac{2n^2 + 3n}{5n^2 - 1} = \lim_{n\to\infty} \frac{2 + 3/n}{5 - 1/n^2} = \frac{2}{5}.$$

**Técnica 2 — Teorema do confronto (Squeeze):**

$$0 \leq \frac{\sin n}{n} \leq \frac{1}{n} \to 0 \implies \frac{\sin n}{n} \to 0.$$

**Técnica 3 — Sequências geométricas:**

<Eq>{`r^n \\to \\begin{cases} 0 & |r| < 1 \\\\ 1 & r = 1 \\\\ \\text{diverge} & r > 1 \\text{ ou } r \\leq -1. \\end{cases}`}</Eq>

**Técnica 4 — Limite fundamental via sequências:**

$$\left(1 + \frac{1}{n}\right)^n \to e \approx 2.71828\ldots$$

Este é o limite fundamental que define a constante de Euler.

**Técnica 5 — Sequências recursivas:** Para $a_{n+1} = g(a_n)$ com $g$ contínua, se $a_n \to L$ então $L = g(L)$ (ponto fixo de $g$).

</Porta>

</DuasPortas>

## Exemplos resolvidos

<Exemplo titulo="Ex. 1 — Prova ε-N de convergência" numero={1} fonte="Guidorizzi §1.4, Ex. 1; Apostol I §10.2">

Prove rigorosamente que $a_n = \dfrac{2n-1}{n+3} \to 2$.

$$\left|\frac{2n-1}{n+3} - 2\right| = \left|\frac{2n-1-2n-6}{n+3}\right| = \frac{7}{n+3} < \frac{7}{n}.$$

Dado $\varepsilon > 0$, escolha $N = \lceil 7/\varepsilon \rceil$. Para $n > N$:

$$\left|\frac{2n-1}{n+3} - 2\right| < \frac{7}{n} < \frac{7}{N} \leq \varepsilon. \quad \blacksquare$$

</Exemplo>

<Exemplo titulo="Ex. 2 — Sequência monótona crescente" numero={2} fonte="Stewart §11.1, Ex. 56; Guidorizzi §1.5">

Mostre que $a_n = \dfrac{n}{n+1}$ é crescente e encontre seu limite.

**Monotonicidade:** $a_{n+1} - a_n = \dfrac{n+1}{n+2} - \dfrac{n}{n+1} = \dfrac{(n+1)^2 - n(n+2)}{(n+2)(n+1)} = \dfrac{1}{(n+2)(n+1)} > 0$.

Logo $(a_n)$ é estritamente crescente. Como $a_n < 1$ para todo $n$, é limitada superiormente por 1.

**Limite:** $\lim_{n\to\infty}\dfrac{n}{n+1} = \lim_{n\to\infty}\dfrac{1}{1+1/n} = 1 = \sup_n a_n$. $\checkmark$

</Exemplo>

<Exemplo titulo="Ex. 3 — Limite fundamental (1 + 1/n)^n" numero={3} fonte="Guidorizzi §1.6; Apostol I §10.5">

Verifique numericamente e use o Teorema da Sequência Monótona para justificar que $a_n = (1+1/n)^n$ converge.

**Crescimento:** Por desigualdade AM-GM: $(1+1/n)^n < (1+1/(n-1))^{n-1}$... na verdade, prova-se que $a_n$ é crescente pela desigualdade de Bernoulli ou pela expansão binomial.

**Limitação:** $a_n = (1+1/n)^n \leq e^1 = e$ (usando $1+x \leq e^x$).

Logo $(a_n)$ é monótona crescente e limitada → converge. O limite é definido como $e \approx 2.71828$.

| $n$ | $a_n = (1+1/n)^n$ |
|-----|-------------------|
| 1 | 2.000 |
| 10 | 2.5937 |
| 100 | 2.7048 |
| 1000 | 2.7169 |
| 10000 | 2.7181 |

</Exemplo>

<Exemplo titulo="Ex. 4 — Sequência recursiva" numero={4} fonte="Stewart §11.1, Ex. 65; Guidorizzi §1.6">

Seja $a_1 = 2$ e $a_{n+1} = \dfrac{1}{2}\left(a_n + \dfrac{4}{a_n}\right)$.

**(a)** Mostre que $(a_n)$ é limitada inferiormente por $\sqrt{4} = 2$.

Por AM-GM: $a_{n+1} = \dfrac{a_n + 4/a_n}{2} \geq \sqrt{a_n \cdot \frac{4}{a_n}} = \sqrt{4} = 2$.

**(b)** Mostre que $(a_n)$ é decrescente.

$a_{n+1} \leq a_n \iff \dfrac{1}{2}\left(a_n + \dfrac{4}{a_n}\right) \leq a_n \iff a_n \geq \dfrac{4}{a_n} \iff a_n^2 \geq 4 \iff a_n \geq 2$ (que vale por (a)).

**(c)** O limite satisfaz $L = \dfrac{1}{2}(L + 4/L) \Rightarrow L^2 = 4 \Rightarrow L = 2$ (tomando $L > 0$).

*Este é o algoritmo babilônico para $\sqrt{4}$.*

</Exemplo>

<Exemplo titulo="Ex. 5 — Divergência por subsequências" numero={5} fonte="Apostol I §10.3; Guidorizzi §1.4">

Prove que $a_n = (-1)^n$ diverge.

Suponha por absurdo que $a_n \to L$. Para $\varepsilon = 1/2$, existe $N$ com $|a_n - L| < 1/2$ para $n > N$.

Para $n$ par grande: $|1 - L| < 1/2$, logo $L > 1/2$.

Para $n$ ímpar grande: $|-1 - L| < 1/2$, logo $L < -1/2$.

Contradição: $L > 1/2$ e $L < -1/2$ são incompatíveis. $\blacksquare$

</Exemplo>

---

## Exercícios

<ListaExercicios seed="cal1-u1-l09-limites-sequencias">

<Exercicio numero="9.1" dificuldade="aplicacao"
  opcoes={[
    { texto: "Diverge (oscila entre 0 e 2)", correta: true },
    { texto: "$0$" },
    { texto: "$1$" },
    { texto: "$2$" }
  ]}
  solucao={<>Para <Eq>{`n`}</Eq> par, <Eq>{`a_n = 1 + 1 = 2`}</Eq>; para <Eq>{`n`}</Eq> ímpar, <Eq>{`a_n = 1 - 1 = 0`}</Eq>. Como os termos alternam entre 0 e 2, a sequência não converge para nenhum valor fixo.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 1", licenca: "CC-BY-NC-SA" }}
>

Analise a convergência da sequência <Eq>{`a_n = 1 + (-1)^n`}</Eq>, para <Eq>{`n \\geq 1`}</Eq>. Calcule os primeiros quatro termos e determine se converge.

</Exercicio>

<Exercicio numero="9.2" dificuldade="aplicacao"
  opcoes={[
    { texto: "Diverge para $+\\infty$", correta: true },
    { texto: "$0$" },
    { texto: "$1$" },
    { texto: "$-1$" }
  ]}
  solucao={<>Temos <Eq>{`a_n = n^2 - 1`}</Eq>. Como <Eq>{`n^2 \\to +\\infty`}</Eq>, a sequência diverge para <Eq>{`+\\infty`}</Eq>. Os primeiros termos são: <Eq>{`a_1 = 0,\\; a_2 = 3,\\; a_3 = 8,\\; a_4 = 15`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 2", licenca: "CC-BY-NC-SA" }}
>

Calcule os primeiros quatro termos da sequência <Eq>{`a_n = n^2 - 1`}</Eq> e determine se converge.

</Exercicio>

<Exercicio numero="9.3" dificuldade="aplicacao"
  opcoes={[
    { texto: "$a_n = \\dfrac{n(n+1)}{2}$", correta: true },
    { texto: "$a_n = n^2$" },
    { texto: "$a_n = 2^n - 1$" },
    { texto: "$a_n = n + 1$" }
  ]}
  solucao={<>Expandindo a recorrência: <Eq>{`a_n = a_{n-1} + n = a_{n-2} + (n-1) + n = \\cdots = 1 + 2 + \\cdots + n = \\frac{n(n+1)}{2}`}</Eq>.</>}
  passos={<>
    <ol>
      <li>Escreva <Eq>{`a_n = a_{n-1} + n`}</Eq> e aplique telescopicamente.</li>
      <li><Eq>{`a_n = a_1 + 2 + 3 + \\cdots + n = 1 + (2 + 3 + \\cdots + n)`}</Eq>.</li>
      <li>Use a fórmula da soma de PA: <Eq>{`\\sum_{k=1}^{n} k = \\frac{n(n+1)}{2}`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 5", licenca: "CC-BY-NC-SA" }}
>

Seja <Eq>{`a_1 = 1`}</Eq> e <Eq>{`a_n = a_{n-1} + n`}</Eq> para <Eq>{`n \\geq 2`}</Eq>. Encontre uma fórmula explícita para <Eq>{`a_n`}</Eq>.

</Exercicio>

<Exercicio numero="9.4" dificuldade="aplicacao"
  opcoes={[
    { texto: "$a_n = 1 + 17(n-1)$", correta: true },
    { texto: "$a_n = 17n$" },
    { texto: "$a_n = 17(n+1)$" },
    { texto: "$a_n = 17n - 16$" }
  ]}
  solucao={<>Progressão aritmética com primeiro termo <Eq>{`a_1 = 1`}</Eq> e razão <Eq>{`d = 17`}</Eq>. A fórmula geral é <Eq>{`a_n = a_1 + (n-1)d = 1 + 17(n-1)`}</Eq>. Verifique: <Eq>{`a_2 = 18`}</Eq>, <Eq>{`a_2 - a_1 = 17`}</Eq>. Correto.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 6", licenca: "CC-BY-NC-SA" }}
>

Encontre uma fórmula para o <Eq>{`n`}</Eq>-ésimo termo da progressão aritmética com <Eq>{`a_1 = 1`}</Eq> e <Eq>{`a_{n+1} - a_n = 17`}</Eq>.

</Exercicio>

<Exercicio numero="9.5" dificuldade="aplicacao"
  opcoes={[
    { texto: "$a_n = -3 + 4(n-1)$", correta: true },
    { texto: "$a_n = 4n - 3$" },
    { texto: "$a_n = -3 \\cdot 4^{n-1}$" },
    { texto: "$a_n = 4n + 3$" }
  ]}
  solucao={<>PA com <Eq>{`a_1 = -3`}</Eq> e razão <Eq>{`d = 4`}</Eq>. Fórmula: <Eq>{`a_n = -3 + 4(n-1)`}</Eq>. Observe que <Eq>{`-3 + 4(n-1) = 4n - 7`}</Eq>, que é equivalente à resposta.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 7", licenca: "CC-BY-NC-SA" }}
>

Encontre uma fórmula para o <Eq>{`n`}</Eq>-ésimo termo da progressão aritmética com <Eq>{`a_1 = -3`}</Eq> e <Eq>{`a_{n+1} - a_n = 4`}</Eq>.

</Exercicio>

<Exercicio numero="9.6" dificuldade="aplicacao"
  opcoes={[
    { texto: "$a_n = 10^{n-1}$", correta: true },
    { texto: "$a_n = 10^n$" },
    { texto: "$a_n = n \\cdot 10$" },
    { texto: "$a_n = 10^{n+1}$" }
  ]}
  solucao={<>Progressão geométrica com <Eq>{`a_1 = 1`}</Eq> e razão <Eq>{`q = 10`}</Eq>. Fórmula geral: <Eq>{`a_n = a_1 \\cdot q^{n-1} = 1 \\cdot 10^{n-1} = 10^{n-1}`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 8", licenca: "CC-BY-NC-SA" }}
>

Encontre uma fórmula para o <Eq>{`n`}</Eq>-ésimo termo da progressão geométrica com <Eq>{`a_1 = 1`}</Eq> e <Eq>{`a_{n+1}/a_n = 10`}</Eq>.

</Exercicio>

<Exercicio numero="9.7" dificuldade="aplicacao"
  opcoes={[
    { texto: "$a_n = 3 \\cdot (1/10)^{n-1}$", correta: true },
    { texto: "$a_n = 3/10^n$" },
    { texto: "$a_n = 3 \\cdot 10^{n-1}$" },
    { texto: "$a_n = (1/10)^n$" }
  ]}
  solucao={<>PG com <Eq>{`a_1 = 3`}</Eq> e razão <Eq>{`q = 1/10`}</Eq>. Fórmula: <Eq>{`a_n = 3 \\cdot (1/10)^{n-1}`}</Eq>. Como <Eq>{`|q| < 1`}</Eq>, esta sequência converge para 0.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 9", licenca: "CC-BY-NC-SA" }}
>

Encontre uma fórmula para o <Eq>{`n`}</Eq>-ésimo termo da progressão geométrica com <Eq>{`a_1 = 3`}</Eq> e <Eq>{`a_{n+1}/a_n = 1/10`}</Eq>. Converge?

</Exercicio>

<Exercicio numero="9.8" dificuldade="aplicacao"
  opcoes={[
    { texto: "$a_n = n^2 - 1$", correta: true },
    { texto: "$a_n = n^2 + 1$" },
    { texto: "$a_n = (n-1)^2$" },
    { texto: "$a_n = n^2$" }
  ]}
  solucao={<>A sequência é <Eq>{`0, 3, 8, 15, 24, \\ldots`}</Eq> Adicionando 1: <Eq>{`1, 4, 9, 16, 25, \\ldots = n^2`}</Eq>. Portanto <Eq>{`a_n = n^2 - 1`}</Eq>.</>}
  passos={<>
    <ol>
      <li>Observe a sequência: <Eq>{`0, 3, 8, 15, 24, 35, \\ldots`}</Eq></li>
      <li>Adicione 1 a cada termo: <Eq>{`1, 4, 9, 16, 25, 36, \\ldots = 1^2, 2^2, 3^2, \\ldots`}</Eq></li>
      <li>Portanto <Eq>{`a_n + 1 = n^2`}</Eq>, ou seja, <Eq>{`a_n = n^2 - 1`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 10", licenca: "CC-BY-NC-SA" }}
>

Encontre uma fórmula explícita para o <Eq>{`n`}</Eq>-ésimo termo da sequência cujos primeiros termos são <Eq>{`\\{0, 3, 8, 15, 24, 35, 48, 63, \\ldots\\}`}</Eq>. (Dica: some 1 a cada termo.)

</Exercicio>

<Exercicio numero="9.9" dificuldade="compreensao"
  opcoes={[
    { texto: "$a_n = \\cos\\!\\left(\\dfrac{n\\pi}{2}\\right)$", correta: true },
    { texto: "$a_n = \\sin\\!\\left(\\dfrac{n\\pi}{2}\\right)$" },
    { texto: "$a_n = (-1)^n$" },
    { texto: "$a_n = \\cos(n\\pi)$" }
  ]}
  solucao={<>A sequência <Eq>{`1, 0, -1, 0, 1, 0, -1, 0, \\ldots`}</Eq> coincide com <Eq>{`\\cos(n\\pi/2)`}</Eq>: para <Eq>{`n=1`}</Eq>, <Eq>{`\\cos(\\pi/2)=0`}</Eq>... Mais precisamente, para <Eq>{`n \\geq 0`}</Eq>: <Eq>{`\\cos(0)=1, \\cos(\\pi/2)=0, \\cos(\\pi)=-1, \\cos(3\\pi/2)=0`}</Eq>. Logo <Eq>{`a_n = \\cos(n\\pi/2)`}</Eq> (indexando de <Eq>{`n=0`}</Eq>).</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 12", licenca: "CC-BY-NC-SA" }}
>

Encontre uma fórmula para o <Eq>{`n`}</Eq>-ésimo termo da sequência <Eq>{`\\{1, 0, -1, 0, 1, 0, -1, 0, \\ldots\\}`}</Eq>. (Dica: pense onde <Eq>{`\\sin x`}</Eq> ou <Eq>{`\\cos x`}</Eq> assume esses valores.)

</Exercicio>

<Exercicio numero="9.10" dificuldade="aplicacao"
  opcoes={[
    { texto: "$a_n = \\dfrac{(-1)^{n+1}}{2n-1}$", correta: true },
    { texto: "$a_n = \\dfrac{1}{2n-1}$" },
    { texto: "$a_n = \\dfrac{(-1)^n}{2n+1}$" },
    { texto: "$a_n = \\dfrac{(-1)^n}{2n-1}$" }
  ]}
  solucao={<>Os denominadores são <Eq>{`1, 3, 5, 7, \\ldots = 2n-1`}</Eq>. Os sinais alternam começando positivo: <Eq>{`+, -, +, -`}</Eq>, logo <Eq>{`(-1)^{n+1}`}</Eq>. Portanto <Eq>{`a_n = \\frac{(-1)^{n+1}}{2n-1}`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 13", licenca: "CC-BY-NC-SA" }}
>

Encontre uma fórmula para o <Eq>{`n`}</Eq>-ésimo termo da sequência <Eq>{`\\left\\{1, -\\frac{1}{3}, \\frac{1}{5}, -\\frac{1}{7}, \\ldots\\right\\}`}</Eq>.

</Exercicio>

<Exercicio numero="9.11" dificuldade="aplicacao"
  opcoes={[
    { texto: "Diverge (oscila entre $-1$ e $1$)", correta: true },
    { texto: "$0$" },
    { texto: "$1$" },
    { texto: "$-1$" }
  ]}
  solucao={<>A recorrência <Eq>{`a_{n+1} = -a_n`}</Eq> com <Eq>{`a_1 = 1`}</Eq> dá <Eq>{`a_n = (-1)^{n+1}`}</Eq>. Os termos alternam: <Eq>{`1, -1, 1, -1, \\ldots`}</Eq> A sequência é limitada mas diverge pois não tem limite único.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 14", licenca: "CC-BY-NC-SA" }}
>

Seja <Eq>{`a_1 = 1`}</Eq> e <Eq>{`a_{n+1} = -a_n`}</Eq> para <Eq>{`n \\geq 1`}</Eq>. Encontre uma fórmula explícita e determine se a sequência converge.

</Exercicio>

<Exercicio numero="9.12" dificuldade="aplicacao"
  opcoes={[
    { texto: "Diverge para $+\\infty$ (dobra a cada passo)", correta: true },
    { texto: "$2$" },
    { texto: "$0$" },
    { texto: "Converge para $e^2$" }
  ]}
  solucao={<>Com <Eq>{`a_1 = 2`}</Eq> e <Eq>{`a_{n+1} = 2a_n`}</Eq>, tem-se <Eq>{`a_n = 2 \\cdot 2^{n-1} = 2^n`}</Eq>. Como <Eq>{`2^n \\to +\\infty`}</Eq>, a sequência diverge.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 15", licenca: "CC-BY-NC-SA" }}
>

Seja <Eq>{`a_1 = 2`}</Eq> e <Eq>{`a_{n+1} = 2a_n`}</Eq> para <Eq>{`n \\geq 1`}</Eq>. Encontre <Eq>{`a_n`}</Eq> explicitamente. A sequência converge?

</Exercicio>

<Exercicio numero="9.13" dificuldade="compreensao"
  opcoes={[
    { texto: "$a_n = n!$", correta: true },
    { texto: "$a_n = n^n$" },
    { texto: "$a_n = 2^n$" },
    { texto: "$a_n = (n-1)!$" }
  ]}
  solucao={<>Com <Eq>{`a_1 = 1`}</Eq> e <Eq>{`a_{n+1} = (n+1)a_n`}</Eq>: <Eq>{`a_2 = 2 \\cdot 1 = 2`}</Eq>, <Eq>{`a_3 = 3 \\cdot 2 = 6`}</Eq>, <Eq>{`a_4 = 4 \\cdot 6 = 24`}</Eq>. Portanto <Eq>{`a_n = n!`}</Eq> (fatorial). Diverge para <Eq>{`+\\infty`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 16", licenca: "CC-BY-NC-SA" }}
>

Seja <Eq>{`a_1 = 1`}</Eq> e <Eq>{`a_{n+1} = (n+1)a_n`}</Eq> para <Eq>{`n \\geq 1`}</Eq>. Identifique a fórmula explícita para <Eq>{`a_n`}</Eq>.

</Exercicio>

<Exercicio numero="9.14" dificuldade="aplicacao"
  opcoes={[
    { texto: "$a_n = \\dfrac{1}{2^{n(n-1)/2}}$", correta: true },
    { texto: "$a_n = \\dfrac{1}{2^n}$" },
    { texto: "$a_n = \\dfrac{1}{n!}$" },
    { texto: "$a_n = 2^{-n^2}$" }
  ]}
  solucao={<>Temos <Eq>{`a_{n+1} = a_n / 2^n`}</Eq>, logo <Eq>{`a_n = a_1 \\cdot \\prod_{k=1}^{n-1} \\frac{1}{2^k} = \\frac{1}{2^{1+2+\\cdots+(n-1)}} = \\frac{1}{2^{n(n-1)/2}}`}</Eq>. Converge para 0 pois o expoente cresce sem limite.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 18", licenca: "CC-BY-NC-SA" }}
>

Seja <Eq>{`a_1 = 1`}</Eq> e <Eq>{`a_{n+1} = a_n / 2^n`}</Eq> para <Eq>{`n \\geq 1`}</Eq>. Encontre uma fórmula explícita para <Eq>{`a_n`}</Eq> e determine se converge.

</Exercicio>

<Exercicio numero="9.15" dificuldade="aplicacao"
  opcoes={[
    { texto: "$7$", correta: true },
    { texto: "$3$" },
    { texto: "$-7$" },
    { texto: "O limite não existe" }
  ]}
  solucao={<>Pela álgebra dos limites: <Eq>{`\\lim(3a_n - 4b_n) = 3 \\cdot 1 - 4 \\cdot (-1) = 3 + 4 = 7`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 23", licenca: "CC-BY-NC-SA" }}
>

Suponha que <Eq>{`\\lim_{n\\to\\infty} a_n = 1`}</Eq> e <Eq>{`\\lim_{n\\to\\infty} b_n = -1`}</Eq>. Calcule <Eq>{`\\lim_{n\\to\\infty}(3a_n - 4b_n)`}</Eq>.

</Exercicio>

<Exercicio numero="9.16" dificuldade="aplicacao"
  opcoes={[
    { texto: "$-1$", correta: true },
    { texto: "$1$" },
    { texto: "$\\dfrac{1}{2}$" },
    { texto: "$0$" }
  ]}
  solucao={<>Pela álgebra dos limites: <Eq>{`\\lim\\left(\\frac{1}{2}b_n - \\frac{1}{2}a_n\\right) = \\frac{1}{2}(-1) - \\frac{1}{2}(1) = -\\frac{1}{2} - \\frac{1}{2} = -1`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 24", licenca: "CC-BY-NC-SA" }}
>

Suponha que <Eq>{`\\lim_{n\\to\\infty} a_n = 1`}</Eq> e <Eq>{`\\lim_{n\\to\\infty} b_n = -1`}</Eq>. Calcule <Eq>{`\\lim_{n\\to\\infty}\\!\\left(\\frac{1}{2}b_n - \\frac{1}{2}a_n\\right)`}</Eq>.

</Exercicio>

<Exercicio numero="9.17" dificuldade="compreensao"
  opcoes={[
    { texto: "$0$", correta: true },
    { texto: "$2$" },
    { texto: "$-2$" },
    { texto: "O limite não existe" }
  ]}
  solucao={<>Limite do numerador: <Eq>{`a_n + b_n \\to 1 + (-1) = 0`}</Eq>. Limite do denominador: <Eq>{`a_n - b_n \\to 1 - (-1) = 2 \\neq 0`}</Eq>. Portanto <Eq>{`\\frac{a_n + b_n}{a_n - b_n} \\to \\frac{0}{2} = 0`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 25", licenca: "CC-BY-NC-SA" }}
>

Suponha que <Eq>{`\\lim_{n\\to\\infty} a_n = 1`}</Eq> e <Eq>{`\\lim_{n\\to\\infty} b_n = -1`}</Eq>. Calcule <Eq>{`\\lim_{n\\to\\infty} \\dfrac{a_n + b_n}{a_n - b_n}`}</Eq>.

</Exercicio>

<Exercicio numero="9.18" dificuldade="compreensao"
  opcoes={[
    { texto: "Não existe (denominador tende a $0$)", correta: true },
    { texto: "$0$" },
    { texto: "$1$" },
    { texto: "$-1$" }
  ]}
  solucao={<>Numerador: <Eq>{`a_n - b_n \\to 1 - (-1) = 2`}</Eq>. Denominador: <Eq>{`a_n + b_n \\to 0`}</Eq>. Como o denominador tende a zero e o numerador a 2, o limite não existe (diverge).</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 26", licenca: "CC-BY-NC-SA" }}
>

Suponha que <Eq>{`\\lim_{n\\to\\infty} a_n = 1`}</Eq> e <Eq>{`\\lim_{n\\to\\infty} b_n = -1`}</Eq>. O limite <Eq>{`\\lim_{n\\to\\infty} \\dfrac{a_n - b_n}{a_n + b_n}`}</Eq> existe?

</Exercicio>

<Exercicio numero="9.19" dificuldade="aplicacao"
  opcoes={[
    { texto: "$0$", correta: true },
    { texto: "$+\\infty$" },
    { texto: "$1$" },
    { texto: "$1/2$" }
  ]}
  solucao={<>Pela Regra de L'Hôpital (ou comparação de crescimento): <Eq>{`\\frac{n^2}{2^n} \\to 0`}</Eq> pois a exponencial cresce mais rápido que qualquer polinômio. Para todo <Eq>{`\\varepsilon > 0`}</Eq>, a partir de algum <Eq>{`N`}</Eq>, <Eq>{`n^2 < (1 + \\varepsilon)^n \\cdot 2^n / (1+\\varepsilon)^n`}</Eq>, e a estimação fecha.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 27", licenca: "CC-BY-NC-SA" }}
>

Calcule <Eq>{`\\lim_{n\\to\\infty} \\dfrac{n^2}{2^n}`}</Eq>.

</Exercicio>

<Exercicio numero="9.20" dificuldade="aplicacao"
  opcoes={[
    { texto: "$1$", correta: true },
    { texto: "$0$" },
    { texto: "$e$" },
    { texto: "$+\\infty$" }
  ]}
  solucao={<>Divida numerador e denominador por <Eq>{`n^2`}</Eq>: <Eq>{`\\frac{(n-1)^2}{(n+1)^2} = \\frac{(1-1/n)^2}{(1+1/n)^2} \\to \\frac{1^2}{1^2} = 1`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 28", licenca: "CC-BY-NC-SA" }}
>

Calcule <Eq>{`\\lim_{n\\to\\infty} \\dfrac{(n-1)^2}{(n+1)^2}`}</Eq>.

</Exercicio>

<Exercicio numero="9.21" dificuldade="aplicacao"
  opcoes={[
    { texto: "$1$", correta: true },
    { texto: "$0$" },
    { texto: "$\\infty$" },
    { texto: "$1/2$" }
  ]}
  solucao={<>Divida por <Eq>{`n`}</Eq>: <Eq>{`\\frac{n}{n+1} = \\frac{1}{1 + 1/n} \\to \\frac{1}{1} = 1`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 29", licenca: "CC-BY-NC-SA" }}
>

Calcule <Eq>{`\\lim_{n\\to\\infty} \\dfrac{n}{n+1}`}</Eq>.

</Exercicio>

<Exercicio numero="9.22" dificuldade="desafio"
  opcoes={[
    { texto: "$1$", correta: true },
    { texto: "$0$" },
    { texto: "$e$" },
    { texto: "$+\\infty$" }
  ]}
  solucao={<>Escreva <Eq>{`n^{1/n} = e^{(\\ln n)/n}`}</Eq>. Como <Eq>{`\\frac{\\ln n}{n} \\to 0`}</Eq> (por L'Hôpital ou confronto), segue <Eq>{`n^{1/n} = e^{(\\ln n)/n} \\to e^0 = 1`}</Eq>.</>}
  passos={<>
    <ol>
      <li>Reescreva: <Eq>{`n^{1/n} = e^{(1/n)\\ln n}`}</Eq>.</li>
      <li>Calcule <Eq>{`\\lim_{n\\to\\infty} \\frac{\\ln n}{n} = 0`}</Eq> (pelo critério da derivada: <Eq>{`(\\ln x)' / (x)' = 1/x \\to 0`}</Eq>).</li>
      <li>Por continuidade da exponencial: <Eq>{`\\lim e^{(\\ln n)/n} = e^0 = 1`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 30", licenca: "CC-BY-NC-SA" }}
>

Calcule <Eq>{`\\lim_{n\\to\\infty} n^{1/n}`}</Eq>. (Dica: escreva <Eq>{`n^{1/n} = e^{(\\ln n)/n}`}</Eq>.)

</Exercicio>

<Exercicio numero="9.23" dificuldade="compreensao"
  opcoes={[
    { texto: "Limitada e eventualmente decrescente", correta: true },
    { texto: "Limitada e crescente" },
    { texto: "Ilimitada e crescente" },
    { texto: "Ilimitada e decrescente" }
  ]}
  solucao={<>Temos <Eq>{`a_n = n/2^n`}</Eq>. É limitada pois converge para 0. Para monotonicidade, calcule <Eq>{`a_{n+1}/a_n = \\frac{(n+1)/2^{n+1}}{n/2^n} = \\frac{n+1}{2n}`}</Eq>. Para <Eq>{`n \\geq 2`}</Eq>: <Eq>{`\\frac{n+1}{2n} < 1`}</Eq>, então a sequência é decrescente para <Eq>{`n \\geq 2`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 31", licenca: "CC-BY-NC-SA" }}
>

Para a sequência <Eq>{`a_n = n/2^n`}</Eq> (<Eq>{`n \\geq 2`}</Eq>), determine se é limitada e se é eventualmente monótona.

</Exercicio>

<Exercicio numero="9.24" dificuldade="compreensao"
  opcoes={[
    { texto: "Limitada e decrescente", correta: true },
    { texto: "Limitada e crescente" },
    { texto: "Ilimitada e decrescente" },
    { texto: "Limitada e não-monótona" }
  ]}
  solucao={<>Temos <Eq>{`a_n = \\ln(1 + 1/n)`}</Eq>. É positiva e <Eq>{`a_n \\to 0`}</Eq>, logo limitada. Como <Eq>{`1 + 1/n`}</Eq> é decrescente em <Eq>{`n`}</Eq> e <Eq>{`\\ln`}</Eq> é crescente, a sequência <Eq>{`\\ln(1+1/n)`}</Eq> é decrescente.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 32", licenca: "CC-BY-NC-SA" }}
>

Para a sequência <Eq>{`a_n = \\ln\\!\\left(1 + \\dfrac{1}{n}\\right)`}</Eq>, determine se é limitada e se é monótona.

</Exercicio>

<Exercicio numero="9.25" dificuldade="compreensao"
  opcoes={[
    { texto: "Limitada e não-monótona", correta: true },
    { texto: "Ilimitada e não-monótona" },
    { texto: "Limitada e crescente" },
    { texto: "Limitada e decrescente" }
  ]}
  solucao={<>Temos <Eq>{`a_n = n^{1/n}`}</Eq> para <Eq>{`n \\geq 3`}</Eq>. É limitada pois <Eq>{`n^{1/n} \\to 1`}</Eq> e para valores iniciais <Eq>{`n^{1/n} \\leq 3^{1/3} \\approx 1{,}44`}</Eq>. É decrescente para <Eq>{`n \\geq 3`}</Eq>, pois a função <Eq>{`x^{1/x}`}</Eq> tem máximo em <Eq>{`x = e`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 35", licenca: "CC-BY-NC-SA" }}
>

Para a sequência <Eq>{`a_n = n^{1/n}`}</Eq> (<Eq>{`n \\geq 3`}</Eq>), determine se é limitada e se é eventualmente monótona.

</Exercicio>

<Exercicio numero="9.26" dificuldade="desafio"
  opcoes={[
    { texto: "Converge para $2$", correta: true },
    { texto: "Converge para $\\sqrt{2}$" },
    { texto: "Converge para $+\\infty$" },
    { texto: "Diverge" }
  ]}
  solucao={<>Defina <Eq>{`a_1 = \\sqrt{2},\\; a_2 = \\sqrt{2\\sqrt{2}},\\; a_3 = \\sqrt{2\\sqrt{2\\sqrt{2}}},\\ldots`}</Eq> A sequência satisfaz <Eq>{`a_{n+1} = \\sqrt{2 a_n}`}</Eq>. É crescente e limitada superiormente por 2. Se <Eq>{`L = \\lim a_n`}</Eq>, então <Eq>{`L = \\sqrt{2L}`}</Eq>, logo <Eq>{`L^2 = 2L`}</Eq>, portanto <Eq>{`L = 2`}</Eq>.</>}
  passos={<>
    <ol>
      <li>Identifique a recorrência: <Eq>{`a_1 = 2`}</Eq>, <Eq>{`a_{n+1} = \\sqrt{2 a_n}`}</Eq>.</li>
      <li>Prove por indução que <Eq>{`a_n < 2`}</Eq> para todo <Eq>{`n`}</Eq>.</li>
      <li>Prove que <Eq>{`(a_n)`}</Eq> é crescente: <Eq>{`a_{n+1}^2 - a_n^2 = 2a_n - a_n^2 = a_n(2-a_n) > 0`}</Eq>.</li>
      <li>Pelo Teorema da Sequência Monótona, <Eq>{`L = \\lim a_n`}</Eq> existe. Tomando limites: <Eq>{`L = \\sqrt{2L} \\Rightarrow L = 2`}</Eq>.</li>
    </ol>
  </>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 38", licenca: "CC-BY-NC-SA" }}
>

Determine se a sequência <Eq>{`a_1 = \\sqrt{2},\\; a_2 = \\sqrt{2\\sqrt{2}},\\; a_3 = \\sqrt{2\\sqrt{2\\sqrt{2}}},\\ldots`}</Eq> tem limite e, em caso afirmativo, encontre-o.

</Exercicio>

<Exercicio numero="9.27" dificuldade="aplicacao"
  opcoes={[
    { texto: "$1$", correta: true },
    { texto: "$0$" },
    { texto: "$\\pi$" },
    { texto: "O limite não existe" }
  ]}
  solucao={<>Pelo limite fundamental <Eq>{`\\lim_{t\\to 0}\\frac{\\sin t}{t} = 1`}</Eq> com <Eq>{`t = 1/n \\to 0`}</Eq>: <Eq>{`n\\sin(1/n) = \\frac{\\sin(1/n)}{1/n} \\to 1`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 40", licenca: "CC-BY-NC-SA" }}
>

Calcule <Eq>{`\\lim_{n\\to\\infty} n\\sin(1/n)`}</Eq>.

</Exercicio>

<Exercicio numero="9.28" dificuldade="aplicacao"
  opcoes={[
    { texto: "$0$", correta: true },
    { texto: "$1$" },
    { texto: "$-1/2$" },
    { texto: "O limite não existe" }
  ]}
  solucao={<>Escreva <Eq>{`a_n = \\frac{\\cos(1/n) - 1}{1/n}`}</Eq>. Com <Eq>{`t = 1/n \\to 0`}</Eq>: <Eq>{`\\frac{\\cos t - 1}{t} \\to 0`}</Eq> (pois <Eq>{`\\cos t - 1 \\sim -t^2/2`}</Eq> próximo de 0, logo a razão <Eq>{`\\sim -t/2 \\to 0`}</Eq>).</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 41", licenca: "CC-BY-NC-SA" }}
>

Calcule <Eq>{`\\lim_{n\\to\\infty} \\dfrac{\\cos(1/n) - 1}{1/n}`}</Eq>.

</Exercicio>

<Exercicio numero="9.29" dificuldade="aplicacao"
  opcoes={[
    { texto: "$0$", correta: true },
    { texto: "$1$" },
    { texto: "$+\\infty$" },
    { texto: "$1/e$" }
  ]}
  solucao={<>Temos <Eq>{`a_n = n!/n^n`}</Eq>. Analise a razão: <Eq>{`\\frac{a_{n+1}}{a_n} = \\frac{(n+1)!}{(n+1)^{n+1}} \\cdot \\frac{n^n}{n!} = \\frac{n^n}{(n+1)^n} = \\left(\\frac{n}{n+1}\\right)^n = \\left(1 - \\frac{1}{n+1}\\right)^n \\to e^{-1} < 1`}</Eq>. Logo <Eq>{`a_n \\to 0`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 42", licenca: "CC-BY-NC-SA" }}
>

Determine o comportamento da sequência <Eq>{`a_n = \\dfrac{n!}{n^n}`}</Eq>. Converge? (Dica: analise a razão <Eq>{`a_{n+1}/a_n`}</Eq>.)

</Exercicio>

<Exercicio numero="9.30" dificuldade="aplicacao"
  opcoes={[
    { texto: "$\\pi/2$", correta: true },
    { texto: "$0$" },
    { texto: "$\\pi$" },
    { texto: "O limite não existe" }
  ]}
  solucao={<>Temos <Eq>{`a_n = \\arctan(n^2)`}</Eq>. Como <Eq>{`n^2 \\to +\\infty`}</Eq> e <Eq>{`\\arctan`}</Eq> é contínua e crescente com <Eq>{`\\lim_{x\\to+\\infty}\\arctan x = \\pi/2`}</Eq>, segue <Eq>{`a_n \\to \\pi/2`}</Eq>.</>}
  fonte={{ livro: "OpenStax Calculus Volume 2", url: "https://openstax.org/books/calculus-volume-2/pages/5-1-sequences", secao: "§5.1", exercicio: "ex. 46", licenca: "CC-BY-NC-SA" }}
>

Calcule <Eq>{`\\lim_{n\\to\\infty} \\arctan(n^2)`}</Eq>.

</Exercicio>

</ListaExercicios>

## Fontes

- [OpenStax Calculus Volume 2](https://openstax.org/books/calculus-volume-2/pages/5-1-sequences) — Strang & Herman · 2016 · CC-BY-NC-SA. **Fonte principal dos exercícios (§5.1).**
